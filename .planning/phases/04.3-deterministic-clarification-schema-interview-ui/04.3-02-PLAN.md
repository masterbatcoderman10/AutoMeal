---
phase: 04.3-deterministic-clarification-schema-interview-ui
plan: 02
type: tdd
wave: 2
depends_on:
  - 04.3-01
files_modified:
  - scripts/assert_clarification_ui_red.py
  - app/services/interview_service.py
  - bot/handlers.py
  - bot/polling.py
  - tests/test_interview_flow.py
  - tests/test_bot_contract.py
autonomous: true
requirements:
  - REASON-03
  - INTERVIEW-01
  - INTERVIEW-04
user_setup: []
must_haves:
  truths:
    - "Telegram meal interviews render deterministic clarification questions from persisted schema data per D-06, with buttons for `single_choice` and `confirm` where the existing PTB callback path supports them per D-07."
    - "Every callback or text answer maps back to a stable `question_id` and persists into durable interview state per D-08, with answered approvals never being re-asked per D-10."
    - "If the user answers only part of a clarification batch, the bot re-prompts only the remaining required questions while preserving prior answers and prompt ownership per D-09."
  artifacts:
    - path: "app/services/interview_service.py"
      provides: "DB-owned deterministic clarification state, remaining-question calculation, and template rendering for meal interviews"
      contains: "current_target_question"
    - path: "bot/handlers.py"
      provides: "Question/choice mapping, callback validation, partial-reply persistence, and prompt-owned deterministic rerendering"
    - path: "bot/polling.py"
      provides: "Deterministic interview kickoff that renders clarification questions instead of asking the interview LLM for user-facing copy"
  key_links:
    - from: "app/services/interview_service.py"
      to: "bot/polling.py"
      via: "kickoff prompt payload becomes Telegram message text and optional reply markup"
      pattern: "question_id|answers_by_question_id|pending_question_ids"
    - from: "bot/handlers.py"
      to: "InterviewSession.current_prompt_payload"
      via: "callback/text answers persist by stable question ID and drive remaining-question rerender"
      pattern: "answers_by_question_id|remaining_required_question_ids|last_bot_message_id"
---

<objective>
Replace meal-interview prompt rendering and reply capture with a deterministic Telegram clarification loop per D-06 through D-10.

Purpose: Phase 04.3 only becomes durable when Telegram replies are keyed by stable question IDs instead of freeform LLM-authored prompts or segment-scoped answer lists.
Output: failing deterministic UI regressions, DB-owned `answers_by_question_id` state, deterministic question rendering, callback/text answer mapping, and partial-reply recovery.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md
@.planning/phases/04.2-llm-threaded-interview-orchestration/04.2-03-SUMMARY.md
@app/services/interview_service.py
@bot/handlers.py
@bot/polling.py
@tests/test_interview_flow.py
@tests/test_bot_contract.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted clarification state -> Telegram prompt | Stored reasoning questions become user-facing prompts and must remain deterministic across retries and restarts. |
| Telegram callback/text reply -> stored answer state | User input arrives through callback data or free text and must be validated against DB-backed question identity before any state mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-03 | Tampering | `bot/handlers.py` | mitigate | Validate `question_id` and `choice_id` against `questions_by_id` in persisted session state, reject unknown IDs fail-closed, and keep prompt ownership on `last_bot_message_id` plus transcript rows. |
| T-04.3-04 | Repudiation | `app/services/interview_service.py` | mitigate | Persist `questions_by_id`, `question_order`, `answers_by_question_id`, rendered prompt metadata, and remaining required questions in `InterviewSession.current_prompt_payload` and mirror every reply into `InterviewMessage`. |
| T-04.3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are allowed; use the existing PTB callback handler and unittest surface already in the repo. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: RED - add failing deterministic Telegram clarification regressions</name>
  <files>scripts/assert_clarification_ui_red.py, tests/test_interview_flow.py, tests/test_bot_contract.py</files>
  <read_first>app/services/interview_service.py, bot/handlers.py, bot/polling.py, tests/test_interview_flow.py, tests/test_bot_contract.py, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md</read_first>
  <acceptance_criteria>
    - "`scripts/assert_clarification_ui_red.py` exits `0` only when the expected pre-04.3 meal-interview failures are present, and exits non-zero on import/bootstrap failures or unexpected passes."
    - "The RED tests require `prepare_interview_session()` and `current_target_question()` to seed deterministic clarification state with `questions_by_id`, `question_order`, `answers_by_question_id`, `pending_question_ids`, and `remaining_required_question_ids` per D-08 and D-09."
    - "The RED tests require `bot/polling.py` to kick off meal interviews from persisted clarification questions rather than `run_interview_turn()` user-facing prompts per D-06, and require `bot/handlers.py` to map callback/text replies back to stable `question_id` and `choice_id` values per D-07 and D-08."
    - "The RED tests require a partial-answer case where one approval candidate is already satisfied and one required question remains, proving only the missing required question is re-rendered and answered approvals are not looped per D-09 and D-10."
  </acceptance_criteria>
  <action>Create `scripts/assert_clarification_ui_red.py` as the RED harness for the deterministic Telegram UI. Extend `tests/test_interview_flow.py` and `tests/test_bot_contract.py` so they fail until meal interviews persist question-indexed state, use the existing PTB callback path for `single_choice` and `confirm` questions per D-07, preserve free-text handling for `free_text` and v1 `multi_choice`, and re-prompt only unanswered required questions per D-09.</action>
  <verify>
    <automated>rtk sh -lc './.venv/bin/python scripts/assert_clarification_ui_red.py'</automated>
  </verify>
  <done>The RED harness proves the repo still lacks deterministic Telegram clarification rendering, question-indexed answer storage, and partial-reply recovery.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - implement deterministic rendering, answer mapping, and remaining-question recovery</name>
  <files>app/services/interview_service.py, bot/handlers.py, bot/polling.py, tests/test_interview_flow.py, tests/test_bot_contract.py</files>
  <read_first>app/services/interview_service.py, bot/handlers.py, bot/polling.py, bot/main.py, tests/test_interview_flow.py, tests/test_bot_contract.py, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md</read_first>
  <acceptance_criteria>
    - "`app/services/interview_service.py` builds meal-interview state from `clarification_schema`, persists `questions_by_id`, `question_order`, `answers_by_question_id`, `pending_question_ids`, and `remaining_required_question_ids`, and preserves the existing `/fix` deterministic branch."
    - "`bot/polling.py` starts meal interviews by rendering deterministic templates from persisted clarification questions per D-06 instead of asking the interview LLM for user-facing prompts."
    - "`bot/handlers.py` uses the existing callback-query path for `single_choice` and `confirm`, validates `question_id` and `choice_id` against DB-backed session state per D-07 and D-08, accepts free-text answers for `free_text` and v1 `multi_choice`, and fails closed on stale or unknown prompt data."
    - "Partial replies preserve prior answers, update `remaining_required_question_ids`, and re-render only missing required questions without re-asking already answered approvals per D-09 and D-10."
    - "`rtk sh -lc './.venv/bin/python -m unittest tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q'` passes."
  </acceptance_criteria>
  <action>Implement the deterministic Telegram clarification loop across `app/services/interview_service.py`, `bot/polling.py`, and `bot/handlers.py`. In `interview_service`, derive meal-interview prompts from persisted `clarification_schema`, add helpers for `remaining_required_question_ids`, and migrate meal sessions from `answers_by_segment` to `answers_by_question_id` while keeping a bounded compatibility read for old state where needed. In `bot/polling.py`, render the first question batch deterministically from templates per D-06. In `bot/handlers.py`, reuse the existing `CallbackQueryHandler` path so `single_choice` and `confirm` prompts emit inline buttons with stable `question_id` and `choice_id` callback data per D-07, keep free-text parsing for `free_text` and v1 `multi_choice`, persist every answer into `InterviewSession.current_prompt_payload` and `InterviewMessage` per D-08, and re-render only the remaining required questions per D-09 while never looping already-satisfied approvals per D-10.</action>
  <verify>
    <automated>rtk sh -lc './.venv/bin/python -m unittest tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q'</automated>
  </verify>
  <done>Telegram clarification is now deterministic, durable, and partial-reply-safe for meal interviews, while `/fix` stays on its existing deterministic branch.</done>
</task>

</tasks>

<verification>
- `rtk sh -lc './.venv/bin/python scripts/assert_clarification_ui_red.py'`
- `rtk sh -lc './.venv/bin/python -m unittest tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q'`
- `rtk rg -n "answers_by_question_id|remaining_required_question_ids|question_id|choice_id|last_bot_message_id|session_mode" app/services/interview_service.py bot/handlers.py bot/polling.py`
</verification>

<success_criteria>
- Meal interviews render deterministic clarification prompts from stored schema data instead of LLM-authored user-facing prompts.
- Every callback or text reply is tied to a stable question ID and survives partial replies and retries.
- The bot re-prompts only unanswered required questions and does not loop already satisfied approval candidates.
</success_criteria>

<output>
Create `.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-02-SUMMARY.md` when done.
</output>
