---
phase: 04.3-deterministic-clarification-schema-interview-ui
verified: 2026-06-01T14:50:23Z
status: gaps_found
score: 9/10 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Live repeated-meal UAT with the saved warm checkpoint proves vector candidates inform reasoning, deterministic questions recover from partial replies, and final save writes the expected DiaryEntry and FoodVisual rows."
    status: failed
    reason: "The recorded evidence artifact is still an in-progress 04.2-clean run, not the required 04.3 warm-checkpoint harness proof."
    artifacts:
      - path: ".planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md"
        issue: "Frontmatter still says `status: in_progress`, `active_database: mealttracker_uat_042_clean`, and `sample_image: sample_images/IMG_4646.HEIC` instead of the required `mealttracker_uat_with_meal_embeddings` harness scenarios."
      - path: ".planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md"
        issue: "The artifact still records open phase gaps `GAP-04.3-P1` and `GAP-04.3-P2` rather than completed warm-state evidence, so the phase's live-proof contract is not closed."
    missing:
      - "Run the 04.3 harness against `mealttracker_uat_with_meal_embeddings` for both `partial-match` (`IMG_4583.HEIC`) and `no-match` (`IMG_4641.HEIC`)."
      - "Update `04.3-UAT-RESULTS.md` with the disposable harness database, report paths, trace IDs, row deltas, and final observed statuses for both scenarios."
      - "Replace the stale gap narrative with the actual post-fix outcome, or record a fresh failing run after the final code state if the scenarios still fail."
---

# Phase 4.3: Deterministic Clarification Schema & Interview UI Verification Report

**Phase Goal:** Replace the freeform interview-turn LLM with a deterministic clarification UI driven by reasoning output: meal reasoning emits a strict per-food-group clarification schema; Telegram renders MCQ/open-field prompts from templates; replies are stored as structured answers; a compact resolver/finalizer consumes the answers and writes through the existing authoritative FoodItem, DiaryEntry, and FoodVisual path.
**Verified:** 2026-06-01T14:50:23Z
**Status:** gaps_found
**Re-verification:** No - initial verification

MVP-mode discrepancy: the roadmap marks Phase 4.3 as `mode: mvp`, but `gsd-sdk query user-story.validate` returns `valid=false` for the roadmap goal because it is not written as `As a ..., I want to ..., so that ...`. The verification below therefore derives user-flow coverage from the roadmap success criteria rather than from a valid user-story sentence.

## User Flow Coverage

| Step | Expected | Evidence | Status |
| --- | --- | --- | --- |
| Reasoning contract | Grouped reasoning owns ranked candidates and ordered clarification actions before Telegram mapping | `app/services/reasoning_service.py:561-760`, `app/services/reasoning_service.py:937-1011`, `tests/test_reasoning_gate.py:780-986` | VERIFIED |
| Telegram rendering | Telegram renders persisted prompts/buttons from deterministic question state, including compact callback tokens and `Other` handling | `bot/callback_data.py:11-32`, `bot/handlers.py:502-543`, `bot/handlers.py:546-640`, `tests/test_bot_contract.py:840-1114` | VERIFIED |
| Partial replies and correction flow | Partial replies preserve prior answers, re-prompt only missing required questions, and `AFFIRMATION` `No` becomes same-group typed correction | `app/services/interview_service.py:1692-1800`, `bot/handlers.py:500-543`, `tests/test_bot_contract.py:840-878` | VERIFIED |
| Resolver/finalizer handoff | Resolver runs only after required deterministic answers and final save stays on the authoritative write path | `app/services/interview_turn_manager.py:34-49`, `app/services/interview_service.py:809-867`, `app/services/meal_resolution_service.py:420-556` | VERIFIED |
| Live warm-checkpoint proof | Warm-checkpoint repeated-meal evidence exists for the named 04.3 harness scenarios and proves final state with traces and row deltas | Runbook requires `mealttracker_uat_with_meal_embeddings` and `IMG_4583.HEIC` / `IMG_4641.HEIC` in `04.3-UAT.md:7-18`, `04.3-UAT.md:82-181`, but `04.3-UAT-RESULTS.md:1-18` and `04.3-UAT-RESULTS.md:141-230` still document a different run and open gaps | FAILED |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Root `top_3` is no longer the live source of truth; grouped candidates drive clarification and gating. | VERIFIED | The response schema keeps `top_3` under each group, while gate evaluation builds behavior from `food_groups` and group actions, not from a root candidate list: `app/services/reasoning_service.py:744-760`, `app/services/reasoning_service.py:937-1011`, `tests/test_reasoning_contract.py:274-300`. |
| 2 | Reasoning emits a strict clarification contract with stable IDs, allowed answer types, validation hints, and contract-owned `Other`. | VERIFIED | Group actions carry `question_id`, `user_prompt`, `answer_type`, `choices`, `allow_other`, and validation hints in `app/services/reasoning_service.py:629-760`; strict schema coverage is asserted in `tests/test_reasoning_contract.py:274-300`. |
| 3 | Source-origin clarification is asked only for materially ambiguous foods and uses the five-state contract. | VERIFIED | Source-origin actions are appended only when `_needs_source_origin_question(...)` is true and they use the fixed five-state choice set in `app/services/reasoning_service.py:711-760`; ordering and choice values are verified in `tests/test_reasoning_gate.py:929-990`. |
| 4 | Telegram renders deterministic clarification prompts from stored question contract, with Telegram-safe callback payloads. | VERIFIED | Callback payloads use compact hashed tokens in `bot/callback_data.py:19-32`; deterministic prompt/callback handling is in `bot/handlers.py:546-640`; the review-fix commit `e4b9764` is present in `HEAD` and the prior callback-size failure is documented in `04.3-UAT-RESULTS.md:31-51`. |
| 5 | Every callback or text answer maps back to a stable `question_id` and persists durable structured answers. | VERIFIED | Stable question/choice IDs are normalized and persisted in `app/services/interview_service.py:1385-1496`, `app/services/interview_service.py:1692-1772`, and `bot/handlers.py:569-640`; tests cover compact-token resolution and persisted answer state in `tests/test_bot_contract.py:880-1114`. |
| 6 | Partial replies re-prompt only the remaining required questions without losing prior answers. | VERIFIED | Remaining-question calculation is driven by `remaining_required_question_ids` in `app/services/interview_service.py:1371-1381`, `app/services/interview_service.py:1724-1744`; bot rerender behavior is exercised in `tests/test_bot_contract.py:445-728`. |
| 7 | `AFFIRMATION` `No` deterministically becomes a same-group free-text correction step and does not loop back through broad confirmation editing. | VERIFIED | `No` answers insert a correction question in `app/services/interview_service.py:1712-1721` and `_build_affirmation_correction_question(...)` in `app/services/interview_service.py:1775-1800`; the live callback path is covered in `tests/test_bot_contract.py:840-878`. |
| 8 | The resolver is final-confirmation-only and cannot run before required deterministic answers are complete. | VERIFIED | `run_interview_turn(...)` rejects resolver execution while `remaining_required_question_ids` are non-empty in `app/services/interview_turn_manager.py:44-49`; bot routing only resolves once no deterministic questions remain in `bot/handlers.py:520-543`. |
| 9 | Final confirmation still uses the authoritative write path, including degraded grounding policy and `FoodVisual` creation for confirmed items. | VERIFIED | Finalization calls `apply_final_meal_resolution(...)` in `app/services/interview_service.py:809-867`; degraded grounding enters `DEGRADED_SAVED` / `HOLD_REQUIRED` in `bot/polling.py:381-430`; `FoodVisual` rows are created from confirmed segments in `app/services/meal_resolution_service.py:503-531`, with regression coverage in `tests/test_match_flow.py:805-960`. |
| 10 | Live repeated-meal UAT with the saved warm checkpoint proves vector influence, deterministic partial-reply recovery, and final save behavior. | FAILED | The runbook requires `mealttracker_uat_with_meal_embeddings` plus the `partial-match` and `no-match` harness scenarios in `04.3-UAT.md:7-18`, `04.3-UAT.md:82-181`, but the recorded artifact remains `status: in_progress` on `mealttracker_uat_042_clean` with `sample_images/IMG_4646.HEIC` in `04.3-UAT-RESULTS.md:1-18`, and it still records open `GAP-04.3-P1`/`P2`/`P3`/`P4` in `04.3-UAT-RESULTS.md:141-260`. |

**Score:** 9/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `app/services/reasoning_schema.py` | Strict grouped reasoning contract and compatibility projection | VERIFIED | Substantive schema/coercion file; passes plan artifact checks and is exercised by the green reasoning suites. |
| `app/services/reasoning_service.py` | Group-owned clarification derivation and multi-signal gate | VERIFIED | Group action builder, threshold/margin gate, and compatibility projection all present at `561-1011`. |
| `app/services/interview_service.py` | Deterministic question state, correction flow, finalization handoff | VERIFIED | Durable question state, correction insertion, and confirmation-item synthesis present at `1368-1875`. |
| `app/services/interview_turn_manager.py` | Resolver-only confirmation seam | VERIFIED | Resolver gate enforced at `34-49`; traced confirmation-only output path remains wired. |
| `bot/handlers.py` | Deterministic callback/text runtime | VERIFIED | Stable question routing, compact-token resolution, `Other`, and confirmation bypass are wired. |
| `bot/polling.py` | Explicit degraded grounding policy | VERIFIED | Failure classifier and `DEGRADED_SAVED`/`HOLD_REQUIRED` branches are live at `368-430` and `939-1008`. |
| `bot/messages.py` | User-facing grounding blocker messaging | VERIFIED | Concrete blocker copy exists and is used by the degraded policy path. |
| `scripts/embed_match_smoke.py` | Repeatable warm-state harness | VERIFIED | Dry-run spot-checks produced the required checkpoint/scenario/report shape for both named scenarios. |
| `docker-compose.uat-043-harness.yml` | Disposable 04.3 harness database wiring | VERIFIED | Plan link verification passed; dry-run output references `mealttracker_043_harness`. |
| `04.3-UAT.md` | Warm-checkpoint runbook and evidence contract | VERIFIED | The runbook explicitly requires `mealttracker_uat_with_meal_embeddings`, named samples, and report fields at `1-181`. |
| `04.3-UAT-RESULTS.md` | Recorded warm-checkpoint pass/fail evidence for the named 04.3 harness scenarios | HOLLOW | The file is substantive, but its contents describe a different 04.2-clean run and still carry unresolved Phase 04.3 gap text instead of final 04.3 harness evidence. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `app/services/reasoning_schema.py` | `app/services/reasoning_service.py` | Grouped clarification payload consumed by the gate and persistence layers | VERIFIED | `gsd-sdk query verify.key-links` passed for Plans 01 and 05. |
| `app/services/interview_service.py` | `bot/polling.py` | Kickoff/finalization payload becomes Telegram state and grounding handoff | VERIFIED | Plan 02 and 06 key-link checks passed; code paths are visible in `bot/handlers.py` and `bot/polling.py`. |
| `bot/handlers.py` | `app/services/interview_turn_manager.py` | Resolver runs only after deterministic questions are complete | VERIFIED | Resolver gate is enforced by `run_interview_turn(...)` and only reached when pending deterministic questions are exhausted. |
| `app/services/interview_service.py` | `app/services/meal_resolution_service.py` | Structured confirmation items flow through authoritative final write | VERIFIED | `finalize_confirmed_interview(...)` routes to `apply_final_meal_resolution(...)` at `app/services/interview_service.py:851`. |
| `docker-compose.uat-043-harness.yml` | `scripts/embed_match_smoke.py` | Shared disposable harness database and API target | VERIFIED | Plan 04 key-link verification passed; dry-run output references the expected target DB and API base URL. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `app/services/reasoning_service.py` | `food_groups[*].clarification_actions` | Group `top_3`, gate thresholds, source-origin heuristics | Yes | FLOWING |
| `app/services/interview_service.py` | `questions_by_id`, `answers_by_question_id`, `confirmation_items` | Persisted reasoning groups/actions and structured user answers | Yes | FLOWING |
| `bot/polling.py` | `confirmation_items`, `grounding_failure`, `grounding_status` | `meal.reasoning_state_json` plus authoritative finalization | Yes | FLOWING |
| `04.3-UAT-RESULTS.md` | Warm-checkpoint live evidence | Intended source is the 04.3 harness runbook and JSON reports | No | STATIC - the artifact exists, but it documents the wrong run and stale gaps instead of the required 04.3 harness outcome |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Scoped 04.3 regression suites stay green | `/Users/mali/Documents/Projects/MealTracker/.venv/bin/python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_match_flow tests.test_interview_schema tests.test_embed_match_smoke -q` | `Ran 176 tests in 0.795s` / `OK` | PASS |
| Warm harness dry-run maps `partial-match` to the saved checkpoint and named sample | `/Users/mali/Documents/Projects/MealTracker/.venv/bin/python scripts/embed_match_smoke.py --mode uat-harness --scenario partial-match --dry-run` | JSON output shows checkpoint `mealttracker_uat_with_meal_embeddings`, target DB `mealttracker_043_harness`, sample `IMG_4583.HEIC` | PASS |
| Warm harness dry-run maps `no-match` to the saved checkpoint and named sample | `/Users/mali/Documents/Projects/MealTracker/.venv/bin/python scripts/embed_match_smoke.py --mode uat-harness --scenario no-match --dry-run` | JSON output shows checkpoint `mealttracker_uat_with_meal_embeddings`, target DB `mealttracker_043_harness`, sample `IMG_4641.HEIC` | PASS |

### Probe Execution

Step 7c: SKIPPED (no documented phase probe scripts and no conventional `scripts/*/tests/probe-*.sh` files found for Phase 04.3)

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `REASON-01` | Plans 01, 05 | When similarity is below threshold, the reasoning stage runs against the segment | SATISFIED | The grouped reasoning/request path remains live and covered by the green `tests.test_reasoning_flow` suite; Phase 04.3 preserved and extended that path rather than bypassing it. |
| `REASON-02` | Plans 01, 05 | Reasoning emits top-3 candidates with confidence/rationale | SATISFIED | Group-owned `top_3` remains mandatory and identity-choice actions derive directly from ranked `top_3` at `app/services/reasoning_service.py:561-583`; see `tests/test_reasoning_gate.py:780-827`. |
| `REASON-03` | Plans 01, 02, 05 | Multi-signal confidence gate uses margin and similarity, not self-reported confidence alone | SATISFIED | Gate logic uses threshold, top-1/top-2 margin, missing evidence, and nutrition impact in `app/services/reasoning_service.py:837-880`. |
| `REASON-04` | Plans 01, 03, 04, 05, 06 | Reasoning trace is written for audit | SATISFIED | Phase 04.3 persists grouped clarification state, structured answers, resolver payload, and grounding metadata into `reasoning_state_json` via `app/services/interview_service.py:785-807` and `app/services/interview_service.py:869-930`. |
| `INTERVIEW-01` | Plans 02, 04, 05, 06 | Low-confidence segments trigger Telegram interview | SATISFIED | Deterministic question state and Telegram handlers are present and exercised by `tests.test_bot_contract` and `tests.test_interview_flow`; the no-match/partial-match harness is wired in `scripts/embed_match_smoke.py`. |
| `INTERVIEW-03` | Plans 03, 06 | Interview answers create/update the authoritative final result path | SATISFIED | Resolver-only confirmation items route through `finalize_confirmed_interview(...)` and `apply_final_meal_resolution(...)` at `app/services/interview_service.py:809-867`. |
| `INTERVIEW-04` | Plans 02, 03, 04, 06 | Post-interview low confidence saves best-effort unverified instead of looping forever | SATISFIED | Degraded-save vs hold policy is explicit in `bot/polling.py:381-430` and regression-covered in `tests/test_match_flow.py:838-960`. |
| `MATCH-04` | Plans 03, 04, 06 | Confirmed identifications write `FoodVisual` rows so the vocabulary grows | SATISFIED | `apply_final_meal_resolution(...)` creates `FoodVisual` rows when embeddings/crops exist in `app/services/meal_resolution_service.py:503-531`; degraded-save preservation is asserted in `tests/test_match_flow.py:805-835`. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md` | 2 | `status: in_progress` in a required end-of-phase evidence artifact | BLOCKER | The phase cannot claim the warm-state repeated-meal proof is complete while the artifact still says the run is in progress. |
| `.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md` | 6 | Wrong activation database (`mealttracker_uat_042_clean`) | BLOCKER | The artifact does not verify the saved 04.3 checkpoint contract from `04.3-UAT.md`. |
| `.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md` | 141 | Stale embedded gap log (`GAP-04.3-P1` etc.) remains as the recorded result | BLOCKER | The live evidence file still documents unresolved pre-fix behavior instead of the post-fix 04.3 harness outcome. |

### Gaps Summary

The shipped codebase shows that the late 04.3 fixes, including review-fix commit `e4b9764`, are present and materially wired: grouped clarification actions are reasoning-owned, Telegram callbacks are compact-token safe, `AFFIRMATION` `No` becomes a same-group correction, source-origin preserves the five-state contract, and degraded grounding now resolves into `DEGRADED_SAVED` or `HOLD_REQUIRED` while preserving authoritative finalization.

The phase still misses a non-negotiable success criterion: the recorded live evidence artifact does not prove the final 04.3 warm-checkpoint scenarios. `04.3-UAT.md` requires the saved checkpoint `mealttracker_uat_with_meal_embeddings`, the named `partial-match` / `no-match` harness runs, trace IDs, and row-delta evidence, but `04.3-UAT-RESULTS.md` still describes a different 04.2-clean run and preserves stale gap text. That is an observable must-have failure, so the correct status is `gaps_found`, not `passed` or `human_needed`.

---

_Verified: 2026-06-01T14:50:23Z_
_Verifier: the agent (gsd-verifier)_
