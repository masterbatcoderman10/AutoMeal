---
phase: 04.1-grouped-reasoning-human-interview-correction
plan: 03
subsystem: testing
tags: [reasoning, interview, uat, postgres, telegram, smoke]
requires:
  - phase: 04.1-01
    provides: grouped reasoning persistence and grouped final-write collapse
  - phase: 04.1-02
    provides: grouped interview targeting and grouped UAT runner
provides:
  - grouped naming authority locked to reasoning or interview instead of per-crop labeling
  - nutrition impact gate no longer downgrades otherwise clear groups by itself
  - empty-db human UAT checklist for repeated-meal learning-loop verification
affects: [phase-04.1-verification, grouped-reasoning, live-uat]
tech-stack:
  added: []
  patterns:
    - detector hints stay weak context only until reasoning or interview confirms the final name
    - nutrition impact is a supporting gate signal, not a standalone downgrade trigger
key-files:
  created:
    - .planning/phases/04.1-grouped-reasoning-human-interview-correction/04.1-03-SUMMARY.md
  modified:
    - scripts/embed_match_smoke.py
    - app/services/reasoning_service.py
    - app/services/vision_service.py
    - bot/polling.py
    - tests/test_embed_match_smoke.py
    - tests/test_match_flow.py
    - tests/test_reasoning_gate.py
    - tests/test_bot_contract.py
    - .planning/phases/04.1-grouped-reasoning-human-interview-correction/04.1-HUMAN-UAT.md
key-decisions:
  - "Grouped UAT and live segmentation now persist detector hints only; per-crop label_model output is no longer a naming authority in the grouped path."
  - "Reasoning group labels override generic candidate labels during grouped final write so persisted FoodItems reflect the meal-level reasoning decision."
  - "High nutrition impact only contributes to interview escalation when another uncertainty signal already exists."
patterns-established:
  - "Reasoning-owned naming: grouped reasoning or interview chooses the persisted label, while segment metadata stays advisory."
  - "Human UAT for the learning loop must start from an empty DB and prove repeated-meal reuse, not a seeded corpus."
requirements-completed: [REASON-01, REASON-02, REASON-04, INTERVIEW-01, INTERVIEW-02]
duration: 55m
completed: 2026-05-30
---

# Phase 04.1 Plan 03: Grouped Reasoning Gap Closure Summary

**Grouped naming now stays with reasoning or interview, the nutrition gate no longer over-downgrades clear groups, and the remaining empty-DB proof is packaged as a concrete human rerun checklist.**

## Performance

- **Duration:** 55 min
- **Started:** 2026-05-30T09:09:00Z
- **Completed:** 2026-05-30T10:04:05Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Added RED coverage that fails when grouped UAT calls the standalone segment labeler or when grouped final writes ignore the reasoning label.
- Moved grouped naming authority to reasoning/interview by replacing per-crop label-model writes with detector hints in the grouped smoke path and live segmentation worker.
- Tightened `_evaluate_group_gate()` so high nutrition impact only supports an existing uncertainty signal instead of forcing chicken/pita interviews on its own.
- Replaced the diagnosis-only human UAT note with an empty-DB rerun checklist that captures DB reset, evidence queries, repeated-meal expectations, and the remaining human-only blockers.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED - lock pipeline ownership** - `aa56df5` (`test`)
2. **Task 2: GREEN - disable labeling in grouped reasoning/UAT and fix the gate** - `a7f27fd` (`fix`)
3. **Task 3: VERIFY - rerun UAT from empty DB with repeated natural meals** - `ace58c2` (`docs`)

**Plan metadata:** pending summary commit

## Files Created/Modified

- `scripts/embed_match_smoke.py` - grouped UAT now stores detector hints on segments and never calls the standalone segment label model.
- `app/services/vision_service.py` - added `segment_debug_label()` so live and smoke paths share the weak-hint behavior.
- `bot/polling.py` - live segmentation now persists detector hints directly instead of label-model names before reasoning.
- `app/services/reasoning_service.py` - nutrition impact is now additive to real uncertainty, and grouped final write input prefers the reasoning group label.
- `tests/test_embed_match_smoke.py` - locks grouped UAT against label-first naming.
- `tests/test_match_flow.py` - locks grouped final writes to the reasoning-owned label.
- `tests/test_reasoning_gate.py` - covers nutrition-impact-as-supporting-signal behavior.
- `tests/test_bot_contract.py` - updates live segmentation coverage to expect detector hints instead of label-model writes.
- `.planning/phases/04.1-grouped-reasoning-human-interview-correction/04.1-HUMAN-UAT.md` - operator checklist for the empty-DB repeated-meal rerun.

## Decisions Made

- Used the existing `MealSegment.label` field as the weak detector-hint surface rather than adding a new column in this corrective plan.
- Fixed reasoning-owned naming inside `reasoning_service.finalize_meal_from_reasoning()` so the grouped writer receives the reasoning label even when the top candidate is more generic.
- Treated the missing `tests/test_vision_flow.py` plan reference as repo drift and verified the vision-side behavior through `tests.test_embed_match_smoke` plus `tests.test_vision_service`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] The plan referenced `tests/test_vision_flow.py`, but the repo only has `tests/test_vision_service.py` and `tests/test_embed_match_smoke.py`.**
- **Found during:** Task 1
- **Issue:** The named test target does not exist, so the plan's file list could not be followed literally.
- **Fix:** Added RED coverage in `tests/test_embed_match_smoke.py` for the grouped UAT ownership change and used `tests.test_vision_service` as the closest live vision verification target.
- **Files modified:** `tests/test_embed_match_smoke.py`
- **Verification:** `rtk /tmp/mealttracker-04.1-03-venv/bin/python -m unittest tests.test_embed_match_smoke tests.test_vision_service`
- **Committed in:** `aa56df5` and `a7f27fd`

**2. [Rule 2 - Missing Critical] The live segmentation worker still wrote `segment.label` from `label_food_segment()` before reasoning.**
- **Found during:** Task 2
- **Issue:** Fixing only the grouped smoke path would have left the production grouped pipeline on the old label-first behavior.
- **Fix:** Updated `bot/polling.py` to persist only weak detector hints and extended `tests/test_bot_contract.py` accordingly.
- **Files modified:** `bot/polling.py`, `tests/test_bot_contract.py`, `app/services/vision_service.py`
- **Verification:** `rtk /tmp/mealttracker-04.1-03-venv/bin/python -m unittest tests.test_bot_contract tests.test_vision_service`
- **Committed in:** `a7f27fd`

---

**Total deviations:** 2 auto-fixed (1 Rule 2, 1 Rule 3)
**Impact on plan:** Both deviations were required to make the grouped ownership fix real in the current repo and to keep verification aligned with the live file layout.

## Issues Encountered

- Host `python3.12` in this environment had no project dependencies, so deterministic verification was moved into a temporary isolated venv at `/tmp/mealttracker-04.1-03-venv`.
- The live empty-DB rerun could not be completed here because `.env`, `.env.langfuse`, and `docker-compose.langfuse.yml` are not present in this worktree, and the final proof requires a human to upload real meal photos and answer Telegram prompts.

## User Setup Required

Human verification is still required. Use `.planning/phases/04.1-grouped-reasoning-human-interview-correction/04.1-HUMAN-UAT.md` to:

- reset the app tables to an empty state
- upload 3-4 real meals through the normal ingest path
- answer Telegram prompts when needed
- record when repeated components begin reusing learned FoodVisuals

## Next Phase Readiness

- The code-side grouped ownership fix is ready for live verification.
- The remaining blocker is a human-run empty-DB UAT with local secrets and Telegram access.
- Do not mark Plan `04.1-03` fully complete until the repeated-meal reuse evidence is captured.

## Known Stubs

None.

## Self-Check

PASSED

- Summary file exists at `.planning/phases/04.1-grouped-reasoning-human-interview-correction/04.1-03-SUMMARY.md`
- Task commits `aa56df5`, `a7f27fd`, and `ace58c2` are present in git history
