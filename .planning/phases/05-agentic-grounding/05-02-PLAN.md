---
phase: 05-agentic-grounding
plan: 02
type: tdd
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/services/grounding_service.py
  - app/services/interview_service.py
  - app/services/meal_resolution_service.py
  - app/services/grounding_stub.py
  - bot/polling.py
  - bot/handlers.py
  - tests/test_interview_flow.py
  - tests/test_match_flow.py
  - tests/test_bot_contract.py
autonomous: true
requirements:
  - GROUND-01
  - GROUND-02
  - GROUND-03
  - INTERVIEW-03
  - INTERVIEW-04
  - MATCH-04
must_haves:
  truths:
    - "Grounding now runs inline in the existing per-group finalizer path; no `poll_post_interview_grounding` handoff is required to finish a meal."
    - "When grounding was needed but bounds, search quality, or tool execution fail, the system commits a traced best-effort result with `is_verified=false` instead of hanging or looping."
    - "The authoritative write boundary remains `apply_final_meal_resolution()`; Phase 5 only enriches the final resolutions that reach it."
    - "Grounding traces append structured plus human-readable evidence into `MealSegment.ai_reasoning` without destroying prior reasoning metadata."
  artifacts:
    - path: "app/services/interview_service.py"
      provides: "Inline per-group grounding orchestration, finalizer integration, degraded-save routing, and handoff retirement"
    - path: "app/services/grounding_service.py"
      provides: "Bounded tool loop, final strict-output pass, and trace assembly"
    - path: "app/services/meal_resolution_service.py"
      provides: "Authoritative persistence of grounded provenance, verification state, and detailed quantity/nutrition fields"
    - path: "tests/test_interview_flow.py"
      provides: "Regression coverage for inline finalization, degraded saves, and removal of handoff-only states"
  key_links:
    - from: "app/services/interview_service.py"
      to: "app/services/grounding_service.py"
      via: "Per-group finalizer input becomes bounded search/scrape/finalize orchestration"
      pattern: "_build_group_finalizer_inputs|_run_group_finalizers|trace_id"
    - from: "app/services/interview_service.py"
      to: "app/services/meal_resolution_service.py"
      via: "Grounded final resolutions flow into the unchanged authoritative save transaction"
      pattern: "apply_final_meal_resolution|FinalSegmentResolution|is_verified"
    - from: "bot/polling.py"
      to: "bot/handlers.py"
      via: "Grounding-pending worker branches and stale state transitions are removed together"
      pattern: "poll_post_interview_grounding|GROUNDING_PENDING|grounding_status"
---

<objective>
Replace the old post-interview grounding handoff with the real inline grounded finalizer.

Purpose: Wire the bounded tool loop into the existing per-group finalizer seam, preserve the current authoritative write path, and make degraded saves explicit and traceable when grounding cannot complete within policy.
Output: Inline grounding orchestration, retired handoff-only worker/state branches, and regression coverage proving meals finish cleanly with either grounded or degraded-save outcomes.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-RESEARCH.md
@.planning/phases/05-agentic-grounding/05-PATTERNS.md
@.planning/phases/05-agentic-grounding/05-01-PLAN.md
@app/services/grounding_service.py
@app/services/interview_service.py
@app/services/meal_resolution_service.py
@app/services/grounding_stub.py
@bot/polling.py
@bot/handlers.py
@tests/test_interview_flow.py
@tests/test_match_flow.py
@tests/test_bot_contract.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - reproduce inline-grounding, degraded-save, and handoff-retirement regressions</name>
  <files>tests/test_interview_flow.py, tests/test_match_flow.py, tests/test_bot_contract.py</files>
  <action>Add failing regressions for the new orchestration path. In `tests/test_interview_flow.py`, assert that confirmed groups now finalize inline through grounding rather than parking the meal in a handoff state, and that bounded failures produce a traced degraded save with `is_verified=false`. In `tests/test_match_flow.py`, assert the authoritative write path still persists grounded provenance, source details, and final quantities/nutrients through `apply_final_meal_resolution()`. In `tests/test_bot_contract.py`, capture the cleanup of `GROUNDING_PENDING`-specific behavior so stale polling/handler branches cannot remain half-alive after the inline refactor. Keep RED scope on orchestration and persistence; compose/runtime verification belongs to the next wave.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>The targeted suites fail only because the current code still relies on handoff-oriented grounding state, cannot degrade inline within Phase 5 bounds, or does not yet append grounded provenance through the authoritative write path.</acceptance_criteria>
  <done>Executable RED coverage exists for inline grounding success, bounded degraded save, and retirement of handoff-only bot behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - integrate the bounded grounding loop into finalization and retire the handoff path</name>
  <files>app/services/grounding_service.py, app/services/interview_service.py, app/services/meal_resolution_service.py, app/services/grounding_stub.py, bot/polling.py, bot/handlers.py, tests/test_interview_flow.py, tests/test_match_flow.py, tests/test_bot_contract.py</files>
  <action>Implement the real Phase 5 orchestration. Upgrade the existing per-group finalizer path in `app/services/interview_service.py` so each group can invoke the bounded grounding loop inline, receive a final strict grounded payload, append tool traces, and flow immediately into `apply_final_meal_resolution()`. Retire `poll_post_interview_grounding`, handoff-only reasoning-state builders, and `GROUNDING_PENDING` routing in `bot/polling.py` and `bot/handlers.py`; salvage only reusable failure categorization and degraded-save logic. Update `app/services/meal_resolution_service.py` as needed so grounded provenance, verification state, and detailed quantity/nutrition fields persist without creating a second save system. Reduce `app/services/grounding_stub.py` to compatibility glue or remove obsolete packaged/restaurant gating logic entirely if the inline model-discretion search loop supersedes it. Preserve trace IDs and append-only reasoning audit behavior throughout.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>Meals no longer depend on a post-interview grounding handoff: grounded groups finish inline, bounded failures commit best-effort with `is_verified=false`, traces are appended to `MealSegment.ai_reasoning`, and the authoritative final write path remains singular and green under targeted regressions.</acceptance_criteria>
  <done>Phase 5 grounding is now truly part of finalization instead of a deferred worker seam, and the old handoff-only state machine is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finalizer input -> grounding loop | User-confirmed answers and reasoning state become tool-using network behavior here |
| grounding result -> authoritative save | External search/scrape evidence becomes persisted nutrition and verification state here |
| retired worker state -> live bot flow | Obsolete interview/polling states can cause ghost behavior if not removed consistently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Integrity | `app/services/interview_service.py` | mitigate | Keep grounding inline with the existing finalizer seam so group-scoped inputs and trace IDs remain intact. |
| T-05-05 | DoS | bounded tool loop | mitigate | Enforce call caps and wall-clock timeout in app code, then degrade save explicitly instead of stalling the meal. |
| T-05-06 | Repudiation | `MealSegment.ai_reasoning` | mitigate | Append structured and human-readable tool traces with stop reasons and failure categories. |
| T-05-07 | Tampering | retired bot worker branches | mitigate | Remove `GROUNDING_PENDING` routing and handoff-only polling logic together so stale states cannot reactivate. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this plan. |
</threat_model>

<verification>
Run `rtk python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q`.
Replay one success path and one bounded-failure path through the inline finalizer during verification. The success path must finish without a grounding handoff; the failure path must save traced best-effort output with `is_verified=false`.
</verification>

<success_criteria>
The Phase 5 grounding loop is now part of the live finalization path, bounded by policy, fully traced, and incapable of leaving meals stranded in a deprecated handoff state.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-02-SUMMARY.md` when done
</output>
