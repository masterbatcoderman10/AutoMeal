---
phase: 04.4-parallel-group-finalization-lite-detect
plan: 02
type: tdd
wave: 2
depends_on:
  - 04.4-01
files_modified:
  - app/services/interview_service.py
  - app/services/interview_schema.py
  - app/services/meal_resolution_service.py
  - tests/test_interview_flow.py
  - tests/test_match_flow.py
  - tests/test_parallel_pipeline.py
autonomous: true
requirements:
  - REASON-01
  - REASON-04
  - INTERVIEW-03
  - INTERVIEW-04
  - MATCH-04
must_haves:
  truths:
    - "After deterministic clarification completes, the saved `FoodItem` and `DiaryEntry` records use dish-level final names plus clarified source/origin and quantity data instead of persisting raw confirmation fragments."
    - "Each food group is finalized only from that group's own reasoning and clarification context, so one group's correction cannot overwrite a sibling group's saved dish metadata."
    - "Finalized `PACKAGED` and `RESTAURANT` groups still enter the existing post-interview grounding handoff seam after the per-group finalizer refactor, so non-`HOME` nutrition enrichment wiring remains unchanged."
    - "Successful groups preserve visual-learning inputs through the authoritative save path, so clarified meals still create the expected `FoodVisual` rows alongside `DiaryEntry` rows."
    - "If one group's finalizer output is malformed or exhausted, that group's degraded save path is explicit and auditable while sibling groups still complete and `MealSegment.ai_reasoning` remains intact."
  artifacts:
    - path: "app/services/interview_service.py"
      provides: "Per-group input builder, semaphore-bounded finalizer fan-out, preserved non-HOME grounding handoff state, `MealSegment.ai_reasoning` preservation, `INTERVIEW_BEST_EFFORT` degraded fallback policy, and explicit mapping into the authoritative resolution contract"
    - path: "app/services/meal_resolution_service.py"
      provides: "Authoritative `ResolvedFoodInput` and `FinalSegmentResolution` contract surface plus `apply_final_meal_resolution()` field persistence for clarified dish/source/quantity/learning metadata"
    - path: "tests/test_interview_flow.py"
      provides: "Regression coverage that finalized `PACKAGED` and `RESTAURANT` groups still set `grounding_required`, `post_interview_grounding`, and the `poll_post_interview_grounding` handoff target after the refactor"
    - path: "tests/test_match_flow.py"
      provides: "Regression coverage that `apply_final_meal_resolution()` persists final name, source/origin, quantity_display/json, and portion_bucket from finalizer metadata while PACKAGED/RESTAURANT groups keep the existing post-interview grounding seam and Python-owned visual-learning flags are asserted at the authoritative handoff"
    - path: "tests/test_parallel_pipeline.py"
      provides: "Bounded concurrency coverage that proves sibling group work continues while one group is slow"
  key_links:
    - from: "MealLog.reasoning_state_json"
      to: "app/services/interview_service.py"
      via: "group-scoped finalizer input assembly"
      pattern: "food_groups|answers_by_question_id|confirmation_items"
    - from: "MealSegment.ai_reasoning"
      to: "app/services/interview_service.py"
      via: "read-only reasoning audit preservation while finalizer audit state is written elsewhere"
      pattern: "ai_reasoning"
    - from: "app/services/interview_service.py"
      to: "bot/polling.py"
      via: "`build_grounding_reasoning_state()` preserves non-HOME handoff markers and `handoff_target: poll_post_interview_grounding` after per-group finalization"
      pattern: "build_grounding_reasoning_state|poll_post_interview_grounding|grounding_handoff_pending"
    - from: "app/services/interview_service.py"
      to: "app/services/meal_resolution_service.py"
      via: "mapped `ResolvedFoodInput` and `FinalSegmentResolution` fields passed into `apply_final_meal_resolution()`"
      pattern: "ResolvedFoodInput|FinalSegmentResolution|apply_final_meal_resolution"
    - from: "app/services/meal_resolution_service.py"
      to: "DiaryEntry|FoodItem|FoodVisual"
      via: "authoritative persistence of canonical name, source_type, quantity_json/display, portion bucket, and visual-learning flags"
      pattern: "canonical_name|source_type|quantity_json|quantity_display|portion_bucket|create_food_visual|visual_learning_eligible"
---

<objective>
Insert the 04.4 per-group metadata composer into `finalize_confirmed_interview()` per D-01 through D-14 while preserving `apply_final_meal_resolution()` as the only writer per D-11 and the existing non-`HOME` grounding handoff seam for INTERVIEW-03.

Purpose: Repair lossy post-clarification saves without reopening the user interview loop, because this finalizer is metadata composition only per D-04 and must stay inside the Phase 4 authoritative transaction boundary per D-20.
Output: Group-scoped finalizer fan-out, explicit degraded fallback, authoritative final write inputs that preserve dish names/source/quantity, and unchanged PACKAGED/RESTAURANT grounding handoff wiring.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-CONTEXT.md
@.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-RESEARCH.md
@.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-PATTERNS.md
@.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-VALIDATION.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-03-SUMMARY.md
@.planning/debug/043-live-uat-three-regressions.md
@app/services/interview_service.py
@app/services/interview_schema.py
@app/services/meal_resolution_service.py
@tests/test_interview_flow.py
@tests/test_match_flow.py
@tests/test_parallel_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - capture per-group fan-out, scoping, and degraded-fallback regressions</name>
  <files>tests/test_interview_flow.py, tests/test_match_flow.py, tests/test_parallel_pipeline.py</files>
  <action>Extend the existing `unittest` suites so they fail against the current direct `confirmation_items` mapping path. Cover one finalizer call per food group per D-01/D-02; per-group input scoping that includes only the active group's reasoning, `top_3`, `group_actions`, selected clarifications, `segment_ids`, and crop refs per D-03; strict fail-closed handling for malformed finalizer JSON per D-05; explicit degraded fallback/audit state instead of silent coercion per D-14/INTERVIEW-04; and bounded concurrency that allows sibling groups to progress while one group is slow without TaskGroup-style cancellation semantics. Add authoritative contract regressions that capture the real handoff surface into `apply_final_meal_resolution()`: assert the mapped `ResolvedFoodInput.canonical_name`/aliases/source metadata plus `FinalSegmentResolution.portion_bucket`, `quantity_display`, and `quantity_json` reflect the per-group finalizer output and surviving clarification answers per D-07/D-08/D-09/D-13, while `create_food_visual` and `visual_learning_eligible` are asserted only after Python mapping at the authoritative handoff based on finalizer metadata plus code-owned crop/embedding state per Pattern 2. Add explicit non-`HOME` regression coverage for INTERVIEW-03: finalized `PACKAGED` and `RESTAURANT` groups must still preserve the existing post-interview grounding seam by setting `grounding_required`, `post_interview_grounding`, `grounding_status`, and `handoff_target: poll_post_interview_grounding` through `build_grounding_reasoning_state()`, without adding new Phase 5 tools or changing the current consumer. Add persistence assertions around the real writer seam so tests fail unless `apply_final_meal_resolution()` carries final dish name, source/origin, quantity_display/json, portion bucket, and the Python-attached learning metadata into `FoodItem`, `DiaryEntry`, and `FoodVisual` writes. Assert the resolved research decision from `04.4-RESEARCH.md` A2: degraded groups reuse existing `INTERVIEW_BEST_EFFORT` write semantics rather than a new identification method, while finalizer-specific failure category and degraded-save markers are recorded in `reasoning_state_json`. Also add regression coverage that `MealSegment.ai_reasoning` remains intact and separate from finalizer audit state per REASON-04. Follow Pattern 1 and Pattern 3 from `04.4-PATTERNS.md`, using the mapped analog files `app/services/interview_service.py`, `app/services/meal_resolution_service.py`, `bot/polling.py`, `tests/test_interview_flow.py`, `tests/test_match_flow.py`, and `tests/test_parallel_pipeline.py`.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_parallel_pipeline -q</automated>
  </verify>
  <done>The targeted suites fail because the current seam still maps raw `confirmation_items` directly into final write objects, does not prove the authoritative contract preserves clarified metadata through `apply_final_meal_resolution()`, does not prove finalized PACKAGED/RESTAURANT groups preserve the existing `poll_post_interview_grounding` handoff, and has no per-group fan-out or explicit degraded-fallback policy.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - implement group-scoped finalizer fan-out and authoritative degraded finalization</name>
  <files>app/services/interview_service.py, app/services/interview_schema.py, app/services/meal_resolution_service.py, tests/test_interview_flow.py, tests/test_match_flow.py, tests/test_parallel_pipeline.py</files>
  <action>Refactor `finalize_confirmed_interview()` to build one finalizer input per food group from `MealLog.reasoning_state_json`, deterministic clarification answers, and matching confirmation payloads per D-01/D-03/D-12. Follow Pattern 1 from `04.4-PATTERNS.md` at the `app/services/interview_service.py` seam so finalization is inserted immediately before `apply_final_meal_resolution()`, follow Pattern 2 so the model emits metadata while Python preserves code-owned write fields, and follow Pattern 3 using the `bot/polling.py` and `tests/test_parallel_pipeline.py` semaphore analog for bounded sibling-safe fan-out. Run the per-group finalizer with `asyncio.Semaphore`-bounded `gather()` fan-out per D-02, using the new strict schema/parser from plan 01 and avoiding `TaskGroup` cancellation semantics. Map successful outputs into the real authoritative handoff types, extending `ResolvedFoodInput` and `FinalSegmentResolution` in `app/services/meal_resolution_service.py` if any required D-07/D-13 field is still missing, rather than hiding rich metadata inside opaque side payloads. The model-emitted contract passed through Python mapping must preserve the dish-level final name, aliases/supporting metadata, source/origin fields, portion bucket, quantity_display, and quantity_json; then Python must attach `create_food_visual` and `visual_learning_eligible` only at the authoritative `apply_final_meal_resolution()` handoff from code-owned crop/embedding/write-context state, not from model output. Keep `apply_final_meal_resolution()` as the only writer per D-06/D-11/D-13, and update its mapping surface if needed so clarified metadata and Python-owned learning controls survive the authoritative transaction instead of being dropped before persistence. Preserve the existing non-`HOME` grounding seam for INTERVIEW-03: when finalized output still requires grounding for `PACKAGED` or `RESTAURANT`, continue routing through `build_grounding_reasoning_state()` with the existing `handoff_target: poll_post_interview_grounding`, `grounding_required`, and `post_interview_grounding` markers instead of short-circuiting those groups into a HOME-style completed save. Preserve `MealSegment.ai_reasoning` as the existing reasoning audit surface for REASON-04 and write new finalizer attempt/failure/degraded-save details only into meal/interview audit state, not back into `ai_reasoning`. When a group returns malformed JSON or exhausts the bounded retry budget, record explicit finalizer audit/failure state in `reasoning_state_json`, route that group through the existing `INTERVIEW_BEST_EFFORT` save semantics resolved in `04.4-RESEARCH.md` A2, and never invent fragment names or drop material clarification fields per D-08/D-09. Do not add grounding tools, user-facing interview prompts, a new identification method, or a second background subsystem per D-18/D-19, because this phase is only repairing final metadata quality per D-20.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_parallel_pipeline -q</automated>
  </verify>
  <done>Per-group finalization is bounded, group-scoped, fail-closed, and auditable; `MealSegment.ai_reasoning` remains intact as the reasoning trace; finalized PACKAGED/RESTAURANT groups still preserve the existing `poll_post_interview_grounding` handoff seam; degraded groups reuse `INTERVIEW_BEST_EFFORT` semantics with explicit finalizer audit state; and every successful or degraded path commits through `apply_final_meal_resolution()` with final name, source/origin, quantity_display/json, portion_bucket, and visual-learning metadata preserved in the authoritative `FoodItem`/`DiaryEntry`/`FoodVisual` writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted reasoning/interview state -> finalizer input builder | User answers and model reasoning become LLM prompt material here |
| per-group finalizer outputs -> authoritative write mapper | Untrusted model JSON is translated into DB write inputs here |
| concurrent group fan-out -> meal-scoped transaction | Parallel metadata composition feeds one shared authoritative commit boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-04 | Tampering | `app/services/interview_service.py` input builder | mitigate | Build one finalizer input per group with only that group's reasoning, candidates, clarification answers, and segment refs per D-01/D-03. |
| T-04.4-05 | Denial of Service | `app/services/interview_service.py` fan-out | mitigate | Use `asyncio.Semaphore` plus explicit result envelopes so one failed/slow group does not cancel siblings; avoid TaskGroup-style first-failure cancellation per D-02/D-14. |
| T-04.4-06 | Tampering | finalizer JSON parser/mapping | mitigate | Parse strict schema only, fail closed on malformed JSON, and record explicit failure metadata before degraded fallback per D-05/D-14. |
| T-04.4-07 | Repudiation | degraded fallback path | mitigate | Persist finalizer audit/failure details in `reasoning_state_json` and never silently coerce fragment labels into saved dish names. |
| T-04.4-08 | Integrity | `apply_final_meal_resolution()` handoff | mitigate | Keep `apply_final_meal_resolution()` as the sole writer and pass one mapped `FinalSegmentResolution` list after fan-out per D-11/D-13. |
| T-04.4-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this plan. |
</threat_model>

<verification>
Run `.venv/bin/python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_parallel_pipeline -q`.
The added regressions in `tests.test_match_flow` and `tests.test_interview_flow` must prove that the `apply_final_meal_resolution()` handoff and write path preserve the final dish name (`ResolvedFoodInput.canonical_name`), source/origin fields, `quantity_display`, `quantity_json`, `portion_bucket`, and `create_food_visual`/`visual_learning_eligible` learning metadata.
The same regressions must also prove that finalized `PACKAGED` and `RESTAURANT` groups still preserve `grounding_required`, `post_interview_grounding`, `grounding_status`, and `handoff_target: poll_post_interview_grounding` so the existing post-interview re-grounding consumer in `bot/polling.py` remains the downstream seam.
The same regressions must treat `create_food_visual` and `visual_learning_eligible` as Python-attached authoritative handoff fields per Pattern 2, not as model-emitted finalizer JSON keys.
Run the 04.4 Nyquist quick suite from `04.4-VALIDATION.md`: `.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_interview_flow -q` after this plan.
Record the live `IMG_4646.HEIC` dish-name and quantity persistence check during `$gsd-verify-work`, not by inventing UAT evidence here.
</verification>

<success_criteria>
Clarified meals no longer save raw answer fragments directly; per-group finalization is bounded, strict, auditable, preserves the existing PACKAGED/RESTAURANT post-interview grounding seam, and still terminates through the existing authoritative FoodItem/DiaryEntry/FoodVisual transaction with clarified name, source/origin, quantity, and learning metadata preserved.
</success_criteria>

<output>
Create `.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-02-SUMMARY.md` when done
</output>
