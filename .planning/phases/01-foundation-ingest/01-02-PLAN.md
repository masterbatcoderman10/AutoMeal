---
phase: 01-foundation-ingest
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/models/base.py
  - app/models/meal_log.py
  - app/models/meal_segment.py
  - app/models/food_item.py
  - app/models/food_visual.py
  - app/models/diary_entry.py
  - app/models/__init__.py
  - app/config.py
  - app/database.py
  - migrations/alembic.ini
  - migrations/env.py
  - migrations/versions/001_initial_schema.py
autonomous: true
requirements: [MATCH-01, INFRA-03]
user_setup: []
must_haves:
  truths:
    - "Vector(1536) on both embedding columns — NOT vector(1024)"
    - "HNSW index m=16, ef_construction=64 on food_visuals.embedding — NOT IVFFlat"
    - "FAILED in MealProcessingStatus enum"
    - "TIMESTAMPTZ on all timestamps with server_default=func.now()"
    - "portion_bucket ENUM on MealSegment — NOT quantity_multiplier float"
    - "image_hash String(64) on MealLog"
    - "is_invalidated Boolean on FoodVisual"
    - "D-07: MealLog.image_url and MealSegment.cropped_image_url store absolute container paths (e.g. `/data/uploads/meals/{uuid}.jpg`)"
    - "D-13: Schema defined via SQLAlchemy 2 declarative models + Alembic migrations; models in `app/models/`; Alembic generates versioned migration files"
  artifacts:
    - "5 SQLAlchemy models + Base in app/models/"
    - "Alembic migration 001 creates full schema"
    - "Settings class in app/config.py with pydantic-settings"
    - "Async engine + session factory in app/database.py"
  key_links: []
---

<objective>
Create the complete SQLAlchemy 2 async database schema with all 7 critical corrections applied, plus Alembic migration and database connection infrastructure.
Purpose: The schema is the project's single source of truth (D-12). Getting dimensions, index types, and time types wrong here propagates as unfixable technical debt.
Output: 5 model files, Base, config.py, database.py, Alembic setup with initial migration
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-ingest/01-CONTEXT.md (D-12, D-13, D-14, locked schema requirements)
@.planning/phases/01-foundation-ingest/01-RESEARCH.md (§1 FastAPI+SQLAlchemy async, §2 Postgres+pgvector schema)
@.planning/phases/01-foundation-ingest/01-PATTERNS.md (all model patterns, TIMESTAMPTZ, Vector, HNSW)
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| SQL injection via raw migration SQL | LOW | Migration SQL only uses literal values (enum values, index params) — no user input |
| Vector dimension mismatch at runtime | HIGH | Enforce Vector(1536) consistently in both model definition and Alembic migration |
| Database credentials in image layers | MEDIUM | DATABASE_URL uses env var substitution — never hardcoded |
| Missing FAILED enum value breaks janitor (Phase 4) | HIGH | Explicitly add FAILED to MealProcessingStatus in model AND migration (D-14) |
| TIMESTAMP vs TIMESTAMPTZ causes timezone bugs | MEDIUM | Use sqlalchemy.dialects.postgresql.TIMESTAMPTZ explicitly — never DateTime |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Create config.py with pydantic-settings</name>
  <files>app/config.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/config.py pattern)</read_first>
  <action>Create app/config.py with Settings(BaseSettings) class containing:
DATABASE_URL: str, INGEST_SECRET: str, TELEGRAM_BOT_TOKEN: str, TELEGRAM_CHAT_ID: str, OPENROUTER_API_KEY: str, OPENROUTER_BASE_URL: str = "https://openrouter.ai/api/v1", UPLOADS_DIR: Path = Path("/data/uploads"), BOT_POLL_INTERVAL: float = 3.0, DEDUP_WINDOW_SECONDS: int = 60.
model_config uses ConfigDict(env_file=".env", env_file_encoding="utf-8", case_sensitive=True).
Add @lru_cache get_settings() function.</action>
  <verify>python -c "from app.config import get_settings; print('ok')" 2>/dev/null || echo "will verify after docker build"</verify>
  <acceptance_criteria>
    - "app/config.py contains 'class Settings(BaseSettings)'"
    - "app/config.py contains 'DATABASE_URL: str'"
    - "app/config.py contains 'INGEST_SECRET: str'"
    - "app/config.py contains 'case_sensitive=True'"
    - "app/config.py contains '@lru_cache'"
    - "app/config.py contains 'def get_settings'"
  </acceptance_criteria>
  <done>config.py has all 9 Settings fields with lru_cached get_settings()</done>
</task>

<task type="auto">
  <name>Task 2: Create database.py with async engine + session</name>
  <files>app/database.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/database.py pattern)</read_first>
  <action>Create app/database.py with:
- Module-level _engine: AsyncEngine | None = None
- create_engine(url, echo=False) → creates create_async_engine with pool_size=5, max_overflow=10, pool_pre_ping=True, stores in _engine
- get_engine() → returns _engine or raises RuntimeError
- get_session_factory() → returns async_sessionmaker bound to get_engine(), with expire_on_commit=False, autoflush=False, class_=AsyncSession
- get_session() async generator → yields session from factory, proper cleanup
Import from sqlalchemy.ext.asyncio: AsyncEngine, AsyncSession, create_async_engine, async_sessionmaker.</action>
  <verify>grep -q "expire_on_commit=False" app/database.py</verify>
  <acceptance_criteria>
    - "app/database.py contains 'create_async_engine'"
    - "app/database.py contains 'expire_on_commit=False'"
    - "app/database.py contains 'pool_pre_ping=True'"
    - "app/database.py contains 'async def get_session'"
    - "app/database.py contains 'def get_session_factory'"
  </acceptance_criteria>
  <done>database.py has engine factory, session factory, and async session dependency</done>
</task>

<task type="auto">
  <name>Task 3: Create Base declarative model</name>
  <files>app/models/base.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/models/base.py pattern)</read_first>
  <action>Create app/models/base.py with:
from sqlalchemy.orm import DeclarativeBase
class Base(DeclarativeBase): pass
Single definition — all models import Base from here.</action>
  <verify>grep -q "DeclarativeBase" app/models/base.py</verify>
  <acceptance_criteria>
    - "app/models/base.py contains 'class Base(DeclarativeBase)'"
  </acceptance_criteria>
  <done>Base class defined in app/models/base.py</done>
</task>

<task type="auto">
  <name>Task 4: Create MealLog model with MealProcessingStatus enum</name>
  <files>app/models/meal_log.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/models/meal_log.py pattern)</read_first>
  <action>Create app/models/meal_log.py with:
- MealProcessingStatus enum: PENDING, DETECTING, SEGMENTING, EMBEDDING, MATCHING, REASONING, INTERVIEWING, COMPLETED, FAILED — FAILED is REQUIRED (D-14)
- MealLog(Base) with __tablename__ = "meal_logs"
- Columns: id String(36) PK, image_url String(512), image_hash String(64) (SHA-256 hex, D-14), processing_status Enum(MealProcessingStatus), created_at TIMESTAMPTZ(timezone=True) server_default=func.now(), updated_at TIMESTAMPTZ(timezone=True) server_default=func.now() onupdate=func.now()
- relationship: segments → List[MealSegment] back_populates="meal_log"
- Index ix_meal_logs_image_hash_created on (image_hash, created_at) for dedup lookups
Use sqlalchemy.dialects.postgresql.TIMESTAMPTZ — NOT DateTime.</action>
  <verify>grep -q "FAILED" app/models/meal_log.py && grep -q "image_hash" app/models/meal_log.py</verify>
  <acceptance_criteria>
    - "app/models/meal_log.py contains 'FAILED = \"FAILED\"'"
    - "app/models/meal_log.py contains 'image_hash' with String(64)"
    - "app/models/meal_log.py contains 'TIMESTAMPTZ(timezone=True)'"
    - "app/models/meal_log.py contains 'server_default=func.now()'"
    - "app/models/meal_log.py contains 'ix_meal_logs_image_hash_created'"
  </acceptance_criteria>
  <done>MealLog model has FAILED enum, image_hash column, TIMESTAMPTZ timestamps, dedup index</done>
</task>

<task type="auto">
  <name>Task 5: Create MealSegment model with Vector(1536) and portion_bucket</name>
  <files>app/models/meal_segment.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/models/meal_segment.py pattern)</read_first>
  <action>Create app/models/meal_segment.py with:
- PortionBucket enum: SMALL, STANDARD, LARGE
- MealSegment(Base) with __tablename__ = "meal_segments"
- Columns: id String(36) PK, meal_log_id FK to meal_logs.id, label String(256) nullable, bounding_box JSON nullable, cropped_image_url String(512) nullable, embedding Vector(1536) nullable (CORRECTION: NOT 1024), ai_reasoning Text nullable, portion_bucket Enum(PortionBucket) nullable (CORRECTION: replaces quantity_multiplier float), created_at TIMESTAMPTZ server_default=func.now()
- relationship: meal_log back_populates="segments"</action>
  <verify>grep -q "Vector(1536)" app/models/meal_segment.py && grep -q "portion_bucket" app/models/meal_segment.py</verify>
  <acceptance_criteria>
    - "app/models/meal_segment.py contains 'Vector(1536)' (NOT Vector(1024))"
    - "app/models/meal_segment.py contains 'portion_bucket' Enum"
    - "app/models/meal_segment.py contains 'class PortionBucket'"
    - "app/models/meal_segment.py does NOT contain 'quantity_multiplier'"
    - "app/models/meal_segment.py contains 'TIMESTAMPTZ(timezone=True)'"
  </acceptance_criteria>
  <done>MealSegment model uses Vector(1536) and portion_bucket enum, no quantity_multiplier</done>
</task>

<task type="auto">
  <name>Task 6: Create FoodItem model</name>
  <files>app/models/food_item.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/models/food_item.py pattern)</read_first>
  <action>Create app/models/food_item.py with:
FoodItem(Base) with __tablename__ = "food_items"
Columns: id String(36) PK, name String(256), aliases JSON nullable, source_type String(32) nullable, brand_name String(256) nullable, restaurant_name String(256) nullable, serving_size_g Float nullable, calories Float nullable, protein_g Float nullable, carbs_g Float nullable, fat_g Float nullable, fiber_g Float nullable, times_confirmed Integer default=0, is_verified Boolean default=False, llm_reasoning Text nullable, created_at TIMESTAMPTZ server_default=func.now(), updated_at TIMESTAMPTZ server_default=func.now() onupdate=func.now()
Relationships: visuals → List[FoodVisual], diary_entries → List[DiaryEntry]</action>
  <verify>grep -q "food_items" app/models/food_item.py && grep -q "TIMESTAMPTZ" app/models/food_item.py</verify>
  <acceptance_criteria>
    - "app/models/food_item.py contains '__tablename__ = \"food_items\"'"
    - "app/models/food_item.py contains 'TIMESTAMPTZ(timezone=True)'"
    - "app/models/food_item.py contains 'times_confirmed' with default=0"
  </acceptance_criteria>
  <done>FoodItem model has nutrition columns, TIMESTAMPTZ timestamps, relationships</done>
</task>

<task type="auto">
  <name>Task 7: Create FoodVisual model with HNSW index</name>
  <files>app/models/food_visual.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/models/food_visual.py pattern — HNSW is critical)</read_first>
  <action>Create app/models/food_visual.py with:
FoodVisual(Base) with __tablename__ = "food_visuals"
Columns: id String(36) PK, food_item_id FK to food_items.id, cropped_image_url String(512), embedding Vector(1536) nullable=False (CORRECTION: NOT 1024), is_invalidated Boolean default=False (D-14), created_at TIMESTAMPTZ server_default=func.now()
__table_args__ with Index "ix_food_visuals_embedding" on embedding, postgresql_using="hnsw", postgresql_ops={"embedding": "vector_cosine_ops"}, postgresql_with={"m": 16, "ef_construction": 64}
CORRECTIONS vs TypeScript reference:
- Vector(1536) not Vector(1024)
- HNSW not IVFFlat
- is_invalidated Boolean added</action>
  <verify>grep -q "postgresql_using=\"hnsw\"" app/models/food_visual.py && grep -q "ef_construction" app/models/food_visual.py</verify>
  <acceptance_criteria>
    - "app/models/food_visual.py contains 'Vector(1536)' (NOT 1024)"
    - "app/models/food_visual.py contains 'postgresql_using=\"hnsw\"'"
    - "app/models/food_visual.py contains 'm\": 16'"
    - "app/models/food_visual.py contains 'ef_construction\": 64'"
    - "app/models/food_visual.py contains 'is_invalidated' with Boolean"
    - "app/models/food_visual.py contains 'postgresql_with='"
  </acceptance_criteria>
  <done>FoodVisual model has Vector(1536), HNSW index with m=16/ef_construction=64, is_invalidated boolean</done>
</task>

<task type="auto">
  <name>Task 8: Create DiaryEntry model</name>
  <files>app/models/diary_entry.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/models/diary_entry.py pattern)</read_first>
  <action>Create app/models/diary_entry.py with:
DiaryEntry(Base) with __tablename__ = "diary_entries"
Columns: id String(36) PK, meal_log_id FK to meal_logs.id, food_item_id FK to food_items.id, segment_id FK to meal_segments.id nullable, portion_bucket String(32), identification_method String(32), is_verified Boolean default=False, created_at TIMESTAMPTZ server_default=func.now()</action>
  <verify>grep -q "diary_entries" app/models/diary_entry.py</verify>
  <acceptance_criteria>
    - "app/models/diary_entry.py contains '__tablename__ = \"diary_entries\"'"
    - "app/models/diary_entry.py contains 'TIMESTAMPTZ(timezone=True)'"
    - "app/models/diary_entry.py contains 'identification_method'"
  </acceptance_criteria>
  <done>DiaryEntry model has FKs to meal_logs, food_items, meal_segments</done>
</task>

<task type="auto">
  <name>Task 9: Create models/__init__.py barrel</name>
  <files>app/models/__init__.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (barrel re-export pattern)</read_first>
  <action>Create app/models/__init__.py importing and re-exporting: Base, MealLog, MealProcessingStatus, MealSegment, PortionBucket, FoodItem, FoodVisual, DiaryEntry. __all__ list with all 9 symbols.</action>
  <verify>grep -q "MealProcessingStatus" app/models/__init__.py && grep -q "PortionBucket" app/models/__init__.py</verify>
  <acceptance_criteria>
    - "app/models/__init__.py imports from all 6 model submodules"
    - "app/models/__init__.py __all__ contains 'MealProcessingStatus'"
    - "app/models/__init__.py __all__ contains 'PortionBucket'"
  </acceptance_criteria>
  <done>Barrel exports all models and enums from app/models/__init__.py</done>
</task>

<task type="auto">
  <name>Task 10: Create Alembic configuration</name>
  <files>migrations/alembic.ini, migrations/env.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-RESEARCH.md (§2 Alembic Migrations)</read_first>
  <action>Create migrations/alembic.ini with:
[alembic] script_location = migrations
sqlalchemy.url = postgresql+asyncpg://mealttracker:password@localhost:5432/mealttracker (placeholder, overridden by env)

Create migrations/env.py with async migration support:
- Import asyncio, asyncpg, alembic.context
- Use run_migrations_online() with async engine from settings.DATABASE_URL
- Import all models: from app.models import Base (ensures metadata is populated)
- Configure context.configure(target_metadata=Base.metadata) for autogenerate
- Use asyncio.run(run_migrations_online()) pattern from alembic async docs</action>
  <verify>grep -q "run_migrations_online" migrations/env.py && grep -q "target_metadata" migrations/env.py</verify>
  <acceptance_criteria>
    - "migrations/alembic.ini contains 'script_location = migrations'"
    - "migrations/env.py contains 'async def run_migrations_online'"
    - "migrations/env.py imports from app.models (Base metadata)"
    - "migrations/env.py contains 'target_metadata=Base.metadata'"
    - "migrations/env.py handles async connections via asyncpg"
  </acceptance_criteria>
  <done>Alembic configured for async migrations with Base.metadata target</done>
</task>

<task type="auto">
  <name>Task 11: Create initial Alembic migration (001_initial_schema)</name>
  <files>migrations/versions/001_initial_schema.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (migration pattern), all model files just created</read_first>
  <action>Create migrations/versions/001_initial_schema.py with upgrade() function:
1. CREATE EXTENSION IF NOT EXISTS vector (MUST come first)
2. Create meal_processing_status enum via DO $$ block with all 9 values including FAILED
3. Create portion_bucket enum via DO $$ block with SMALL/STANDARD/LARGE
4. Create tables in FK order: food_items → food_visuals → meal_logs → meal_segments → diary_entries
   - Use op.create_table with correct column types: String, Float, Boolean, Integer, Text, JSON, Vector(1536), TIMESTAMPTZ, Enum
   - Foreign keys: food_visuals.food_item_id → food_items.id, meal_segments.meal_log_id → meal_logs.id, diary_entries has 3 FKs
5. HNSW index via op.execute raw SQL: CREATE INDEX ix_food_visuals_embedding ON food_visuals USING hnsw (embedding vector_cosine_ops) WITH (m=16, ef_construction=64)
6. Dedup index: op.create_index ix_meal_logs_image_hash_created on meal_logs (image_hash, created_at)
7. downgrade() drops in reverse order, then types, then extension</action>
  <verify>grep -q "hnsw" migrations/versions/001_initial_schema.py && grep -q "FAILED" migrations/versions/001_initial_schema.py</verify>
  <acceptance_criteria>
    - "Migration contains 'CREATE EXTENSION IF NOT EXISTS vector'"
    - "Migration enum includes 'FAILED' value"
    - "Migration contains 'Vector(1536)' or 'vector(1536)' for embedding columns"
    - "Migration HNSW index has 'm=16' and 'ef_construction=64'"
    - "Migration uses 'USING hnsw' (NOT 'USING ivfflat')"
    - "Migration creates 'portion_bucket' enum"
    - "Migration adds 'image_hash' column to meal_logs"
    - "Migration adds 'is_invalidated' column to food_visuals"
    - "Migration tables in correct FK order: food_items before food_visuals, meal_logs before meal_segments"
  </acceptance_criteria>
  <done>Initial migration creates full schema with all 7 corrections applied</done>
</task>
</tasks>

<verification>
- [ ] All model files contain TIMESTAMPTZ (not DateTime)
- [ ] grep -r "Vector(1536)" app/models/ returns 2 hits (food_visual + meal_segment)
- [ ] grep -r "Vector(1024)" app/models/ returns 0 hits
- [ ] MealProcessingStatus enum contains FAILED
- [ ] MealSegment uses portion_bucket enum (not quantity_multiplier)
- [ ] FoodVisual has is_invalidated boolean
- [ ] MealLog has image_hash String(64)
- [ ] FoodVisual HNSW index has m=16, ef_construction=64
- [ ] Alembic migration creates vector extension before vector columns
</verification>

<success_criteria>
- All 11 tasks completed
- MATCH-01 satisfied: Vector(1536) on food_visuals and meal_segments, HNSW index m=16/ef_construction=64
- All 7 schema corrections applied: vector(1536), HNSW, FAILED enum, portion_bucket, image_hash, is_invalidated, TIMESTAMPTZ
- Alembic migration 001 creates complete schema with correct FK ordering
- INFRA-03 satisfied: Settings class loads all config from env
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ingest/01-02-SUMMARY.md`
</output>
