---
phase: 04.2-llm-threaded-interview-orchestration
plan: 03
subsystem: api
tags: [telegram, interview, routing, unittest, langfuse, corrections]
requires:
  - phase: 04.2-llm-threaded-interview-orchestration
    provides: strict turn schema, traced interview turns, persisted Telegram prompt ids
provides:
  - RED regressions for prompt-owned meal reply routing and ambiguous concurrent interviews
  - fail-closed meal reply resolution scoped by Telegram prompt ownership
  - live 04.2 UAT checklist for bottle-gourd composition, concurrent interviews, corrections, and Langfuse evidence
affects: [04.2 verification, Telegram interview routing, correction safety]
tech-stack:
  added: []
  patterns: [prompt-owned reply routing, fail-closed ambiguous meal replies, deterministic fix-path isolation]
key-files:
  created:
    - scripts/assert_interview_routing_red.py
    - .planning/phases/04.2-llm-threaded-interview-orchestration/04.2-UAT.md
  modified:
    - bot/handlers.py
    - tests/test_bot_contract.py
    - tests/test_fix_flow.py
key-decisions:
  - "Meal reply resolution now matches Telegram reply ownership before any fallback, using `reply_to_message.message_id` against persisted prompt ids."
  - "Newest-session fallback is allowed only when there is exactly one active meal interview and no active fix interview competing for the same text."
  - "Ambiguous free-text replies fail closed with a short retry instruction instead of guessing which meal to mutate."
patterns-established:
  - "Prompt-owned meal routing: resolve by `InterviewSession.last_bot_message_id`, then by the latest bot transcript message id, then fall back only when a single meal session is active."
  - "Correction isolation: `SESSION_MODE_FIX` remains on `apply_confirmed_entry_correction()` and is covered separately from LLM meal routing."
requirements-completed: [INTERVIEW-01, INTERVIEW-02, INTERVIEW-06]
duration: 13min
completed: 2026-05-30
---

# Phase 04.2 Plan 03: LLM-Threaded Interview Routing Hardening Summary

**Telegram meal replies now resolve to the intended interview prompt, ambiguous concurrent replies fail closed, and Phase 04.2 ships with a live routing-and-trace checklist**

## Performance

- **Duration:** 13 min
- **Started:** 2026-05-30T14:31:00Z
- **Completed:** 2026-05-30T14:44:03Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments
- Added RED coverage and a dedicated harness for concurrent meal interviews, prompt-owned reply routing, and `/fix` path preservation.
- Replaced newest-session meal reply ownership with prompt-scoped resolution plus fail-closed ambiguity handling, while keeping `SESSION_MODE_FIX` on the deterministic correction branch.
- Added a live 04.2 UAT checklist covering `It's bottle gourd`, two active meal interviews, approval-candidate correction, and Langfuse trace evidence.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED - add failing routing and correction regressions for concurrent interviews** - `53860bb` (`test`)
2. **Task 2: GREEN - resolve meal replies by prompt ownership, fail closed on ambiguity, and preserve `/fix`** - `ab5ac64` (`feat`)
3. **Task 3: VERIFY - add the live 04.2 UAT checklist for natural-answer composition, concurrent interviews, and Langfuse traces** - `f9d85f9` (`docs`)

**Plan metadata:** pending summary commit

## Files Created/Modified

- `scripts/assert_interview_routing_red.py` - RED harness that only passes when the pre-hardening concurrent-routing failures are still present.
- `bot/handlers.py` - Adds prompt-owned interview resolution, explicit ambiguous-reply failure messaging, and keeps fix-mode traffic isolated from meal LLM routing.
- `tests/test_bot_contract.py` - Adds concurrent-interview routing regressions and updates text-routing tests to stub the new resolver directly.
- `tests/test_fix_flow.py` - Adds explicit coverage that `/fix` confirmation stays off the meal-turn LLM path.
- `.planning/phases/04.2-llm-threaded-interview-orchestration/04.2-UAT.md` - Operator checklist for live Telegram and Langfuse verification.

## Decisions Made

- Resolve meal text by prompt ownership first because `updated_at` ordering alone is not a safe trust boundary when two interviews are open.
- Fail closed on ambiguous free-text meal replies instead of guessing ownership, because the threat model marks stale-session mutation as a High-severity tampering risk.
- Leave `/fix` on the existing deterministic finalizer path and test that branch explicitly rather than merging it into the meal-session LLM resolver.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The handed-off worktree did not include a repo-local `.venv`, so verification used `/Users/mali/Documents/Projects/MealTracker/.venv/bin/python` while all implementation edits stayed inside the worktree.
- The targeted and full plan-level suites still emit existing expected polling-path error logs (`telegram down`, `db down`) from negative-path tests; the suites completed `OK` and no product regressions were introduced.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Concurrent meal interviews now have a deterministic routing contract for Telegram replies and a manual checklist to validate the live bot and Langfuse trace surface.
- Phase verification can now focus on real Telegram runs rather than unresolved stale-session ownership bugs.

## Verification

- `/Users/mali/Documents/Projects/MealTracker/.venv/bin/python scripts/assert_interview_routing_red.py`
- `/Users/mali/Documents/Projects/MealTracker/.venv/bin/python -m unittest tests.test_bot_contract tests.test_fix_flow -q`
- `/Users/mali/Documents/Projects/MealTracker/.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q`
- `rtk rg -n "reply_to_message|last_bot_message_id|SESSION_MODE_FIX|USER_CORRECTED" bot/handlers.py app/services/interview_service.py tests/test_bot_contract.py tests/test_fix_flow.py`
- `rtk bash -lc 'file=.planning/phases/04.2-llm-threaded-interview-orchestration/04.2-UAT.md; ...'`

## Known Stubs

None.

## Self-Check: PASSED

---
*Phase: 04.2-llm-threaded-interview-orchestration*
*Completed: 2026-05-30*
