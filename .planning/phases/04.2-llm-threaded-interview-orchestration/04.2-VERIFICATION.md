---
phase: 04.2-llm-threaded-interview-orchestration
verified: 2026-05-30T15:11:51Z
status: human_needed
score: 9/11 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 8/11 must-haves verified
  gaps_closed:
    - "On `ready_to_confirm`, the structured confirmation items flow through the existing final write-back/grounding path so FoodItems, DiaryEntries, and FoodVisuals are written by the same authoritative transaction used by Phase 4.1."
    - "Wave 0 bootstrap verification runs on supported repo runtimes and proves the targeted Phase 04.2 suites can execute before RED/GREEN work."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Run a live Telegram meal interview where the bot asks about an egg-and-vegetable curry and the user replies `It's bottle gourd`."
    expected: "The next turn stays in meal context and composes a natural item such as `egg curry with bottle gourd` instead of asking a duplicate `What should I call...` question."
    why_human: "Automated tests stub the LLM output; prompt wording and real-model composition quality are not proven by code-only verification."
  - test: "Open the Langfuse trace for a live 04.2 interview turn and inspect the recorded model call plus surrounding interview trace."
    expected: "The trace shows the interview model, authoritative prompt context, and the structured response payload for the turn."
    why_human: "The interview path uses Langfuse-wrapped OpenAI transport plus a custom span, but unlike `reasoning_service`, it does not explicitly end the span with parsed output."
  - test: "Run two live meal interviews at once and reply directly to one older prompt, then send one ambiguous free-text message without reply metadata."
    expected: "The direct reply advances only the intended meal thread, and the ambiguous free-text reply fails closed with the retry instruction."
    why_human: "Contract tests cover the routing branches, but this is still a live Telegram ownership flow."
  - test: "Run a live grouped meal where one clear approval candidate is corrected in the same reply that resolves an unclear item."
    expected: "The corrected item survives into `confirmation_items` and the meal still completes through the authoritative write path."
    why_human: "Automated tests mock the turn result and finalizer path; a full operator-visible end-to-end turn is still deferred to UAT."
---

# Phase 4.2: LLM-Threaded Interview Orchestration Verification Report

**Phase Goal:** Replace the deterministic Telegram interview continuation with an LLM-assisted interview agent that starts from grouped reasoning context, includes both unclear targets and clear auto-proposed items for approval, uses conversation history, asks natural follow-ups, determines when the answers are sufficient, and returns structured confirmation items for final write-back or post-interview grounding.
**Verified:** 2026-05-30T15:11:51Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure

## Goal Achievement

MVP-mode note: ROADMAP marks Phase 4.2 as `mode: mvp`, but the roadmap goal is not in valid user-story sentence form. User-flow coverage below is therefore derived from the roadmap success criteria plus the plan must-haves.

### User Flow Coverage

| Step | Expected | Evidence | Status |
| --- | --- | --- | --- |
| 1 | A meal interview starts from grouped reasoning context with unresolved targets, approval candidates, candidate choices, missing evidence, segment IDs, refs, and the current question. | `build_interview_turn_state()` seeds `unresolved_targets`, `approval_candidates`, `segment_refs`, and `reasoning_summary` in [app/services/interview_service.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_service.py:462), with grouped-state coverage in [tests/test_interview_flow.py](/Users/mali/Documents/Projects/MealTracker/tests/test_interview_flow.py:573). | ✓ VERIFIED |
| 2 | The bot starts and continues meal interviews through the LLM turn manager rather than the old deterministic continuation path. | Kickoff path calls `run_interview_turn()` in [bot/polling.py](/Users/mali/Documents/Projects/MealTracker/bot/polling.py:368); live-turn path calls it in [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:720); coverage in [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:305) and [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:382). | ✓ VERIFIED |
| 3 | Natural-language replies are durably recorded, routed to the active thread, and resent with transcript plus authoritative state. | User replies are appended to state and persisted rows in [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:575); transcript rebuild is in [app/services/interview_service.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_service.py:374); routing coverage is in [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:541) and [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:615). | ✓ VERIFIED |
| 4 | Invalid structured output gets one repair attempt and then fails closed without mutating confirmation state or calling the finalizer. | One repair retry in [app/services/interview_turn_manager.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_turn_manager.py:66); fail-closed bot path in [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:608); coverage in [tests/test_interview_schema.py](/Users/mali/Documents/Projects/MealTracker/tests/test_interview_schema.py:109) and [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:823). | ✓ VERIFIED |
| 5 | `ready_to_confirm` confirmations reach the authoritative save path, including grouped post-interview grounding for packaged or restaurant items. | `ConfirmationItem.segment_ids` is enforced in [app/services/interview_schema.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_schema.py:11); `run_interview_turn()` backfills authoritative group membership in [app/services/interview_turn_manager.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_turn_manager.py:209); state persistence keeps those items intact in [app/services/interview_service.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_service.py:414) and [app/services/interview_service.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_service.py:707); polling expands grouped items by every segment in [bot/polling.py](/Users/mali/Documents/Projects/MealTracker/bot/polling.py:62) and [bot/polling.py](/Users/mali/Documents/Projects/MealTracker/bot/polling.py:171); covered by [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:1212) and [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:1245). | ✓ VERIFIED |
| 6 | Concurrent meal replies are scoped by prompt ownership and `/fix` stays on its deterministic correction path. | Prompt-owned routing in [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:226); deterministic correction path preserved in [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:273); coverage in [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:541) and [tests/test_fix_flow.py](/Users/mali/Documents/Projects/MealTracker/tests/test_fix_flow.py:102). | ✓ VERIFIED |

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | When reasoning creates unresolved food groups, the interview prompt starter includes the grouped reasoning payload, candidate choices, missing evidence, segment IDs, image/crop references where available, and the current user-facing question. | ✓ VERIFIED | Grouped state assembly in [app/services/interview_service.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_service.py:462) and grouped-session assertions in [tests/test_interview_flow.py](/Users/mali/Documents/Projects/MealTracker/tests/test_interview_flow.py:573). |
| 2 | Clear/high-confidence food groups are included in the same interview turn as approval candidates, so the user can accept or correct them while answering unclear items. | ✓ VERIFIED | Approval candidates are added to state in [app/services/interview_service.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_service.py:469) and surfaced in kickoff coverage in [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:305). |
| 3 | Each Telegram user reply is appended to durable conversation history and sent to the interview LLM with unresolved targets, approval candidates, prior assistant prompts, prior user answers, and current structured state. | ✓ VERIFIED | Durable state/message persistence in [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:575) and transcript rebuild into `run_interview_turn()` in [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:720). |
| 4 | The interview LLM returns strict structured output: `continue_interview` with the next natural prompt, `need_clarification` with a targeted correction prompt, or `ready_to_confirm` with normalized confirmation items and explicit approval/correction status for every clear and unclear group. | ✓ VERIFIED | Strict contract in [app/services/interview_schema.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_schema.py:93), response-format helper in [app/services/interview_schema.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_schema.py:127), and coverage in [tests/test_interview_schema.py](/Users/mali/Documents/Projects/MealTracker/tests/test_interview_schema.py:32). |
| 5 | A reply like `bottle gourd` to an egg/vegetable curry target resolves the missing vegetable detail and composes the confirmed item name instead of advancing to a duplicate deterministic naming question. | ? UNCERTAIN | The prompt/state context is wired for this behavior, and tests exercise the downstream handling with mocked turn results in [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:692), but no automated check runs a live model against the production prompt wording. |
| 6 | The active interview is scoped to the meal/thread being answered; stale active sessions cannot steal replies from the newest meal. | ✓ VERIFIED | Reply ownership logic in [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:226) with direct and ambiguous routing coverage in [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:541) and [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:615). |
| 7 | On `ready_to_confirm`, the structured confirmation items flow through the existing final write-back/grounding path so FoodItems, DiaryEntries, and FoodVisuals are written by the same authoritative transaction used by Phase 4.1. | ✓ VERIFIED | Grouped `segment_ids` now persist through contract, state, and worker boundaries in [app/services/interview_schema.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_schema.py:17), [app/services/interview_turn_manager.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_turn_manager.py:209), [app/services/interview_service.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_service.py:428), [app/services/interview_service.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_service.py:722), and [bot/polling.py](/Users/mali/Documents/Projects/MealTracker/bot/polling.py:171). The regression covers both helper expansion and the full `poll_post_interview_grounding()` path in [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:1212) and [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:1245). |
| 8 | The interview model and fallback are explicit config keys, defaulting to a low-cost text model such as `google/gemini-3.1-flash-lite`, and Langfuse traces show the model, prompt context, and structured response. | ? UNCERTAIN | Config keys are explicit in [app/config.py](/Users/mali/Documents/Projects/MealTracker/app/config.py:23), and the LLM transport is Langfuse-wrapped in [app/services/llm_client.py](/Users/mali/Documents/Projects/MealTracker/app/services/llm_client.py:12). But unlike `reasoning_service`, the interview path does not explicitly end its custom trace span with parsed output in [app/services/interview_turn_manager.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_turn_manager.py:46) and [app/services/tracing_service.py](/Users/mali/Documents/Projects/MealTracker/app/services/tracing_service.py:121), so live trace completeness still needs inspection. |
| 9 | A malformed or contradictory interview response is repaired at most once, then fails closed without finalizer handoff and without confirmation-state mutation. | ✓ VERIFIED | Repair-then-fail-closed path in [app/services/interview_turn_manager.py](/Users/mali/Documents/Projects/MealTracker/app/services/interview_turn_manager.py:72) and [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:608), covered in [tests/test_interview_schema.py](/Users/mali/Documents/Projects/MealTracker/tests/test_interview_schema.py:177) and [tests/test_bot_contract.py](/Users/mali/Documents/Projects/MealTracker/tests/test_bot_contract.py:823). |
| 10 | The new LLM meal-interview routing does not break `/fix` corrections or the existing `USER_CORRECTED` invalidation path. | ✓ VERIFIED | Correction branch isolation in [bot/handlers.py](/Users/mali/Documents/Projects/MealTracker/bot/handlers.py:273) with regression coverage in [tests/test_fix_flow.py](/Users/mali/Documents/Projects/MealTracker/tests/test_fix_flow.py:102). |
| 11 | Manual validation has an explicit UAT checklist for bottle-gourd composition, concurrent interviews, corrections, and Langfuse trace inspection. | ✓ VERIFIED | Checklist exists in [.planning/phases/04.2-llm-threaded-interview-orchestration/04.2-UAT.md](/Users/mali/Documents/Projects/MealTracker/.planning/phases/04.2-llm-threaded-interview-orchestration/04.2-UAT.md:1). |

**Score:** 9/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `scripts/assert_phase_04_2_preflight.py` | Preflight harness for supported bootstrap runtimes | ✓ VERIFIED | Supports both Python 3.12 and 3.13 in [scripts/assert_phase_04_2_preflight.py](/Users/mali/Documents/Projects/MealTracker/scripts/assert_phase_04_2_preflight.py:26), and the command passed under the current 3.13 `.venv`. |
| `app/services/interview_schema.py` | Strict structured-output contract for interview turns | ✓ VERIFIED | `ConfirmationItem.segment_ids` is part of the strict schema and auto-normalized for grouped items. |
| `app/services/interview_turn_manager.py` | One-repair/fail-closed turn orchestration with authoritative grouped membership | ✓ VERIFIED | The manager now backfills `segment_ids` from authoritative state after parse/repair, preventing model omissions from dropping sibling segments. |
| `app/services/interview_service.py` | Authoritative grouped state scaffold and finalizer handoff persistence | ✓ VERIFIED | `apply_interview_turn_result()` persists ready-to-confirm items unchanged and `build_grounding_reasoning_state()` carries them into post-interview grounding state. |
| `bot/polling.py` | Kickoff turn, grounding handoff, and post-interview grounding continuation | ✓ VERIFIED | Post-interview grounding rebuild now indexes confirmation items by every group segment, not only one `segment_id`. |
| `tests/test_interview_schema.py` | Contract coverage for schema, repair/fail-closed behavior, and grouped segment membership | ✓ VERIFIED | Covers schema presence, `segment_ids` normalization, repair success, and fail-closed invalid output. |
| `tests/test_interview_flow.py` | Grouped-state and preflight contract coverage | ✓ VERIFIED | Includes grouped target preservation and the Python 3.13 preflight runtime check. |
| `tests/test_bot_contract.py` | Routing and ready-to-confirm orchestration coverage, including grouped grounding worker path | ✓ VERIFIED | Covers helper expansion and the full `poll_post_interview_grounding()` grouped handoff regression. |
| `tests/test_fix_flow.py` | `/fix` regression coverage | ✓ VERIFIED | Correction flow remains covered and green. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `MealLog.reasoning_state_json` | `build_interview_turn_state()` | grouped reasoning state becomes authoritative interview state | ✓ WIRED | Group IDs, segment IDs, and question metadata are read into the interview state scaffold. |
| `app/config.py` | `app/services/interview_turn_manager.py` | explicit interview model and fallback selection | ✓ WIRED | `INTERVIEW_MODEL` and `INTERVIEW_FALLBACK_MODEL` are read by `_models_to_try()`. |
| `app/services/interview_schema.py` | `app/services/interview_turn_manager.py` | strict JSON Schema response validation plus grouped segment-id enrichment | ✓ WIRED | `run_interview_turn()` parses through the strict schema and then enriches grouped membership from authoritative state. |
| `bot/polling.py` | `app/services/interview_turn_manager.py` | initial meal interview turn generation | ✓ WIRED | `_start_meal_interview_turn()` calls `run_interview_turn()` and persists the returned prompt/message id. |
| `bot/handlers.py` | `InterviewMessage` | durable transcript persistence | ✓ WIRED | User/bot turns are written both to state and persisted `InterviewMessage` rows. |
| `bot/handlers.py` | `finalize_confirmed_interview()` / `poll_post_interview_grounding()` | validated `ready_to_confirm` payload into the existing finalization path | ✓ WIRED | `ready_to_confirm` items persist into `confirmation_items`, grouped `segment_ids` survive handoff, and the worker rebuild now consumes them per segment. |
| `reply_to_message.message_id` | `InterviewSession.last_bot_message_id` | message-scoped interview resolution | ✓ WIRED | Direct replies are resolved against prompt ownership before any fallback routing. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `app/services/interview_service.py` | `unresolved_targets`, `approval_candidates`, `segment_refs`, `reasoning_summary` | `meal.reasoning_state_json` + `segments` | Yes | ✓ FLOWING |
| `app/services/interview_turn_manager.py` | `confirmation_items[*].segment_ids` | authoritative grouped state after parse/repair | Yes | ✓ FLOWING |
| `app/services/interview_service.py` | persisted `confirmation_items` during `ready_to_confirm` and grounding handoff | turn-manager output | Yes | ✓ FLOWING |
| `bot/polling.py` | per-segment grounding candidate rebuild | `meal.reasoning_state_json["confirmation_items"]` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Preflight accepts supported runtimes and runs targeted suites | `rtk ./.venv/bin/python scripts/assert_phase_04_2_preflight.py` | `Preflight passed: targeted suites executed cleanly.` | ✓ PASS |
| Strict schema / grouped membership / contract suite | `rtk ./.venv/bin/python -m unittest tests.test_interview_schema tests.test_interview_flow tests.test_bot_contract -q` | `Ran 77 tests ... OK` | ✓ PASS |
| Phase-targeted reasoning/interview/routing/fix suite | `rtk ./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q` | `Ran 97 tests ... OK` | ✓ PASS |
| Grouped confirmation helper expands to every sibling segment | `rtk ./.venv/bin/python -m unittest tests.test_bot_contract.PollingTests.test_rebuild_grounding_match_results_expands_grouped_confirmation_items -q` | `Ran 1 test ... OK` | ✓ PASS |
| Full post-interview grounding worker consumes grouped confirmations end to end | `rtk ./.venv/bin/python -m unittest tests.test_bot_contract.PollingTests.test_post_interview_grounding_worker_runs_reasoning_and_closes_completed_handoff -q` | `Ran 1 test ... OK` | ✓ PASS |
| Schema drift / ORM contract check | `rtk ./.venv/bin/python scripts/check_schema_contract.py` | `schema contract ok` | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| --- | --- | --- | --- |
| Phase probe scripts | `rtk find scripts -path '*/tests/probe-*.sh' -type f` | No conventional probe scripts declared for this phase | ? SKIP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `REASON-01` | 04.2-01 | When similarity is below threshold, reasoning runs on the cropped segment | ✓ SATISFIED | Phase 4.2 still consumes grouped reasoning output from `meal.reasoning_state_json`; no regression observed in the targeted suites. |
| `REASON-04` | 04.2-01, 04.2-02 | Reasoning trace is written for audit | ? NEEDS HUMAN | The interview path is traced, but live Langfuse evidence for model + prompt context + structured response still needs inspection. |
| `INTERVIEW-01` | 04.2-01, 04.2-02, 04.2-03 | Low-confidence segments trigger a Telegram interview session | ✓ SATISFIED | Kickoff/continue-turn tests prove the meal interview session path is live. |
| `INTERVIEW-02` | 04.2-01, 04.2-02, 04.2-03 | Interview follows the structured `InterviewMessageKey` flow | ✓ SATISFIED (intent) | 04.2 replaces deterministic continuation with a strict three-action contract while preserving authoritative meal state and prompt ownership. |
| `INTERVIEW-03` | 04.2-02 | Interview answers create/update a `FoodItem`; brands/restaurants re-ground with tools to populate nutrition | ✓ SATISFIED | Grouped `PACKAGED`/`RESTAURANT` confirmations now survive into `poll_post_interview_grounding()` and the grouped worker regression passes. |
| `INTERVIEW-06` | 04.2-03 | `USER_CORRECTED` invalidates the offending `FoodVisual` and writes a new one for the correct `FoodItem` | ✓ SATISFIED | `/fix` regression coverage remains green in [tests/test_fix_flow.py](/Users/mali/Documents/Projects/MealTracker/tests/test_fix_flow.py:102). |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `bot/handlers.py`, `app/services/interview_turn_manager.py` | `582-607`, `253-261` | The newest user reply is appended into transcript state and also supplied separately as `latest_user_text` | ⚠️ Warning | The model sees the latest reply twice each turn, which increases context weight and token usage. |
| `app/services/interview_turn_manager.py`, `app/services/tracing_service.py` | `46-86`, `121-125` | Interview trace span is not explicitly ended with parsed output the way `reasoning_service` does | ⚠️ Warning | Live trace completeness for the structured response remains a manual verification item. |
| `app/services/interview_turn_manager.py` | `25-31` | Blanket settings fallback still hides config failures behind baked-in models | ⚠️ Warning | A broken settings load can silently route interview traffic to defaults instead of failing closed. |
| `app/services/interview_service.py` | `524-535` | Meal-session reuse still returns the newest active session for the meal without checking `session_mode` | ⚠️ Warning | If a fix session and meal session ever overlap on the same meal id, reuse behavior is ambiguous. |

### Human Verification Required

### 1. Bottle Gourd Composition

**Test:** Trigger a live meal interview for an egg-and-vegetable curry group and reply `It's bottle gourd`.
**Expected:** The next turn stays in context and yields a natural composition like `egg curry with bottle gourd`, not a duplicate naming prompt.
**Why human:** The code and tests prove the state/prompt plumbing, not the live model's wording quality.

### 2. Langfuse Interview Trace

**Test:** Open the Langfuse trace for a live 04.2 interview turn.
**Expected:** The trace shows the interview model, authoritative prompt context, and the structured response payload.
**Why human:** The custom interview trace span is not explicitly ended with parsed output, so UI-level trace completeness is not proven automatically.

### 3. Concurrent Active Interviews

**Test:** Start two meal interviews, reply directly to one prompt, then send one ambiguous free-text message with no reply metadata.
**Expected:** The direct reply advances only the intended meal; the ambiguous reply produces the retry instruction and changes neither meal.
**Why human:** This is a live Telegram ownership flow even though the routing branches are unit-tested.

### 4. Approval Candidate Correction

**Test:** Run a live grouped meal where one clear approval candidate is corrected in the same reply that resolves an unclear item.
**Expected:** The corrected candidate survives into `confirmation_items` and the authoritative write path still completes the meal.
**Why human:** Automated tests mock the turn result and finalizer response rather than observing a real operator-visible interaction.

### Gaps Summary

The two prior blockers are closed. Grouped `PACKAGED` and `RESTAURANT` confirmations now carry sibling `segment_ids` through the strict interview contract, authoritative turn enrichment, persisted confirmation state, grounding handoff state, and the post-interview grounding worker. The preflight harness also now accepts both supported repo runtimes, Python 3.12 and 3.13.

Status remains `human_needed`, not `passed`, because two roadmap-level claims still depend on live observation rather than code-only evidence: natural-language composition quality for real model turns, and Langfuse trace completeness for the interview path.

---

_Verified: 2026-05-30T15:11:51Z_
_Verifier: the agent (gsd-verifier)_
