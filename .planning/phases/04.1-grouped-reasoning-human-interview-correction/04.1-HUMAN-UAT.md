---
status: diagnosed
phase: 04.1-grouped-reasoning-human-interview-correction
source:
  - 04.1-VERIFICATION.md
started: 2026-05-29T20:39:37Z
updated: 2026-05-30T09:27:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Run the grouped UAT sample with real env/secrets
expected: "`scripts/embed_match_smoke.py --mode grouped-uat --sample sample_images/IMG_4646.HEIC` returns separate `food_groups` for egg curry, chicken curry, and pita/flatbread, with only unresolved groups listed in `unresolved_group_ids` and the grouped egg-curry prompt rendered in `current_prompt`."
result: issue
reported: "Regression suite passed, and the grouped UAT sample completed in the API container, but output listed all three groups in `unresolved_group_ids` (`fg-chicken-curry`, `fg-pita-bread`, `fg-egg-vegetable-curry`) and rendered `current_prompt` as `What should I call the Chicken Curry? If my label is off, just tell me the name you normally use.` Expected only the egg curry group to be unresolved with the locked egg-curry vegetable prompt."
severity: major

### 2. Inspect the Langfuse trace for the grouped UAT run
expected: "The `meal_reasoning` trace for the reported `trace_id` includes the meal image plus crop images in input and grouped `food_groups` data in output, not a single whole-meal `top_3` race."
result: pass
evidence: "Langfuse `meal_reasoning` trace `6dd10d6aa85ed992f71c2635685930fa` for `grouped-uat-meal-e01eb66ffe6b-pass-1` includes image input, `segment_count=4`, and grouped parsed output: chicken curry and pita bread are AUTO_CONFIRM/READY_TO_WRITE; egg vegetable curry is INTERVIEW/PENDING_INTERVIEW."

## Summary

total: 2
passed: 1
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Phase 04.1 UAT should start from an empty app DB, let reasoning/interview create specific FoodItems/FoodVisuals, and prove repeated meal components use learned embeddings without relying on standalone segment labeling or seeded candidates."
  status: failed
  reason: "User reported: current UAT is confusing and too seeded. The live grouped probe uses pre-existing/unknown seeded candidates and a standalone segment labeling step before reasoning. Desired UAT is natural empty-DB flow: upload 3-4 meals, confirm/interview specific Indo-Pak food names, then observe learned embeddings activate on repeated meals."
  severity: major
  test: 1
  root_cause: "UAT and pipeline ownership are misaligned. Classification should only decide food/not-food; segmentation should only produce crops/groups; reasoning should own culturally specific food naming and FoodItem/FoodVisual creation after confirmation. Current grouped UAT still calls standalone `label_food_segment()` before reasoning and evaluates against seeded/unknown candidate state. Separately, `reasoning_service._evaluate_group_gate` downgrades otherwise AUTO_CONFIRM groups to ASK_CHOICE when `nutrition_impact > 0.40`, causing chicken/pita prompts before the true egg-curry clarification."
  artifacts:
    - path: "scripts/embed_match_smoke.py"
      issue: "Grouped UAT calls `label_food_segment()` before reasoning and uses a single seeded-style sample instead of an empty-DB repeated-meal learning loop."
    - path: "app/services/reasoning_service.py"
      issue: "_evaluate_group_gate` treats high nutrition impact as sufficient to force interview even when identity, quantity, and match consistency are high."
    - path: "app/services/interview_service.py"
      issue: "Correctly consumes pending groups, but it inherits labels/state from upstream reasoning/gate; UAT should verify reasoning/interview labels from empty DB, not standalone label output."
  missing:
    - "Disable or bypass standalone per-segment labeling as a naming authority in grouped reasoning/UAT."
    - "Make reasoning/interview produce specific user-meaningful FoodItem names for Indo-Pak cuisine before FoodVisual creation."
    - "Adjust grouped gate policy so high nutrition impact alone does not downgrade an otherwise confident AUTO_CONFIRM group."
    - "Replace seeded/single-image UAT with empty-DB repeated-meal UAT: upload 3-4 meals and verify learned embeddings trigger on repeated components."
  debug_session: "inline-2026-05-30-grouped-uat"
