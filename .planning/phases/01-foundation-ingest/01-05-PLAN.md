---
phase: 01-foundation-ingest
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - app/services/llm_client.py
  - app/__init__.py
  - app/dependencies.py
  - app/routers/__init__.py
  - app/services/__init__.py
  - app/schemas/__init__.py
  - app/database.py
  - .env
autonomous: true
requirements: [INFRA-02]
user_setup:
  - "Create Telegram bot via BotFather, set TELEGRAM_BOT_TOKEN in .env"
  - "Get Telegram chat ID, set TELEGRAM_CHAT_ID in .env"
  - "Get OpenRouter API key, set OPENROUTER_API_KEY in .env"
  - "Set INGEST_SECRET in .env"
  - "Set POSTGRES_PASSWORD in .env"
  - "Copy .env.example to .env and fill in all values"
must_haves:
  truths:
    - "Two LLM client paths: OpenAI SDK for chat/tools, httpx for multimodal embeddings"
    - "No actual LLM calls in Phase 1 — skeleton only"
    - "User must fill .env before docker compose up"
    - "OpenRouter headers: HTTP-Referer and X-Title"
  artifacts:
    - "OpenRouterClient with chat_completion + embed_multimodal methods"
    - "get_llm_client() singleton accessor"
    - "app/__init__.py and remaining __init__.py files"
    - "Populated .env file (user-created)"
  key_links: []
---

<objective>
Wire the LLM client skeleton (OpenRouter dual-path: SDK for chat, httpx for multimodal embeddings), fill remaining __init__.py files, add database.py session factory accessor, and provide the user setup checklist for filling .env.
Purpose: The LLM client skeleton is required infrastructure for Phase 2+. Remaining package wiring ensures clean imports. User setup gates the final docker compose up.
Output: llm_client.py, package __init__.py files, .env setup instructions
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-ingest/01-CONTEXT.md (LLM client skeleton note from Phase 1)
@.planning/phases/01-foundation-ingest/01-RESEARCH.md (§6 OpenRouter Client Skeleton)
@.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/services/llm_client.py pattern)
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| OpenRouter API key leaked in logs | HIGH | Key only used in client init — never logged. structlog should redact Bearer tokens |
| Multimodal embedding payload includes full image base64 | MEDIUM | Payload size ~3-5MB for typical photos; OpenRouter handles it. No additional exposure beyond normal API traffic |
| LLM client initialized without valid key | MEDIUM | get_llm_client() raises if Settings.OPENROUTER_API_KEY is empty — fail fast |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Create LLM client skeleton with dual-path routing</name>
  <files>app/services/llm_client.py</files>
  <read_first>.planning/phases/01-foundation-ingest/01-PATTERNS.md (app/services/llm_client.py pattern)</read_first>
  <action>Create app/services/llm_client.py with OpenRouterClient class:
- __init__(api_key, base_url="https://openrouter.ai/api/v1"): stores base_url, creates AsyncOpenAI(_chat_client) with api_key, base_url, max_retries=2, timeout=60.0, default_headers={"HTTP-Referer": "MealTracker", "X-Title": "MealTracker"}. Creates httpx.AsyncClient(_http) with same base_url, timeout=60.0, Authorization Bearer header, same referer/title headers.
- async chat_completion(model, messages, response_format=None, tools=None): passes to _chat_client.chat.completions.create with optional response_format and tools. Returns response.model_dump()
- async embed_multimodal(model, content, output_dimensionality=1536, task_type="RETRIEVE_DOCUMENT"): POST /embeddings with payload {model, input:[{content}], dimensions:output_dimensionality, task_type}. Returns data[0].embedding
- Module-level _client: OpenRouterClient | None = None
- get_llm_client() -> OpenRouterClient: lazy-init from settings on first call
CRITICAL: Two paths are mandatory — SDK for chat/tools, httpx for multimodal embeddings (INFRA-02).</action>
  <verify>grep -q "AsyncOpenAI" app/services/llm_client.py && grep -q "httpx.AsyncClient" app/services/llm_client.py</verify>
  <acceptance_criteria>
    - "app/services/llm_client.py contains 'class OpenRouterClient'"
    - "app/services/llm_client.py creates 'AsyncOpenAI' for chat completions"
    - "app/services/llm_client.py creates 'httpx.AsyncClient' for embeddings"
    - "app/services/llm_client.py contains 'async def chat_completion'"
    - "app/services/llm_client.py contains 'async def embed_multimodal'"
    - "app/services/llm_client.py contains 'HTTP-Referer' and 'X-Title' headers"
    - "app/services/llm_client.py contains 'def get_llm_client'"
    - "app/services/llm_client.py embed_multimodal defaults to output_dimensionality=1536"
  </acceptance_criteria>
  <done>LLM client skeleton has dual-path routing, OpenRouter headers, lazy singleton init</done>
</task>

<task type="auto">
  <name>Task 2: Fill remaining __init__.py files</name>
  <files>app/__init__.py, app/routers/__init__.py, app/services/__init__.py, app/schemas/__init__.py</files>
  <read_first>Files created in previous plans</read_first>
  <action>Create/fill remaining __init__.py files:
- app/__init__.py: empty (just package marker)
- app/routers/__init__.py: import and re-export ingest.router, health.router
- app/services/__init__.py: import and re-export image_service functions, OpenRouterClient, get_llm_client
- app/schemas/__init__.py: import and re-export IngestResponse, HealthResponse</action>
  <verify>test -f app/__init__.py && test -f app/routers/__init__.py && test -f app/services/__init__.py && test -f app/schemas/__init__.py</verify>
  <acceptance_criteria>
    - "app/__init__.py exists"
    - "app/routers/__init__.py imports ingest and health routers"
    - "app/services/__init__.py imports OpenRouterClient"
    - "app/schemas/__init__.py imports IngestResponse"
  </acceptance_criteria>
  <done>All package __init__.py files populated with re-exports</done>
</task>

<task type="auto">
  <name>Task 3: Add get_session_factory accessor to database.py</name>
  <files>app/database.py</files>
  <read_first>app/database.py (created in Plan 02)</read_first>
  <action>Add to app/database.py: a module-level function get_session_factory() that returns the async_sessionmaker bound to get_engine(). NOTE: bot/polling.py does NOT import from app.database — it initializes its own engine via create_async_engine() directly inside bot/polling.py (see Plan 04). This accessor is retained only for the API's own lifespan use.
Also add an explicit export in __all__ if not already present: ["create_engine", "get_engine", "get_session_factory", "get_session"]</action>
  <verify>grep -q "def get_session_factory" app/database.py</verify>
  <acceptance_criteria>
    - "app/database.py contains 'def get_session_factory'"
    - "app/database.py get_session_factory returns async_sessionmaker"
    - "app/database.py raises RuntimeError if engine not initialized"
  </acceptance_criteria>
  <done>database.py exposes get_session_factory() for API lifespan (bot initializes its own engine)</done>
</task>

<task type="auto">
  <name>Task 4: Preserve or create local .env from .env.example template</name>
  <files>.env</files>
  <read_first>.env.example (created in Plan 01); .planning/phases/01-foundation-ingest/01-CONTEXT.md execution_inputs</read_first>
  <action>If `.env` already exists, do not overwrite it. Validate that it contains the required keys and keep it untracked. If running inside an isolated worktree and `.env` is absent, copy the primary checkout's gitignored `.env` into the worktree as an untracked local file using:
`PRIMARY_WT=$(git worktree list --porcelain | awk '/^worktree /{print substr($0,10); exit}') && [ -f "$PRIMARY_WT/.env" ] && cp "$PRIMARY_WT/.env" .env`
If no populated `.env` exists in either place, copy `.env.example` to `.env` with placeholder values and add a comment at the top: "Fill in real values below — never commit this file." Never print, stage, or commit `.env` values.
Add SEARXNG_SECRET=changeme to `.env.example` if not already present.</action>
  <verify>test -f .env && grep -q "INGEST_SECRET" .env && git check-ignore -q .env</verify>
  <acceptance_criteria>
    - ".env file exists"
    - ".env contains 'POSTGRES_PASSWORD='"
    - ".env contains 'INGEST_SECRET='"
    - ".env contains 'TELEGRAM_BOT_TOKEN='"
    - ".env contains 'TELEGRAM_CHAT_ID='"
    - ".env contains 'OPENROUTER_API_KEY='"
    - ".env contains 'SEARXNG_SECRET='"
    - ".env is in .gitignore"
    - ".env was not staged or committed"
  </acceptance_criteria>
  <done>.env exists with required keys and remains gitignored/uncommitted</done>
</task>

<task type="auto">
  <name>Task 5: Create .env.example with SEARXNG_SECRET</name>
  <files>.env.example</files>
  <read_first>.env.example (from Plan 01, Task 4)</read_first>
  <action>Update .env.example to include SEARXNG_SECRET=changeme alongside the existing 6 variables. The docker-compose searxng service uses ${SEARXNG_SECRET:-changeme} so this env var should be documented.</action>
  <verify>grep -q "SEARXNG_SECRET" .env.example</verify>
  <acceptance_criteria>
    - ".env.example contains 'SEARXNG_SECRET='"
    - ".env.example still contains all 6 original env vars"
  </acceptance_criteria>
  <done>.env.example includes SEARXNG_SECRET (7 total env vars)</done>
</task>
</tasks>

<verification>
- [ ] python -c "from app.services.llm_client import OpenRouterClient, get_llm_client; print('LLM client imports OK')"
- [ ] python -c "from app.routers import ingest, health; print('Routers import OK')"
- [ ] python -c "from app.models import MealLog, FoodVisual, MealProcessingStatus; print('Models import OK')"
- [ ] python -c "from app.config import get_settings; s = get_settings(); print(f'DB URL: {s.DATABASE_URL[:20]}...')"
- [ ] docker compose config validates
- [ ] docker compose up -d starts postgres, api, bot, searxng, firecrawl-api
- [ ] docker compose ps shows all services running (postgres, api, bot, searxng, firecrawl-api)
- [ ] curl http://localhost:8000/health → 200
- [ ] curl -X POST -F picture=@test.jpg http://localhost:8000/ingest/photo → 401
- [ ] curl -X POST -F picture=@test.jpg -H "X-Ingest-Secret: <correct>" http://localhost:8000/ingest/photo → 202
</verification>

<success_criteria>
- All 5 tasks completed
- INFRA-02 satisfied: OpenRouter client with dual-path routing (SDK for chat, httpx for multimodal embeddings)
- LLM client skeleton ready for Phase 2 (no actual LLM calls yet)
- All imports resolve cleanly across packages
- User can fill .env and run docker compose up to start full stack
- Phase 1 complete: full infrastructure stack with correct schema, ingest endpoint, and Telegram bot
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ingest/01-05-SUMMARY.md`
</output>
