---
phase: 04.5-source-aware-clarification-expansion
plan: 03
type: tdd
wave: 3
depends_on:
  - 04.5-02
files_modified:
  - app/services/reasoning_schema.py
  - app/services/interview_service.py
  - tests/test_reasoning_contract.py
  - tests/test_interview_flow.py
  - tests/test_bot_contract.py
  - tests/test_match_flow.py
autonomous: true
gap_closure: true
requirements:
  - REASON-01
  - REASON-04
  - INTERVIEW-01
  - INTERVIEW-03
  - INTERVIEW-04
  - MATCH-04
must_haves:
  truths:
    - "Live grouped reasoning payloads that persist malformed legacy `source_question_policy = DETERMINISTIC_ONLY` are repaired to the supported deterministic policy implied by the group's existing trigger metadata, so generic-source capture is restored without flattening strong learned-source affirmation cases into the wrong path (per D-01, D-03, D-08, D-09)."
    - "After a bread `Other` free-text answer, the deterministic interview inserts `group-*:source_origin` before quantity/finalization whenever the repaired source policy requires source capture (per D-01, D-03)."
    - "If the same malformed legacy policy appears on a group whose selected identity still has strong learned-source consensus, the repaired flow preserves post-identity source affirmation and its existing correction path instead of coercing that group into the generic-source branch (per D-08, D-09, D-13)."
    - "Every rendered `SOURCE_ORIGIN` prompt comes from the canonical deterministic builder with the full option set: homemade, store bought, packaged, restaurant, and not sure (per D-03, D-06)."
    - "The repaired bread/source answer set still reaches the existing grouped finalizer/save seam with stable `segment_ids` and authoritative source fields, so the 04.4 downstream learning path stays intact (per D-06, D-14, D-15)."
    - "Legacy or unsupported deterministic-only source-policy strings are handled explicitly at the schema boundary instead of being preserved unchanged into live interview state (per D-03, D-15)."
  artifacts:
    - path: "app/services/reasoning_schema.py"
      provides: "Schema-level normalization or repair for legacy `DETERMINISTIC_ONLY` source-policy values before grouped interview state consumes them, including preservation of supported learned-source affirmation semantics when the group metadata warrants it"
    - path: "app/services/interview_service.py"
      provides: "Deterministic post-identity source-question insertion and post-identity affirmation preservation using the repaired source policy plus canonical source prompt generation"
    - path: "tests/test_reasoning_contract.py"
      provides: "Regression coverage for legacy source-policy normalization, generic-source repair, and affirmation-preserving grouped contract behavior"
    - path: "tests/test_interview_flow.py"
      provides: "Bread `Other` regression coverage plus malformed-legacy learned-source affirmation state-machine coverage"
    - path: "tests/test_bot_contract.py"
      provides: "Telegram rerender coverage proving the repaired source question appears next with the full canonical option set and no `Other`"
    - path: "tests/test_match_flow.py"
      provides: "Finalizer/save seam regression coverage proving repaired source-policy answers still preserve stable `segment_ids` and authoritative source detail fields downstream"
  key_links:
    - from: "app/services/reasoning_schema.py"
      to: "app/services/interview_service.py"
      via: "normalized `source_question_policy` on grouped reasoning payloads"
      pattern: "source_question_policy|DETERMINISTIC_ONLY|ask_generic|ask_affirmation"
    - from: "app/services/interview_service.py"
      to: "tests/test_interview_flow.py"
      via: "_apply_clarification_answer / _next_dynamic_question / _build_post_identity_source_question / _build_source_origin_question"
      pattern: "IDENTITY|SOURCE_ORIGIN|AFFIRMATION|question_order|source_question_policy"
    - from: "app/services/interview_service.py"
      to: "tests/test_bot_contract.py"
      via: "deterministic current prompt and callback/text rerender"
      pattern: "current_question|choices|allow_other|SOURCE_ORIGIN"
    - from: "app/services/interview_service.py"
      to: "tests/test_match_flow.py"
      via: "grouped confirmation_items / finalizer input assembly"
      pattern: "confirmation_items|segment_ids|source_type|source_origin_state"
---

<objective>
Close the diagnosed 04.5 clean-rerun regression where live grouped reasoning persisted `source_question_policy = DETERMINISTIC_ONLY` and the deterministic interview skipped or misrouted source handling after clarification answers.

Purpose: Repair legacy deterministic-only source-policy values before interview state is built, then prove the repaired policy preserves the correct deterministic branch for both generic-source insertion and learned-source affirmation while keeping the finalizer seam intact.
Output: Focused RED/GREEN coverage for `DETERMINISTIC_ONLY` normalization, deterministic post-identity source insertion, affirmation-preserving repair, canonical source prompt rendering, and grouped finalizer-handoff persistence.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-CONTEXT.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-RESEARCH.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-PATTERNS.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-UAT.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-VALIDATION.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-01-SUMMARY.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-02-SUMMARY.md
@app/services/reasoning_schema.py
@app/services/interview_service.py
@tests/test_reasoning_contract.py
@tests/test_interview_flow.py
@tests/test_bot_contract.py
@tests/test_match_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - reproduce the live `DETERMINISTIC_ONLY` policy regression</name>
  <read_first>
    - .planning/phases/04.5-source-aware-clarification-expansion/04.5-UAT.md
    - .planning/phases/04.5-source-aware-clarification-expansion/04.5-02-SUMMARY.md
    - app/services/reasoning_schema.py
    - app/services/interview_service.py
    - tests/test_reasoning_contract.py
    - tests/test_interview_flow.py
    - tests/test_bot_contract.py
    - tests/test_match_flow.py
  </read_first>
  <files>tests/test_reasoning_contract.py, tests/test_interview_flow.py, tests/test_bot_contract.py, tests/test_match_flow.py</files>
  <behavior>
    - Test 1: A grouped reasoning payload carrying malformed legacy `source_question_policy = DETERMINISTIC_ONLY` is repaired into the supported deterministic generic-source policy only when the group's trigger metadata indicates a generic-source branch, instead of preserving the raw string unchanged (per D-01, D-03).
    - Test 2: A malformed legacy `DETERMINISTIC_ONLY` payload whose selected identity still has strong learned-source consensus is repaired into the supported affirmation path instead of being flattened into the generic-source branch, so post-identity source affirmation remains available (per D-08, D-09, D-13).
    - Test 3: After a bread `IDENTITY` question is answered through the `Other` free-text path, `group-*:source_origin` is inserted immediately before quantity/finalization when the repaired source policy requires source capture, yielding `group-1:identity -> group-1:identity (free text) -> group-1:source_origin -> group-1:quantity` for the diagnosed bread replay (per D-01, D-03).
    - Test 4: The inserted source question is built from the canonical full source option set: `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, still suppresses `Other` in Telegram rendering, and the repaired answer set keeps stable `segment_ids` plus authoritative source fields in the existing grouped finalizer/save handoff (per D-03, D-06, D-14).
  </behavior>
  <action>Write failing regressions that mirror the diagnosed live state from `04.5-UAT.md` without overfitting the repair to bread-only generic-source behavior. Add contract coverage in `tests/test_reasoning_contract.py` for two malformed legacy inputs: one where existing trigger metadata requires the supported generic-source branch, and one where selected-identity learned-source consensus should still surface the supported affirmation branch instead of being coerced to generic source. Add `tests/test_interview_flow.py` state-machine coverage proving the bread path remains `IDENTITY -> free_text -> SOURCE_ORIGIN -> QUANTITY` for the repaired generic-source case, specifically `group-1:identity -> group-1:identity (free text) -> group-1:source_origin -> group-1:quantity`, and that the affirmation-preserving case still produces the post-identity affirmation/correction flow. Extend `tests/test_bot_contract.py` so the repaired generic-source prompt rerenders via the canonical full source-option builder with no `Other`. Restore one downstream regression in `tests/test_match_flow.py` proving the repaired bread/source answer set still reaches the existing 04.4 grouped finalizer/save seam with stable `segment_ids`, `source_type`, and source detail fields. Keep the RED scope on malformed-legacy repair behavior plus the already-approved downstream seam; do not reopen broader 04.5 heuristics, new brand/restaurant UX, or Phase 5 grounding.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_reasoning_contract tests.test_interview_flow tests.test_bot_contract tests.test_match_flow -q</automated>
  </verify>
  <acceptance_criteria>The targeted suites fail only because malformed legacy `DETERMINISTIC_ONLY` values are still preserved, misrepaired into the wrong supported branch, still allow the bread `Other` free-text path to skip deterministic source insertion, or drop repaired source fields at the finalizer/save seam.</acceptance_criteria>
  <done>Executable RED coverage exists for the clean-rerun bread regression, the malformed-legacy learned-affirmation branch, and the existing grouped finalizer-handoff seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - repair legacy source-policy values and keep source capture in deterministic order</name>
  <read_first>
    - .planning/phases/04.5-source-aware-clarification-expansion/04.5-CONTEXT.md
    - .planning/phases/04.5-source-aware-clarification-expansion/04.5-RESEARCH.md
    - app/services/reasoning_schema.py
    - app/services/interview_service.py
    - tests/test_reasoning_contract.py
    - tests/test_interview_flow.py
    - tests/test_bot_contract.py
    - tests/test_match_flow.py
  </read_first>
  <files>app/services/reasoning_schema.py, app/services/interview_service.py, tests/test_reasoning_contract.py, tests/test_interview_flow.py, tests/test_bot_contract.py, tests/test_match_flow.py</files>
  <action>Implement a focused compatibility repair for the diagnosed regression. In `app/services/reasoning_schema.py`, repair the legacy live value `DETERMINISTIC_ONLY` into the supported deterministic policy implied by the group's existing metadata instead of blindly coercing every malformed legacy value to generic source: generic-source trigger conditions should normalize to `ask_generic`, while selected-identity groups with compatible strong learned-source consensus should normalize to the supported affirmation/defer path that `interview_service.py` already understands per D-08 and D-09. Make any truly unsupported deterministic-only alias handling explicit rather than silently preserving raw strings. In `app/services/interview_service.py`, consume the repaired source policy so `IDENTITY`-origin `Other` free-text answers queue `group-*:source_origin` before quantity/finalization for the generic-source case, specifically preserving the diagnosed `group-1:identity -> group-1:identity (free text) -> group-1:source_origin -> group-1:quantity` order, and the learned-consensus case still preserves post-identity source affirmation plus the existing correction flow per D-03 and D-13, while preserving current `question_order`, `pending_question_ids`, `remaining_required_question_ids`, and partial-answer recovery. Route every rendered `SOURCE_ORIGIN` question through `_build_source_origin_question()` so the canonical full choice set is authoritative and reduced live payloads cannot leak into Telegram rendering per D-03 and D-06. Restore and keep green one downstream `tests/test_match_flow.py` assertion proving the repaired source answers still reach the existing grouped finalizer/save seam with stable `segment_ids` and authoritative source fields per MATCH-04. Keep the scope tight: do not reopen broader 04.5 heuristics, new brand/restaurant UX, Phase 5 grounding, or Phase 4.4 finalizer ownership.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_reasoning_contract tests.test_interview_flow tests.test_bot_contract tests.test_match_flow -q</automated>
  </verify>
  <acceptance_criteria>The live-regression path now repairs malformed `DETERMINISTIC_ONLY` values into the correct supported deterministic branch, inserts `SOURCE_ORIGIN` after bread `Other` free text when generic-source capture is required, preserves learned-source affirmation when consensus is strong, renders the canonical full-option source prompt deterministically, and keeps repaired source answers intact at the grouped finalizer/save seam.</acceptance_criteria>
  <done>The diagnosed 04.5 clean-rerun regression is closed at the schema/interview seam without collapsing affirmation-capable legacy groups into the wrong branch or regressing downstream finalizer handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reasoning payload -> grouped interview state | Legacy source-policy strings become user-visible question ordering here |
| callback/text answer -> grouped clarification state | User free text and button answers become authoritative source fields here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-08 | Integrity | `app/services/reasoning_schema.py` | mitigate | Repair `DETERMINISTIC_ONLY` at schema normalization into the correct supported branch so unsupported source-policy strings cannot silently bypass deterministic source capture or flatten learned-source affirmation into the wrong path. |
| T-04.5-09 | Tampering | `app/services/interview_service.py` | mitigate | Insert source questions and preserve affirmation follow-ups through the same deterministic `question_order` state after free-text identity answers so required source capture cannot be skipped by path shape. |
| T-04.5-10 | Tampering | deterministic source prompt inventory | mitigate | Canonicalize every `SOURCE_ORIGIN` prompt through `_build_source_origin_question()` so reduced or model-authored option sets cannot reach Telegram rendering. |
| T-04.5-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this plan. |
</threat_model>

<verification>
Run `rtk python -m unittest tests.test_reasoning_contract tests.test_interview_flow tests.test_bot_contract tests.test_match_flow -q`.
Then rerun the targeted 04.5 regression suite from `04.5-VALIDATION.md`.
During `$gsd-verify-work`, replay the diagnosed bread path from `04.5-UAT.md` and confirm the live prompt sequence becomes `group-1:identity -> group-1:identity (free text) -> group-1:source_origin -> group-1:quantity`, while the existing ambiguous-distribution affirmation path from `04.5-VALIDATION.md` still reaches the same grouped finalizer/save seam.
</verification>

<success_criteria>
The clean-rerun `DETERMINISTIC_ONLY` regression is closed: malformed legacy source-policy values no longer slip through unchanged or collapse affirmation-capable groups into the wrong branch, bread `Other` free-text answers cannot bypass deterministic source capture, and the repaired source answers still reach the existing grouped finalizer/save seam intact.
</success_criteria>

<output>
Create `.planning/phases/04.5-source-aware-clarification-expansion/04.5-03-SUMMARY.md` when done
</output>
