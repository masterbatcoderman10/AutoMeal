---
phase: 05-agentic-grounding
plan: 03
type: tdd
wave: 3
depends_on:
  - 05-02
files_modified:
  - docker-compose.yml
  - searxng/settings.yml
  - .env.example
  - app/config.py
  - app/models/meal_segment.py
  - app/models/diary_entry.py
  - app/services/meal_resolution_service.py
  - migrations/versions/phase5_grounding_quantity.py
  - tests/test_grounding_service.py
  - tests/test_interview_flow.py
  - tests/test_match_flow.py
autonomous: true
requirements:
  - GROUND-01
  - GROUND-02
  - GROUND-03
  - MATCH-04
must_haves:
  truths:
    - "The compose/runtime stack actually supports the chosen search architecture in this repo: official Firecrawl `/search` with SearXNG JSON enabled, not just an abstract client contract."
    - "Phase 5 verification explicitly proves allowlist enforcement, timeout/call-cap exits, and sequential scrape resource stability on the target deployment shape."
    - "The obsolete portion-bucket persistence can be removed cleanly only if the grounded finalizer and authoritative write path already preserve the detailed quantity model required by Phase 5 context."
    - "If official Firecrawl search fails verification in this environment, the codebase has a documented fallback plan rather than silently drifting to an unsupported stack."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Verified official Firecrawl service shape and env wiring for `/search` plus `/scrape`"
    - path: "searxng/settings.yml"
      provides: "JSON-enabled SearXNG configuration suitable for Firecrawl-backed search"
    - path: "migrations/versions/phase5_grounding_quantity.py"
      provides: "Clean quantity-model migration away from obsolete portion-bucket persistence when Phase 5 save-path support is ready"
    - path: "tests/test_grounding_service.py"
      provides: "Runtime-oriented regression coverage for search availability probes, timeout exits, and allowlist behavior"
  key_links:
    - from: "docker-compose.yml"
      to: "searxng/settings.yml"
      via: "Firecrawl search backend depends on SearXNG JSON output being enabled"
      pattern: "SEARXNG_ENDPOINT|formats|json"
    - from: "app/models/meal_segment.py"
      to: "app/models/diary_entry.py"
      via: "Detailed quantity model and grounded provenance replace obsolete portion-bucket persistence together"
      pattern: "quantity_json|quantity_display|portion_bucket"
    - from: "migrations/versions/phase5_grounding_quantity.py"
      to: "app/services/meal_resolution_service.py"
      via: "Schema migration must match authoritative write-path fields exactly"
      pattern: "DiaryEntry|MealSegment|quantity|provenance"
---

<objective>
Finish Phase 5 with the real runtime wiring, migration cleanup, and verification gates that prove the chosen stack behaves on this machine.

Purpose: Wire the compose stack to the official Firecrawl search backend, enable SearXNG JSON output, remove obsolete quantity persistence only after the new model is live, and add the verification surfaces that guard against self-host search drift and resource instability.
Output: Compose/config changes, quantity-model cleanup, and runtime-oriented regressions plus verification steps that make Phase 5 shippable instead of merely plausible.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-RESEARCH.md
@.planning/phases/05-agentic-grounding/05-PATTERNS.md
@.planning/phases/05-agentic-grounding/05-02-PLAN.md
@docker-compose.yml
@searxng/settings.yml
@.env.example
@app/config.py
@app/models/meal_segment.py
@app/models/diary_entry.py
@app/services/meal_resolution_service.py
@tests/test_grounding_service.py
@tests/test_interview_flow.py
@tests/test_match_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - capture compose/runtime and quantity-migration regressions</name>
  <files>tests/test_grounding_service.py, tests/test_interview_flow.py, tests/test_match_flow.py</files>
  <action>Add failing regressions for the final runtime and persistence surface. Cover the chosen official Firecrawl search configuration, including one probe that expects search results to be valid scrape candidates and one probe that proves fabricated URLs are rejected even when search is enabled. Add a regression that models a bounded timeout/call-cap exit and requires a clean degraded-save stop reason. If the Phase 5 quantity model is now authoritative, add persistence regressions proving detailed quantity fields survive through `MealSegment`/`DiaryEntry` without relying on obsolete portion-bucket state. Keep RED scope on deployable runtime assumptions and persistence cleanup; broader UX polish is out of scope.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_grounding_service tests.test_interview_flow tests.test_match_flow -q</automated>
  </verify>
  <acceptance_criteria>The targeted suites fail only because compose/runtime assumptions, final verification probes, or quantity-model cleanup are not yet fully wired to the chosen Phase 5 architecture.</acceptance_criteria>
  <done>Executable RED coverage exists for official Firecrawl search availability, allowlist-safe scraping, bounded loop exits, and the final quantity persistence shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - wire official Firecrawl search runtime, cleanup persistence, and lock verification</name>
  <files>docker-compose.yml, searxng/settings.yml, .env.example, app/config.py, app/models/meal_segment.py, app/models/diary_entry.py, app/services/meal_resolution_service.py, migrations/versions/phase5_grounding_quantity.py, tests/test_grounding_service.py, tests/test_interview_flow.py, tests/test_match_flow.py</files>
  <action>Implement the final runtime and cleanup wave. Update `docker-compose.yml` and `searxng/settings.yml` so the repo's default Phase 5 stack actually runs official Firecrawl `/search` backed by SearXNG JSON output, and document the required env keys in `.env.example`. If the new detailed quantity model is already live from prior waves, remove obsolete portion-bucket persistence with a matching migration and authoritative write-path updates; if a field must remain for compatibility, keep it as a derived compatibility shim rather than the source of truth. Add verification-oriented tests and any small support code needed to exercise search availability, allowlist-safe scraping, bounded exits, and detailed quantity persistence. Explicitly document the runtime fallback: if official Firecrawl search verification fails in this stack, Phase 5 must switch to direct `searxng_search` + Firecrawl scrape rather than `firecrawl-simple` pretending to support `/search`.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_grounding_service tests.test_interview_flow tests.test_match_flow -q</automated>
    <manual>rtk docker compose up -d</manual>
    <manual>rtk docker compose exec app python -m pytest tests/test_grounding_service.py -q</manual>
  </verify>
  <acceptance_criteria>The checked-in runtime defaults now match the researched architecture, SearXNG JSON search is enabled for Firecrawl, bounded grounding exits are testable, obsolete quantity persistence is cleaned up or reduced to compatibility-only behavior, and Phase 5 has an explicit runtime fallback if official Firecrawl search is unstable.</acceptance_criteria>
  <done>Phase 5 is now planned as a shippable runtime slice with verified search wiring, bounded degradation, and persistence cleanup aligned to the new grounded finalizer path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compose env -> networked search stack | Firecrawl and SearXNG configuration becomes live external lookup behavior here |
| migrated DB schema -> authoritative save path | Quantity/provenance persistence must stay aligned with the final write transaction here |
| runtime verification -> shipping decision | Search support on paper becomes trusted only after live probes pass here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Availability | `docker-compose.yml` / Firecrawl search stack | mitigate | Use official Firecrawl search wiring by default and require live search probes before closeout. |
| T-05-09 | Tampering | `searxng/settings.yml` | mitigate | Enable JSON explicitly so Firecrawl-backed search cannot silently degrade into unusable responses. |
| T-05-10 | Integrity | DB migration / write path | mitigate | Match any quantity-model cleanup to the authoritative save path and prove it with persistence regressions. |
| T-05-11 | Repudiation | verification process | mitigate | Capture explicit probes for allowlist rejection, bounded exits, and sequential scrape stability before phase completion. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this plan. |
</threat_model>

<verification>
Run `rtk python -m unittest tests.test_grounding_service tests.test_interview_flow tests.test_match_flow -q`.
Then, during `$gsd-verify-work`, perform:
- a Firecrawl `/search` probe against the configured self-host stack
- a search-result-to-scrape happy path
- a fabricated-URL rejection check
- a forced bounded-timeout or call-cap degraded-save check
- five sequential scrape calls to observe memory/worker stability
</verification>

<success_criteria>
Phase 5 ends with a runtime shape that matches the researched architecture, a verification story that can catch self-host search regressions early, and persistence cleanup aligned with the grounded finalizer's detailed quantity model.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-03-SUMMARY.md` when done
</output>
