---
phase: 04.2-llm-threaded-interview-orchestration
plan: 01
subsystem: api
tags: [openrouter, pydantic, langfuse, interview, telegram, unittest]
requires:
  - phase: 04.1-grouped-reasoning-human-interview-correction
    provides: grouped reasoning state, authoritative interview finalization, deterministic /fix surfaces
provides:
  - Python 3.12 preflight harness for 04.2 execution
  - Strict interview turn schema and response_format helper
  - Traced one-repair interview turn manager scaffold
  - Authoritative interview state payload with unresolved and approval groups
affects: [04.2-02 live turn orchestration, 04.2-03 routing and bot handoff, interview finalization]
tech-stack:
  added: []
  patterns: [strict structured outputs, authoritative interview state, fail-closed repair retry]
key-files:
  created:
    - scripts/assert_phase_04_2_preflight.py
    - scripts/assert_interview_contract_red.py
    - tests/test_interview_schema.py
    - app/services/interview_schema.py
    - app/services/interview_turn_manager.py
  modified:
    - app/config.py
    - app/services/interview_service.py
    - tests/test_interview_flow.py
key-decisions:
  - "Use explicit INTERVIEW_MODEL/INTERVIEW_FALLBACK_MODEL settings instead of inheriting reasoning defaults."
  - "Keep interview turn parsing on native OpenRouter chat completions plus Pydantic validation with a single repair retry."
  - "Seed meal interviews from one DB-owned state object that carries unresolved targets and approval candidates together."
patterns-established:
  - "Interview turn contract: strict JSON schema, explicit approval_status enum, full-group coverage, fail closed on contradiction."
  - "Interview state builder: grouped reasoning becomes authoritative state with segment refs and approval-aware prompt context."
requirements-completed: [REASON-01, REASON-04, INTERVIEW-01, INTERVIEW-02]
duration: 12min
completed: 2026-05-30
---

# Phase 04.2 Plan 01: LLM-Threaded Interview Orchestration Summary

**Strict interview turn contract with explicit interview model wiring, one-repair fail-closed parsing, and DB-owned state that carries unresolved and approval groups together**

## Performance

- **Duration:** 12 min
- **Started:** 2026-05-30T14:00:37Z
- **Completed:** 2026-05-30T14:11:50Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments
- Added a Wave 0 preflight harness that bootstraps `.venv` on Python 3.12 and proves the targeted 04.2 suites can execute without import/bootstrap failures.
- Added RED coverage for explicit interview model config, strict turn schema, grouped authoritative state assembly, and bounded repair/fail-closed behavior.
- Implemented the 04.2 scaffold: explicit interview model settings, `InterviewTurnResult`/`ConfirmationItem`, traced `run_interview_turn()`, and meal interview state with unresolved targets plus approval candidates.

## Task Commits

Each task was committed atomically:

1. **Task 1: W0 - bootstrap a repo-local Python 3.12 environment and prove targeted suites can execute** - `15dc642` (`chore`)
2. **Task 2: RED - add failing contract tests for strict interview turns, grouped state assembly, and bounded repair/fail-closed behavior** - `4562b54` (`test`)
3. **Task 3: GREEN - implement explicit interview config, strict schema, traced turn manager, and authoritative grouped state** - `8223fc6` (`feat`)

**Plan metadata:** pending summary commit

## Files Created/Modified

- `scripts/assert_phase_04_2_preflight.py` - Boots `.venv`, installs requirements when needed, and treats import/bootstrap failures as hard preflight failures.
- `scripts/assert_interview_contract_red.py` - Ensures the RED gate only passes when the expected Phase 04.2 contract failures are present.
- `tests/test_interview_schema.py` - Covers explicit interview config, strict schema shape, approval status, and one-repair/fail-closed manager behavior.
- `tests/test_interview_flow.py` - Adds state-builder assertions for unresolved targets, approval candidates, reasoning summary, segment refs, and approval-aware prompt context.
- `app/config.py` - Adds explicit interview primary/fallback model keys.
- `app/services/interview_schema.py` - Defines strict Pydantic turn contract and confirmation coverage validation.
- `app/services/interview_turn_manager.py` - Wraps the OpenRouter transport with bounded transcript context, Langfuse tracing, and a single repair retry.
- `app/services/interview_service.py` - Builds authoritative meal interview state and seeds meal interview sessions with approval-aware prompt payloads.

## Decisions Made

- Used `google/gemini-3.1-flash-lite` as the default interview model and `google/gemini-3-flash-preview` as the explicit fallback, matching the phase brief's low-cost interview path.
- Kept prompt context lean in the new turn manager: system rules, authoritative state JSON, last six transcript turns, and latest user text only.
- Preserved `/fix` on the deterministic path while upgrading only meal interview session scaffolding in this plan.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Restored `/fix` FOOD_NAME prompt wording during regression verification**
- **Found during:** Task 3 (GREEN - implement explicit interview config, strict schema, traced turn manager, and authoritative grouped state)
- **Issue:** The plan's required broader regression suite surfaced an existing `/fix` contract mismatch: the FOOD_NAME prompt no longer matched the tested wording.
- **Fix:** Updated the fix-mode FOOD_NAME prompt to use `What exact name should I log...` while leaving meal-interview wording unchanged.
- **Files modified:** `app/services/interview_service.py`
- **Verification:** `rtk ./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q`
- **Committed in:** `8223fc6`

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** The fix preserved an existing deterministic `/fix` contract while keeping the new 04.2 meal-interview scaffold within scope.

## Issues Encountered

- The RED harness initially loaded from `scripts/` and could not import `tests.*`; adding the repo root to `sys.path` fixed the harness without changing the plan scope.
- Pydantic emitted the confirmation item schema via `$ref`; the response-format helper now inlines the nested item schema so the strict contract is explicit and testable.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 01 now leaves Phase 04.2 with the strict contract, model config, traced turn manager scaffold, and approval-aware interview state required for live Telegram orchestration.
- No unmitigated High issue remains against the plan threat model: authoritative grouped state, strict output validation, explicit model wiring, and traced repair/fail-closed behavior are all in place.
- Live reply routing, handler integration, and session-stealing prevention remain for the next 04.2 plans, not this scaffold slice.

## Verification

- `rtk python3.12 scripts/assert_phase_04_2_preflight.py`
- `rtk ./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_schema tests.test_interview_flow -q`
- `rtk ./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q`
- `rtk rg -n "INTERVIEW_MODEL|INTERVIEW_FALLBACK_MODEL|InterviewTurnResult|build_interview_turn_state|approval_candidates|unresolved_targets" app/config.py app/services/interview_schema.py app/services/interview_turn_manager.py app/services/interview_service.py`
- `rtk rg -n "approval_status|APPROVED|CORRECTED|reasoning_schema|reasoning_service|llm_client|tracing_service" app/services/interview_schema.py app/services/interview_turn_manager.py .planning/phases/04.2-llm-threaded-interview-orchestration/04.2-01-PLAN.md`

## Known Stubs

None.

## Self-Check: PASSED

---
*Phase: 04.2-llm-threaded-interview-orchestration*
*Completed: 2026-05-30*
