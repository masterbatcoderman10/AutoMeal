---
phase: 05-agentic-grounding
plan: 10
type: tdd
wave: 9
depends_on:
  - 05-09
files_modified:
  - app/services/interview_service.py
  - tests/test_interview_flow.py
autonomous: true
gap_closure: true
requirements:
  - GROUND-01
  - GROUND-03
must_haves:
  truths:
    - "The group finalizer system prompt encodes MealTracker source taxonomy and grounding obligations for HOME, STORE_BOUGHT_PREPARED, PACKAGED_BRANDED, and RESTAURANT flows."
    - "Store-bought prepared, packaged branded, and restaurant items are told to use online grounding instead of silently completing from model knowledge alone."
    - "The finalizer receives a compact semantic context package with distilled question/answer facts, not raw clarification records, choice arrays, or unrelated bookkeeping."
  artifacts:
    - path: "app/services/interview_service.py"
      provides: "Domain-specific finalizer prompt and distilled finalizer context builder"
    - path: "tests/test_interview_flow.py"
      provides: "Prompt-contract and payload-shape regressions for taxonomy and compact semantic context"
  key_links:
    - from: "app/services/interview_service.py"
      to: "app/services/grounding_service.py"
      via: "finalizer prompt requires online grounding when source taxonomy indicates brand, store-bought, or restaurant nutrition"
      pattern: "STORE_BOUGHT_PREPARED|PACKAGED_BRANDED|RESTAURANT|firecrawl_search|firecrawl_scrape"
    - from: "app/services/interview_service.py"
      to: "MealSegment.ai_reasoning"
      via: "distilled context preserves semantic source and grounding facts while existing trace merge still appends readable grounding evidence per D-29"
      pattern: "grounding_trace|semantic|clarification|source_origin_state"
---

<objective>
Close the Phase 05 UAT major gaps around shallow finalizer prompting and noisy raw finalizer input.

Purpose: make the grounded finalizer easier for the model to execute correctly by giving it explicit taxonomy/grounding rules and a clean semantic context package instead of broad raw interview state.
Output: one TDD gap-closure slice that expands `_group_finalizer_messages()` with MealTracker-specific rules and replaces raw clarification payload dumping with distilled facts.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-UAT.md
@.planning/phases/05-agentic-grounding/05-09-SUMMARY.md
@app/services/interview_service.py
@tests/test_interview_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - lock finalizer taxonomy prompt and compact input contract</name>
  <read_first>
    .planning/phases/05-agentic-grounding/05-UAT.md
    app/services/interview_service.py
    tests/test_interview_flow.py
  </read_first>
  <files>tests/test_interview_flow.py</files>
  <behavior>
    - `_group_finalizer_messages()` system content names the source taxonomy states `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, and `RESTAURANT`.
    - The system content says online grounding is mandatory for store-bought prepared, packaged branded, and restaurant items, and that HOME foods may use model knowledge only when nutrition is complete and confidence is explicit.
    - The user payload contains distilled semantic clarification facts and does not expose raw `clarification_answers`, `choices`, `choice_id`, `answer_type`, `questions_by_id`, candidate option arrays, or unrelated bookkeeping.
  </behavior>
  <action>Add failing prompt-contract tests in `tests/test_interview_flow.py` next to `test_group_finalizer_messages_treat_tool_content_as_untrusted_evidence`. Build a `GroupFinalizerInput` with noisy clarification records that include `question_id`, `question_kind`, `answer_type`, `choices`, `choice_id`, `candidate_options`, `segment_ids`, source-origin answers, brand or restaurant answers, and quantity answers. Assert the system prompt contains the four taxonomy names and explicit mandatory-grounding wording for `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, and `RESTAURANT`. Parse the `GROUP_FINALIZER_INPUT_JSON` user message and assert it contains compact sections such as `semantic_clarifications`, `resolved_source`, `resolved_brand_or_restaurant`, `resolved_quantity`, and `selected_identity`; assert it does not contain the raw key `clarification_answers` or the noisy scaffolding keys listed above. Keep D-07 intact: do not add image content to the finalizer message.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_flow -q</automated>
  </verify>
  <acceptance_criteria>The new tests fail on the current code because `_group_finalizer_messages()` has only a generic system prompt and dumps raw `clarification_answers`, `reasoning_group`, `confirmation_item`, and `segment_ref` into one opaque JSON payload.</acceptance_criteria>
  <done>RED coverage exists for the finalizer prompt taxonomy and compact semantic input contract described by the UAT gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - build distilled finalizer context and domain-specific prompt rules</name>
  <read_first>
    app/services/interview_service.py
    tests/test_interview_flow.py
    .planning/phases/05-agentic-grounding/05-CONTEXT.md
    .planning/phases/05-agentic-grounding/05-UAT.md
  </read_first>
  <files>app/services/interview_service.py, tests/test_interview_flow.py</files>
  <behavior>
    - `_group_finalizer_messages()` sends clearly labeled compact context instead of raw interview-state blobs.
    - The compact context preserves the facts needed by the grounded finalizer: group id, segment ids, selected identity, source type/origin, brand or restaurant name, quantity display/json, salient reasoning evidence, semantic Q&A, trace id, and crop path reference only as text metadata.
    - The prompt explains when to call `firecrawl_search` / `firecrawl_scrape`, how to treat tool content as untrusted evidence, and how D-29 grounding trace expectations relate to provenance.
  </behavior>
  <action>Refactor `app/services/interview_service.py` around `_group_finalizer_messages()`. Add helpers with concrete names such as `_build_group_finalizer_context()`, `_semantic_clarification_facts()`, `_resolved_source_context()`, and `_selected_identity_context()`; use them to build a payload with explicit sections rather than dumping `reasoning_group`, `confirmation_item`, `clarification_answers`, and `segment_ref` verbatim. Each semantic clarification fact should keep only the user-facing question or prompt, the normalized answer text/value, the question kind, and resolved fields that affect persistence (`source_type`, `source_origin_state`, `brand_name`, `restaurant_name`, `quantity_display`, `quantity_json`, `name`). Drop raw choices, answer-type scaffolding, internal pending lists, candidate arrays, and bookkeeping. Expand the system prompt with MealTracker taxonomy: `HOME_COOKED` means home/prepared from general knowledge can use `model_knowledge` if complete; `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, and `RESTAURANT` must use online grounding when saving as verified; model-only completion for those source origins must fail the success invariant from 05-09 and become degraded/non-success instead. Preserve existing prompt-injection safety language and D-07 by sending no image input, only structured text and crop path metadata. Ensure existing trace merge behavior still records queries, fetched URLs, snippet excerpts, provenance, iteration count, and stop reason into `MealSegment.ai_reasoning` per D-29.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_flow -q</automated>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>With the GREEN fix in place, the finalizer prompt carries explicit MealTracker taxonomy/grounding rules, the user payload is compact and semantic, raw clarification scaffolding is absent, and the existing grounding trace and degraded-save tests remain green.</acceptance_criteria>
  <done>The finalizer has a domain-specific prompt and clean input package: brand, store-bought, and restaurant flows are steered toward tools, while the model no longer has to reason over noisy raw clarification records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interview state -> finalizer prompt | Durable human answers and reasoning state are transformed into model input here |
| finalizer prompt -> tool loop | Prompt instructions steer whether the model uses grounding tools or model knowledge here |
| tool evidence -> persisted trace | Search/scrape evidence becomes provenance and human-readable audit data here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-31 | Integrity | `_group_finalizer_messages()` | mitigate | Encode source taxonomy and mandatory grounding rules directly in the finalizer system prompt. |
| T-05-32 | Information Disclosure | `_group_finalizer_messages()` payload | mitigate | Send compact semantic facts instead of raw interview state and unrelated bookkeeping. |
| T-05-33 | Spoofing | finalizer tool evidence | mitigate | Preserve untrusted-tool-content warnings and D-29 trace requirements so scraped evidence cannot masquerade as instructions. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this gap-closure plan. |
</threat_model>

<verification>
Run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_flow -q`.
Then run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q`.
Confirm `_group_finalizer_messages()` names all source taxonomy states, states mandatory online grounding for store-bought/packaged/restaurant items, and emits compact semantic context without raw clarification scaffolding.
</verification>

<success_criteria>
The Phase 05 finalizer receives the right information in the right shape: explicit grounding rules, clean semantic answers, and preserved trace expectations, with no raw clarification payload noise driving model confusion.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-10-SUMMARY.md` when done
</output>
