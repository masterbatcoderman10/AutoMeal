---
phase: 05-agentic-grounding
plan: 09
type: tdd
wave: 8
depends_on:
  - 05-07
  - 05-08
files_modified:
  - app/config.py
  - app/services/interview_schema.py
  - app/services/interview_service.py
  - tests/test_interview_schema.py
  - tests/test_interview_flow.py
autonomous: true
gap_closure: true
requirements:
  - GROUND-01
  - GROUND-02
must_haves:
  truths:
    - "The group finalizer output-token budget is configurable and high enough that realistic grounded finalizer JSON is not truncated by the Phase 05 default."
    - "A schema-valid but nutrition-empty finalizer response is not treated as a successful completion for an ordinary confirmed meal."
    - "Successful group finalizer output contains usable nutrition, explicit verification, and provenance before the meal is saved as COMPLETED."
  artifacts:
    - path: "app/config.py"
      provides: "Validated `FINALIZER_MAX_TOKENS` runtime setting"
    - path: "app/services/interview_service.py"
      provides: "Config-driven finalizer token budget and post-parse successful-output invariants"
    - path: "app/services/interview_schema.py"
      provides: "Finalizer contract support for required nutrition/provenance success checks"
    - path: "tests/test_interview_schema.py"
      provides: "Settings/schema regressions for finalizer token and save-ready fields"
    - path: "tests/test_interview_flow.py"
      provides: "Regression for truncated first attempt followed by unusable null-macro finalizer output"
  key_links:
    - from: "app/config.py"
      to: "app/services/interview_service.py"
      via: "`Settings.FINALIZER_MAX_TOKENS` controls `llm_client.chat_completion(max_tokens=...)`"
      pattern: "FINALIZER_MAX_TOKENS|max_tokens"
    - from: "app/services/interview_service.py"
      to: "app/services/meal_resolution_service.py"
      via: "only usable finalizer outcomes can pass `MealProcessingStatus.COMPLETED` into `apply_final_meal_resolution()`"
      pattern: "_successful_group_finalizer_outcome|apply_final_meal_resolution|MealProcessingStatus\\.COMPLETED"
---

<objective>
Close the Phase 05 UAT blocker where the group finalizer can truncate real JSON, retry, then treat a null-macro unverified payload as a successful completion.

Purpose: preserve the bounded grounding contract from D-11, D-14, and D-17 while preventing empty nutrition from being saved as a normal completed meal.
Output: a TDD gap-closure slice that makes finalizer token budget configurable and requires usable nutrition, verification, and provenance before a group finalizer outcome is marked `SUCCEEDED`.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-UAT.md
@.planning/phases/05-agentic-grounding/05-VALIDATION.md
@.planning/phases/05-agentic-grounding/05-08-SUMMARY.md
@app/config.py
@app/services/interview_schema.py
@app/services/interview_service.py
@tests/test_interview_schema.py
@tests/test_interview_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - capture finalizer truncation and unusable success output</name>
  <read_first>
    .planning/phases/05-agentic-grounding/05-UAT.md
    .planning/phases/05-agentic-grounding/05-VALIDATION.md
    app/config.py
    app/services/interview_schema.py
    app/services/interview_service.py
    tests/test_interview_schema.py
    tests/test_interview_flow.py
  </read_first>
  <files>tests/test_interview_schema.py, tests/test_interview_flow.py</files>
  <behavior>
    - `Settings` exposes a validated `FINALIZER_MAX_TOKENS` field with a Phase 05 default greater than the current hardcoded `1200`.
    - `_bounded_group_finalizer_response()` passes the setting value to `llm_client.chat_completion(max_tokens=...)`.
    - If the first finalizer attempt returns truncated JSON and the retry returns schema-valid output with `serving_size_g`, all macro fields, `provenance`, and `source_url` null plus `is_verified=false`, the meal is not saved as a normal `COMPLETED` success.
  </behavior>
  <action>Add failing tests for the exact UAT blocker. In `tests/test_interview_schema.py`, extend the existing settings/finalizer contract coverage so it expects `Settings.model_fields["FINALIZER_MAX_TOKENS"]` to exist, default to at least `4096`, and reject values below a documented lower bound. In `tests/test_interview_flow.py`, add one async regression for `finalize_confirmed_interview()` where the mocked LLM first returns truncated JSON that raises the existing parse error, then returns a syntactically valid `FinalizedGroupResult` with all nutrition fields null, `is_verified=false`, and no provenance. Capture the `apply_final_meal_resolution()` call and assert the code does not mark the finalizer group `SUCCEEDED`, does not pass `MealProcessingStatus.COMPLETED` with null nutrition, and does not allow visual learning for that group. Also add a small assertion that `_bounded_group_finalizer_response()` uses `settings.FINALIZER_MAX_TOKENS` rather than the module-level `FINALIZER_MAX_TOKENS = 1200` constant. Per D-17, do not reject legitimate verified model-knowledge HOME foods that include complete macro fields and `provenance="model_knowledge"`.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_schema tests.test_interview_flow -q</automated>
  </verify>
  <acceptance_criteria>The targeted suite fails on the current code because `FINALIZER_MAX_TOKENS` is hardcoded at 1200 and `_successful_group_finalizer_outcome()` accepts nutrition-empty, unverified finalizer output as a successful save path.</acceptance_criteria>
  <done>RED coverage exists for the live UAT failure mode: truncation followed by a null-macro unverified finalizer response cannot be counted as a successful completed meal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - make finalizer success require usable nutrition and provenance</name>
  <read_first>
    app/config.py
    app/services/interview_schema.py
    app/services/interview_service.py
    tests/test_interview_schema.py
    tests/test_interview_flow.py
    .planning/phases/05-agentic-grounding/05-UAT.md
  </read_first>
  <files>app/config.py, app/services/interview_schema.py, app/services/interview_service.py, tests/test_interview_schema.py, tests/test_interview_flow.py</files>
  <behavior>
    - Finalizer LLM calls use a configurable `FINALIZER_MAX_TOKENS` setting, defaulting to at least `4096`.
    - A finalizer response can be marked `SUCCEEDED` only when it has complete save-ready nutrition fields and a valid verification/provenance story.
    - Packaged, store-bought-prepared, and restaurant groups require searched provenance and a grounded source URL or fetched URL evidence for a successful verified save; HOME groups may use `model_knowledge` when complete macros are present.
  </behavior>
  <action>Implement the narrow finalizer safety fix. In `app/config.py`, add `FINALIZER_MAX_TOKENS: int = 4096` or higher and validate it in a dedicated field validator or the existing positive-int validator with a lower bound of at least 2048. In `app/services/interview_service.py`, remove the effective dependency on the module-level `FINALIZER_MAX_TOKENS = 1200` by reading `int(getattr(settings, "FINALIZER_MAX_TOKENS", 4096))` at the `llm_client.chat_completion()` call. Add a helper such as `_validate_successful_finalizer_result(parsed, group_input, grounding_trace)` and call it immediately after `parse_group_finalizer_response_payload()` and before appending a `status="SUCCEEDED"` attempt. The helper must require non-null `serving_size_g`, `calories`, `protein_g`, `carbs_g`, `fat_g`, and `fiber_g`; require `parsed.is_verified is True`; require `parsed.provenance` to be `searched` or `model_knowledge`; and for `source_type in {"PACKAGED","RESTAURANT"}` or non-empty `brand_name`/`restaurant_name`, require searched provenance plus `parsed.source_url` or a non-empty fetched URL in the merged grounding trace. If validation fails, raise the same validation/error type the finalizer retry loop already catches, so the attempt is audited as `FAILED` rather than `SUCCEEDED`. Ensure an all-null retry result cannot flow into `apply_final_meal_resolution()` as `MealProcessingStatus.COMPLETED`; use the existing explicit degraded or terminal failure path that prevents normal completed empty entries and visual learning. In `app/services/interview_schema.py`, keep strict schema shape but avoid making best-effort degraded fallback impossible; enforce the success invariant in the service layer where attempt outcome is known.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_schema tests.test_interview_flow -q</automated>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>With the GREEN fix in place, realistic finalizer responses are not constrained by a 1200-token cap, nutrition-empty finalizer responses are failed attempts, `finalizer_groups[*].status` cannot be `SUCCEEDED` for null-macro output, and no ordinary confirmed meal is saved as `COMPLETED` with only empty unverified nutrition.</acceptance_criteria>
  <done>The live finalizer safety blocker is closed: completed Phase 05 saves require usable nutrition and provenance, while unusable finalizer output exits through explicit non-success handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM finalizer output -> parser | Untrusted model JSON becomes authoritative group metadata here |
| parsed finalizer result -> successful save | Parsed nutrition and provenance become persisted `FoodItem` / `DiaryEntry` state here |
| retry/degraded path -> meal completion | Failed tool/finalizer attempts decide whether a meal can complete or must fail explicitly here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-28 | Tampering | `app/services/interview_service.py` | mitigate | Validate parsed finalizer output before any `SUCCEEDED` audit state or completed save can be produced. |
| T-05-29 | Integrity | `app/services/interview_schema.py` / finalizer contract | mitigate | Keep null nutrition acceptable only as parsed data, never as a successful save invariant. |
| T-05-30 | DoS | `FINALIZER_MAX_TOKENS` | mitigate | Move the output token budget to validated config with a Phase 05-safe default, reducing avoidable truncation retries. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this gap-closure plan. |
</threat_model>

<verification>
Run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_schema tests.test_interview_flow -q`.
Then run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q`.
Confirm `FINALIZER_MAX_TOKENS` is config-driven, the mocked finalizer call receives the configured max token value, and the truncation-then-null-macro retry scenario cannot produce a normal completed save.
</verification>

<success_criteria>
The Phase 05 group finalizer no longer treats empty nutrition as success: realistic output has enough token budget, successful saves require usable nutrition and provenance, and unusable finalizer responses are audited as failed/degraded rather than normal completions.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-09-SUMMARY.md` when done
</output>
