---
phase: 04.3-deterministic-clarification-schema-interview-ui
plan: 05
type: execute
wave: 5
depends_on:
  - 04.3-04
files_modified:
  - scripts/assert_043_group_contract_red.py
  - app/services/reasoning_schema.py
  - app/services/reasoning_service.py
  - tests/test_reasoning_contract.py
  - tests/test_reasoning_gate.py
  - tests/test_reasoning_flow.py
autonomous: true
gap_closure: true
requirements:
  - REASON-01
  - REASON-02
  - REASON-03
  - REASON-04
  - INTERVIEW-01
must_haves:
  truths:
    - "Per GAP-04.3-P4 and D-01 through D-05, each `food_group` owns its clarification contract, with ordered `clarification_actions` and a deprecated compatibility alias only where migration requires it."
    - "Per GAP-04.3-P5 and D-01, identity-choice actions derive their primary options from `food_group.top_3` in rank order, with contract-owned `Other`/`Not listed` instead of Telegram-injected identity choices."
    - "Per D-11 and D-12 plus GAP-04.3-P4 and GAP-04.3-P6, `SOURCE_ORIGIN` is emitted only as a reasoning-owned per-group `clarification_actions[*].type` when source can materially change nutrition or grounding, and it is skipped for obvious home-cooked groups."
    - "Per D-13 and GAP-04.3-P4/P6, ordered action flows such as `AFFIRMATION -> SOURCE_ORIGIN` are persisted inside the same `food_group`, and `SOURCE_ORIGIN` always uses the fixed contract states `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, mapped to the user-facing labels `homemade`, `store bought`, `packaged`, `restaurant`, and `not sure` instead of mapper- or Telegram-invented prompts."
    - "Per GAP-04.3-P6 and GAP-04.3-P7, reasoning distinguishes learned auto-confirm from visual-only affirmation, emits model-owned `user_prompt` plus action `type`, and persists the expanded per-group contract before any Telegram mapping."
  artifacts:
    - path: "app/services/reasoning_schema.py"
      provides: "Strict per-group clarification models, ordered `clarification_actions`, compatibility rules, and schema validation for reasoning output"
    - path: "app/services/reasoning_service.py"
      provides: "Reasoning prompt, gate, and normalization logic that emit per-group clarification ownership, top-3-derived choices, and affirmation-vs-learned actions"
    - path: "tests/test_reasoning_contract.py"
      provides: "Contract coverage for the per-group clarification payload and compatibility alias behavior"
    - path: "tests/test_reasoning_gate.py"
      provides: "Behavior coverage for top-3-derived identity actions, conditional `SOURCE_ORIGIN` ownership/order, and affirmation-required routing"
  key_links:
    - from: "app/services/reasoning_schema.py"
      to: "app/services/reasoning_service.py"
      via: "validated per-group clarification payload consumed by the gate and persistence layers"
      pattern: "clarification_actions|clarification_needed|SOURCE_ORIGIN|HOME_COOKED|STORE_BOUGHT_PREPARED|PACKAGED_BRANDED|RESTAURANT|UNKNOWN|AFFIRMATION_REQUIRED|AUTO_CONFIRM_LEARNED"
    - from: "app/services/reasoning_service.py"
      to: "tests/test_reasoning_flow.py"
      via: "run_reasoning_request must persist the expanded food-group contract before Telegram transformation"
      pattern: "user_prompt|top_3|clarification_actions|SOURCE_ORIGIN"
---

<objective>
Close GAP-04.3-P4, GAP-04.3-P5, GAP-04.3-P6, and GAP-04.3-P7 by moving clarification ownership into each `food_group` and expanding the reasoning payload before Telegram mapping.

Purpose: The current 04.3 contract still forces Telegram and interview mappers to invent semantics from `question_focus`, duplicate choice lists, and collapse visual-only confidence into silent auto-confirm. That drift is the root cause behind the open UAT clarification bugs.
Output: failing contract regressions, a strict per-group clarification payload, ordered `clarification_actions`, top-3-derived identity options plus contract-owned `Other`, reasoning-owned conditional `SOURCE_ORIGIN` actions with fixed choices, and explicit learned-vs-affirmation group states persisted in reasoning output.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-01-SUMMARY.md
@app/services/reasoning_schema.py
@app/services/reasoning_service.py
@tests/test_reasoning_contract.py
@tests/test_reasoning_gate.py
@tests/test_reasoning_flow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - add failing per-group clarification contract regressions</name>
  <files>scripts/assert_043_group_contract_red.py, tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_reasoning_flow.py</files>
  <read_first>app/services/reasoning_schema.py, app/services/reasoning_service.py, tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_reasoning_flow.py, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md</read_first>
  <acceptance_criteria>
    - "`scripts/assert_043_group_contract_red.py` exits `0` only when the expected pre-fix GAP-04.3-P4/P5/P6/P7 failures are reproduced, and exits non-zero for import errors, unexpected passes, or unrelated runtime failures."
    - "The RED tests require each unresolved or approval-needed `food_group` to carry `clarification_needed`, ordered `clarification_actions`, and a `clarification` compatibility alias only where migration is explicitly supported, per GAP-04.3-P4 and D-02 through D-05."
    - "The RED tests require identity-choice actions to derive primary choices from `food_group.top_3` in order, with stable candidate/value IDs, `quick_prompt` display labels, and a contract-owned `Other` option per GAP-04.3-P5 and D-01."
    - "The RED tests require `SOURCE_ORIGIN` to be emitted only as a reasoning-owned per-group `clarification_actions[*].type` when D-11 materially-change conditions apply, to be skipped for obvious home-cooked groups per D-12, and to use only the fixed contract states `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, mapped to the user-facing labels `homemade`, `store bought`, `packaged`, `restaurant`, and `not sure` per D-13."
    - "The RED tests require high-confidence groups without learned embedding support to emit `AFFIRMATION_REQUIRED` plus an `AFFIRMATION` action, while strong learned matches remain `AUTO_CONFIRM_LEARNED`, per GAP-04.3-P6."
    - "The RED tests require ordered per-group action flows such as `AFFIRMATION -> SOURCE_ORIGIN` to be emitted by reasoning, with `user_prompt` and renderable action `type` already present and `question_focus` reduced to optional trace/debug metadata instead of a rendering dependency, per GAP-04.3-P4, GAP-04.3-P6, and GAP-04.3-P7."
  </acceptance_criteria>
  <action>Create `scripts/assert_043_group_contract_red.py` and extend the reasoning contract suites so the current gap is reproduced before implementation. Lock the new Phase 04.3 gap contract into tests: per D-01 and GAP-04.3-P5, group candidates come from `food_groups[*].top_3`; per D-02 through D-05 and GAP-04.3-P4, clarification lives under each `food_group` with ordered `clarification_actions`; per D-11 and D-12, `SOURCE_ORIGIN` appears only when it materially affects nutrition or grounding and is omitted for obvious home-cooked groups; per D-13, `SOURCE_ORIGIN` uses only the fixed contract states `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, mapped to the user-facing labels `homemade`, `store bought`, `packaged`, `restaurant`, and `not sure`; per GAP-04.3-P6, ordered flows such as `AFFIRMATION -> SOURCE_ORIGIN` are reasoning-owned rather than mapper-created; and per GAP-04.3-P7, the reasoning payload must already contain the renderable `type` and `user_prompt` that Telegram will later consume.</action>
  <verify>
    <automated>rtk sh -lc '/Users/mali/Documents/Projects/MealTracker/.venv/bin/python scripts/assert_043_group_contract_red.py'</automated>
  </verify>
  <done>The repo has failing, gap-specific regressions proving the current reasoning payload still leaves clarification orchestration to Telegram and still mishandles identity choices and visual-only affirmations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - emit the per-group clarification contract and ranked identity options</name>
  <files>app/services/reasoning_schema.py, app/services/reasoning_service.py, tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_reasoning_flow.py</files>
  <behavior>
    - Test 1: reasoning normalization rejects or clearly migrates stale top-level clarification payloads, then exposes each `food_group` with `clarification_needed`, ordered `clarification_actions`, and an optional deprecated `clarification` alias per GAP-04.3-P4.
    - Test 2: identity actions use `food_group.top_3` candidate order as the primary answer surface, carry stable candidate/value IDs plus `quick_prompt`, and append only the contract-owned `Other` entry per GAP-04.3-P5.
    - Test 3: per D-11 and D-12, `SOURCE_ORIGIN` is included only for materially relevant groups, skipped for obvious home-cooked groups, and when present it is a reasoning-owned action inside `clarification_actions` rather than a mapper-created prompt.
    - Test 4: per D-13 and GAP-04.3-P6, ordered flows such as `AFFIRMATION -> SOURCE_ORIGIN` are preserved within the same `food_group`, and `SOURCE_ORIGIN` exposes only the fixed contract states `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, mapped to `homemade`, `store bought`, `packaged`, `restaurant`, and `not sure`.
    - Test 5: visual-only high-confidence groups emit `AFFIRMATION_REQUIRED` and an `AFFIRMATION` action with deterministic Yes/No semantics, while learned matches remain direct `AUTO_CONFIRM_LEARNED` per GAP-04.3-P6.
    - Test 6: persisted reasoning output stores `user_prompt`, `type`, `kind`, `reason`, `validation_hints`, and action order under each `food_group` before any Telegram transformation, with `question_focus` kept only as optional trace/debug metadata per GAP-04.3-P7.
  </behavior>
  <read_first>app/services/reasoning_schema.py, app/services/reasoning_service.py, tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_reasoning_flow.py, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md</read_first>
  <acceptance_criteria>
    - "`app/services/reasoning_schema.py` defines strict per-group clarification action models with `type`, `kind`, `user_prompt`, `answer_type`, `choices`, `allow_other`, `other_label`, `required`, `reason`, and `validation_hints`, and validates ordered `clarification_actions` per GAP-04.3-P4 and D-02 through D-05."
    - "`app/services/reasoning_service.py` derives identity choice options directly from `food_group.top_3` in stable order, preserves candidate IDs and `quick_prompt` labels, and includes contract-owned `Other` only for identity actions per GAP-04.3-P5 and D-01."
    - "`app/services/reasoning_service.py` emits `SOURCE_ORIGIN` only as a per-group reasoning-owned `clarification_actions[*].type` when D-11 materially-change conditions apply, skips it for obvious home-cooked groups per D-12, and never leaves Telegram or mappers to invent source-origin questions from `question_focus` or labels."
    - "When `SOURCE_ORIGIN` is present, the reasoning contract fixes both order and options: flows such as `AFFIRMATION -> SOURCE_ORIGIN` stay inside the same `food_group`, and `SOURCE_ORIGIN` exposes only the contract states `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, mapped to `homemade`, `store bought`, `packaged`, `restaurant`, and `not sure` per D-13 and GAP-04.3-P4/P6."
    - "`app/services/reasoning_service.py` distinguishes `AUTO_CONFIRM_LEARNED`, `AFFIRMATION_REQUIRED`, and `IDENTITY_CLARIFICATION_REQUIRED`, using learned embedding support plus ambiguity signals instead of a blanket `AUTO_CONFIRM` fallback, per GAP-04.3-P6."
    - "The reasoning prompt, normalization, and persisted output carry the full per-group clarification contract before Telegram mapping, so renderers no longer need `question_focus` to invent prompts, source-origin steps, or option surfaces, per GAP-04.3-P7 and REASON-04."
    - "`rtk sh -lc '/Users/mali/Documents/Projects/MealTracker/.venv/bin/python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_reasoning_flow -q'` passes."
  </acceptance_criteria>
  <action>Implement the per-group clarification contract in `app/services/reasoning_schema.py` and `app/services/reasoning_service.py`. Replace the current top-level clarification ownership with `food_group`-owned `clarification_needed` and ordered `clarification_actions`, keeping a deprecated `clarification` alias only when explicitly needed for migration per GAP-04.3-P4. Per D-01 and GAP-04.3-P5, derive identity-choice surfaces from `food_group.top_3` in order and carry stable candidate/value IDs plus `quick_prompt` labels and a contract-owned `Other`. Per D-11 and D-12, make `SOURCE_ORIGIN` conditional and reasoning-owned: emit it only when source materially changes nutrition or grounding behavior, skip it for obvious home-cooked groups, and keep the action inside the same group's ordered `clarification_actions` list. Per D-13 and GAP-04.3-P6, fix `SOURCE_ORIGIN` to the contract states `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, mapped to the user-facing labels `homemade`, `store bought`, `packaged`, `restaurant`, and `not sure`, and preserve ordered flows such as `AFFIRMATION -> SOURCE_ORIGIN` without allowing Telegram or mapper layers to synthesize source-origin prompts or collapse those states. Per GAP-04.3-P6, split learned auto-confirm from visual-only affirmation so blank-state high-confidence groups emit `AFFIRMATION_REQUIRED` with a deterministic `AFFIRMATION` action. Per GAP-04.3-P7 and REASON-04, expand the reasoning prompt and persisted payload so `type`, `user_prompt`, ordered action flow, and validation hints exist under each `food_group` before any Telegram-specific logic runs. Do not add Phase 5 tool-loop behavior or broaden nutrition grounding here.</action>
  <verify>
    <automated>rtk sh -lc '/Users/mali/Documents/Projects/MealTracker/.venv/bin/python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_reasoning_flow -q'</automated>
    <automated>rtk rg -n "clarification_actions|clarification_needed|SOURCE_ORIGIN|HOME_COOKED|STORE_BOUGHT_PREPARED|PACKAGED_BRANDED|RESTAURANT|UNKNOWN|homemade|store bought|packaged|restaurant|not sure|AUTO_CONFIRM_LEARNED|AFFIRMATION_REQUIRED|quick_prompt|user_prompt" app/services/reasoning_schema.py app/services/reasoning_service.py tests/test_reasoning_contract.py tests/test_reasoning_gate.py tests/test_reasoning_flow.py</automated>
  </verify>
  <done>The reasoning layer now emits the full, per-group clarification contract needed for deterministic Telegram rendering, with ranked identity options and explicit affirmation-vs-learned states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenRouter reasoning output -> normalized grouped reasoning state | Untrusted model JSON crosses into the app and must be normalized before any clarification action or save-path decision is trusted. |
| normalized food-group contract -> Telegram renderer | The renderer will trust `user_prompt`, action ordering, and candidate IDs from reasoning, so contract gaps here create user-facing semantic drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-05-01 | Tampering | `app/services/reasoning_schema.py` | mitigate | Enforce strict per-group clarification models with explicit required fields, bounded action types, ordered `clarification_actions`, and compatibility handling that is either deterministic migration or clear rejection. |
| T-04.3-05-02 | Tampering | `app/services/reasoning_service.py` | mitigate | Derive identity choices only from ranked `top_3` candidates plus the explicit contract-owned `Other`, and enforce the five-way D-13 `SOURCE_ORIGIN` mapping (`HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, `UNKNOWN` -> `homemade`, `store bought`, `packaged`, `restaurant`, `not sure`) without collapsing states or inventing prompt text from `question_focus`. |
| T-04.3-05-03 | Repudiation | persisted reasoning payload | mitigate | Persist the full per-group clarification contract, learned-vs-affirmation state, and action order in reasoning audit data so REASON-04 shows what Telegram consumed. |
| T-04.3-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are allowed; reuse the existing Pydantic/OpenRouter/Python stack already pinned in the repo. |
</threat_model>

<verification>
- `rtk sh -lc '/Users/mali/Documents/Projects/MealTracker/.venv/bin/python scripts/assert_043_group_contract_red.py'`
- `rtk sh -lc '/Users/mali/Documents/Projects/MealTracker/.venv/bin/python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_reasoning_flow -q'`
- `rtk rg -n "clarification_actions|clarification_needed|SOURCE_ORIGIN|HOME_COOKED|STORE_BOUGHT_PREPARED|PACKAGED_BRANDED|RESTAURANT|UNKNOWN|homemade|store bought|packaged|restaurant|not sure|AUTO_CONFIRM_LEARNED|AFFIRMATION_REQUIRED|quick_prompt|user_prompt" app/services/reasoning_schema.py app/services/reasoning_service.py tests/test_reasoning_contract.py tests/test_reasoning_gate.py tests/test_reasoning_flow.py`
</verification>

<success_criteria>
- Every unresolved or approval-needed `food_group` owns its clarification contract and ordered action list.
- Identity-choice actions use the ranked `top_3` candidates plus a contract-owned `Other`, without Telegram inventing food options.
- `SOURCE_ORIGIN` is emitted conditionally by reasoning, uses the five D-13 contract states `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, mapped to `homemade`, `store bought`, `packaged`, `restaurant`, and `not sure`, and stays inside ordered per-group flows such as `AFFIRMATION -> SOURCE_ORIGIN`.
- Reasoning distinguishes learned auto-confirm from visual-only affirmation and persists the full per-group contract before Telegram mapping.
</success_criteria>

<output>
Create `.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-05-SUMMARY.md` when done
</output>
