---
phase: 04.4-parallel-group-finalization-lite-detect
plan: 01
subsystem: api
tags: [openrouter, pydantic, vision, interview, testing]
requires:
  - phase: 04.3-deterministic-clarification-schema-interview-ui
    provides: deterministic clarification state, interview schema surface, shared detect smoke path
provides:
  - Lite detect defaults pinned in shared settings
  - Explicit finalizer model and bounded group parallelism settings
  - Strict per-group finalizer schema, response_format helper, and parser helpers
  - Live smoke CLI override for explicit detect-model verification
affects: [vision, interview, finalizer, verification]
tech-stack:
  added: []
  patterns: [explicit per-stage model ownership, strict Pydantic JSON-schema contracts, shared smoke-path verification]
key-files:
  created: []
  modified:
    - app/config.py
    - app/services/interview_schema.py
    - scripts/vision_smoke.py
    - tests/test_vision_service.py
    - tests/test_interview_schema.py
    - tests/test_bot_contract.py
key-decisions:
  - "DETECT_MODEL now defaults to google/gemini-3.1-flash-lite while the detect response contract stays unchanged."
  - "Finalizer schema fields are required-or-null and reject extra keys before any save-path handoff."
  - "vision_smoke.py keeps using detect_food_photo and accepts an explicit --detect-model override instead of a separate probe path."
patterns-established:
  - "Per-group finalizer contracts belong in interview_schema, not in persistence models."
  - "Live smoke overrides should flow through existing runtime helpers rather than direct raw SDK calls."
requirements-completed: [VISION-01, INTERVIEW-03, INTERVIEW-04]
duration: 11 min
completed: 2026-06-02
---

# Phase 04.4 Plan 01: Lite Detect Defaults And Strict Finalizer Contract Summary

**Flash-lite detect defaults, explicit finalizer settings, and a strict save-ready per-group finalizer schema locked behind the existing smoke path**

## Performance

- **Duration:** 11 min
- **Started:** 2026-06-02T07:16:10Z
- **Completed:** 2026-06-02T07:27:31Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Switched the shared detect default to `google/gemini-3.1-flash-lite` and added explicit `FINALIZER_MODEL` plus `FINALIZER_GROUP_PARALLELISM` settings.
- Added strict per-group finalizer schema helpers with required save-ready metadata, strict nested quantity payload validation, and fail-closed JSON parsing.
- Kept the live detect smoke on the shared runtime path and added a minimal `--detect-model` override for explicit Lite detect verification.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED - capture Lite detect defaults and strict finalizer contract failures** - `993937b` (`test`)
2. **Task 2: GREEN - implement explicit detect/finalizer settings, smoke coverage, and save-ready schema helpers** - `a317a52` (`feat`)

## Files Created/Modified

- `app/config.py` - Lite detect default plus explicit finalizer model and bounded parallelism settings.
- `app/services/interview_schema.py` - strict group finalizer models, response-format helper, JSON parsing helpers, and shared source-metadata validation.
- `scripts/vision_smoke.py` - explicit `--detect-model` CLI override that still routes through `detect_food_photo`.
- `tests/test_vision_service.py` - RED/GREEN contract coverage for Lite detect defaults and smoke override wiring.
- `tests/test_interview_schema.py` - strict finalizer schema/parser regressions and config defaults coverage.
- `tests/test_bot_contract.py` - explicit finalizer settings contract coverage and stale mock expectation cleanup.

## Decisions Made

- Used the existing `InterviewTurnValidationError` for finalizer parser failures so malformed model output and malformed interview output fail through one known validation channel.
- Required the finalizer contract to carry `quantity_display` and `quantity_json` as present-or-null fields, which prevents quantity metadata from disappearing silently before the authoritative save seam.
- Reused the existing source-type metadata guardrails for finalizer payloads so HOME/PACKAGED/RESTAURANT rules stay consistent across interview and finalizer parsing.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Cleaned stale targeted-suite expectations before RED**
- **Found during:** Task 1 (RED - capture Lite detect defaults and strict finalizer contract failures)
- **Issue:** Two owned tests were already stale against the seeded runtime surface, which obscured the intended RED failures for this plan.
- **Fix:** Tightened one interview evidence fixture and aligned one llm-client contract expectation with the active runtime surface so the RED state isolated the missing 04.4 contract work.
- **Files modified:** `tests/test_interview_schema.py`, `tests/test_bot_contract.py`
- **Verification:** `/Users/mali/Documents/Projects/MealTracker/.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_bot_contract -q`
- **Committed in:** `993937b`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The deviation removed unrelated suite noise and kept the TDD red/green cycle focused on the Lite detect and finalizer contract surface.

## Issues Encountered

- The seeded worktree did not contain `sample_images/IMG_4583.HEIC`, so the live smoke used the adjacent main-checkout sample path while still executing the worktree's `scripts/vision_smoke.py`.
- The live smoke emitted Langfuse export/DNS noise after printing a successful detect payload; the command still exited `0` and the structured detect result showed `next_action: "segment"`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 04.4 downstream work now has an explicit config contract for detect and per-group finalizer model ownership.
- The save seam can consume strict per-group finalizer metadata without guessing field shape, and the live detect smoke can explicitly pin Flash Lite for future verification.

## Known Stubs

None.

## Deviations from Threat Model

None.

## Self-Check: PASSED

- Verified `.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-01-SUMMARY.md` exists.
- Verified task commits `993937b` and `a317a52` exist in git history.
