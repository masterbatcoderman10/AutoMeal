---
phase: 04.5-source-aware-clarification-expansion
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/services/reasoning_schema.py
  - app/services/reasoning_service.py
  - tests/test_reasoning_contract.py
  - tests/test_reasoning_gate.py
  - tests/test_interview_flow.py
autonomous: true
requirements:
  - REASON-01
  - REASON-04
  - INTERVIEW-01
  - MATCH-04
must_haves:
  truths:
    - "Food groups with no usable learned evidence or fewer than 5 learned matches always synthesize a deterministic source question even when the model did not emit `ASK_SOURCE_ORIGIN`."
    - "Ambiguous learned groups still ask identity first; learned-source consensus cannot bypass the identity decision."
    - "A selected identity with strong learned source dominance can produce a deterministic source affirmation policy instead of the generic source question."
    - "Stable `segment_ids` remain explicit on normalized group payloads and any synthesized clarification actions that downstream interview code consumes."
  artifacts:
    - path: "app/services/reasoning_schema.py"
      provides: "Stable grouped contract with first-class `segment_ids` and any additive source-policy metadata fields needed by downstream interview logic"
    - path: "app/services/reasoning_service.py"
      provides: "Source-trigger heuristics, learned-source consensus policy synthesis, and deterministic clarification-action generation"
    - path: "tests/test_reasoning_contract.py"
      provides: "Contract coverage for additive grouped source-policy fields and explicit `segment_ids` invariants"
    - path: "tests/test_reasoning_gate.py"
      provides: "Gate-level regressions for no-match, few-match, and ambiguous-distribution source-trigger behavior"
  key_links:
    - from: "app/services/reasoning_schema.py"
      to: "app/services/reasoning_service.py"
      via: "normalized grouped payload fields consumed by gate/action synthesis"
      pattern: "segment_ids|source_question_policy|learned_source_distribution"
    - from: "app/services/reasoning_service.py"
      to: "app/services/interview_service.py"
      via: "clarification actions and source-policy metadata consumed by deterministic question-state building"
      pattern: "clarification_actions|source_trigger_reason|selected_identity"
---

<objective>
Establish the 04.5 reasoning-side contract before touching Telegram behavior.

Purpose: Make source-aware clarification a deterministic output of grouped reasoning so the interview layer consumes clear policy instead of guessing from raw candidates.
Output: Stable grouped source-policy metadata, explicit no-match/few-match trigger rules, and conservative learned-source consensus synthesis that preserves identity-first ordering.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-CONTEXT.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-RESEARCH.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-PATTERNS.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-VALIDATION.md
@.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-CONTEXT.md
@app/services/reasoning_schema.py
@app/services/reasoning_service.py
@tests/test_reasoning_contract.py
@tests/test_reasoning_gate.py
@tests/test_interview_flow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - capture grouped source-trigger and consensus-policy regressions</name>
  <files>tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_interview_flow.py</files>
  <action>Add failing regressions for the exact 04.5 reasoning-side contract. Assert that normalized grouped payloads keep explicit `segment_ids` and expose any additive source-policy metadata without regressing existing contract consumers. Add gate-level coverage proving: groups with no usable matches or fewer than 5 learned matches force a generic source-origin action even without model-authored `ASK_SOURCE_ORIGIN`; ambiguous groups still choose identity-first; and a selected identity with strong learned source dominance is marked for source affirmation rather than a generic source prompt. Mirror the analog seams mapped in `04.5-PATTERNS.md`: use `tests/test_reasoning_contract.py` for schema/coercion assertions, `tests/test_reasoning_gate.py` for `_evaluate_group_gate()` behavior, and `tests/test_interview_flow.py` only for the thin handoff proof that synthesized clarification actions remain consumable by the deterministic interview builder. Do not add Telegram rendering assertions yet.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_interview_flow -q</automated>
  </verify>
  <done>The targeted suites fail only because grouped source-trigger policy and learned-source consensus synthesis are not yet represented in the reasoning contract.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - implement source-trigger heuristics and identity-first consensus metadata</name>
  <files>app/services/reasoning_schema.py, app/services/reasoning_service.py, tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_interview_flow.py</files>
  <action>Update the grouped reasoning normalization and gate layers so 04.5 source-aware clarification is emitted deterministically. Preserve explicit `segment_ids` as a first-class field in normalized groups and synthesized clarification actions. In `app/services/reasoning_service.py`, extend the gate/action synthesis around `_evaluate_group_gate()` and `_build_group_clarification_actions()` so source questioning can be triggered by learned/vector evidence rather than only model-authored `ASK_SOURCE_ORIGIN`. Implement a conservative source-policy shape: no-match or fewer-than-5-match groups force a generic source question; ambiguous groups still ask identity first; after identity is selected, strong learned source dominance can request source affirmation instead of the generic source prompt. Keep the result additive and compact: expose policy/reason metadata that `interview_service.py` can consume later, but do not move Telegram behavior or finalizer logic into this wave. Follow the contract and pattern surfaces documented in `04.5-RESEARCH.md` and `04.5-PATTERNS.md`, and keep Phase 05 grounding, broader historical UX, and Phase 04.4 finalizer changes out of scope.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_interview_flow -q</automated>
  </verify>
  <done>Grouped reasoning deterministically synthesizes source-question policy and learned-source affirmation hints while preserving stable `segment_ids` and identity-first ordering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learned/matched evidence -> gate synthesis | Learned counts and candidate metadata become deterministic user-question policy here |
| normalized group payload -> interview state builder | Source-policy metadata crosses from reasoning into durable interview state here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-01 | Tampering | `app/services/reasoning_schema.py` | mitigate | Keep `segment_ids` and additive source-policy fields explicit and normalized so downstream code never infers group scope from lossy indexes. |
| T-04.5-02 | Integrity | `app/services/reasoning_service.py` | mitigate | Enforce identity-first ordering and conservative source-trigger heuristics so learned consensus cannot override ambiguous identity selection. |
| T-04.5-03 | Repudiation | reasoning/group audit state | mitigate | Persist a machine-readable source trigger reason / policy so later interview behavior is explainable. |
| T-04.5-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this plan. |
</threat_model>

<verification>
Run `rtk python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_interview_flow -q`.
The targeted regressions must prove no-match and few-match groups synthesize a generic source question, ambiguous groups remain identity-first, and grouped payloads preserve stable `segment_ids` plus planner-approved source-policy metadata.
Run the quick 04.5 validation command from `04.5-VALIDATION.md` after each task commit.
</verification>

<success_criteria>
The reasoning contract, gate behavior, and grouped clarification metadata are explicit enough that the Telegram layer can implement 04.5 source-aware prompts without re-deciding source policy from scratch.
</success_criteria>

<output>
Create `.planning/phases/04.5-source-aware-clarification-expansion/04.5-01-SUMMARY.md` when done
</output>
