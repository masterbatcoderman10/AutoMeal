---
phase: 04.1-grouped-reasoning-human-interview-correction
plan: 02
subsystem: api
tags: [interview, telegram, reasoning, uat, langfuse]
requires:
  - phase: 04.1-grouped-reasoning-human-interview-correction
    provides: grouped meal reasoning state on `MealLog.reasoning_state_json`
provides:
  - grouped interview sessions derived from unresolved `food_groups`
  - deterministic human prompt rendering with locked egg-curry detail wording
  - grouped UAT runner and checklist for `IMG_4646.HEIC`
affects: [interview, reasoning, tracing, verification]
tech-stack:
  added: []
  patterns:
    - grouped interview targets from persisted meal reasoning state
    - production-path grouped UAT probe that materializes interview state after reasoning
key-files:
  created:
    - scripts/assert_grouped_interview_red.py
    - .planning/phases/04.1-grouped-reasoning-human-interview-correction/04.1-UAT.md
  modified:
    - app/services/interview_service.py
    - scripts/embed_match_smoke.py
    - tests/test_interview_flow.py
    - tests/test_match_flow.py
    - tests/test_embed_match_smoke.py
key-decisions:
  - "Interview sessions keep the existing DB-owned roadmap and message history, but unresolved targets now come from grouped `food_groups` instead of raw segment labels."
  - "The locked egg-curry follow-up is rendered from structured question hints in code, not from free-form model prose."
  - "The live UAT path reuses detect -> segment -> match -> reason -> prepare_interview_session rather than introducing a separate verification-only code path."
patterns-established:
  - "Grouped prompt targeting: `group_id`, `primary_segment_id`, and `question_*` fields stay in session payloads and drive prompt text deterministically."
  - "Nested reasoning-state compatibility: interview targeting reads both root `food_groups` and persisted `meal_reasoning.food_groups`."
requirements-completed: [REASON-01, REASON-02, REASON-04, INTERVIEW-01, INTERVIEW-02, INTERVIEW-03, INTERVIEW-06]
duration: 15m
completed: 2026-05-29
---

# Phase 04.1 Plan 02: Grouped Interview & UAT Summary

**Grouped unresolved foods now create one recoverable interview target per food group, render the locked egg-curry clarification prompt, and ship with a production-path `IMG_4646.HEIC` UAT runner plus Langfuse checklist.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-05-29T20:00:30Z
- **Completed:** 2026-05-29T20:15:37Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments
- Added RED coverage that fails only on the grouped interview gaps: grouped pending targets, locked curry wording, and unresolved worker handoff.
- Retargeted `prepare_interview_session()` and `current_target_question()` to grouped `food_groups`, keeping the existing roadmap and persistence surfaces intact.
- Added a `grouped-uat` smoke mode for `sample_images/IMG_4646.HEIC` plus a phase UAT checklist that records the exact deterministic suite, live sample command, and Langfuse inspection steps.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED - add failing grouped interview contracts for pending targets, curry wording, and unresolved handoff** - `320601b` (test)
2. **Task 2: GREEN - target unresolved food groups and render deterministic human prompts** - `a685713` (feat)
3. **Task 3: REFACTOR - wire grouped unresolved handoff into the sample-image UAT runner and checklist** - `75f490a` (refactor)

## Files Created/Modified

- `scripts/assert_grouped_interview_red.py` - RED harness that only succeeds when the grouped interview gaps fail for the expected pre-fix reasons.
- `app/services/interview_service.py` - grouped pending-target extraction, deterministic prompt rendering, quantity-aware roadmap entry, and nested grouped-state compatibility.
- `scripts/embed_match_smoke.py` - grouped UAT mode that reuses the production detect/segment/match/reason/interview path for `IMG_4646.HEIC`.
- `tests/test_interview_flow.py` - grouped pending-target and exact egg-curry wording regressions.
- `tests/test_match_flow.py` - unresolved worker regression proving the grouped prompt is used when reasoning routes to interview.
- `tests/test_embed_match_smoke.py` - helper regression for nested grouped-state extraction in the smoke runner.
- `.planning/phases/04.1-grouped-reasoning-human-interview-correction/04.1-UAT.md` - repeatable grouped UAT and Langfuse evidence checklist.

## Decisions Made

- Kept the interview roadmap structure unchanged and changed only target selection plus prompt rendering, which preserves the existing confirmation, grounding, and recovery surfaces.
- Stored grouped target metadata directly in `pending_targets` and derived the operational `segment_id` from `primary_segment_id`, so grouped prompts work without inventing a parallel answer model.
- Made the smoke runner tolerant of Wave 1 persistence shape by reading grouped state from either root `food_groups` or nested `meal_reasoning.food_groups`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Bootstrapped a repo-local `.venv` for plan verification**
- **Found during:** Task 1
- **Issue:** The handed-off worktree had no `.venv`, and `python3.12` in the shell could not import required project dependencies.
- **Fix:** Created `.venv` in the worktree and installed `requirements.txt`, then ran all deterministic verification through the repo environment.
- **Files modified:** none committed
- **Verification:** `scripts/assert_grouped_interview_red.py`, `tests.test_interview_flow`, and the full reasoning/interview suite all ran successfully afterward.
- **Committed in:** not committed (environment-only)

**2. [Rule 1 - Bug] Fixed grouped interview targeting to read persisted nested meal reasoning**
- **Found during:** Task 3
- **Issue:** The new grouped interview targeting only read root-level `food_groups`, but Wave 1 persists grouped state under `MealLog.reasoning_state_json["meal_reasoning"]["food_groups"]`.
- **Fix:** Added nested-state fallback in `app/services/interview_service.py` and covered the smoke helper with a regression test.
- **Files modified:** `app/services/interview_service.py`, `tests/test_embed_match_smoke.py`
- **Verification:** `python3.12 -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_reasoning_gate tests.test_interview_flow tests.test_match_flow tests.test_embed_match_smoke`
- **Committed in:** `75f490a`

---

**Total deviations:** 2 auto-fixed (1 blocking fix, 1 bug fix)
**Impact on plan:** Both deviations were required to execute the planned verification path against the real Phase 04.1 persistence shape. No scope expansion beyond grouped interview delivery and its UAT surface.

## Issues Encountered

- The live `grouped-uat` command could not be completed in this worktree because `.env` secrets were absent. The attempted run failed before contacting services with missing `DATABASE_URL`, `INGEST_SECRET`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, and `OPENROUTER_API_KEY`.

## User Setup Required

None - no new external service configuration was introduced by this plan. The live `grouped-uat` runner uses the existing project `.env` contract.

## Next Phase Readiness

- Grouped interview prompting is now wired to the grouped reasoning state produced in Plan 01.
- The phase has a deterministic regression gate plus a production-path grouped UAT command and Langfuse checklist.
- Live `IMG_4646.HEIC` verification is ready to run as soon as the existing `.env` secrets are present in the execution environment.

## Self-Check: PASSED
