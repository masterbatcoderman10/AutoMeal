---
phase: 04.5-source-aware-clarification-expansion
plan: 02
type: tdd
wave: 2
depends_on:
  - 04.5-01
files_modified:
  - app/services/interview_service.py
  - bot/handlers.py
  - bot/polling.py
  - tests/test_interview_flow.py
  - tests/test_bot_contract.py
  - tests/test_match_flow.py
autonomous: true
requirements:
  - INTERVIEW-01
  - INTERVIEW-03
  - INTERVIEW-04
  - MATCH-04
must_haves:
  truths:
    - "After identity selection, deterministic question state can inject either a source affirmation or a generic source question without breaking required-first ordering or partial-reply recovery."
    - "Rejecting a learned-source affirmation creates a same-group free-text correction prompt using the existing failed-affirmation recovery pattern."
    - "Selecting packaged/store-bought or restaurant source continues to collect brand or restaurant details inside the same durable question inventory."
    - "The resulting grouped answers still flow into `confirmation_items` with stable `segment_ids`, source fields, and follow-up details for the existing 04.4 finalizer seam."
    - "Warm-state verification covers no-match, few-match, and ambiguous-distribution paths without requiring a new interview subsystem."
  artifacts:
    - path: "app/services/interview_service.py"
      provides: "Dynamic post-identity source prompt insertion, durable source/brand/restaurant answer persistence, and grouped finalizer handoff preservation"
    - path: "bot/handlers.py"
      provides: "Callback/text rerender behavior that remains correct after dynamic question insertion"
    - path: "tests/test_interview_flow.py"
      provides: "State-machine regressions for source affirmation, correction fallback, and grouped handoff assembly"
    - path: "tests/test_bot_contract.py"
      provides: "Telegram callback/text contract coverage for dynamic follow-up rendering and stale prompt handling"
    - path: "tests/test_match_flow.py"
      provides: "Integration coverage that enriched grouped answers still reach the existing 04.4 finalizer/save seam without losing source detail fields"
  key_links:
    - from: "app/services/interview_service.py"
      to: "bot/handlers.py"
      via: "question inventory and current prompt payload consumed by callback/text routing"
      pattern: "questions_by_id|pending_question_ids|current_question"
    - from: "app/services/interview_service.py"
      to: "bot/polling.py"
      via: "warm-state kickoff and continuation of inserted follow-up prompts"
      pattern: "current_target_question|has_deterministic_question_state"
    - from: "app/services/interview_service.py"
      to: "Phase 04.4 finalizer seam"
      via: "grouped `confirmation_items` enriched with source detail fields and stable `segment_ids`"
      pattern: "confirmation_items|source_type|brand_name|restaurant_name|segment_ids"
---

<objective>
Implement the 04.5 interview-layer behavior on top of the reasoning contract from plan 01.

Purpose: Extend the deterministic Telegram clarification loop with source-aware follow-ups and learned-source affirmations while preserving existing rerender, recovery, and finalizer-handoff behavior.
Output: Dynamic source-related follow-up insertion, durable same-group answer capture, callback/text regression coverage, and explicit warm-state verification steps.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-CONTEXT.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-RESEARCH.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-PATTERNS.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-VALIDATION.md
@app/services/interview_service.py
@bot/handlers.py
@bot/polling.py
@tests/test_interview_flow.py
@tests/test_bot_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - capture dynamic source follow-up and correction-path regressions</name>
  <files>tests/test_interview_flow.py, tests/test_bot_contract.py, tests/test_match_flow.py</files>
  <action>Add failing regressions for the 04.5 interview-state behavior. Cover identity-first dynamic insertion of a learned-source affirmation after a chosen identity; generic source-question insertion for no-match and few-match cases; rejection of a learned-source affirmation producing a same-group free-text correction prompt; store-bought/packaged answers producing a brand follow-up; restaurant answers producing a restaurant-name follow-up; and grouped `confirmation_items` preserving `segment_ids`, source, and follow-up detail fields. Extend `tests/test_bot_contract.py` so callback/text routing still rerenders the correct next prompt after dynamic insertion, rejects stale callbacks correctly, and preserves the existing no-`Other` behavior for `SOURCE_ORIGIN`. Add one downstream integration regression in `tests/test_match_flow.py` or the closest existing finalizer/save seam test surface to prove the enriched grouped handoff still reaches the existing 04.4 finalizer/save path without dropping source detail fields. Keep all assertions on deterministic state and current downstream contract behavior; do not add live UAT scripting here.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_interview_flow tests.test_bot_contract tests.test_match_flow -q</automated>
  </verify>
  <done>The targeted suites fail only because source-aware dynamic follow-up insertion and learned-source correction behavior do not yet exist in the interview state machine.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - implement source-aware dynamic question insertion and durable grouped handoff</name>
  <files>app/services/interview_service.py, bot/handlers.py, bot/polling.py, tests/test_interview_flow.py, tests/test_bot_contract.py, tests/test_match_flow.py</files>
  <action>Implement the 04.5 interview behavior inside the existing deterministic question-state machine. Consume the source-policy metadata from plan 01 and, after identity answers are recorded, dynamically insert either a learned-source affirmation or a generic source-origin question in the same group's ordered question inventory. Reuse `_build_affirmation_correction_question()` for rejected source affirmations so the correction path remains same-group, free-text, and durable. When the chosen or affirmed source implies more detail, inject brand or restaurant follow-up prompts inside `questions_by_id` / `question_order` rather than inventing handler-local branches. Keep bot handlers thin: they should continue rendering the current prompt, persisting answers, and rerendering the next prompt from state. Preserve required-first ordering, partial-reply recovery, stale callback protection, and the existing `SOURCE_ORIGIN` no-`Other` rule. Ensure grouped `confirmation_items` still carry stable `segment_ids`, `source_type`, `source_origin_state`, `brand_name`, and `restaurant_name` into the current Phase 04.4 finalizer seam without changing finalizer ownership, and lock that expectation with one downstream integration assertion on the existing save/finalizer path.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_interview_flow tests.test_bot_contract tests.test_match_flow -q</automated>
  </verify>
  <done>Source-aware follow-ups, learned-source affirmations, correction recovery, and grouped finalizer handoff all work within the deterministic interview loop without regressing rerender or persistence behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reasoning-side source policy -> interview state machine | Deterministic policy metadata becomes user-visible prompts here |
| Telegram callback/text input -> grouped answer persistence | User selections and free text become authoritative grouped clarification answers here |
| interview state -> finalizer handoff | Enriched grouped answers are transformed into save-ready confirmation inputs here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-04 | Integrity | `app/services/interview_service.py` | mitigate | Inject source follow-ups through the existing ordered question inventory so required-first sequencing and same-group scope are preserved. |
| T-04.5-05 | Tampering | `bot/handlers.py` | mitigate | Keep handlers state-driven only; dynamic insertion logic stays in interview service to avoid callback-only ghost state. |
| T-04.5-06 | Repudiation | grouped answer persistence | mitigate | Persist source affirmation answers, rejection corrections, and brand/restaurant follow-ups as normal question answers with stable IDs. |
| T-04.5-07 | Integrity | finalizer handoff | mitigate | Preserve stable `segment_ids` and grouped source fields in `confirmation_items` so Phase 04.4 receives complete, auditable input. |
| T-04.5-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this plan. |
</threat_model>

<verification>
Run `.venv/bin/python -m unittest tests.test_interview_flow tests.test_bot_contract tests.test_match_flow -q`.
Run the full targeted 04.5 suite from `04.5-VALIDATION.md` after this wave.
During `$gsd-verify-work`, rerun a warm-state meal that exercises the three target paths:
- no-match -> generic source question
- few-match -> generic source question
- ambiguous identity -> identity first, then learned-source affirmation or generic source fallback
</verification>

<success_criteria>
The deterministic Telegram clarification loop can ask and recover from source-aware prompts without losing ordering, persistence, or grouped finalizer-handoff fidelity.
</success_criteria>

<output>
Create `.planning/phases/04.5-source-aware-clarification-expansion/04.5-02-SUMMARY.md` when done
</output>
