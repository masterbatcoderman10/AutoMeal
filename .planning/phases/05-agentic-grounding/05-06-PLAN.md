---
phase: 05-agentic-grounding
plan: 06
type: tdd
wave: 6
depends_on:
  - 05-05
files_modified:
  - app/services/reasoning_service.py
  - bot/polling.py
  - tests/test_reasoning_flow.py
  - tests/test_match_flow.py
  - tests/test_bot_contract.py
autonomous: true
gap_closure: true
requirements:
  - GROUND-01
  - GROUND-02
must_haves:
  truths:
    - "Reasoning-model failures never synthesize `AUTO_CONFIRM` or `READY_TO_WRITE` groups from raw vector hits; failed or unparseable reasoning stays in a failure/review path per D-01, D-02, D-05, and D-06."
    - "A meal claimed by one `EMBEDDING` or `MATCHING` worker cannot be claimed by a second poller until the first worker advances status or marks failure, so the per-meal grounding budget remains hard under concurrency per D-11 and D-12."
    - "Duplicate worker execution cannot trigger duplicate reasoning/finalizer loops, duplicate writes, or duplicate notifications for the same meal while slow stage work is still running."
  artifacts:
    - path: "app/services/reasoning_service.py"
      provides: "Fail-closed reasoning failure handling that never upgrades a failed model response into save-ready groups"
    - path: "bot/polling.py"
      provides: "Durable stage ownership across slow embedding and matching/reasoning work"
    - path: "tests/test_reasoning_flow.py"
      provides: "Regression proving invalid reasoning plus a strong vector hit does not auto-confirm"
    - path: "tests/test_match_flow.py"
      provides: "Regression proving a second poller cannot pick the same meal while a slow stage is in flight"
    - path: "tests/test_bot_contract.py"
      provides: "Runtime-level guard against duplicate stage execution and duplicate completion side effects"
  key_links:
    - from: "app/services/reasoning_service.py"
      to: "tests/test_reasoning_flow.py"
      via: "`run_reasoning_request()` and `_fallback_food_groups_from_match_results()` failure gating"
      pattern: "run_reasoning_request|_fallback_food_groups_from_match_results|FAILED_UNCLEAR|NEEDS_SCHEMA_REVIEW"
    - from: "bot/polling.py"
      to: "tests/test_match_flow.py"
      via: "`poll_and_embed_food_segments()` and `poll_and_match_food_segments()` keep their `skip_locked` claim until stage completion"
      pattern: "with_for_update\\(skip_locked=True\\)|EMBEDDING|MATCHING|commit\\("
    - from: "bot/polling.py"
      to: "tests/test_bot_contract.py"
      via: "One live worker owns the meal through `_run_reasoning_pipeline()` and final notification"
      pattern: "_run_reasoning_pipeline|send_message|skip_locked"
---

<objective>
Close the two remaining Phase 05 verification blockers without replanning the executed baseline.

Purpose: Restore fail-closed grounded reasoning and make the per-meal grounding budget hard even when multiple pollers are running.
Output: One TDD gap-closure slice that removes the reasoning auto-confirm bypass and makes `EMBEDDING` / `MATCHING` stage claims durable through slow work.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-RESEARCH.md
@.planning/phases/05-agentic-grounding/05-VERIFICATION.md
@.planning/phases/05-agentic-grounding/05-04-SUMMARY.md
@.planning/phases/05-agentic-grounding/05-05-SUMMARY.md
@app/services/reasoning_service.py
@bot/polling.py
@tests/test_reasoning_flow.py
@tests/test_match_flow.py
@tests/test_bot_contract.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - capture the reasoning-fallback bypass and duplicate-worker claim regressions</name>
  <read_first>
    .planning/phases/05-agentic-grounding/05-VERIFICATION.md
    .planning/phases/05-agentic-grounding/05-04-SUMMARY.md
    .planning/phases/05-agentic-grounding/05-05-SUMMARY.md
    app/services/reasoning_service.py
    bot/polling.py
    tests/test_reasoning_flow.py
    tests/test_match_flow.py
    tests/test_bot_contract.py
  </read_first>
  <files>tests/test_reasoning_flow.py, tests/test_match_flow.py, tests/test_bot_contract.py</files>
  <behavior>
    - Invalid or unparseable reasoning output plus a strong cached vector candidate never yields `AUTO_CONFIRM` or `READY_TO_WRITE`.
    - While one worker is blocked inside `EMBEDDING` or `MATCHING`, a second poller sees no claimable row for that same meal.
    - Duplicate worker ownership cannot produce duplicate finalization or duplicate completion side effects for one meal.
  </behavior>
  <action>Add failing regressions for the two remaining blockers only. In `tests/test_reasoning_flow.py`, reproduce the exact bypass from `05-VERIFICATION.md`: make `run_reasoning_request()` receive invalid or unparseable reasoning output while the segment still has a strong cached vector candidate, then assert the result stays in `FAILED_UNCLEAR` or `NEEDS_SCHEMA_REVIEW` and never re-enters `AUTO_CONFIRM` / `READY_TO_WRITE`. Target both `run_reasoning_request()` and `_fallback_food_groups_from_match_results()` so GREEN work cannot hide the bypass behind a helper. In `tests/test_match_flow.py` and `tests/test_bot_contract.py`, create a blocking stage harness around `poll_and_embed_food_segments()` and `poll_and_match_food_segments()` that proves the first worker keeps ownership of the meal until stage completion and the second poller cannot re-pick it, re-run `_run_reasoning_pipeline()`, or emit a duplicate notification. Keep this scope minimal: no new worker types, no new phase behavior, and no warning-only gaps unless they fall out directly from the same assertions. Cite D-01, D-02, D-05, D-06, D-11, and D-12 in test names or comments where helpful.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>The targeted suite fails because current code still repopulates failed reasoning with save-ready fallback groups and still releases the `skip_locked` claim before slow `EMBEDDING` or `MATCHING` work finishes, allowing a second poller to pick the same meal.</acceptance_criteria>
  <done>Executable RED coverage exists for blocker 1 and blocker 2, with one regression locking the reasoning-failure bypass and one regression locking durable single-worker stage ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - make reasoning failure fail closed and keep stage claims durable through slow work</name>
  <read_first>
    app/services/reasoning_service.py
    bot/polling.py
    tests/test_reasoning_flow.py
    tests/test_match_flow.py
    tests/test_bot_contract.py
  </read_first>
  <files>app/services/reasoning_service.py, bot/polling.py, tests/test_reasoning_flow.py, tests/test_match_flow.py, tests/test_bot_contract.py</files>
  <behavior>
    - `run_reasoning_request()` preserves a failed/review result when the model failed or returned unusable output.
    - `poll_and_embed_food_segments()` and `poll_and_match_food_segments()` retain exclusive ownership of the meal until the stage transitions away from `EMBEDDING` or `MATCHING` or is marked `FAILED`.
    - The same meal cannot enter duplicate reasoning/finalizer or notification work from two concurrent pollers.
  </behavior>
  <action>Implement the smallest live fix that closes both blockers without reopening baseline Phase 05 scope. In `app/services/reasoning_service.py`, remove the post-failure upgrade path in `run_reasoning_request()`: when `_run_reasoning_model()` fails or returns unusable output, preserve the failure/review payload and do not synthesize save-ready groups from `_fallback_food_groups_from_match_results()`. If the helper must remain for a non-error compatibility path, it must never emit `AUTO_CONFIRM` / `READY_TO_WRITE` when the upstream action or meal state is `FAILED_UNCLEAR` or `NEEDS_SCHEMA_REVIEW`. This keeps the grounded path fail-closed per D-01, D-02, D-05, and D-06. In `bot/polling.py`, make the `FOR UPDATE SKIP LOCKED` claim durable across the slow stage by keeping the owning transaction or an equivalent persisted in-progress claim alive until embedding or matching/reasoning work has completed and the meal has been advanced out of `EMBEDDING` / `MATCHING` or marked `FAILED`; do not add new enums or migrations unless there is no smaller way to prove single-owner behavior. Remove the early `await session.commit()` claim-release points around the current `EMBEDDING` and `MATCHING` workers, and keep finalization plus notification side effects behind that single-owner boundary so D-11 and D-12 remain true under concurrency.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>With the GREEN fix in place, invalid reasoning plus a strong vector hit still ends in failure/review rather than auto-confirmation, and a second poller cannot claim or complete the same meal while the first worker is still running the slow `EMBEDDING` or `MATCHING` stage.</acceptance_criteria>
  <done>The remaining Phase 05 blockers are closed in live code: failed reasoning cannot bypass grounded handling, and hard per-meal grounding caps survive concurrent pollers because stage ownership stays exclusive until completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| failed reasoning output -> finalization path | Untrusted or missing model output can only become save behavior through app-side failure handling here |
| worker row claim -> slow stage work | A locked meal row becomes long-running embed/match/reason/finalize work here |
| single-owner stage execution -> notification/write side effects | Duplicate worker ownership can fan out into duplicate writes or bot sends here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | Integrity | `app/services/reasoning_service.py` | mitigate | Preserve failure/review state on unusable reasoning output and forbid save-ready fallback synthesis after LLM failure. |
| T-05-20 | DoS | `bot/polling.py` | mitigate | Keep the stage claim durable until the meal leaves `EMBEDDING` or `MATCHING`, preventing duplicate expensive loops from concurrent pollers. |
| T-05-21 | Repudiation | `bot/polling.py` / bot side effects | mitigate | Lock tests and runtime so duplicate worker ownership cannot produce duplicate finalization or duplicate completion notifications for one meal. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this gap-closure plan. |
</threat_model>

<verification>
Run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_flow tests.test_match_flow tests.test_bot_contract -q`.
Confirm one invalid-reasoning strong-vector scenario stays failed/review and one concurrent-poller scenario leaves the second worker with no claimable meal until the first worker advances status or fails.
</verification>

<success_criteria>
Phase 05 no longer has a failure-path escape hatch or a concurrency hole: grounded reasoning remains fail-closed, and per-meal grounding budgets remain hard because only one worker can own a slow stage at a time.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-06-SUMMARY.md` when done
</output>
