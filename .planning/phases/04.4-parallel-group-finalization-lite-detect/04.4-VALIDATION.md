---
phase: 04.4
slug: parallel-group-finalization-lite-detect
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-02
last_audited: 2026-06-02
---

# Phase 04.4 - Validation Strategy

> Per-phase validation contract for Flash Lite detect, per-group finalization, and authoritative save-path preservation.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `unittest` (stdlib) |
| **Config file** | none |
| **Quick run command** | `rtk sh -lc './.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_interview_flow -q'` |
| **Full suite command** | `rtk sh -lc './.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_bot_contract tests.test_interview_flow tests.test_match_flow tests.test_parallel_pipeline -q'` |
| **Estimated runtime** | ~2 seconds targeted, live OpenRouter/Telegram UAT excluded |

---

## Sampling Rate

- **After every task commit:** Run the quick command.
- **After every plan wave:** Run the full targeted suite.
- **During `$gsd-verify-work`:** Full targeted suite must stay green, then refresh the live Flash Lite detect smoke and the `IMG_4646.HEIC` end-to-end save-quality UAT evidence.
- **Max feedback latency:** 2 seconds for automated tests; one live detect run plus one Telegram meal pass for manual verification.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.4-01-01 | 01 | 1 | VISION-01 | T-04.4-02 | Detect defaults to `google/gemini-3.1-flash-lite`, stays fail-closed for low-confidence/non-food payloads, and accepts an explicit smoke override without bypassing the shared runtime path. | unit | `rtk sh -lc './.venv/bin/python -m unittest tests.test_vision_service tests.test_bot_contract -q'` | yes | green |
| 04.4-01-02 | 01 | 1 | INTERVIEW-03, INTERVIEW-04 | T-04.4-01, T-04.4-03 | Per-group finalizer schema is strict save-ready JSON, rejects missing fields and extra keys, and fails closed before the authoritative save path. | unit | `rtk sh -lc './.venv/bin/python -m unittest tests.test_interview_schema tests.test_bot_contract -q'` | yes | green |
| 04.4-02-01 | 02 | 2 | REASON-01, REASON-04 | T-04.4-04, T-04.4-05, T-04.4-06, T-04.4-07 | Finalization fans out one bounded call per food group, keeps sibling progress moving while one group is slow, preserves `MealSegment.ai_reasoning`, and records explicit degraded audit state on malformed outputs. | integration | `rtk sh -lc './.venv/bin/python -m unittest tests.test_interview_flow tests.test_parallel_pipeline -q'` | yes | green |
| 04.4-02-02 | 02 | 2 | INTERVIEW-03, INTERVIEW-04, MATCH-04 | T-04.4-08 | Finalized dish/source/quantity metadata reaches `apply_final_meal_resolution()`, degraded groups reuse `INTERVIEW_BEST_EFFORT`, and PACKAGED/RESTAURANT groups preserve the existing `poll_post_interview_grounding` handoff seam. | integration | `rtk sh -lc './.venv/bin/python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q'` | yes | green |

*Audit status: automated verification passed. Live OpenRouter detect smoke and live Telegram/DB UAT remain manual-only evidence refresh tasks for `$gsd-verify-work`.*

---

## Wave 0 Requirements

Existing `unittest` infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live Flash Lite detect smoke against OpenRouter | VISION-01 | Requires configured OpenRouter credentials, network access, and the local sample image path used by the smoke script. | Run `rtk sh -lc './.venv/bin/python scripts/vision_smoke.py --mode detect --sample sample_images/IMG_4583.HEIC --detect-model google/gemini-3.1-flash-lite'` and capture the structured detect result plus any trace/reference output. |
| Clarified `IMG_4646.HEIC` meal persists dish-level names and quantity/source metadata instead of raw fragments | INTERVIEW-03, INTERVIEW-04, MATCH-04 | Requires the deployed bot, a live meal run, and inspection of saved `DiaryEntry` / `FoodItem` / `FoodVisual` surfaces. | Redeploy for `$gsd-verify-work`, run the 04.4 clarification flow with `IMG_4646.HEIC`, confirm saved names are dish-level rather than fragments like `Green masala` or `Lauki`, verify quantity/source fields persisted, and record the result in a 04.4 UAT evidence note. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify.
- [x] Wave 0 covers all missing references.
- [x] No watch-mode flags.
- [x] Feedback latency < 2 seconds for automated tests.
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** approved 2026-06-02

## Validation Audit 2026-06-02

| Metric | Count |
|--------|-------|
| Automated gaps found | 0 |
| Manual-only gaps found | 2 |
| Resolved now | 0 |
| Escalated to `$gsd-verify-work` | 2 |

Automated audit commands:

```bash
rtk sh -lc './.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_interview_flow -q'
rtk sh -lc './.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_bot_contract tests.test_interview_flow tests.test_match_flow tests.test_parallel_pipeline -q'
```

Results:

- Quick suite passed on 2026-06-02: 81 tests.
- Full targeted suite passed on 2026-06-02: 169 tests.

Key coverage evidence:

- `tests.test_vision_service` asserts the Flash Lite detect default and shared smoke override path.
- `tests.test_interview_schema` asserts the strict per-group finalizer schema and fail-closed parser behavior.
- `tests.test_interview_flow` covers packaged grounding handoff preservation and degraded malformed-finalizer fallback.
- `tests.test_match_flow` covers authoritative write mapping for final name, source, quantity, and `FoodVisual` preservation.
- `tests.test_parallel_pipeline` covers sibling-safe bounded concurrency for per-group finalization.

Manual-only verification remains intentionally open for `$gsd-verify-work`: re-run the live detect smoke with configured credentials and capture a live `IMG_4646.HEIC` end-to-end persistence check.
