---
phase: 03-embed-match
plan: 04
type: tdd
wave: 3
depends_on: [03-03]
files_modified:
  - .planning/phases/03-embed-match/03-UAT.md
  - bot/messages.py
  - bot/polling.py
  - tests/test_bot_contract.py
autonomous: true
requirements: [MATCH-04]
user_setup: []
must_haves:
  truths:
    - "D-17, D-18, D-19, and D-20: the completion push is terse, user-facing, includes method and verified flag, uses two lines per item, and totals only known nutrition fields."
    - "D-14: the Telegram result names the portion bucket but does not caveat that Phase 03 defaults it."
    - "D-25: Telegram delivery is best-effort after the `COMPLETED` transaction and cannot roll back committed diary or visual writes."
  artifacts:
    - path: "bot/messages.py"
      provides: "Phase 03 meal-result message formatter with per-item identity, nutrition lines, and totals"
    - path: "tests/test_bot_contract.py"
      provides: "RED/GREEN coverage for post-commit completion push behavior"
    - path: ".planning/phases/03-embed-match/03-UAT.md"
      provides: "Repeatable live verification steps for calibration, same-food match, duplicate visual growth, and Telegram content"
  key_links:
    - from: "bot/polling.py"
      to: "bot/messages.py"
      via: "best-effort completion notification after commit"
      pattern: "send_message"
    - from: ".planning/phases/03-embed-match/03-UAT.md"
      to: "scripts/embed_match_smoke.py"
      via: "manual/live Phase 03 evidence"
      pattern: "embed_match_smoke.py"
---

## Phase Goal

**As a** MealTracker user, **I want to** receive a clean nutrition result message after a similarity-resolved meal completes, **so that** repeated meals feel finished without needing database inspection.

<objective>
Implement the post-commit Telegram completion push and live Phase 03 verification scaffold for D-14, D-17, D-18, D-19, D-20, and D-25 while staying attached to the confirmed-identification path from MATCH-04.

Purpose: Phase 03 is not complete when the database writes succeed silently; the user must receive a terse meal-result push that reflects the committed resolution without leaking scores or making notification success transactional.
Output: completion message formatting, best-effort send behavior, and a Phase 03 UAT checklist tied to the smoke helpers from earlier waves.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-embed-match/03-CONTEXT.md
@.planning/phases/03-embed-match/03-RESEARCH.md
@.planning/phases/03-embed-match/03-PATTERNS.md
@.planning/phases/03-embed-match/03-VALIDATION.md
@.planning/phases/03-embed-match/03-03-PLAN.md
@.planning/phases/02-vision-slice/02-04-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed meal rows -> Telegram chat | User-facing output is derived from committed DB state and must avoid leaking internal matching/debug data. |
| formatter -> partial nutrition fields | Missing macros cross into user messaging and must be omitted cleanly rather than fabricated. |
| manual UAT -> production-like behavior | Human verification relies on repeatable scripts and checklist steps, not ad hoc interpretation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Information Disclosure | `bot/messages.py` | mitigate | Exclude scores, vector details, and debug traces; show only D-17 through D-20 user-facing fields. |
| T-03-14 | Integrity | `bot/messages.py` | mitigate | Sum only known nutrition fields per D-19 and omit missing fields rather than inventing zeros or estimates. |
| T-03-15 | Denial of Service | `bot/polling.py` | mitigate | Keep send failure outside the `COMPLETED` transaction per D-25 and cover it in `tests/test_bot_contract.py`. |
| T-03-16 | Repudiation | `.planning/phases/03-embed-match/03-UAT.md` | mitigate | Record exact commands, sample assets, and expected Telegram content so live verification is reproducible. |
| T-03-SC | Tampering | Python dependency/runtime surface | accept | No new packages are installed in this plan; rely on the existing locked stack from Phase 1. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: RED - add failing bot-contract tests for post-commit completion pushes</name>
  <files>tests/test_bot_contract.py</files>
  <read_first>tests/test_bot_contract.py, bot/messages.py, bot/polling.py, .planning/phases/03-embed-match/03-CONTEXT.md, .planning/phases/03-embed-match/03-RESEARCH.md</read_first>
  <acceptance_criteria>
    - "The initial `rtk .venv/bin/python -m unittest tests.test_bot_contract` run fails because Phase 03 completion message behavior is not yet implemented."
    - "Tests assert D-17 through D-20: per-item output includes food name, identification method, verified flag, portion bucket, and only known nutrition fields."
    - "Tests assert D-25: a Telegram send failure does not roll back already committed `COMPLETED` meal state."
  </acceptance_criteria>
  <action>Write failing async bot-contract tests for the Phase 03 completion push. The tests must require the final message to use the D-18 two-line per-item shape, include method and verified flag per D-17, avoid default-portion caveats per D-14, and sum only known fields per D-19. Add a send-failure case proving the notification happens after commit per D-25.</action>
  <verify>
    <automated>rtk .venv/bin/python -m unittest tests.test_bot_contract</automated>
  </verify>
  <done>The RED gate defines the exact Telegram completion contract before formatting or send logic changes land.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - implement terse completion formatting and best-effort send timing</name>
  <files>bot/messages.py, bot/polling.py, tests/test_bot_contract.py</files>
  <read_first>bot/messages.py, bot/polling.py, app/models/diary_entry.py, app/models/food_item.py, .planning/phases/03-embed-match/03-CONTEXT.md, .planning/phases/03-embed-match/03-RESEARCH.md, tests/test_bot_contract.py</read_first>
  <acceptance_criteria>
    - "`bot/messages.py` formats each meal item on two lines with identity/status first and nutrition second."
    - "The result includes portion bucket, method, and verified flag, but omits scores and vector/debug data."
    - "Totals only include known calories/macros and skip missing values cleanly."
    - "`rtk .venv/bin/python -m unittest tests.test_bot_contract` passes after implementation."
  </acceptance_criteria>
  <action>Implement the Phase 03 completion formatter in `bot/messages.py` and wire `bot/polling.py` so the nutrition push occurs only after the successful-match transaction has committed. Preserve D-25 by keeping send failures non-transactional. Do not mention that the portion bucket is defaulted, and do not invent absent nutrition fields.</action>
  <verify>
    <automated>rtk .venv/bin/python -m unittest tests.test_bot_contract</automated>
  </verify>
  <done>Similarity-resolved meals now produce a user-facing nutrition push that reflects committed data without making Telegram delivery part of the transaction.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR - create Phase 03 UAT evidence checklist tied to the smoke helpers</name>
  <files>.planning/phases/03-embed-match/03-UAT.md</files>
  <read_first>.planning/phases/03-embed-match/03-VALIDATION.md, .planning/ROADMAP.md, scripts/embed_match_smoke.py, .planning/phases/02-vision-slice/02-04-SUMMARY.md</read_first>
  <acceptance_criteria>
    - "`03-UAT.md` includes explicit checks for Wave 0 calibration, same-food re-photo success, below-threshold `REASONING`, duplicate `FoodVisual` growth, and Telegram message content."
    - "Each UAT item includes exact commands or steps plus evidence fields."
    - "The UAT checklist references the demo seed/calibration helper introduced earlier in the phase."
  </acceptance_criteria>
  <action>Create `.planning/phases/03-embed-match/03-UAT.md` as the live verification scaffold for the full phase. Include numbered checks for D-04 self-similarity evidence, D-08 same-food similarity >= 0.85, D-09 unresolved routing, D-22 duplicate `FoodVisual` writes, and the final Telegram message contract from D-17 through D-20. Use the shared smoke helper commands rather than inventing separate manual scripts.</action>
  <verify>
    <automated>rtk rg -n "self-similarity|REASONING|FoodVisual|Telegram|embed_match_smoke.py" .planning/phases/03-embed-match/03-UAT.md</automated>
  </verify>
  <done>Phase 03 ends with a repeatable live-verification checklist instead of an implicit "trust the tests" handoff.</done>
</task>

</tasks>

<verification>
- `rtk .venv/bin/python -m unittest tests.test_bot_contract`
- `rtk rg -n "method|verified|portion|Calories|Protein|Carbs|Fat" bot/messages.py`
- `rtk rg -n "self-similarity|same-food|REASONING|FoodVisual|Telegram" .planning/phases/03-embed-match/03-UAT.md`
</verification>

<success_criteria>
- Completed similarity-matched meals send a terse nutrition push after commit, not during the transaction.
- The message includes only the Phase 03 user-facing fields: food name, portion bucket, macros, method, and verified status.
- Phase 03 has a live UAT checklist covering calibration, matching, write-back growth, unresolved routing, and Telegram output.
</success_criteria>

<output>
Create `.planning/phases/03-embed-match/03-04-SUMMARY.md` when done.
</output>
