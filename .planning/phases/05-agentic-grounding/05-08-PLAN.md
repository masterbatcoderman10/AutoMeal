---
phase: 05-agentic-grounding
plan: 08
type: tdd
wave: 7
depends_on:
  - 05-06
files_modified:
  - app/services/interview_schema.py
  - app/services/interview_service.py
  - app/services/grounding_stub.py
  - tests/test_interview_schema.py
  - tests/test_interview_flow.py
autonomous: true
gap_closure: true
requirements:
  - GROUND-01
must_haves:
  truths:
    - "Finalizer-facing `source_type` values are validated strictly; unknown values are rejected explicitly instead of being coerced to `HOME`."
    - "Packaged and restaurant grounding intent survives confirmation-item and finalizer parsing into `build_grounding_prep()` or a clear degraded failure path; malformed values cannot silently suppress grounding."
    - "Invalid finalizer output yields an explicit validation or tool-failure outcome in the inline finalizer path rather than a seemingly valid HOME save."
  artifacts:
    - path: "app/services/interview_schema.py"
      provides: "Strict finalizer-facing `source_type` validation for `ConfirmationItem` and `FinalizedGroupResult`"
    - path: "app/services/interview_service.py"
      provides: "Explicit runtime handling when inline finalizer output fails strict `source_type` validation"
    - path: "app/services/grounding_stub.py"
      provides: "Grounding-prep helper behavior that does not silently downgrade malformed authoritative source types to HOME"
    - path: "tests/test_interview_schema.py"
      provides: "Validation regressions proving invalid authoritative `source_type` values fail instead of normalizing"
    - path: "tests/test_interview_flow.py"
      provides: "Runtime regression proving malformed finalizer output degrades or fails explicitly rather than suppressing grounding"
  key_links:
    - from: "app/services/interview_schema.py"
      to: "app/services/interview_service.py"
      via: "`ConfirmationItem` and `FinalizedGroupResult` validation feeds the inline finalizer and degraded-save path"
      pattern: "ConfirmationItem|FinalizedGroupResult|parse_group_finalizer_result|ValidationError"
    - from: "app/services/grounding_stub.py"
      to: "app/services/interview_service.py"
      via: "`build_grounding_prep()` only receives valid authoritative source types or raises a clear failure path"
      pattern: "build_grounding_prep|needs_grounding|source_type"
---

<objective>
Close the last Phase 05 grounding-intent gap by making finalizer-facing source typing strict.

Purpose: prevent malformed authoritative `source_type` values from being rewritten to `HOME`, which can silently skip packaged or restaurant grounding even when the finalizer or confirmation path produced bad data.
Output: one TDD gap-closure slice that rejects invalid authoritative source types explicitly and routes those failures into the existing degraded-save/error handling instead of silently suppressing grounding.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-VERIFICATION.md
@.planning/phases/05-agentic-grounding/05-05-SUMMARY.md
@app/services/interview_schema.py
@app/services/interview_service.py
@app/services/grounding_stub.py
@tests/test_interview_schema.py
@tests/test_interview_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - reproduce authoritative source-type coercion as an explicit contract failure</name>
  <read_first>
    .planning/phases/05-agentic-grounding/05-VERIFICATION.md
    app/services/interview_schema.py
    app/services/interview_service.py
    app/services/grounding_stub.py
    tests/test_interview_schema.py
    tests/test_interview_flow.py
  </read_first>
  <files>tests/test_interview_schema.py, tests/test_interview_flow.py</files>
  <behavior>
    - `ConfirmationItem` rejects invalid `source_type` values such as `takeout?` instead of normalizing them to `HOME`.
    - `parse_group_finalizer_result()` rejects invalid finalizer `source_type` values instead of producing a valid HOME-shaped result.
    - Inline interview finalization records malformed finalizer `source_type` output as an explicit degraded or validation failure rather than silently skipping grounding.
  </behavior>
  <action>Add failing regressions for the exact Phase 05 source-type gap. In `tests/test_interview_schema.py`, require `ConfirmationItem.model_validate(...)` and `parse_group_finalizer_result(...)` to raise `ValidationError` when `source_type` is not one of the authoritative Phase 05 states. Assert explicitly that invalid payloads do not come back as `HOME`. In `tests/test_interview_flow.py`, drive `finalize_confirmed_interview()` with finalizer output that contains `source_type=\"takeout?\"` and assert the runtime records an explicit degraded or validation-failure outcome instead of accepting the payload as a HOME result with grounding suppressed. Keep the scope finalizer-facing only; do not broaden this into unrelated user free-text parsing paths.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_schema tests.test_interview_flow -q</automated>
  </verify>
  <acceptance_criteria>The targeted suite fails because finalizer-facing `source_type` values still normalize through `normalize_source_type()` and malformed authoritative values still land as `HOME` instead of raising or degrading explicitly.</acceptance_criteria>
  <done>Executable RED coverage exists for strict authoritative `source_type` validation and the runtime consequence of malformed finalizer output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - make finalizer-facing source validation strict and fail explicit on malformed output</name>
  <read_first>
    app/services/interview_schema.py
    app/services/interview_service.py
    app/services/grounding_stub.py
    tests/test_interview_schema.py
    tests/test_interview_flow.py
  </read_first>
  <files>app/services/interview_schema.py, app/services/interview_service.py, app/services/grounding_stub.py, tests/test_interview_schema.py, tests/test_interview_flow.py</files>
  <behavior>
    - `ConfirmationItem` and `FinalizedGroupResult` accept only valid authoritative `source_type` values.
    - The inline finalizer path turns malformed authoritative `source_type` output into an explicit degraded or validation-failure outcome rather than a HOME save.
    - `build_grounding_prep()` no longer acts as a silent downgrade for authoritative Phase 05 source typing.
  </behavior>
  <action>Implement the narrowest live fix that closes the grounding-intent hole without destabilizing general interview parsing. In `app/services/interview_schema.py`, replace the finalizer-facing `source_type` normalization on `ConfirmationItem` and `FinalizedGroupResult` with strict validation against the authoritative allowed values and keep the existing Pydantic error surface. In `app/services/interview_service.py`, preserve the current degraded-save behavior for invalid finalizer output by treating strict source-type validation failures as explicit finalizer/tool failures rather than letting malformed payloads proceed as HOME. In `app/services/grounding_stub.py`, ensure the authoritative grounding-prep path does not silently rewrite malformed source types to HOME; use an explicit rejection path or a strict helper where needed, while leaving unrelated user-input normalization patterns alone unless they are on the finalizer-facing seam.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_schema tests.test_interview_flow -q</automated>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>With the GREEN fix in place, invalid authoritative `source_type` values fail validation, packaged or restaurant intent is either preserved into grounding prep or rejected explicitly, and malformed finalizer output no longer suppresses grounding by becoming `HOME`.</acceptance_criteria>
  <done>The last grounding-intent gap is closed in live code: authoritative source typing is strict, and malformed finalizer output fails explicitly instead of silently skipping packaged or restaurant grounding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finalizer JSON -> strict schema | Model-emitted authoritative source typing becomes saveable state here |
| confirmation item -> grounding prep | Parsed authoritative source type determines whether grounding is required here |
| malformed authoritative payload -> degraded save | Invalid finalizer output must route into explicit failure handling here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-25 | Integrity | `app/services/interview_schema.py` | mitigate | Reject invalid authoritative `source_type` values instead of coercing them to HOME. |
| T-05-26 | Integrity | `app/services/grounding_stub.py` | mitigate | Ensure malformed authoritative source types cannot silently suppress packaged or restaurant grounding. |
| T-05-27 | Repudiation | `app/services/interview_service.py` | mitigate | Surface malformed finalizer output as an explicit degraded or validation failure rather than a seemingly valid HOME result. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this gap-closure plan. |
</threat_model>

<verification>
Run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_schema tests.test_interview_flow -q`.
Then run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q`.
Confirm `ConfirmationItem` and `parse_group_finalizer_result()` reject `source_type="takeout?"`.
Confirm one inline-finalizer run with malformed authoritative `source_type` records an explicit degraded or validation-failure outcome instead of a HOME save.
</verification>

<success_criteria>
Phase 05 no longer loses grounding intent through silent source-type coercion: authoritative packaged or restaurant paths are either preserved or rejected explicitly, and malformed finalizer output cannot masquerade as HOME.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-08-SUMMARY.md` when done
</output>
