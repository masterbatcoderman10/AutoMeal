---
phase: 05-agentic-grounding
plan: 05
type: tdd
wave: 5
depends_on:
  - 05-04
files_modified:
  - app/services/interview_service.py
  - app/services/meal_resolution_service.py
  - app/services/matching_service.py
  - bot/polling.py
  - tests/test_interview_flow.py
  - tests/test_match_flow.py
  - tests/test_bot_contract.py
autonomous: true
gap_closure: true
requirements:
  - GROUND-01
  - GROUND-02
  - GROUND-03
must_haves:
  truths:
    - "Degraded or unverified grounded saves never create `FoodVisual` rows and never re-enter the learned visual corpus."
    - "Vector matching only considers non-invalidated visuals whose linked `FoodItem.is_verified` is true."
    - "Grounding tool results are treated as untrusted evidence: only sanitized snippet/title/url fields are re-fed to the model, and the finalizer prompt explicitly forbids following instructions inside tool output."
    - "No callable `GROUNDING_PENDING` polling worker remains in `bot/polling.py`; dead helpers that reference deleted grounding handoff APIs are removed."
  artifacts:
    - path: "app/services/interview_service.py"
      provides: "Safe degraded-save finalizer behavior and prompt-injection-hardened tool evidence handling"
    - path: "app/services/meal_resolution_service.py"
      provides: "Authoritative guard that blocks `FoodVisual` writes for degraded or unverified outcomes"
    - path: "app/services/matching_service.py"
      provides: "Verified-only match corpus filter"
    - path: "bot/polling.py"
      provides: "Legacy grounding worker cleanup with no broken entry points"
  key_links:
    - from: "app/services/interview_service.py"
      to: "app/services/meal_resolution_service.py"
      via: "`create_food_visual` and `visual_learning_eligible` forced off for degraded/unverified outcomes"
      pattern: "create_food_visual|visual_learning_eligible|INTERVIEW_BEST_EFFORT|is_verified"
    - from: "app/services/matching_service.py"
      to: "app.models.FoodItem"
      via: "Candidate selection filters to verified foods only"
      pattern: "FoodVisual.is_invalidated|FoodItem.is_verified"
    - from: "bot/polling.py"
      to: "app/services/interview_service.py"
      via: "Removal of broken `build_grounding_reasoning_state()`-dependent worker code"
      pattern: "poll_grounding_handoffs|poll_post_interview_grounding|GROUNDING_PENDING"
---

<objective>
Close the Phase 5 safety and runtime-cleanup gaps after the live grounding behavior is fixed.

Purpose: Prevent degraded/unverified outputs from poisoning future matches, harden grounded tool evidence against prompt injection, and delete the dead grounding worker runtime that verification still found in `bot/polling.py`.
Output: Safe learning guards, verified-only match corpus selection, sanitized tool-evidence handling, and a fully removed `GROUNDING_PENDING` polling path.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-VERIFICATION.md
@.planning/phases/05-agentic-grounding/05-04-PLAN.md
@app/services/interview_service.py
@app/services/meal_resolution_service.py
@app/services/matching_service.py
@bot/polling.py
@tests/test_interview_flow.py
@tests/test_match_flow.py
@tests/test_bot_contract.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - reproduce degraded-learning, verified-corpus, prompt-hardening, and dead-worker regressions</name>
  <read_first>
    .planning/phases/05-agentic-grounding/05-VERIFICATION.md
    app/services/interview_service.py
    app/services/meal_resolution_service.py
    app/services/matching_service.py
    bot/polling.py
    tests/test_interview_flow.py
    tests/test_match_flow.py
    tests/test_bot_contract.py
  </read_first>
  <files>tests/test_interview_flow.py, tests/test_match_flow.py, tests/test_bot_contract.py</files>
  <action>Add failing regressions for actionable verification gaps 4 and 5. In `tests/test_interview_flow.py`, prove `final_resolution_from_confirmation()` and the grounded finalizer never request visual learning when `best_effort=True`, `needs_grounding=True`, or parsed `is_verified` is false, and require `_tool_result_message()` plus the finalizer prompt builder to pass only sanitized evidence fields rather than raw tool JSON back to the model. In `tests/test_match_flow.py`, require the match query in `app/services/matching_service.py` to exclude any `FoodVisual` whose linked `FoodItem.is_verified` is false even when the visual itself is not invalidated. In `tests/test_bot_contract.py`, assert that `bot/polling.py` no longer exports or calls `poll_grounding_handoffs`, `poll_post_interview_grounding`, `_rebuild_grounding_match_results`, or any `GROUNDING_PENDING` helper that depends on the deleted `build_grounding_reasoning_state()` API.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>The targeted suites fail because degraded/unverified results can still learn visuals, the match corpus still includes unverified food items, tool output is still re-injected as trusted raw JSON, or dead grounding workers remain callable from `bot/polling.py`.</acceptance_criteria>
  <done>Executable RED coverage exists for safe degraded-save learning behavior, verified-only matching, prompt-injection hardening, and legacy worker cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - block unsafe learning, sanitize tool evidence, and remove the dead grounding runtime</name>
  <read_first>
    app/services/interview_service.py
    app/services/meal_resolution_service.py
    app/services/matching_service.py
    bot/polling.py
    tests/test_interview_flow.py
    tests/test_match_flow.py
    tests/test_bot_contract.py
  </read_first>
  <files>app/services/interview_service.py, app/services/meal_resolution_service.py, app/services/matching_service.py, bot/polling.py, tests/test_interview_flow.py, tests/test_match_flow.py, tests/test_bot_contract.py</files>
  <action>Implement the safe failure-path cleanup. In `app/services/interview_service.py`, when `best_effort=True`, `needs_grounding=True`, or the finalizer result is unverified, force `create_food_visual=False` and `visual_learning_eligible=False` so degraded outcomes never request new `FoodVisual` learning; keep this explicit in `final_resolution_from_confirmation()` and any fallback finalizer branch. Harden tool-evidence handling in the same file by replacing raw `_tool_result_message()` payloads with a sanitized evidence envelope that contains only bounded snippet text, title, URL, and provenance fields, and add explicit system-prompt rules in `_group_finalizer_messages()` that scraped/search content is untrusted data and must never be treated as executable instructions. In `app/services/meal_resolution_service.py`, add an authoritative guard so `FoodVisual` rows are only persisted when the resolution is learning-eligible and the resolved food remains verified. In `app/services/matching_service.py`, extend the candidate query to require `FoodItem.is_verified.is_(True)` alongside `FoodVisual.is_invalidated.is_(False)`. In `bot/polling.py`, delete `poll_grounding_handoffs`, `poll_post_interview_grounding`, `_rebuild_grounding_match_results`, and any helper that only exists for the dead `GROUNDING_PENDING` handoff runtime, so no callable code still references the removed `build_grounding_reasoning_state()` path.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>Degraded or unverified saves can no longer create new `FoodVisual` rows, candidate matching ignores unverified foods, grounded tool evidence is sanitized and explicitly marked untrusted before re-entering the model context, and `bot/polling.py` no longer contains broken `GROUNDING_PENDING` worker entry points.</acceptance_criteria>
  <done>Verification gaps 4 and 5 are closed in live code: unsafe learning is blocked, grounded evidence is hardened, the match corpus is trust-scoped, and the legacy grounding runtime is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool output -> finalizer prompt | External search/scrape content becomes model-visible evidence here |
| finalizer resolution -> authoritative save | Grounded outputs can create durable `FoodVisual` learning rows here |
| match corpus query -> future meals | Previously learned visuals influence future meal identity decisions here |
| dead worker code -> runtime | Obsolete entry points can reactivate broken state transitions here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Tampering | `app/services/interview_service.py` | mitigate | Sanitize tool payloads and add explicit prompt rules that scraped content is untrusted evidence, not instructions. |
| T-05-16 | Integrity | `app/services/meal_resolution_service.py` | mitigate | Refuse `FoodVisual` creation for degraded or unverified outcomes even if an upstream caller mis-flags eligibility. |
| T-05-17 | Integrity | `app/services/matching_service.py` | mitigate | Limit the similarity corpus to verified food items so bad grounded saves cannot pollute future matches. |
| T-05-18 | Availability | `bot/polling.py` | mitigate | Delete the dead `GROUNDING_PENDING` worker path so broken handoff entry points cannot run in production. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this gap-closure plan. |
</threat_model>

<verification>
Run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q`.
During closeout, inspect one degraded grounded save and verify it produces no `FoodVisual`, then inspect the match query path and verify an unverified `FoodItem` cannot return as a similarity candidate.
</verification>

<success_criteria>
Phase 5 no longer trusts grounded failures as learnable truth: degraded outputs cannot poison the corpus, the model only sees sanitized tool evidence, and the broken `GROUNDING_PENDING` runtime is fully removed.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-05-SUMMARY.md` when done
</output>
