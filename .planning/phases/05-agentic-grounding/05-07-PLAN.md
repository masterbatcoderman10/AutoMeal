---
phase: 05-agentic-grounding
plan: 07
type: tdd
wave: 7
depends_on:
  - 05-06
files_modified:
  - bot/polling.py
  - tests/test_bot_contract.py
  - tests/test_match_flow.py
autonomous: true
gap_closure: true
requirements:
  - GROUND-02
must_haves:
  truths:
    - "DETECTING and SEGMENTING workers keep exclusive ownership of a meal until the slow detect or segment work completes and the meal advances or fails, so a second poller cannot duplicate detect calls, crop creation, or downstream grounding work."
    - "MATCHING meals with zero segment rows fail or recover immediately in the live runtime instead of parking in `REASONING` with no worker that consumes that state."
    - "The Phase 05 hard per-meal cap is true end-to-end under concurrent pollers, not only for EMBEDDING and MATCHING."
  artifacts:
    - path: "bot/polling.py"
      provides: "Durable detect and segment claims plus a non-stranding zero-segment MATCHING exit"
    - path: "tests/test_bot_contract.py"
      provides: "Duplicate-worker regressions for DETECTING and SEGMENTING ownership and side effects"
    - path: "tests/test_match_flow.py"
      provides: "Regression proving zero-segment MATCHING meals cannot strand into `REASONING`"
  key_links:
    - from: "bot/polling.py"
      to: "tests/test_bot_contract.py"
      via: "`poll_and_detect_food()` and `poll_and_segment_food()` keep the `skip_locked` claim until stage completion"
      pattern: "with_for_update\\(skip_locked=True\\)|DETECTING|SEGMENTING|detect_food_photo|segment_food_photo_with_retry|commit\\("
    - from: "bot/polling.py"
      to: "tests/test_match_flow.py"
      via: "`poll_and_match_food_segments()` exits the zero-segment branch without leaving the meal in an unconsumed runtime state"
      pattern: "MATCHING|REASONING|FAILED|MealSegment"
---

<objective>
Close the remaining Phase 05 runtime-cap gaps in the live poller path.

Purpose: extend the durable single-owner pattern from EMBEDDING and MATCHING back up to DETECTING and SEGMENTING, and remove the zero-segment MATCHING path that still strands meals into a dead-end runtime state.
Output: one TDD gap-closure slice that makes per-meal caps true end-to-end and guarantees zero-segment MATCHING meals exit cleanly without janitor rescue.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-VERIFICATION.md
@.planning/phases/05-agentic-grounding/05-06-SUMMARY.md
@bot/polling.py
@tests/test_bot_contract.py
@tests/test_match_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - lock the remaining poller ownership and zero-segment runtime regressions</name>
  <read_first>
    .planning/phases/05-agentic-grounding/05-VERIFICATION.md
    .planning/phases/05-agentic-grounding/05-06-SUMMARY.md
    bot/polling.py
    tests/test_bot_contract.py
    tests/test_match_flow.py
  </read_first>
  <files>tests/test_bot_contract.py, tests/test_match_flow.py</files>
  <behavior>
    - While one worker is blocked inside `detect_food_photo()` or `segment_food_photo_with_retry()`, a second poller sees no claimable row for that same meal.
    - Duplicate detect or segment ownership cannot produce duplicate slow-model calls, duplicate crop creation, duplicate status advancement, or duplicate Telegram side effects for one meal.
    - A `MATCHING` meal with zero segment rows exits immediately into an explicit terminal or recoverable state that a live worker actually consumes, never bare `REASONING`.
  </behavior>
  <action>Add failing regressions for the three remaining runtime truths only. In `tests/test_bot_contract.py`, mirror the existing EMBEDDING and MATCHING ownership harnesses for `poll_and_detect_food()` and `poll_and_segment_food()`: use two workers, a claim-active guard, and blocked slow work so the current early `commit()` release reproduces the duplicate-worker hole from `05-VERIFICATION.md`. Assert the second worker cannot re-enter the same DETECTING or SEGMENTING meal while worker one still owns it, cannot rerun detect or segmentation, and cannot duplicate crop-persistence side effects. In `tests/test_match_flow.py`, add a zero-segment MATCHING regression that proves the branch at `poll_and_match_food_segments()` does not leave the meal in `MealProcessingStatus.REASONING` with no live consumer. Keep scope narrow: no new worker types, no new phase behavior beyond durable ownership and a clean zero-segment exit.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_bot_contract tests.test_match_flow -q</automated>
  </verify>
  <acceptance_criteria>The targeted suite fails because DETECTING and SEGMENTING still release their claim before slow work, allowing a second poller to reacquire the same meal, or because zero-segment MATCHING still parks the meal in bare `REASONING`.</acceptance_criteria>
  <done>Executable RED coverage exists for detect/segment single-owner runtime behavior and the zero-segment MATCHING no-hang contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - make detect and segment claims durable and remove the stranded zero-segment branch</name>
  <read_first>
    bot/polling.py
    tests/test_bot_contract.py
    tests/test_match_flow.py
    .planning/phases/05-agentic-grounding/05-VERIFICATION.md
  </read_first>
  <files>bot/polling.py, tests/test_bot_contract.py, tests/test_match_flow.py</files>
  <behavior>
    - `poll_and_detect_food()` keeps exclusive ownership of a DETECTING meal until detect completes and the meal transitions to SEGMENTING or COMPLETED, or is marked FAILED.
    - `poll_and_segment_food()` keeps exclusive ownership of a SEGMENTING meal until segmentation, dedupe, and crop persistence complete and the meal transitions to EMBEDDING or FAILED.
    - `poll_and_match_food_segments()` handles a zero-segment MATCHING meal by failing or explicitly recovering it in-line rather than moving it to an unconsumed runtime state.
  </behavior>
  <action>Implement the smallest live fix that closes the remaining end-to-end cap hole. In `bot/polling.py`, remove the early claim-release `commit()` boundaries in `poll_and_detect_food()` and `poll_and_segment_food()` and keep the `FOR UPDATE SKIP LOCKED` ownership durable until the stage finishes, advances status, or fails. Preserve the existing phase behavior and side effects; the fix is about transaction/claim lifetime, not new business logic. Ensure crop creation and follow-on status changes stay behind the same owner boundary so a second poller cannot duplicate them. In the same file, replace the zero-segment branch in `poll_and_match_food_segments()` with an immediate fail-closed or recoverable exit that a live worker path already understands, so the meal does not rely on janitor recovery after entering MATCHING with no segments.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_bot_contract tests.test_match_flow -q</automated>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>With the GREEN fix in place, DETECTING and SEGMENTING meals stay single-owned through slow work, zero-segment MATCHING meals exit cleanly without bare `REASONING`, and the targeted runtime suite stays green under concurrent-poller harnesses.</acceptance_criteria>
  <done>The last runtime-cap gap is closed in live code: per-meal hard caps now survive concurrent workers from detect through matching, and zero-segment MATCHING meals no longer strand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| row claim -> slow detect / segment work | A locked meal row becomes expensive model work and crop writes here |
| stage ownership -> downstream side effects | Duplicate owners can fan out into duplicate state changes, crops, and notifications here |
| MATCHING zero-segment edge -> runtime state machine | A malformed runtime exit can strand the meal without a live consumer here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | DoS | `bot/polling.py` detect worker | mitigate | Keep the DETECTING claim durable until the meal leaves the stage or fails. |
| T-05-23 | Integrity | `bot/polling.py` segment worker | mitigate | Keep the SEGMENTING claim durable through segmentation and crop persistence so duplicate crops and downstream work cannot happen. |
| T-05-24 | Availability | `bot/polling.py` matching branch | mitigate | Replace the zero-segment `REASONING` park with an immediate live exit path. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this gap-closure plan. |
</threat_model>

<verification>
Run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_bot_contract tests.test_match_flow -q`.
Then run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow tests.test_match_flow tests.test_bot_contract -q`.
Confirm one blocked detect-worker scenario and one blocked segment-worker scenario leave the second poller with no claimable meal until worker one advances status or fails.
Confirm a zero-segment MATCHING scenario ends in an explicit live-runtime outcome rather than bare `MealProcessingStatus.REASONING`.
</verification>

<success_criteria>
Phase 05's hard cap is finally true in the whole live runtime: only one worker can own a meal through slow detect and segment work, and the zero-segment MATCHING edge no longer strands meals outside the active worker graph.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-07-SUMMARY.md` when done
</output>
