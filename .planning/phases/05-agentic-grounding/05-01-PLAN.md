---
phase: 05-agentic-grounding
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/services/llm_client.py
  - app/services/reasoning_schema.py
  - app/services/reasoning_service.py
  - app/services/grounding_service.py
  - tests/test_reasoning_contract.py
  - tests/test_reasoning_flow.py
  - tests/test_grounding_service.py
autonomous: true
requirements:
  - GROUND-01
  - GROUND-02
  - GROUND-03
  - REASON-04
  - INTERVIEW-03
must_haves:
  truths:
    - "Phase 5 foundations add an app-owned grounding service and strict grounding/finalizer contracts without reintroducing a second save path or a background queue."
    - "Official Firecrawl plus `SEARXNG_ENDPOINT` is treated as the default search architecture, while fallback direct-SearXNG search remains a planner-controlled contingency rather than the main path."
    - "The model-facing reasoning/finalizer contracts expose the data the inline grounding loop needs: constituents, serving_count, provenance-ready nutrition output, and machine-readable stop reasons."
    - "Loop bounds are represented explicitly in config and service contracts: tool-call cap, per-call timeout, wall-clock timeout, allowlist scope, and duplicate-call policy."
  artifacts:
    - path: "app/services/grounding_service.py"
      provides: "App-owned Firecrawl search/scrape client surface, tool schemas, allowlist enforcement helpers, and reusable bounded-loop primitives"
    - path: "app/config.py"
      provides: "Grounding model, Firecrawl/SearXNG endpoints, timeout, cap, and verification knobs"
    - path: "app/services/reasoning_schema.py"
      provides: "Strict schema helpers for constituent-aware group inputs and provenance-bearing grounded outputs"
    - path: "tests/test_grounding_service.py"
      provides: "Executable contract coverage for Firecrawl request shaping, allowlist rejection, timeout knobs, and duplicate-call policy"
  key_links:
    - from: "app/config.py"
      to: "app/services/grounding_service.py"
      via: "Grounding endpoints, search/scrape limits, and model-selection config"
      pattern: "FIRECRAWL|SEARXNG|GROUNDING|FINALIZER"
    - from: "app/services/reasoning_schema.py"
      to: "app/services/reasoning_service.py"
      via: "Constituent-aware group payloads and strict final grounded-output schema"
      pattern: "constituents|serving_count|provenance|stop_reason"
    - from: "app/services/grounding_service.py"
      to: "app/services/llm_client.py"
      via: "OpenRouter tool-call request shape and final strict-output pass"
      pattern: "tools|tool_choice|response_format|timeout"
---

<objective>
Establish the Phase 5 contract and client foundations before touching the interview/finalization orchestration.

Purpose: Add the configuration, service seams, and strict schema surfaces required for a bounded Firecrawl-backed grounding loop that can be called inline from the existing per-group finalizer path.
Output: Grounding service scaffolding, config knobs, strict constituent/provenance contracts, and RED/GREEN tests proving the new surfaces are stable and bounded.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-RESEARCH.md
@.planning/phases/05-agentic-grounding/05-PATTERNS.md
@.planning/phases/05-agentic-grounding/05-AI-SPEC.md
@app/config.py
@app/services/llm_client.py
@app/services/reasoning_schema.py
@app/services/reasoning_service.py
@app/services/tracing_service.py
@tests/test_reasoning_contract.py
@tests/test_reasoning_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - capture grounding contract, allowlist, and schema regressions</name>
  <files>tests/test_reasoning_contract.py, tests/test_reasoning_flow.py, tests/test_grounding_service.py</files>
  <action>Add failing tests for the new Phase 5 contract layer. In `tests/test_reasoning_contract.py`, assert that grouped reasoning/finalizer payloads can represent `constituents`, `serving_count`, provenance-bearing nutrient outputs, and explicit stop reasons without allowing extra fields. In `tests/test_grounding_service.py`, write contract tests for Firecrawl-backed `search` and `scrape` request shaping, per-group URL allowlist rejection, duplicate-call suppression, and bounded timeout/call-cap configuration. In `tests/test_reasoning_flow.py`, add one trace-oriented regression proving the new grounding trace block can be appended without destroying existing reasoning trace structure. Keep RED scope limited to contract and service boundaries; do not wire interview orchestration yet.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service -q</automated>
  </verify>
  <acceptance_criteria>The targeted suites fail only because Phase 5 grounding config, strict schema helpers, and the new app-owned search/scrape service surface do not yet exist.</acceptance_criteria>
  <done>Executable RED coverage exists for the official-Firecrawl default surface, per-group allowlist policy, and strict constituent/provenance schema expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - implement config, Firecrawl client seams, and strict grounding schemas</name>
  <files>app/config.py, app/services/llm_client.py, app/services/reasoning_schema.py, app/services/reasoning_service.py, app/services/grounding_service.py, tests/test_reasoning_contract.py, tests/test_reasoning_flow.py, tests/test_grounding_service.py</files>
  <action>Implement the Phase 5 foundation layer without changing the orchestration owner yet. Add config for grounding endpoints, search/scrape caps, per-call timeout, per-group wall-clock timeout, finalizer/grounding model selection, and any duplicate-call guard knobs. Create `app/services/grounding_service.py` as the app-owned seam for Firecrawl-backed `search` and `scrape`, plus per-group URL allowlist helpers and reusable loop bookkeeping primitives. Extend `app/services/reasoning_schema.py` with the strict input/output shapes the inline grounding finalizer will rely on: constituent-aware group input, serving counts, provenance-bearing grounded output, and explicit loop stop reasons. Update `app/services/llm_client.py` only as needed to support the native OpenRouter tool-call envelope and a final strict-output pass through existing patterns. Keep the current save path untouched and do not yet retire the handoff worker in this wave.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service -q</automated>
  </verify>
  <acceptance_criteria>The codebase now has a stable Phase 5 foundation: official Firecrawl search/scrape client seams, explicit grounding bounds in config, strict grounding schemas, and trace-compatible contract helpers, all green under targeted tests.</acceptance_criteria>
  <done>Phase 5 can now call an app-owned grounded tool surface with explicit bounds and strict result contracts, ready for inline finalizer integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app config -> grounding client | Endpoint URLs, timeouts, and call caps become runtime network behavior here |
| model output -> strict grounded schema | Untrusted model responses become save-candidate metadata here |
| search results -> URL allowlist | External URLs become scrape-eligible only through app validation here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `app/services/grounding_service.py` | mitigate | Enforce per-group allowlists so only URLs returned by search become scrape candidates. |
| T-05-02 | DoS | grounding client / model loop | mitigate | Represent tool-call caps and timeouts explicitly in config and service primitives before orchestration wiring. |
| T-05-03 | Integrity | `app/services/reasoning_schema.py` | mitigate | Use strict schemas with provenance and stop reasons so malformed grounded outputs cannot silently pass through. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this plan. |
</threat_model>

<verification>
Run `rtk python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service -q`.
Confirm the new grounding client rejects fabricated URLs before any scrape call, preserves duplicate-call bookkeeping, and exposes strict constituent/provenance output helpers ready for the inline finalizer.
</verification>

<success_criteria>
Phase 5 has a stable, bounded grounding contract and client surface that fits the existing OpenRouter and tracing architecture without yet disturbing interview/final-write orchestration.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-01-SUMMARY.md` when done
</output>
