---
phase: 04.3-deterministic-clarification-schema-interview-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/assert_reasoning_clarification_red.py
  - app/services/reasoning_schema.py
  - app/services/reasoning_service.py
  - tests/test_reasoning_contract.py
  - tests/test_reasoning_gate.py
  - tests/test_reasoning_flow.py
autonomous: true
requirements:
  - REASON-01
  - REASON-02
  - REASON-03
  - REASON-04
user_setup: []
must_haves:
  truths:
    - "Reasoning uses `food_groups[*].top_3` as the only live candidate source per D-01; root `top_3` never drives gate, persistence, or clarification branching."
    - "When a food group needs approval or clarification, reasoning emits a strict `clarification_schema` with stable IDs, allowed answer types, and compact validation metadata per D-02, D-03, D-04, and D-05."
    - "Source-origin clarification is emitted only for materially ambiguous foods such as flatbreads, packaged-looking items, bakery items, sauces, desserts, or restaurant/brand-looking items per D-11, D-12, and D-13."
  artifacts:
    - path: "app/services/reasoning_schema.py"
      provides: "Strict grouped reasoning contract with `clarification_schema` and legacy-read compatibility shim"
      exports: ["reasoning_response_format", "coerce_reasoning_response"]
    - path: "app/services/reasoning_service.py"
      provides: "Reasoning gate, clarification-batch derivation, and persisted audit output that no longer branches on root `top_3`"
      contains: "evaluate_reasoning_gate"
    - path: "tests/test_reasoning_contract.py"
      provides: "Contract coverage for live grouped candidates, `clarification_schema`, and compatibility boundaries"
  key_links:
    - from: "app/services/reasoning_schema.py"
      to: "app/services/reasoning_service.py"
      via: "strict grouped reasoning payload consumed by gate and persistence"
      pattern: "clarification_schema|food_groups"
    - from: "app/services/reasoning_service.py"
      to: "tests/test_reasoning_gate.py"
      via: "gate output must expose deterministic clarification inputs instead of freeform interview routing"
      pattern: "evaluate_reasoning_gate|clarification_schema"
---

<objective>
Lock the grouped reasoning clarification contract for Phase 04.3 per D-01 through D-05 and D-11 through D-13.

Purpose: Deterministic Telegram UI cannot be safe until reasoning emits one stable source of truth for candidates and one compact clarification payload for unresolved or approval-needed groups.
Output: failing reasoning-contract regressions, a strict `clarification_schema`, source-origin question rules, and reasoning persistence/gate code that treats group-owned candidates as authoritative.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md
@app/services/reasoning_schema.py
@app/services/reasoning_service.py
@tests/test_reasoning_contract.py
@tests/test_reasoning_gate.py
@tests/test_reasoning_flow.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenRouter reasoning output -> persisted grouped reasoning state | Untrusted model JSON crosses into the app and must be normalized before any gate or Telegram flow can consume it. |
| grouped reasoning state -> clarification batch | Candidate ranking and source-origin rules determine user-facing questions and must not silently fall back to root `top_3`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-01 | Tampering | `app/services/reasoning_schema.py` | mitigate | Enforce strict grouped reasoning models with `clarification_schema`, `additionalProperties=False`, stable structural IDs, and explicit allowed answer types per D-02 to D-05. |
| T-04.3-02 | Tampering | `app/services/reasoning_service.py` | mitigate | Remove root `top_3` from live gating/persistence branches per D-01, leaving only an explicit read-side compatibility shim for stale payloads and tests. |
| T-04.3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are allowed in Phase 04.3; reuse the pinned Python/PTB/Pydantic stack already in the repo. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: RED - add failing grouped-reasoning and clarification-schema regressions</name>
  <files>scripts/assert_reasoning_clarification_red.py, tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_reasoning_flow.py</files>
  <read_first>app/services/reasoning_schema.py, app/services/reasoning_service.py, tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_reasoning_flow.py, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md</read_first>
  <acceptance_criteria>
    - "`scripts/assert_reasoning_clarification_red.py` exits `0` only when the expected pre-04.3 failures are present, and exits non-zero on import failures, unexpected passes, or unrelated runtime errors."
    - "The RED tests require `reasoning_response_format()` and `coerce_reasoning_response()` to expose `clarification_schema` per D-02 and D-03, while proving root `top_3` is no longer part of the live grouped contract per D-01."
    - "The RED tests require `evaluate_reasoning_gate()` to derive deterministic clarification batch inputs from per-group gate output per REASON-03, including source-origin prompts only for D-11 and never for D-12 cases."
    - "The RED tests require all four live answer types from D-04 and compact validation hints from D-05, with stable `question_id` values derived from structural fields rather than rendered prompt copy."
  </acceptance_criteria>
  <action>Create `scripts/assert_reasoning_clarification_red.py` as the RED harness for the 04.3 reasoning contract. Extend `tests/test_reasoning_contract.py`, `tests/test_reasoning_gate.py`, and `tests/test_reasoning_flow.py` so they fail until reasoning emits a strict `clarification_schema`, group-owned candidates are the only live source of truth per D-01, and source-origin clarification is present only for materially ambiguous food groups per D-11 through D-13.</action>
  <verify>
    <automated>rtk sh -lc './.venv/bin/python scripts/assert_reasoning_clarification_red.py'</automated>
  </verify>
  <done>The RED harness proves the current repo still lacks the 04.3 grouped clarification contract and fails for the expected reasons only.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - implement strict clarification-schema reasoning output and remove live root `top_3` branching</name>
  <files>app/services/reasoning_schema.py, app/services/reasoning_service.py, tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_reasoning_flow.py</files>
  <read_first>app/services/reasoning_schema.py, app/services/reasoning_service.py, tests/test_reasoning_contract.py, tests/test_reasoning_gate.py, tests/test_reasoning_flow.py, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md</read_first>
  <acceptance_criteria>
    - "`app/services/reasoning_schema.py` defines strict clarification question/choice models, includes `clarification_schema` in the response format, and limits live answer types to `single_choice`, `multi_choice`, `free_text`, and `confirm` per D-02 to D-05."
    - "`app/services/reasoning_schema.py` and `app/services/reasoning_service.py` stop using root `top_3` for live gate, persistence, or UI branching per D-01; any compatibility shim is read-side only and explicitly bounded to stale payloads/tests."
    - "`evaluate_reasoning_gate()` and the persisted reasoning output expose per-group clarification batches, source-origin eligibility per D-11 to D-13, and audit-ready reasoning state per REASON-04 without reintroducing freeform interview prompts."
    - "`rtk sh -lc './.venv/bin/python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_reasoning_flow -q'` passes."
  </acceptance_criteria>
  <action>Implement the 04.3 reasoning contract in `app/services/reasoning_schema.py` and `app/services/reasoning_service.py`. Add strict `clarification_schema` models and response-format generation per D-02 through D-05, hard-cut root `top_3` from live semantics per D-01, preserve only an explicit legacy-read adapter for stale payloads, and make the reasoning gate/persistence path emit deterministic clarification questions for unresolved or approval-needed food groups. The source-origin branch must follow D-11 through D-13 exactly, and the persisted reasoning snapshot must remain audit-friendly for REASON-04 without introducing any Phase 5 grounding logic.</action>
  <verify>
    <automated>rtk sh -lc './.venv/bin/python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_reasoning_flow -q'</automated>
  </verify>
  <done>Reasoning now produces the strict 04.3 grouped clarification contract and the repo no longer depends on root `top_3` for live clarification behavior.</done>
</task>

</tasks>

<verification>
- `rtk sh -lc './.venv/bin/python scripts/assert_reasoning_clarification_red.py'`
- `rtk sh -lc './.venv/bin/python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_reasoning_flow -q'`
- `rtk rg -n "clarification_schema|question_id|answer_type|source_origin|food_groups\\[\\*\\]\\.top_3|legacy" app/services/reasoning_schema.py app/services/reasoning_service.py`
</verification>

<success_criteria>
- Group-owned candidates are the only live reasoning source of truth for Phase 04.3.
- Reasoning emits a compact, strict `clarification_schema` whenever approval or clarification is needed.
- Source-origin clarification is emitted only for materially ambiguous foods and not for obvious home-cooked foods.
</success_criteria>

<output>
Create `.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-01-SUMMARY.md` when done.
</output>
