---
phase: 02-vision-slice
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - scripts/vision_smoke.py
  - tests/test_vision_service.py
  - .env.example
  - .planning/phases/02-vision-slice/02-UAT.md
autonomous: true
requirements: [VISION-01, VISION-03, VISION-04]
user_setup: []
must_haves:
  truths:
    - "Roadmap phase note: Phase 2 must verify OpenRouter vision input + structured output + bounding-box format during this phase"
    - "Sample image grouping should follow the discussion example: grouped breads, curry/dish region, and vegetables/side region"
    - "Capability smoke coverage must exercise the same shared client path used by the runtime, not a throwaway one-off request script"
  artifacts:
    - "Live smoke script exists for detect, segment, and label stages"
    - "Phase 2 UAT evidence file captures manual sample-image and non-food checks"
    - "Environment template documents any new Phase 2 model settings"
  key_links:
    - "Shared client -> smoke script -> sample image -> UAT evidence"
---

<objective>
Add capability smoke coverage and execution-time verification scaffolding for Phase 2.
Purpose: Before Phase 3 depends on the same OpenRouter multimodal path, the team needs a concrete live probe and a UAT checklist that prove detect, segment, crops, and labels behave as planned on a real sample image.
Output: live smoke script, updated env template, and Phase 2 UAT evidence template
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vision-slice/02-CONTEXT.md
@.planning/phases/02-vision-slice/02-RESEARCH.md (§2, §6, §7)
@.planning/phases/02-vision-slice/02-PATTERNS.md
@.planning/phases/02-vision-slice/02-VALIDATION.md
@.planning/phases/02-vision-slice/02-03-PLAN.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Shared runtime path works only in mocks and not against real OpenRouter payloads | HIGH | Create a live smoke script that imports project code and fails non-zero on parse/schema mismatch |
| Phase 2 model settings are added in code but not documented for operators | MEDIUM | Update `.env.example` with the new optional model-setting keys and defaults/explanations |
| Manual verification is forgotten or unrepeatable | MEDIUM | Create `02-UAT.md` with concrete step-by-step checks and evidence slots |
| Smoke script drifts away from the production code path | LOW | Route the script through `app/services/vision_service.py` and `get_llm_client()` instead of raw ad hoc HTTP |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Build a live OpenRouter smoke script for detect, segment, and label</name>
  <files>scripts/vision_smoke.py, tests/test_vision_service.py</files>
  <read_first>app/services/llm_client.py, app/services/vision_service.py, sample_images/IMG_4583.HEIC, .planning/phases/02-vision-slice/02-RESEARCH.md §2 and §6, .planning/phases/02-vision-slice/02-PATTERNS.md</read_first>
  <action>Create `scripts/vision_smoke.py` so it accepts `--mode detect|segment|label|all` plus `--sample <path>`, loads the shared project settings/client, and prints concise structured results for the chosen stage. `--mode all` must run detect, segment, and label in sequence using the same shared project code paths. Make the script exit non-zero when a structured response cannot be parsed or when segment boxes fail the Phase 2 validator. Add a focused test or helper coverage in `tests/test_vision_service.py` for any parser utility extracted to support the script.</action>
  <verify>.venv/bin/python scripts/vision_smoke.py --mode all --sample sample_images/IMG_4583.HEIC</verify>
  <acceptance_criteria>
    - "`scripts/vision_smoke.py` exists and supports `--mode detect`, `--mode segment`, `--mode label`, and `--mode all`"
    - "The script imports project services instead of embedding a separate raw request implementation"
    - "`--mode all` exercises detect, segment, and label through the same shared client path used by the runtime"
    - "Segment mode exits non-zero if the returned boxes fail the same validator used by the runtime"
  </acceptance_criteria>
  <done>Phase 2 has a live probe for the exact multimodal path the runtime uses</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 2 model settings in the environment template</name>
  <files>.env.example</files>
  <read_first>.env.example, app/config.py, .planning/phases/02-vision-slice/02-01-PLAN.md, .planning/phases/02-vision-slice/02-RESEARCH.md §2</read_first>
  <action>Update `.env.example` to include commented optional overrides for `DETECT_MODEL`, `SEGMENT_MODEL`, `SEGMENT_RETRY_MODEL`, `LABEL_MODEL`, and `VISION_MAX_SEGMENTS`. Keep placeholder/comment style consistent with the existing file and explain that the defaults implement the Phase 2 plan if the keys are omitted.</action>
  <verify>rtk rg -n \"DETECT_MODEL|SEGMENT_MODEL|SEGMENT_RETRY_MODEL|LABEL_MODEL|VISION_MAX_SEGMENTS\" .env.example</verify>
  <acceptance_criteria>
    - ".env.example documents all five Phase 2 vision-related settings"
    - ".env.example makes clear that the keys are optional overrides rather than mandatory secrets"
  </acceptance_criteria>
  <done>Operators can discover and override Phase 2 vision settings without reading source code</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 2 UAT evidence template</name>
  <files>.planning/phases/02-vision-slice/02-UAT.md</files>
  <read_first>.planning/phases/01-foundation-ingest/01-UAT.md, .planning/ROADMAP.md, .planning/phases/02-vision-slice/02-RESEARCH.md §6 and §7, .planning/phases/02-vision-slice/02-VALIDATION.md</read_first>
  <action>Create `.planning/phases/02-vision-slice/02-UAT.md` with numbered checks for: non-food silent completion, simple meal success sentence, crop-file existence, invalid-box retry/soft-failure behavior, and sample-image grouping against `sample_images/IMG_4583.HEIC`. Include exact commands or manual steps, a pass/fail field for each check, and an evidence note field that can capture Telegram output text and filesystem observations during execution.</action>
  <verify>rtk rg -n \"IMG_4583|non-food|soft failure|crop\" .planning/phases/02-vision-slice/02-UAT.md</verify>
  <acceptance_criteria>
    - "02-UAT.md contains a check for non-food silent completion"
    - "02-UAT.md contains a check for crop files under `/data/uploads/crops/`"
    - "02-UAT.md references `sample_images/IMG_4583.HEIC`"
    - "Each UAT item has space for result and evidence notes"
  </acceptance_criteria>
  <done>Phase 2 execution will have a repeatable human-verification checklist instead of ad hoc manual testing</done>
</task>
</tasks>

<verification>
- [ ] `.venv/bin/python scripts/vision_smoke.py --mode detect --sample sample_images/IMG_4583.HEIC` succeeds with a parseable detect result
- [ ] `.venv/bin/python scripts/vision_smoke.py --mode segment --sample sample_images/IMG_4583.HEIC` succeeds or exits non-zero with validator output
- [ ] `.venv/bin/python scripts/vision_smoke.py --mode label --sample sample_images/IMG_4583.HEIC` succeeds with a parseable label result
- [ ] `.venv/bin/python scripts/vision_smoke.py --mode all --sample sample_images/IMG_4583.HEIC` exercises detect, segment, and label in sequence
- [ ] `.env.example` documents all new Phase 2 model settings
- [ ] `02-UAT.md` exists with manual checks for sample-image grouping, non-food silent completion, and crop-file existence
</verification>
