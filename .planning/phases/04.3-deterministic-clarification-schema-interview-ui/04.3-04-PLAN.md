---
phase: 04.3-deterministic-clarification-schema-interview-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 04.3-03
files_modified:
  - scripts/assert_043_uat_harness_red.py
  - scripts/embed_match_smoke.py
  - tests/test_embed_match_smoke.py
  - docker-compose.uat-043-harness.yml
  - .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md
autonomous: true
gap_closure: true
requirements:
  - REASON-04
  - INTERVIEW-01
  - INTERVIEW-04
  - MATCH-04
must_haves:
  truths:
    - "A repeatable local harness can activate a disposable warm database cloned from `mealttracker_uat_with_meal_embeddings` per D-18 and run the named partial-match (`sample_images/IMG_4583.HEIC`) and no-match (`sample_images/IMG_4641.HEIC`) regressions without ad-hoc DB edits or one-off replay steps."
    - "For each named regression sample, the harness waits until the meal reaches interview, retry-pending, grounding-pending, or completed state, then emits structured evidence for prompts, answers, diary entries, food visuals, and grounding status needed to prove D-19."
    - "Harness output includes Langfuse trace IDs plus serialized reasoning/interview/resolver state needed for D-20, while protecting the default `mealttracker` database from destructive reset paths."
  artifacts:
    - path: "scripts/embed_match_smoke.py"
      provides: "Named 04.3 UAT harness modes for warm-state replay, polling, and evidence dumps"
    - path: "tests/test_embed_match_smoke.py"
      provides: "Unit coverage for scenario mapping, warm-db safety gates, and evidence-report shape"
    - path: "docker-compose.uat-043-harness.yml"
      provides: "Tracked compose override for the disposable 04.3 warm-harness database and local API/bot wiring"
    - path: ".planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md"
      provides: "Exact commands for the partial-match and no-match harness runs plus evidence expectations"
  key_links:
    - from: "docker-compose.uat-043-harness.yml"
      to: "scripts/embed_match_smoke.py"
      via: "shared disposable `mealttracker_043_harness` database and local API target"
      pattern: "mealttracker_043_harness|API_HOST_PORT"
    - from: "scripts/embed_match_smoke.py"
      to: "sample_images/IMG_4583.HEIC"
      via: "named `partial-match` scenario mapping"
      pattern: "IMG_4583|partial-match"
    - from: "scripts/embed_match_smoke.py"
      to: "sample_images/IMG_4641.HEIC"
      via: "named `no-match` scenario mapping"
      pattern: "IMG_4641|no-match"
---

<objective>
Close GAP-04.3-P3 by shipping a repeatable warm-state UAT harness for the named partial-match and no-match regressions while preserving the Phase 04.3 boundary from D-18 through D-20.

Purpose: 04.3 still lacks an executable way to replay the warm repeated-meal state for `IMG_4583.HEIC` and `IMG_4641.HEIC`, which makes deterministic clarification regressions expensive to verify and easy to drift.
Output: a RED harness, implemented warm-state replay/report tooling, a tracked compose override for the disposable harness database, and updated 04.3 UAT commands that record the exact evidence required for REASON-04, INTERVIEW-01, INTERVIEW-04, and MATCH-04.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-01-SUMMARY.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-02-SUMMARY.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-03-SUMMARY.md
@scripts/embed_match_smoke.py
@tests/test_embed_match_smoke.py
@docker-compose.uat-042-clean.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing RED coverage for the 04.3 warm-state harness contract</name>
  <files>scripts/assert_043_uat_harness_red.py, tests/test_embed_match_smoke.py</files>
  <read_first>scripts/embed_match_smoke.py, tests/test_embed_match_smoke.py, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md</read_first>
  <action>Create `scripts/assert_043_uat_harness_red.py` and extend `tests/test_embed_match_smoke.py` so the gap is reproduced before implementation. Lock the missing behavior from GAP-04.3-P3 into tests: a named `partial-match` scenario resolves to `sample_images/IMG_4583.HEIC`, a named `no-match` scenario resolves to `sample_images/IMG_4641.HEIC`, the harness refuses destructive work against the default `mealttracker` database unless an explicit disposable target is selected, and the final JSON report includes reasoning, interview, diary, food-visual, grounding, and trace sections required by D-18, D-19, and D-20.</action>
  <verify>
    <automated>rtk sh -lc './.venv/bin/python scripts/assert_043_uat_harness_red.py'</automated>
  </verify>
  <done>The repo has failing, gap-specific regression coverage proving there is still no safe, named, repeatable warm-state UAT harness for `IMG_4583.HEIC` and `IMG_4641.HEIC`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the disposable warm-db replay harness and evidence dump</name>
  <files>scripts/embed_match_smoke.py, tests/test_embed_match_smoke.py, docker-compose.uat-043-harness.yml</files>
  <behavior>
    - Test 1: `--mode uat-harness --scenario partial-match` clones or validates a disposable warm database from `mealttracker_uat_with_meal_embeddings`, uses `sample_images/IMG_4583.HEIC`, and emits a structured report path without touching the default `mealttracker` database per D-18.
    - Test 2: `--mode uat-harness --scenario no-match` uses `sample_images/IMG_4641.HEIC`, waits until the meal reaches interview, retry-pending, grounding-pending, or completed state, and dumps prompts, answers, diary entries, food visuals, and grounding status needed for D-19.
    - Test 3: both scenarios surface reasoning/interview/resolver trace IDs and serialized state in the report for D-20, and the harness can be exercised in a `--dry-run` or equivalent non-live mode from unit tests.
  </behavior>
  <read_first>scripts/embed_match_smoke.py, docker-compose.uat-042-clean.yml, tests/test_embed_match_smoke.py, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md</read_first>
  <action>Extend `scripts/embed_match_smoke.py` instead of creating a separate ad-hoc replay script. Add a dedicated `uat-harness` mode plus named `partial-match` and `no-match` scenarios, reuse the existing smoke-helper patterns for image preparation and DB access, and introduce a tracked `docker-compose.uat-043-harness.yml` override for a disposable `mealttracker_043_harness` database and local API/bot wiring. The harness must select or clone the warm checkpoint per D-18, trigger ingest through the normal local API path instead of DB inserts, poll until the meal reaches a terminal or waiting state, and dump structured evidence for reasoning, clarification answers, diary entries, food visuals, grounding status, and trace IDs per D-19 and D-20. Do not add any Phase 5 grounding logic, do not script raw Telegram sends, and do not permit destructive reset paths against the default `mealttracker` database.</action>
  <verify>
    <automated>rtk sh -lc './.venv/bin/python -m unittest tests.test_embed_match_smoke -q'</automated>
    <automated>rtk sh -lc './.venv/bin/python scripts/embed_match_smoke.py --mode uat-harness --scenario partial-match --dry-run'</automated>
    <automated>rtk sh -lc './.venv/bin/python scripts/embed_match_smoke.py --mode uat-harness --scenario no-match --dry-run'</automated>
    <automated>rtk sh -lc 'API_HOST_BIND=127.0.0.1 API_HOST_PORT=18043 docker compose -f docker-compose.yml -f docker-compose.uat-043-harness.yml config | grep -E '\''mealttracker_043_harness|host_ip: 127.0.0.1|published: "18043"|postgres:5432/mealttracker_043_harness'\'''</automated>
  </verify>
  <done>A single repo-owned harness can safely prepare a warm disposable DB, run the named 04.3 regression scenarios, and emit the exact state/evidence needed for repeatable partial-match and no-match verification.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the 04.3 UAT runbook around the harness commands</name>
  <files>.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md</files>
  <read_first>.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md, docker-compose.uat-043-harness.yml, scripts/embed_match_smoke.py</read_first>
  <action>Replace the live-runbook-only gap area in `04.3-UAT.md` with concrete harness commands and expected outputs. Document the disposable warm DB activation path, the exact commands for `partial-match` (`IMG_4583.HEIC`) and `no-match` (`IMG_4641.HEIC`), where the JSON evidence reports are written, which fields must be copied into `04.3-UAT-RESULTS.md`, and how the operator should interpret interview, retry-pending, grounding-pending, and completed outcomes. Keep the runbook scoped to GAP-04.3-P3 only, cite D-18 through D-20, and do not re-open the already-fixed optional approval or grounding behavior outside the evidence that the harness reports.</action>
  <verify>
    <automated>rtk rg -n "embed_match_smoke.py --mode uat-harness --scenario partial-match|embed_match_smoke.py --mode uat-harness --scenario no-match|IMG_4583|IMG_4641|mealttracker_043_harness|trace|grounding|DiaryEntry|FoodVisual|reports/04.3" .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md</automated>
  </verify>
  <done>The 04.3 runbook now gives exact, repeatable commands for the named warm-state regressions and tells the operator exactly which evidence to record from harness output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local harness -> Postgres checkpoint clone/reset | The harness can destroy or replace DB state if it is allowed to point at the wrong database. |
| local harness -> API/bot ingest path | The harness drives normal ingest and must not bypass the application state machine with DB writes. |
| harness report -> planning evidence artifacts | The report must capture trace and state evidence without leaking secrets or omitting the fields needed for REASON-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-G1 | Tampering | `scripts/embed_match_smoke.py` | mitigate | Require an explicit disposable DB target such as `mealttracker_043_harness`, refuse destructive reset/clone steps against `mealttracker`, and cover the safety gate in `tests/test_embed_match_smoke.py`. |
| T-04.3-G2 | Repudiation | harness evidence JSON | mitigate | Persist meal ID, scenario name, DB target, prompts, answers, diary rows, food-visual rows, grounding state, and trace IDs in one report so GAP-04.3-P3 can be reproduced and audited consistently for D-19 and D-20. |
| T-04.3-G3 | Information Disclosure | `04.3-UAT.md` and harness output | mitigate | Limit artifact output to application state, row snapshots, and trace identifiers; do not dump env secrets, raw auth headers, or provider keys into the report. |
| T-04.3-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are allowed; reuse the existing Python stack, compose wiring, and smoke-helper dependencies already present in the repo. |
</threat_model>

<verification>
- `rtk sh -lc './.venv/bin/python scripts/assert_043_uat_harness_red.py'`
- `rtk sh -lc './.venv/bin/python -m unittest tests.test_embed_match_smoke -q'`
- `rtk sh -lc './.venv/bin/python scripts/embed_match_smoke.py --mode uat-harness --scenario partial-match --dry-run'`
- `rtk sh -lc './.venv/bin/python scripts/embed_match_smoke.py --mode uat-harness --scenario no-match --dry-run'`
- `rtk sh -lc 'API_HOST_BIND=127.0.0.1 API_HOST_PORT=18043 docker compose -f docker-compose.yml -f docker-compose.uat-043-harness.yml config | grep -E '\''mealttracker_043_harness|host_ip: 127.0.0.1|published: "18043"|postgres:5432/mealttracker_043_harness'\'''`
- `rtk rg -n "embed_match_smoke.py --mode uat-harness --scenario partial-match|embed_match_smoke.py --mode uat-harness --scenario no-match|IMG_4583|IMG_4641|mealttracker_043_harness|trace|grounding|DiaryEntry|FoodVisual|reports/04.3" .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md`
</verification>

<success_criteria>
- The repo contains one safe, named harness path for the 04.3 warm-state partial-match and no-match regressions.
- The harness report surfaces the interview and persistence evidence needed to close GAP-04.3-P3 without manual DB edits or ad-hoc replay steps.
- The 04.3 runbook documents exact commands and evidence capture for `IMG_4583.HEIC` and `IMG_4641.HEIC`.
</success_criteria>

<output>
Create `.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-04-SUMMARY.md` when done
</output>
