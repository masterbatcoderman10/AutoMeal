---
phase: 04-reason-interview-learning-loop
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - requirements.txt
  - app/config.py
  - app/services/meal_resolution_service.py
  - app/services/taxonomy_service.py
  - app/services/tracing_service.py
  - config/reasoning_taxonomy.json
autonomous: true
requirements:
  - REASON-04
  - PIPELINE-01
  - INTERVIEW-03
  - INTERVIEW-05
  - INTERVIEW-06
  - INFRA-04
user_setup: []
must_haves:
  truths:
    - "D-08: specificity and quantity policy live in an editable taxonomy file, not only in prompt prose."
    - "D-39, D-40, D-62, D-66, and D-67: later reasoning, interview, correction, and recovery slices reuse one shared final-write interface that resolves or creates the canonical `FoodItem` before any `DiaryEntry` or `FoodVisual` write and still propagates optional trace IDs."
    - "D-65 through D-68: optional Langfuse tracing is pinned consistently to `langfuse==4.7.0`, is disabled by default when env is absent, and defaults to metadata-only image handling."
  artifacts:
    - path: "app/services/meal_resolution_service.py"
      provides: "Shared final-write contract plus canonical `FoodItem` resolve/create boundary for reasoning, interview, and correction flows"
    - path: "app/services/taxonomy_service.py"
      provides: "Validated loader for `config/reasoning_taxonomy.json`"
    - path: "app/services/tracing_service.py"
      provides: "Failure-safe optional tracing wrapper"
    - path: "app/config.py"
      provides: "Phase 4 config surface for models, gating, reminders, janitor, and tracing"
    - path: "config/reasoning_taxonomy.json"
      provides: "Editable specificity, quantity, composite, and question-budget policy data"
  key_links:
    - from: "app/config.py"
      to: "app/services/reasoning_service.py"
      via: "model, threshold, parallelism, and tracing config"
      pattern: "REASONING_MODEL|REASONING_MATCH_THRESHOLD|REASONING_SEGMENT_PARALLELISM|LANGFUSE_ENABLED"
    - from: "app/services/taxonomy_service.py"
      to: "config/reasoning_taxonomy.json"
      via: "validated taxonomy load"
      pattern: "nutrition_impact|quantity_units|question_budget"
    - from: "app/services/tracing_service.py"
      to: "app/services/meal_resolution_service.py"
      via: "trace_id propagation"
      pattern: "trace_id"
    - from: "app/services/meal_resolution_service.py"
      to: "app/models/food_item.py"
      via: "canonical FoodItem lookup or creation before final write"
      pattern: "resolve_or_create_food_item|source_type|brand_name|restaurant_name|needs_grounding"
---

## Phase Goal

**As a** MealTracker user, **I want to** have the reasoning and interview runtime share one consistent config and write path, **so that** Phase 4 slices can add behavior without re-deciding settings, tracing, or how final meal state is committed.

<objective>
Create the shared runtime interfaces for Phase 4: config keys, editable taxonomy, final-write contract, and a failure-safe optional tracing wrapper with reconciled Langfuse versioning.

Purpose: The old Plan 01 mixed this layer into the schema plan. Splitting it keeps the foundation executable while giving the runtime layer its own clear contract and version reconciliation.
Output: Phase 4 config surface, shared resolution interface, taxonomy loader plus JSON asset, and optional tracing wrapper pinned to `langfuse==4.7.0`.
Implements: D-08, D-39, D-40, D-65, D-66, D-67, D-68.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-reason-interview-learning-loop/04-CONTEXT.md
@.planning/phases/04-reason-interview-learning-loop/04-AI-SPEC.md
@.planning/phases/04-reason-interview-learning-loop/04-RESEARCH.md
@.planning/phases/04-reason-interview-learning-loop/04-VALIDATION.md
@.planning/phases/04-reason-interview-learning-loop/04-PATTERNS.md
@.planning/phases/04-reason-interview-learning-loop/04-03-PLAN.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config and taxonomy -> model/runtime behavior | Environment and editable taxonomy files influence later reasoning and interview logic. |
| tracing wrapper -> production workflow | Optional telemetry must never become authoritative relative to DB state. |

## STRIDE Threat Register

High-severity gate: none in this plan. Every listed threat is medium or low severity and non-blocking once the listed mitigation is implemented.

| Threat ID | Category | Component | Severity | Blocking | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|----------|-------------|-----------------|
| T-04-03 | Information Disclosure | `config/reasoning_taxonomy.json` | low | no | mitigate | Keep only policy data in the taxonomy file; no secrets, raw prompts, or provider payloads. |
| T-04-04 | Denial of Service | `app/services/tracing_service.py` | medium | no | mitigate | Make tracing a best-effort wrapper that returns `None` trace IDs and never blocks model or write paths. |
| T-04-07 | Repudiation | `app/services/meal_resolution_service.py` | medium | no | mitigate | Centralize the final-write contract so later plans cannot fork diary-write behavior, FoodItem mutation, or audit metadata. |
| T-04-SC | Tampering | `requirements.txt` | low | no | mitigate | Reconcile all artifacts on `langfuse==4.7.0` and do not leave competing `>=3,<4` guidance in the phase contract. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add shared meal-resolution and taxonomy interfaces per D-08 and D-39</name>
  <files>app/services/meal_resolution_service.py, app/services/taxonomy_service.py, config/reasoning_taxonomy.json</files>
  <read_first>app/services/matching_service.py, app/models/meal_log.py, app/models/meal_segment.py, .planning/phases/04-reason-interview-learning-loop/04-CONTEXT.md, .planning/phases/04-reason-interview-learning-loop/04-PATTERNS.md</read_first>
  <action>Create `app/services/meal_resolution_service.py` as the single shared final-write boundary for later reasoning, interview, and correction plans. It must expose `resolve_or_create_food_item(...)` and `apply_final_meal_resolution(...)` as the concrete contract future slices call to commit canonical `FoodItem` mutation, `DiaryEntry`, `FoodVisual`, invalidation, correction-event, packaged/restaurant `NEEDS_GROUNDING` preparation, and final meal-state changes in one transaction per D-39, D-40, and D-62. The `FoodItem` path must be explicit: reuse an existing row when the resolved payload carries a trusted `food_item_id` or matches a normalized identity tuple such as `{canonical_name, source_type, brand_name, restaurant_name}`, otherwise create a new unverified or `needs_grounding` `FoodItem` when nutrition is incomplete. Add `app/services/taxonomy_service.py` plus `config/reasoning_taxonomy.json` so D-08 lives in editable structured data, with explicit sections for nutrition-impact specificity, quantity-unit preferences, composite-food handling, and question-budget policy. The loader must reject malformed taxonomy data before any model call uses it.</action>
  <verify>
    <automated>rtk .venv/bin/python -c "from app.services.meal_resolution_service import apply_final_meal_resolution, resolve_or_create_food_item; from app.services.taxonomy_service import load_reasoning_taxonomy; load_reasoning_taxonomy(); print(apply_final_meal_resolution.__name__, resolve_or_create_food_item.__name__)"</automated>
  </verify>
  <done>The shared write boundary and editable taxonomy surface exist and can be imported and exercised directly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 4 config keys and reconcile optional tracing on `langfuse==4.7.0`</name>
  <files>requirements.txt, app/config.py, app/services/tracing_service.py</files>
  <read_first>requirements.txt, app/config.py, app/services/llm_client.py, .planning/phases/04-reason-interview-learning-loop/04-AI-SPEC.md, .planning/phases/04-reason-interview-learning-loop/04-RESEARCH.md</read_first>
  <action>Pin `langfuse==4.7.0` in `requirements.txt` and keep the same version in phase documentation. Extend `app/config.py` with the concrete Phase 4 settings surface: reasoning and fallback model IDs, parser and fallback model IDs, match threshold, top-candidate floor, segment parallelism, interview reminder delay, janitor interval, stale timeout, max recoveries, `LANGFUSE_ENABLED`, and `LANGFUSE_CAPTURE_IMAGES`. Implement `app/services/tracing_service.py` as a failure-safe wrapper that emits trace IDs when enabled, defaults to metadata-only image handling unless the required storage env vars are present, and degrades to no-op behavior when tracing is disabled or unavailable.</action>
  <verify>
    <automated>rtk .venv/bin/python -c "from app.config import Settings; from app.services.tracing_service import maybe_start_trace; print(Settings.model_fields['LANGFUSE_ENABLED'].default, maybe_start_trace.__name__)"</automated>
  </verify>
  <done>Phase 4 runtime settings and optional tracing are consistent across code and artifacts, with no remaining Langfuse version mismatch.</done>
</task>

</tasks>

<verification>
- `rtk .venv/bin/python -c "from app.services.meal_resolution_service import apply_final_meal_resolution, resolve_or_create_food_item; from app.services.taxonomy_service import load_reasoning_taxonomy; from app.services.tracing_service import maybe_start_trace; load_reasoning_taxonomy(); print(apply_final_meal_resolution.__name__, resolve_or_create_food_item.__name__, maybe_start_trace.__name__)"`
- `rtk rg -n "langfuse==4.7.0|REASONING_MODEL|LANGFUSE_ENABLED|LANGFUSE_CAPTURE_IMAGES|question_budget" requirements.txt app/config.py app/services config/reasoning_taxonomy.json`
</verification>

<success_criteria>
- The shared runtime layer for Phase 4 exists independently of the schema plan.
- Optional tracing guidance is internally consistent on `langfuse==4.7.0` across the phase artifacts and plan set.
- Later plans can import one shared resolution interface, one taxonomy loader, and one tracing wrapper instead of inventing their own.
</success_criteria>

<output>
Create `.planning/phases/04-reason-interview-learning-loop/04-04-SUMMARY.md` when done.
</output>
