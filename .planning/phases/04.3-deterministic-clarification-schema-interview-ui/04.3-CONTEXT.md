# Phase 04.3: Deterministic Clarification Schema & Interview UI - Context

**Gathered:** 2026-06-01
**Status:** Ready for planning

<domain>
## Phase Boundary

Phase 4.3 is a corrective architecture phase between Phase 4.2 and Phase 5. It replaces user-facing LLM-authored interview turns with a deterministic Telegram clarification UI driven by structured reasoning output. Meal reasoning should emit grouped food records plus a strict `clarification_schema`; Telegram should render template questions as MCQ, confirmation, or open-field prompts; replies should be stored against stable question IDs; and the existing interview/finalizer LLM should be reduced to a barebones resolver skeleton that turns structured answers into `confirmation_items` for the existing authoritative FoodItem, DiaryEntry, and FoodVisual write path.

This phase is not Phase 5 grounding. It must not add SearXNG/Firecrawl tool loops, nutrition derivation expansion, slash-command surface work, or a new nutrition resolver. It should leave a clean resolver seam that Phase 5 can replace or extend with nutrition grounding.

</domain>

<decisions>
## Implementation Decisions

### Reasoning Output Contract
- **D-01:** `food_groups[*].top_3` is the active source of truth for candidate rankings. The current root-level `top_3` should be removed from active reasoning semantics or kept only as a short-lived compatibility adapter during migration.
- **D-02:** Meal reasoning must emit a strict `clarification_schema` alongside `food_groups` when any food group needs user input or approval.
- **D-03:** `clarification_schema` questions must have stable IDs, group IDs, primary segment ID, segment IDs, question kind, answer type, required/optional status, choices when applicable, and validation hints.
- **D-04:** Valid answer types for this phase are `single_choice`, `multi_choice`, `free_text`, and `confirm`. The planner may add internal enum names, but the user-facing behavior must stay this small.
- **D-05:** Question schema should be compact and cache-friendly. Do not push long prose instructions into every question; templates render the prose.

### Deterministic Telegram Clarification UI
- **D-06:** Telegram questions are rendered deterministically from templates, not composed by an interview LLM.
- **D-07:** MCQ/confirm questions should prefer Telegram buttons or reply markup where the existing bot architecture supports it; free text remains available for open-field corrections and "other" answers.
- **D-08:** Every user answer must map back to a stable question ID and be stored durably in `InterviewSession.current_prompt_payload` and/or `InterviewMessage` payloads.
- **D-09:** Partial replies are first-class. If the user answers only some required questions, the system must deterministically re-prompt only the remaining required questions and preserve prior answers.
- **D-10:** The bot must not loop on approval candidates that were already answered in prior structured state.

### Conditional Source-Origin Clarification
- **D-11:** Add source-origin clarification only when it can materially change nutrition or grounding behavior. Examples: flatbreads, packaged-looking foods, bakery items, sauces, takeout, desserts, branded/label-visible foods, or restaurant-looking items.
- **D-12:** Do not ask homemade/store-bought/source-origin for obvious home-cooked foods such as a clearly home-style curry unless visual evidence or candidate context indicates ambiguity.
- **D-13:** Source-origin options should distinguish at least `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, but the user-facing copy can say "homemade", "store bought", "packaged", "restaurant", and "not sure".

### Final Resolver Skeleton
- **D-14:** Reuse the current interview LLM only as a toned-down final resolver skeleton after deterministic answers are collected.
- **D-15:** The resolver must not ask user-facing interview questions. It receives compact grouped reasoning, structured answers, and prior confirmations, then emits strict `confirmation_items`.
- **D-16:** The existing final write path remains authoritative. The resolver output must flow through the same transaction that creates or updates FoodItems, DiaryEntries, and FoodVisual rows.
- **D-17:** Phase 5 may replace or extend this resolver with a nutrition/grounding resolver. Keep the Phase 4.3 resolver contract intentionally small and easy to swap.

### UAT And Checkpoints
- **D-18:** Preserve the temporary UAT database checkpoint `mealttracker_uat_with_meal_embeddings` as a useful repeated-meal state for validation.
- **D-19:** Live UAT should prove that repeated-meal vector candidates influence reasoning, template questions are rendered, a partial reply re-prompts only missing required questions, and final save creates the expected DiaryEntry and FoodVisual rows.
- **D-20:** Langfuse traces should show the reasoning output schema, clarification schema, deterministic answer payloads, and final resolver payload, without relying on model-authored user-facing prompt text.

### the agent's Discretion
- The planner may choose exact Python model names and storage shape for clarification questions and answers as long as question IDs are stable and durable.
- The planner may choose whether root-level `top_3` removal is hard-cut or compatibility-shimmed, provided tests lock the new source of truth.
- The planner may choose exact Telegram rendering primitives inside the existing python-telegram-bot setup.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase Scope And Prior Decisions
- `.planning/ROADMAP.md` - Phase 4.3 goal, success criteria, dependency on Phase 4.2, and Phase 5 boundary.
- `.planning/REQUIREMENTS.md` - `REASON-01` through `REASON-04`, `INTERVIEW-01`, `INTERVIEW-03`, `INTERVIEW-04`, and `MATCH-04`.
- `.planning/PROJECT.md` - single-user Telegram/iOS Shortcut product shape, OpenRouter-only constraint, and lowest-friction logging value.
- `.planning/STATE.md` - accumulated decisions and current execution context.
- `.planning/phases/04-reason-interview-learning-loop/04-CONTEXT.md` - original reasoning, interview, final write, and audit decisions.
- `.planning/phases/04.1-grouped-reasoning-human-interview-correction/04.1-CONTEXT.md` - grouped reasoning contract and group-targeted interview decisions.
- `.planning/phases/04.2-llm-threaded-interview-orchestration/04.2-CONTEXT.md` - current LLM-threaded interview design being simplified.
- `.planning/phases/04.2-llm-threaded-interview-orchestration/04.2-UAT.md` - live UAT failures, clean UAT DB setup, embedding checkpoint, and retry-loop evidence.

### Runtime Code
- `app/services/reasoning_schema.py` - current strict reasoning schema, grouped food records, and active root/top-level output contract.
- `app/services/reasoning_service.py` - reasoning prompt, grouped gate evaluation, root `top_3` compatibility, and final reasoning write path.
- `app/services/interview_service.py` - durable interview state, current deterministic roadmap helpers, confirmation item adapters, and final write handoff.
- `app/services/interview_schema.py` - current strict interview-turn/finalizer schema.
- `app/services/interview_turn_manager.py` - current LLM interview turn manager to reduce into a final resolver skeleton.
- `app/services/meal_resolution_service.py` - authoritative FoodItem, DiaryEntry, and FoodVisual write transaction.
- `bot/polling.py` - meal worker, interview kickoff, and Telegram prompt delivery paths.
- `bot/handlers.py` - reply routing, prompt ownership, and Telegram answer handling.
- `tests/test_reasoning_contract.py`, `tests/test_reasoning_flow.py`, `tests/test_reasoning_gate.py`, `tests/test_interview_flow.py`, `tests/test_interview_schema.py`, `tests/test_bot_contract.py`, `tests/test_match_flow.py` - coverage surfaces to update.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `reasoning_schema.reasoning_response_format()` already defines strict JSON for grouped reasoning and is the natural place to add `clarification_schema`.
- `reasoning_service.evaluate_reasoning_gate()` already normalizes group actions and currently returns root `top_3`; this is the obvious cleanup point for the new source-of-truth rule.
- `interview_service.current_target_question()` and related helpers already render deterministic prompts for the older roadmap flow; useful patterns can be reused for schema-driven templates.
- `InterviewSession.current_prompt_payload` and `InterviewMessage.parsed_payload` already provide durable state surfaces for question IDs, answers, and replay.
- `interview_turn_manager.run_interview_turn()` already has structured output, repair, and Langfuse tracing; it can be reduced or wrapped as the final resolver skeleton.
- `meal_resolution_service.apply_final_meal_resolution()` already creates the authoritative database writes and must remain the save path.

### Established Patterns
- Postgres is the source of truth for interview state; Telegram/PTB context is optional only.
- User-facing bot copy should be brief, human, and avoid developer terms like segment, confidence, schema, or nutrition relevance.
- Pipeline stages fail closed and repair once instead of guessing.
- Final writes happen only after all relevant groups are resolved and confirmation items are normalized.

### Integration Points
- Add or extend reasoning schema models for `clarification_schema`.
- Add a deterministic question renderer and answer mapper in the interview/bot layer.
- Persist structured answers in the existing interview session payload.
- Replace user-facing calls to the interview LLM with deterministic template rendering.
- Keep a compact resolver call, likely using current interview model config, only after required answers are complete.

</code_context>

<specifics>
## Specific Ideas

- This phase intentionally reverses part of Phase 4.2: the LLM should no longer own the user-facing interview loop.
- The current same-meal UAT showed vector search working and exposed the failure mode: after one partial reply, the system asked the right missing flatbread question but got stuck validating the model-authored follow-up.
- Template questions should make the missing thing physically answerable, for example choosing the flatbread type or selecting source-origin only when relevant.
- The final resolver can remain "dumb" for now because Phase 5 will replace it with a nutrition/grounding resolver.

</specifics>

<deferred>
## Deferred Ideas

- Phase 5 owns SearXNG/Firecrawl tool loops and nutrition grounding.
- A full nutrition resolver or richer post-interview grounding agent belongs to Phase 5.
- Broader bot polish, slash commands, daily summaries, and result formatting remain Phase 6.

</deferred>

---

*Phase: 04.3-deterministic-clarification-schema-interview-ui*
*Context gathered: 2026-06-01*
