---
phase: 05-agentic-grounding
plan: 04
type: tdd
wave: 4
depends_on:
  - 05-03
files_modified:
  - app/services/reasoning_service.py
  - app/services/grounding_service.py
  - app/services/interview_schema.py
  - app/services/interview_service.py
  - app/services/reasoning_schema.py
  - tests/test_reasoning_contract.py
  - tests/test_reasoning_flow.py
  - tests/test_grounding_service.py
  - tests/test_interview_flow.py
autonomous: true
gap_closure: true
requirements:
  - GROUND-01
  - GROUND-02
  - GROUND-03
must_haves:
  truths:
    - "Every reasoning-complete food group routes through the same shared bounded enrichment finalizer used after interview, with tools available on every finalizer call and search/scrape invoked only at model discretion per D-05 and D-06; the direct reasoning write-through save path is removed while `_run_reasoning_model()` stays tool-free per D-01 and D-02."
    - "Every grounding HTTP call consumes the same per-loop wall-clock budget and tool-call cap used by the model turn, so `WALL_CLOCK_TIMEOUT` and `MAX_TOOL_CALLS` stop the loop before Firecrawl requests overrun policy."
    - "MealSegment.ai_reasoning appends query text, fetched URLs, snippet excerpts, provenance, source URLs, iteration counts, and stop reasons for both reasoning-stage and post-interview grounding."
  artifacts:
    - path: "app/services/reasoning_service.py"
      provides: "Reasoning-path handoff into shared grounded group finalization"
    - path: "app/services/grounding_service.py"
      provides: "Budget-aware search/scrape methods that accept remaining timeout context"
    - path: "app/services/interview_schema.py"
      provides: "Live finalizer trace and result schema with snippet/provenance/source-url fields"
    - path: "tests/test_reasoning_flow.py"
      provides: "Regression coverage for reasoning-stage grounded finalization and trace append behavior"
  key_links:
    - from: "app/services/reasoning_service.py"
      to: "app/services/interview_service.py"
      via: "Shared bounded group finalizer on the auto-confirm reasoning path"
      pattern: "finalize_meal_from_reasoning|_run_group_finalizers|_bounded_group_finalizer_response"
    - from: "app/services/interview_service.py"
      to: "app/services/grounding_service.py"
      via: "Remaining loop budget passed into `search()` and `scrape()`"
      pattern: "remaining_time_s|search\\(|scrape\\(|timeout"
    - from: "app/services/interview_schema.py"
      to: "MealSegment.ai_reasoning"
      via: "Merged `grounding_trace` and human-readable trace text"
      pattern: "snippet|provenance|source_url|grounding_trace"
---

<objective>
Close the live grounding-behavior gaps in Phase 5 without rewriting the completed Phase 05 plans.

Purpose: Reuse the bounded grounding finalizer on the reasoning auto-complete path, thread remaining wall-clock budget into every Firecrawl request, and persist the missing trace/provenance fields required by verification.
Output: One shared bounded grounding path for reasoning and post-interview finalization, budget-aware Firecrawl calls, and auditable trace/provenance coverage in `MealSegment.ai_reasoning`.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-agentic-grounding/05-CONTEXT.md
@.planning/phases/05-agentic-grounding/05-RESEARCH.md
@.planning/phases/05-agentic-grounding/05-PATTERNS.md
@.planning/phases/05-agentic-grounding/05-AI-SPEC.md
@.planning/phases/05-agentic-grounding/05-VERIFICATION.md
@.planning/phases/05-agentic-grounding/05-03-SUMMARY.md
@app/services/reasoning_service.py
@app/services/grounding_service.py
@app/services/interview_schema.py
@app/services/interview_service.py
@app/services/reasoning_schema.py
@tests/test_reasoning_contract.py
@tests/test_reasoning_flow.py
@tests/test_grounding_service.py
@tests/test_interview_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - reproduce reasoning-path grounding, loop-budget, and trace-provenance regressions</name>
  <read_first>
    .planning/phases/05-agentic-grounding/05-VERIFICATION.md
    app/services/reasoning_service.py
    app/services/grounding_service.py
    app/services/interview_schema.py
    app/services/interview_service.py
    app/services/reasoning_schema.py
    tests/test_reasoning_contract.py
    tests/test_reasoning_flow.py
    tests/test_grounding_service.py
    tests/test_interview_flow.py
  </read_first>
  <files>tests/test_reasoning_contract.py, tests/test_reasoning_flow.py, tests/test_grounding_service.py, tests/test_interview_flow.py</files>
  <action>Add failing regressions that lock the missing live behavior. In `tests/test_reasoning_flow.py`, prove `reasoning_service.finalize_meal_from_reasoning()` routes every reasoning-complete group through the same bounded grounded finalizer used after interview per D-05 and D-06, with tools available on every finalizer call, search/scrape exercised only when the model decides it needs them, and no remaining direct reasoning write-through save path; keep `_run_reasoning_model()` strict-json and tool-free per D-01 and D-02. In `tests/test_grounding_service.py`, require `GroundingService.search()` and `GroundingService.scrape()` to accept remaining loop-budget context and stop with `WALL_CLOCK_TIMEOUT` or `MAX_TOOL_CALLS` before Firecrawl can outlive the enclosing loop. In `tests/test_reasoning_contract.py` and `tests/test_interview_flow.py`, require the live `GroundingTracePayload` and `FinalizedGroupResult` surfaces to carry snippet excerpts, provenance, and `source_url`, and require those fields to survive the merged `segment.ai_reasoning["grounding_trace"]`. Use the exact identifiers `_bounded_group_finalizer_response()`, `_execute_grounding_tool()`, `group_finalizer_response_format()`, and `grounding_result_response_format()` in assertions so GREEN work lands in the live path rather than a side shim.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow -q</automated>
  </verify>
  <acceptance_criteria>The targeted suites fail because the reasoning path never reuses the bounded grounded finalizer, `GroundingService` ignores remaining loop budget, and the live finalizer/trace contracts still drop snippet, provenance, or `source_url` evidence.</acceptance_criteria>
  <done>Executable RED coverage exists for actionable verification gaps 1-3: reasoning-path grounding, loop-budget enforcement, and auditable trace/provenance persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - share the bounded finalizer across reasoning and interview paths, then persist the richer trace</name>
  <read_first>
    app/services/reasoning_service.py
    app/services/grounding_service.py
    app/services/interview_schema.py
    app/services/interview_service.py
    app/services/reasoning_schema.py
    tests/test_reasoning_contract.py
    tests/test_reasoning_flow.py
    tests/test_grounding_service.py
    tests/test_interview_flow.py
  </read_first>
  <files>app/services/reasoning_service.py, app/services/grounding_service.py, app/services/interview_schema.py, app/services/interview_service.py, app/services/reasoning_schema.py, tests/test_reasoning_contract.py, tests/test_reasoning_flow.py, tests/test_grounding_service.py, tests/test_interview_flow.py</files>
  <action>Implement the live gap closure without violating the locked Phase 5 decisions. In `app/services/reasoning_service.py`, keep `_run_reasoning_model()` tool-free per D-01 and D-02, remove any direct reasoning write-through save path, and route `finalize_meal_from_reasoning()` through the existing shared bounded group finalizer for every reasoning-complete group per D-05 and D-06 so GROUND-01 is satisfied by one uniform code path. In `app/services/interview_service.py`, extract the bounded group finalizer into a reusable helper callable from both `finalize_confirmed_interview()` and `finalize_meal_from_reasoning()`, ensure tools are exposed on every finalizer invocation while letting the model decide whether to call search/scrape for that group, and thread one `GroundingLoopState` through model turns plus tool executions. In `app/services/grounding_service.py`, change `search()` and `scrape()` to accept remaining-budget context and use `min(loop_state.remaining_time_s(), budget.tool_timeout_s)` for every Firecrawl HTTP timeout, while surfacing `WALL_CLOCK_TIMEOUT`, `MAX_TOOL_CALLS`, and duplicate-call stops consistently. In `app/services/interview_schema.py` and `app/services/reasoning_schema.py`, mirror the existing provenance contract from `grounding_result_response_format()` into the live finalizer result and trace surfaces so snippet excerpts, provenance, and `source_url` are first-class fields. Update the grounding-trace merge/formatter in `app/services/interview_service.py` so `MealSegment.ai_reasoning` appends both the richer machine-readable trace and a readable summary for reasoning-stage and post-interview grounded completions.</action>
  <verify>
    <automated>rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow -q</automated>
  </verify>
  <acceptance_criteria>Reasoning-complete meals can invoke the bounded grounding finalizer without adding tools to `_run_reasoning_model()`, every Firecrawl request obeys the remaining per-loop budget, and the saved grounding trace now includes snippet/provenance/source-url evidence for both live grounding entry points.</acceptance_criteria>
  <done>Verification gaps 1-3 are closed in the live code path: reasoning-stage grounding exists via the shared finalizer seam, loop bounds govern HTTP tools as well as model turns, and `MealSegment.ai_reasoning` is fully auditable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reasoning-complete meal -> shared finalizer | Auto-complete reasoning output becomes tool-using grounding behavior here |
| bounded loop -> Firecrawl HTTP client | Remaining wall-clock and call-cap policy becomes network timeout behavior here |
| finalizer output -> `MealSegment.ai_reasoning` | Untrusted grounding evidence becomes persisted audit trace here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Integrity | `app/services/reasoning_service.py` | mitigate | Reuse the existing bounded finalizer instead of adding a second ad hoc grounding path; keep `_run_reasoning_model()` tool-free per D-01 and D-02. |
| T-05-13 | DoS | `app/services/grounding_service.py` | mitigate | Pass remaining loop budget into every Firecrawl call so HTTP timeouts cannot exceed the loop wall-clock. |
| T-05-14 | Repudiation | `app/services/interview_schema.py` | mitigate | Persist snippet excerpts, provenance, `source_url`, iteration count, and stop reason inside the live grounding trace. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this gap-closure plan. |
</threat_model>

<verification>
Run `rtk docker run --rm -v "$PWD":/app -w /app mealttracker-plan05-test python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_grounding_service tests.test_interview_flow -q`.
Confirm one reasoning-complete branded/restaurant flow and one post-interview grounded flow both append snippet/provenance/source-url evidence and stop cleanly when the loop budget is exhausted.
</verification>

<success_criteria>
The Phase 5 live grounding behavior is consistent across reasoning and post-interview paths, bounded by one shared policy surface, and auditable down to snippet/provenance details in `MealSegment.ai_reasoning`.
</success_criteria>

<output>
Create `.planning/phases/05-agentic-grounding/05-04-SUMMARY.md` when done
</output>
