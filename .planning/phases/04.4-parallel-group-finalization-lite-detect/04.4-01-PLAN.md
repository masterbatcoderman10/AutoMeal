---
phase: 04.4-parallel-group-finalization-lite-detect
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/services/interview_schema.py
  - scripts/vision_smoke.py
  - tests/test_vision_service.py
  - tests/test_interview_schema.py
  - tests/test_bot_contract.py
autonomous: true
requirements:
  - VISION-01
  - INTERVIEW-03
  - INTERVIEW-04
must_haves:
  truths:
    - "Submitting a non-food or low-confidence photo still fails closed at the detect stage while the default detect model has switched to google/gemini-3.1-flash-lite."
    - "The existing live detect smoke surface still succeeds against `google/gemini-3.1-flash-lite`, proving the structured food-vs-not-food contract holds outside mocked/unit coverage."
    - "A per-group finalizer payload that omits required save metadata or includes unexpected fields is rejected before it can reach the authoritative meal save path."
    - "Operators can configure the finalizer model and bounded per-group parallelism independently of the detect and interview settings, so the phase can run Lite detect plus parallel finalization without changing external bot behavior."
  artifacts:
    - path: "app/config.py"
      provides: "Explicit DETECT_MODEL Lite default plus FINALIZER_MODEL and FINALIZER_GROUP_PARALLELISM settings"
    - path: "app/services/interview_schema.py"
      provides: "Strict per-group finalizer schema, response_format helper, and parser helpers"
    - path: "scripts/vision_smoke.py"
      provides: "Live detect structured-output smoke surface that can explicitly exercise `google/gemini-3.1-flash-lite` without bypassing the shared runtime path"
    - path: "tests/test_interview_schema.py"
      provides: "Schema strictness and parser fail-closed regression coverage"
  key_links:
    - from: "app/config.py"
      to: "bot/polling.py"
      via: "settings.DETECT_MODEL and settings.FINALIZER_MODEL"
      pattern: "DETECT_MODEL|FINALIZER_MODEL"
    - from: "app/services/interview_schema.py"
      to: "app/services/interview_service.py"
      via: "strict finalizer response_format and parser helper"
      pattern: "response_format|parse"
    - from: "app/config.py"
      to: "scripts/vision_smoke.py"
      via: "shared detect model default and optional explicit Lite detect override"
      pattern: "DETECT_MODEL|detect-model"
---

<objective>
Lock the 04.4 model and schema contract surface per D-05, D-15, D-16, and D-17 before touching the authoritative save seam.

Purpose: Prevent service work from guessing at finalizer payload shape or detect defaults while proving the Lite detect swap still passes the repo's live structured-output smoke surface.
Output: Explicit Lite detect/finalizer settings, strict per-group finalizer schema/parser coverage, and an explicit Lite detect smoke path in `scripts/vision_smoke.py`.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-CONTEXT.md
@.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-RESEARCH.md
@.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-PATTERNS.md
@.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-VALIDATION.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-03-SUMMARY.md
@app/config.py
@app/services/interview_schema.py
@scripts/vision_smoke.py
@tests/test_vision_service.py
@tests/test_interview_schema.py
@tests/test_bot_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - capture Lite detect defaults and strict finalizer contract failures</name>
  <files>scripts/vision_smoke.py, tests/test_vision_service.py, tests/test_interview_schema.py, tests/test_bot_contract.py</files>
  <action>Extend the existing `unittest` suites so they fail against the current code for the exact 04.4 contract: assert `DETECT_MODEL` defaults to `google/gemini-3.1-flash-lite` per D-16/D-17; assert `Settings` exposes `FINALIZER_MODEL` and `FINALIZER_GROUP_PARALLELISM` per D-15/D-02; and add strict finalizer-schema/parser regressions that require final dish name, source type, quantity payload/display, portion bucket, aliases/supporting metadata, and group/segment IDs while rejecting extra keys, missing required fields, and malformed JSON per D-05/D-07/D-08/D-10. Also wire the existing live smoke surface `scripts/vision_smoke.py` into this contract: if the script cannot yet pin detect mode to `google/gemini-3.1-flash-lite` explicitly, add the minimal argument/config plumbing needed so Phase 04.4 can verify the Lite detect structured-output path without bypassing the shared runtime code path. Follow Pattern 4 from `04.4-PATTERNS.md` for explicit config ownership in `app/config.py`, and mirror the exact analog test surfaces mapped there: `tests/test_vision_service.py`, `tests/test_interview_schema.py`, `tests/test_bot_contract.py`, and the existing smoke-script surface from Phase 02 rather than inventing a throwaway harness.</action>
  <verify>
    <automated>.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_bot_contract -q</automated>
  </verify>
  <done>The targeted suites fail only because the Lite detect default, any needed smoke-script override wiring, and the strict finalizer schema/config surface do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - implement explicit detect/finalizer settings, smoke coverage, and save-ready schema helpers</name>
  <files>app/config.py, app/services/interview_schema.py, scripts/vision_smoke.py, tests/test_vision_service.py, tests/test_interview_schema.py, tests/test_bot_contract.py</files>
  <action>Update `Settings` so `DETECT_MODEL` defaults to `google/gemini-3.1-flash-lite` per D-16/D-17, add explicit `FINALIZER_MODEL` plus bounded `FINALIZER_GROUP_PARALLELISM` per D-15/D-02, and preserve the current conservative detect call contract. Follow Pattern 4 from `04.4-PATTERNS.md` for separate detect/finalizer config ownership, preserving the exact `app/config.py` analog shape. Keep `scripts/vision_smoke.py` on the shared runtime path from Phase 02, and add only the minimal wiring needed for an explicit Lite detect smoke run if the current script cannot already pin detect mode to `google/gemini-3.1-flash-lite`; do not fork a raw HTTP-only probe or a mock harness. In `app/services/interview_schema.py`, add strict save-ready per-group finalizer models plus `response_format` and parser helpers that produce only metadata for the existing write path per D-06/D-07, preserve materially important clarification outputs per D-08/D-09, and forbid sibling-group/global grounding fields per D-03/D-18. Use the `app/services/interview_schema.py` and `tests/test_interview_schema.py` analog surfaces mapped in `04.4-PATTERNS.md` so the helpers stay aligned with the repo's existing strict-schema conventions. Validate with Pydantic strictness and fail closed on malformed JSON per D-05; do not introduce a second persistence contract or any Phase 5 tool fields.</action>
  <verify>
    <automated>sh -lc '.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_bot_contract -q && .venv/bin/python scripts/vision_smoke.py --mode detect --sample sample_images/IMG_4583.HEIC --detect-model google/gemini-3.1-flash-lite'</automated>
  </verify>
  <done>`Settings` exposes the Lite detect/finalizer contract, the live detect smoke surface succeeds against `google/gemini-3.1-flash-lite`, and the per-group finalizer schema/parser passes targeted tests while rejecting malformed payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/env -> detect and finalizer services | Runtime model IDs and concurrency settings cross from env/config into service behavior here |
| OpenRouter finalizer response -> parser | Untrusted model JSON becomes application data here |
| schema helpers -> service seam | Contract drift here would let malformed or lossy payloads reach the finalizer flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-01 | Tampering | `app/services/interview_schema.py` | mitigate | Enforce strict schema generation with `additionalProperties: false`, required save-ready fields, and parser rejection of malformed/extra JSON per D-05. |
| T-04.4-02 | Tampering | `app/config.py` detect/finalizer defaults | mitigate | Lock `DETECT_MODEL`, `FINALIZER_MODEL`, and `FINALIZER_GROUP_PARALLELISM` in settings/tests so Lite detect and bounded fan-out cannot silently drift from D-15/D-17. |
| T-04.4-03 | Information Disclosure | finalizer input/output schema | mitigate | Keep the schema group-scoped and exclude sibling-group/global grounding payloads per D-03/D-18. |
| T-04.4-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this plan. |
</threat_model>

<verification>
Run `.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_bot_contract -q`.
Run the live Lite detect smoke required by Roadmap success criterion 6: `.venv/bin/python scripts/vision_smoke.py --mode detect --sample sample_images/IMG_4583.HEIC --detect-model google/gemini-3.1-flash-lite`.
Run the 04.4 Nyquist quick suite from `04.4-VALIDATION.md`: `.venv/bin/python -m unittest tests.test_vision_service tests.test_interview_schema tests.test_interview_flow -q` before handing the wave forward.
</verification>

<success_criteria>
The repo has an explicit Lite detect default, an explicit live smoke command that proves `google/gemini-3.1-flash-lite` still satisfies the detect structured-output contract, explicit finalizer settings, and a strict per-group finalizer schema/parser with failing-then-passing regression coverage on the existing `unittest` surfaces.
</success_criteria>

<output>
Create `.planning/phases/04.4-parallel-group-finalization-lite-detect/04.4-01-SUMMARY.md` when done
</output>
