---
phase: 04-reason-interview-learning-loop
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_interview_flow.py
  - tests/test_fix_flow.py
  - tests/test_janitor.py
autonomous: true
requirements:
  - INTERVIEW-01
  - INTERVIEW-02
  - INTERVIEW-03
  - INTERVIEW-04
  - INTERVIEW-05
  - INTERVIEW-06
  - INFRA-04
user_setup: []
must_haves:
  truths:
    - "D-43 through D-54 and INTERVIEW-01 through INTERVIEW-04: Wave 0 defines the pinned-chat interview backbone, reminder behavior, best-effort unresolved closeout, and minimal `NEEDS_GROUNDING` handoff before Telegram logic changes begin."
    - "D-48 and D-49: Wave 0 explicitly locks item-specific confirmation edits, free-text bulk correction before write, updated confirmation replay, and the photo-aware `all wrong` branch."
    - "D-55 through D-64 plus D-70 through D-79: Wave 0 defines `/fix` mutation boundaries and janitor recovery semantics before correction or recovery services are implemented."
  artifacts:
    - path: "tests/test_interview_flow.py"
      provides: "Interview backbone, confirmation-edit loop, and `all wrong` branch coverage"
    - path: "tests/test_fix_flow.py"
      provides: "Scoped invalidation, diff confirmation, and cancel-safety coverage"
    - path: "tests/test_janitor.py"
      provides: "Artifact-first recovery, retry exhaustion, and duplicate-notify suppression coverage"
  key_links:
    - from: "tests/test_interview_flow.py"
      to: "bot/handlers.py"
      via: "callback and free-text interview contract"
      pattern: "CONFIRMATION|all wrong|updated confirmation"
    - from: "tests/test_fix_flow.py"
      to: "app/services/correction_service.py"
      via: "identity-vs-quantity mutation contract"
      pattern: "visual_learning_eligible|before_json|after_json"
    - from: "tests/test_janitor.py"
      to: "app/services/recovery_service.py"
      via: "stale-stage and retry contract"
      pattern: "recovery_attempt_count|FAILED|INTERVIEWING"
---

## Phase Goal

**As a** MealTracker user, **I want to** have the interview, correction, and recovery flows specified in executable tests before implementation starts, **so that** later plans can add Telegram and janitor behavior without weakening the locked confirmation and mutation rules.

<objective>
Create the Wave 0 interaction-and-recovery validation slice for Phase 4: targeted test modules for Telegram interview progression, confirmation edit loops, `/fix`, and janitor recovery.

Purpose: The checker called out that D-48 and D-49 were only named, not planned. This Wave 0 slice converts those decisions and the recovery rules into executable contracts before any implementation plan modifies handlers or worker state.
Output: discoverable targeted test modules for interview, correction, and janitor behavior.
Implements: D-43, D-44, D-45, D-46, D-47, D-48, D-49, D-50, D-51, D-52, D-53, D-54, D-55, D-56, D-57, D-58, D-59, D-60, D-61, D-62, D-63, D-64, D-70, D-71, D-72, D-73, D-74, D-75, D-76, D-77, D-78, D-79.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-reason-interview-learning-loop/04-CONTEXT.md
@.planning/phases/04-reason-interview-learning-loop/04-AI-SPEC.md
@.planning/phases/04-reason-interview-learning-loop/04-RESEARCH.md
@.planning/phases/04-reason-interview-learning-loop/04-VALIDATION.md
@.planning/phases/04-reason-interview-learning-loop/04-PATTERNS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram/user input -> future business logic | Wave 0 tests define how untrusted callbacks and text may advance or edit state. |
| stale worker state -> recovery mutation | Wave 0 tests define how background recovery may reset or fail rows. |

## STRIDE Threat Register

High-severity gate: none in this plan. Every listed threat is medium severity and non-blocking once the listed mitigation is implemented.

| Threat ID | Category | Component | Severity | Blocking | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|----------|-------------|-----------------|
| T-04-09 | Elevation of Privilege | `tests/test_interview_flow.py` | medium | no | mitigate | Lock pinned-chat session ownership and reject callback-payload-as-truth behavior before handlers are implemented. |
| T-04-13 | Tampering | `tests/test_fix_flow.py` | medium | no | mitigate | Lock identity-only invalidation, quantity-only no-invalidation, and confirm-or-cancel semantics before correction code lands. |
| T-04-17 | Denial of Service | `tests/test_janitor.py` | medium | no | mitigate | Lock artifact-first recovery, exact retry ceiling, and duplicate-notify suppression before scheduler code lands. |
| T-04-SC | Repudiation | Wave 0 targeted tests | medium | no | mitigate | Encode D-48, D-49, `/fix`, and janitor semantics in named regression files so future changes remain auditable. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 interview tests covering confirmation edits and the photo-aware `all wrong` branch</name>
  <files>tests/test_interview_flow.py</files>
  <read_first>tests/test_bot_contract.py, bot/handlers.py, bot/messages.py, .planning/phases/04-reason-interview-learning-loop/04-CONTEXT.md, .planning/phases/04-reason-interview-learning-loop/04-VALIDATION.md</read_first>
  <action>Author `tests/test_interview_flow.py` as the executable contract for the unresolved-meal path. The module must encode the pinned-chat interview backbone, one-targeted-question progression, parser fallback expectations, one-shot reminder behavior, and best-effort unresolved closeout per D-43 through D-54. It must also explicitly cover D-48 and D-49: item-specific confirmation edits, free-text bulk correction before final write, replaying an updated confirmation after edits, and a photo-aware `all wrong` branch that references the segment or group plus brief evidence before collecting a corrected identity.</action>
  <verify>
    <automated>rtk .venv/bin/python -m py_compile tests/test_interview_flow.py</automated>
  </verify>
  <done>The repo has a discoverable Wave 0 interview contract that includes the locked confirmation-edit and `all wrong` behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 `/fix` and janitor tests for mutation scope and recovery semantics</name>
  <files>tests/test_fix_flow.py, tests/test_janitor.py</files>
  <read_first>tests/test_match_flow.py, bot/handlers.py, bot/polling.py, .planning/phases/04-reason-interview-learning-loop/04-CONTEXT.md, .planning/phases/04-reason-interview-learning-loop/04-VALIDATION.md</read_first>
  <action>Author `tests/test_fix_flow.py` and `tests/test_janitor.py` as the rest of the Wave 0 surface. The `/fix` module must lock D-55 through D-64: direct and recent-entry entry points, diff preview, confirm-or-cancel behavior, identity-only visual invalidation, quantity-only no-invalidation, correction history append, and `visual_learning_eligible` gating. The janitor module must lock D-70 through D-79: artifact-first stale-stage recovery, exact three-attempt exhaustion, interview-state exclusions, and single failure-notification behavior even across repeated janitor runs.</action>
  <verify>
    <automated>rtk .venv/bin/python -m py_compile tests/test_fix_flow.py tests/test_janitor.py</automated>
  </verify>
  <done>The repo has discoverable Wave 0 correction and recovery contracts before any production mutation code or scheduler logic is changed.</done>
</task>

</tasks>

<verification>
- `rtk .venv/bin/python -m py_compile tests/test_interview_flow.py tests/test_fix_flow.py tests/test_janitor.py`
- `rtk rg -n "all wrong|updated confirmation|visual_learning_eligible|recovery_attempt_count|duplicate" tests/test_interview_flow.py tests/test_fix_flow.py tests/test_janitor.py`
</verification>

<success_criteria>
- The interaction and recovery side of Wave 0 exists as real files on disk before any implementation plan begins.
- D-48 and D-49 are now enforced by test artifacts, not only named in plan prose.
- `/fix` and janitor behavior are specified in regression tests before mutation code or scheduler code is written.
</success_criteria>

<output>
Create `.planning/phases/04-reason-interview-learning-loop/04-02-SUMMARY.md` when done.
</output>
