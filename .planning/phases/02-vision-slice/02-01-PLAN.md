---
phase: 02-vision-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/services/llm_client.py
  - app/services/vision_service.py
  - bot/main.py
  - bot/polling.py
  - tests/test_vision_service.py
  - tests/test_bot_contract.py
autonomous: true
requirements: [VISION-01, VISION-02]
user_setup: []
must_haves:
  truths:
    - "D-01: Detect, segment, and label remain separate model calls even if they share one client wrapper"
    - "D-02: Detect is conservative; uncertain photos bias toward skipping, not continuing"
    - "D-03 and D-16: Confident non-food photos produce no final result message"
    - "D-04: Visible meal clutter is acceptable if a meal is clearly present anywhere in the frame"
  artifacts:
    - "Runtime settings exist for detect/segment/label model IDs and max segment count"
    - "app/services/vision_service.py exposes detect-stage schemas and parsing helpers"
    - "Bot process runs a detect worker in addition to the existing ack loop"
  key_links:
    - "MealLog(PENDING) -> ack loop -> MealLog(DETECTING) -> detect worker -> COMPLETED or SEGMENTING"
---

<objective>
Build the detect-stage gate and non-food completion slice for Phase 2.
Purpose: A real user can send a clear non-meal photo and watch the pipeline stop safely without downstream segmentation work, while Phase 2 also establishes the shared multimodal client path all later slices depend on.
Output: detect-stage settings, shared vision service scaffolding, detect worker wiring, and contract tests for conservative skip behavior
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vision-slice/02-CONTEXT.md (D-01 through D-04, D-16)
@.planning/phases/02-vision-slice/02-RESEARCH.md (§1, §2, §5)
@.planning/phases/02-vision-slice/02-PATTERNS.md
@.planning/phases/02-vision-slice/02-VALIDATION.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Wrong model or malformed multimodal payload silently bypasses the detect gate | HIGH | Add explicit structured detect schema parsing and tests that fail closed when required keys are missing |
| Borderline non-food photo continues deeper into the meal pipeline | MEDIUM | Keep a conservative detect threshold and unit-test skip behavior for uncertain payloads |
| Final Telegram output is sent for confident non-food images | MEDIUM | Separate ack behavior from final result behavior and assert no follow-up message is sent on non-food completion |
| Bot background worker leaks on shutdown | LOW | Register detect task in `bot/main.py` and cancel it symmetrically in `post_shutdown()` |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Extend runtime settings and shared OpenRouter chat wrapper for vision calls</name>
  <files>app/config.py, app/services/llm_client.py</files>
  <read_first>app/config.py, app/services/llm_client.py, .planning/phases/02-vision-slice/02-RESEARCH.md §2, .planning/phases/02-vision-slice/02-PATTERNS.md</read_first>
  <action>Add Phase 2 vision settings to `Settings`: `DETECT_MODEL` defaulting to `google/gemma-4-31b-it`, `SEGMENT_MODEL` defaulting to `google/gemini-3-flash-preview`, `SEGMENT_RETRY_MODEL` defaulting to `google/gemini-3.5-flash`, `LABEL_MODEL` defaulting to `google/gemini-3-flash-preview`, and `VISION_MAX_SEGMENTS` defaulting to `8`. Extend `OpenRouterClient.chat_completion()` so callers can pass multimodal content arrays unchanged and can also pass `extra_body` for provider-specific options without bypassing the shared wrapper.</action>
  <verify>rtk rg -n "DETECT_MODEL|SEGMENT_MODEL|SEGMENT_RETRY_MODEL|LABEL_MODEL|VISION_MAX_SEGMENTS|extra_body" app/config.py app/services/llm_client.py</verify>
  <acceptance_criteria>
    - "app/config.py contains `DETECT_MODEL`, `SEGMENT_MODEL`, `SEGMENT_RETRY_MODEL`, `LABEL_MODEL`, and `VISION_MAX_SEGMENTS`"
    - "app/services/llm_client.py `chat_completion` accepts an `extra_body` argument"
    - "No new OpenRouter client class is introduced outside `app/services/llm_client.py`"
  </acceptance_criteria>
  <done>Shared settings and client wrapper can support Phase 2 detect, segment, and label calls</done>
</task>

<task type="auto">
  <name>Task 2: Create detect-stage schemas and conservative food/non-food parsing helpers</name>
  <files>app/services/vision_service.py, tests/test_vision_service.py</files>
  <read_first>app/services/llm_client.py, .planning/phases/02-vision-slice/02-CONTEXT.md, .planning/phases/02-vision-slice/02-RESEARCH.md §2 and §5, tests/test_bot_contract.py</read_first>
  <action>Create `app/services/vision_service.py` with detect-stage request/response schemas, a helper that builds the multimodal detect prompt, and a `detect_food_photo(...)` path that returns a plain Python result containing `is_food`, `confidence`, and the normalized next action (`skip` or `segment`). Create `tests/test_vision_service.py` coverage that proves missing keys, malformed booleans, and low-confidence payloads fail closed into the skip path rather than continuing to segmentation.</action>
  <verify>.venv/bin/python -m unittest tests.test_vision_service</verify>
  <acceptance_criteria>
    - "app/services/vision_service.py defines a detect-stage schema or parser with `is_food` and `confidence`"
    - "tests/test_vision_service.py covers malformed detect payload rejection"
    - "tests/test_vision_service.py covers conservative skip behavior for uncertain detect responses"
  </acceptance_criteria>
  <done>Detect-stage parsing is deterministic, test-covered, and conservative</done>
</task>

<task type="auto">
  <name>Task 3: Add a detect worker slice for non-food completion and food handoff</name>
  <files>bot/main.py, bot/polling.py, tests/test_bot_contract.py</files>
  <read_first>bot/main.py, bot/polling.py, bot/messages.py, app/models/meal_log.py, .planning/phases/02-vision-slice/02-RESEARCH.md §1 and §5, .planning/phases/02-vision-slice/02-PATTERNS.md</read_first>
  <action>Register a second bot background task dedicated to `MealLog(DETECTING)` rows. In `bot/polling.py`, claim one detectable meal at a time, call the new detect helper, set `processing_status=COMPLETED` for confident non-food photos without sending a final result message, and set `processing_status=SEGMENTING` for accepted meal photos. Add async tests proving the worker moves non-food meals to `COMPLETED`, sends no follow-up Telegram message on that path, and shuts down cleanly through `post_shutdown()`.</action>
  <verify>.venv/bin/python -m unittest tests.test_bot_contract</verify>
  <acceptance_criteria>
    - "bot/main.py stores a second background task for the detect worker"
    - "bot/polling.py claims `MealProcessingStatus.DETECTING` rows"
    - "tests/test_bot_contract.py asserts no final `bot.send_message(...)` call occurs for the confident non-food path"
    - "Food photos advance from `DETECTING` to `SEGMENTING` without being marked complete in this plan"
  </acceptance_criteria>
  <done>Detect worker drives the first real Phase 2 end-to-end slice: non-food photos terminate safely and food photos are handed to segmentation</done>
</task>
</tasks>

<verification>
- [ ] `.venv/bin/python -m unittest tests.test_vision_service` passes
- [ ] `.venv/bin/python -m unittest tests.test_bot_contract` passes
- [ ] Detect worker registers on startup and cancels on shutdown
- [ ] Confident non-food photos end in `COMPLETED` without a final result message
- [ ] Accepted meal photos advance to `SEGMENTING`
</verification>
