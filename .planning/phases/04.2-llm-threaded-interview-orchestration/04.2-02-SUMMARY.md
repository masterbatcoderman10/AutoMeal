---
phase: 04.2-llm-threaded-interview-orchestration
plan: 02
subsystem: api
tags: [openrouter, pydantic, telegram, interview, langfuse, unittest]
requires:
  - phase: 04.2-llm-threaded-interview-orchestration
    provides: strict turn schema, traced interview turn manager, approval-aware authoritative state
provides:
  - RED regressions for meal interview kickoff, continuation, ready_to_confirm, and fail-closed retry behavior
  - LLM-driven meal interview kickoff from grouped authoritative state with persisted Telegram prompt ids
  - Meal-session reply orchestration through the interview turn manager with approval_status-preserving finalizer handoff
affects: [04.2-03 routing hardening, interview finalization, telegram meal interviews]
tech-stack:
  added: []
  patterns: [bounded transcript replay, approval-status preserving confirmation state, fail-closed meal turn retry]
key-files:
  created:
    - scripts/assert_interview_turn_red.py
  modified:
    - app/services/interview_service.py
    - bot/handlers.py
    - bot/polling.py
    - tests/test_interview_flow.py
    - tests/test_bot_contract.py
key-decisions:
  - "Meal interview kickoff now calls the same turn manager as replies so the first Telegram prompt is generated from authoritative grouped state instead of a deterministic question template."
  - "Structured ready_to_confirm items are preserved directly on session state, rather than being rebuilt from flattened answers, so approval_status survives until finalize_confirmed_interview()."
  - "Meal-session validation failures append transcript metadata and send a neutral retry prompt, but they do not mutate authoritative confirmation state or introduce a second write path."
patterns-established:
  - "Meal interview turn loop: persist user transcript, replay bounded state + recent transcript through run_interview_turn(), then either send a follow-up or hand off unchanged confirmation items to the authoritative finalizer."
  - "Kickoff prompt persistence: send the first assistant turn, then store the Telegram message_id on InterviewSession.last_bot_message_id and the matching InterviewMessage row."
requirements-completed: [INTERVIEW-01, INTERVIEW-02, INTERVIEW-03, REASON-04]
duration: 14min
completed: 2026-05-30
---

# Phase 04.2 Plan 02: LLM-Threaded Interview Orchestration Summary

**Meal interview kickoff and continuation now run through the traced LLM turn manager, preserve per-group approval status, and fail closed without bypassing the existing finalizer**

## Performance

- **Duration:** 14 min
- **Started:** 2026-05-30T14:16:00Z
- **Completed:** 2026-05-30T14:30:34Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Added RED coverage for the live meal interview slice: kickoff prompt generation, turn-manager continuation, ready-to-confirm handoff, approval-status preservation, and neutral retry behavior.
- Replaced deterministic meal-session free-text parsing with a bounded `run_interview_turn()` flow that persists user and assistant transcript turns across kickoff and replies.
- Kept the authoritative write path intact by passing validated `ready_to_confirm` items, including explicit `approval_status`, through `finalize_confirmed_interview()` and the existing grounding branch.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED - add failing meal-interview orchestration regressions for natural replies, bounded repair, and fail-closed finalizer safety** - `2442fca` (`test`)
2. **Task 2: GREEN - run live meal interview turns through the traced LLM manager and preserve the authoritative finalizer** - `f51683c` (`feat`)

**Plan metadata:** pending summary commit

## Files Created/Modified

- `scripts/assert_interview_turn_red.py` - RED harness that only passes when the expected live interview orchestration gaps fail.
- `tests/test_interview_flow.py` - Adds approval-status preservation coverage for explicit `ready_to_confirm` state.
- `tests/test_bot_contract.py` - Covers kickoff prompt generation, meal-session turn routing, ready-to-confirm finalizer handoff, and fail-closed retry behavior.
- `app/services/interview_service.py` - Adds transcript/state helpers, preserves explicit confirmation items, and exposes neutral retry/state serialization helpers for the meal turn loop.
- `bot/handlers.py` - Routes meal interview replies through the LLM turn manager, persists transcript rows, and keeps invalid turns fail-closed.
- `bot/polling.py` - Starts meal interviews with an LLM-generated first turn and persists the sent Telegram prompt id.

## Decisions Made

- Reused the existing `InterviewSession.current_prompt_payload` as the authoritative meal turn state and kept transcript replay bounded to that state plus recent durable turns.
- Left `/fix` on the deterministic correction path for this plan; only meal interview sessions switch to the LLM turn manager.
- Preferred direct state + transcript helpers over a second write surface so all `ready_to_confirm` results still flow through the same finalization code.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `rtk` would not execute `./.venv/bin/python` as a direct command, so verification commands were rerun through `rtk sh -lc` to stay within the RTK rule while using the repo-local interpreter.
- The target suites intentionally log negative-path worker errors (`telegram down`, `db down`) from existing polling contract tests; they remained expected test noise and not product regressions.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Meal interviews can now start with an LLM-generated grouped question, accept natural replies like `bottle gourd`, `pita`, and `chicken leg`, and either continue, clarify, or finalize through the existing writer.
- The next 04.2 slice can focus on tighter thread routing and stale-session handling on top of this live turn loop.

## Verification

- `rtk sh -lc './.venv/bin/python scripts/assert_interview_turn_red.py'`
- `rtk sh -lc './.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract -q'`
- `rtk sh -lc './.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q'`
- `rtk rg -n "run_interview_turn|last_bot_message_id|ready_to_confirm|GROUNDING_PENDING|finalize_confirmed_interview" app/services/interview_turn_manager.py app/services/interview_service.py bot/handlers.py bot/polling.py`
- `rtk rg -n "approval_status|APPROVED|CORRECTED|reasoning_schema|reasoning_service|llm_client|tracing_service" app/services/interview_turn_manager.py app/services/interview_service.py bot/handlers.py bot/polling.py`

## Known Stubs

None.

## Self-Check: PASSED

---
*Phase: 04.2-llm-threaded-interview-orchestration*
*Completed: 2026-05-30*
