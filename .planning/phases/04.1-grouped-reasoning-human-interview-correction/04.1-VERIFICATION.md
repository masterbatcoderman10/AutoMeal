---
phase: 04.1-grouped-reasoning-human-interview-correction
verified: 2026-05-29T20:36:31Z
status: human_needed
score: 6/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run the grouped UAT sample with real env/secrets"
    expected: "`scripts/embed_match_smoke.py --mode grouped-uat --sample sample_images/IMG_4646.HEIC` returns separate `food_groups` for egg curry, chicken curry, and pita/flatbread, with only unresolved groups listed in `unresolved_group_ids` and the grouped egg-curry prompt rendered in `current_prompt`."
    why_human: "Requires `DATABASE_URL`, `INGEST_SECRET`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, and `OPENROUTER_API_KEY`, plus real model/database access."
  - test: "Inspect the Langfuse trace for the grouped UAT run"
    expected: "The `meal_reasoning` trace for the reported `trace_id` includes the meal image plus crop images in input and grouped `food_groups` data in output, not a single whole-meal `top_3` race."
    why_human: "Requires the live run above plus access to the external Langfuse UI."
---

# Phase 04.1: Grouped Reasoning & Human Interview Correction Verification Report

**Phase Goal:** Fix the Phase 4 UAT failure where the system treats separate foods in one meal as competing top-3 candidates and asks robotic raw-segment interview questions; the system must reason over food groups, rank candidates per group, and ask concise human questions only for unresolved groups.
**Verified:** 2026-05-29T20:36:31Z
**Status:** human_needed
**Re-verification:** No — initial verification

## User Flow Coverage

Roadmap mode is `mvp`, but the stored roadmap goal is not in strict `As a ..., so that ...` format. `gsd-sdk query user-story.validate` rejects it with `Must contain ", so that "`. The coverage below is therefore derived from the supplied phase intent plus the roadmap success criteria, not from a validator-clean roadmap goal string.

| Step | Expected | Evidence | Status |
|------|----------|----------|--------|
| Separate foods | One meal-level reasoning pass produces distinct grouped records, each with stable segment ownership and exactly three candidates. | `app/services/reasoning_schema.py` requires `food_groups[*]` with `group_id`, `primary_segment_id`, `segment_ids`, `question_*`, and exactly 3 `top_3` entries; `app/services/reasoning_service.py` evaluates each group independently; `tests/test_reasoning_contract.py` and `tests/test_reasoning_gate.py` cover the grouped contract and separate races. | ✓ VERIFIED |
| Resolve independently | Clear groups can auto-confirm without competing against unrelated foods in the same meal. | `app/services/reasoning_service.py` computes threshold, margin, missing-evidence, and nutrition-impact checks inside `_evaluate_group_gate()` and aggregates meal state afterward; `tests/test_reasoning_gate.py` proves pita can auto-confirm while egg curry remains unresolved. | ✓ VERIFIED |
| Ask one useful question | Unresolved groups become grouped interview targets and render concise human prompt copy instead of raw-segment wording. | `app/services/interview_service.py` builds `pending_targets` from grouped reasoning state and renders deterministic prompts; `bot/polling.py` sends the grouped prompt; `tests/test_interview_flow.py` and `tests/test_match_flow.py` cover grouped target ordering and the exact egg-curry prompt. | ✓ VERIFIED |
| Outcome | Egg curry, chicken curry, and pita are treated as separate decisions instead of a whole-meal alternative race. | Group-local gate logic, compact per-segment snapshots, grouped final-write collapse, and grouped interview handoff are all present and regression-tested across `reasoning_service`, `meal_resolution_service`, `interview_service`, and `polling`. | ✓ VERIFIED |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Reasoning output contains grouped food records with stable segment IDs, human labels, action/state fields, evidence, missing evidence, and exactly three candidates per group. | ✓ VERIFIED | `app/services/reasoning_schema.py:19-35, 62-173, 285-386, 428-510`; `tests/test_reasoning_contract.py:24-98, 209-272` |
| 2 | Meal-level gating compares candidates within each food group instead of treating unrelated foods as one shared top-3 race. | ✓ VERIFIED | `app/services/reasoning_service.py:280-420`; `tests/test_reasoning_gate.py:197-274` |
| 3 | Persisted reasoning keeps meal-level grouped state and compact per-segment auditability instead of copying whole-meal candidates onto every segment. | ✓ VERIFIED | `app/services/reasoning_service.py:466-510, 929-1026`; `tests/test_reasoning_flow.py:218-330` |
| 4 | Grouped auto-confirm writes one logical final-write item per resolved food group and still routes through the shared correction-aware transaction. | ✓ VERIFIED | `app/services/meal_resolution_service.py:97-189, 420-544`; `app/services/reasoning_service.py:1110-1177`; `tests/test_match_flow.py:362-460` |
| 5 | Telegram interview sessions target unresolved food groups from persisted reasoning state and keep stable group metadata in DB-owned session payloads. | ✓ VERIFIED | `app/services/interview_service.py:370-431, 729-829`; `bot/handlers.py:23-35`; `tests/test_interview_flow.py:438-603` |
| 6 | User-facing interview copy is brief, human, and answerable without developer jargon, including the locked egg-curry detail prompt. | ✓ VERIFIED | `app/services/interview_service.py:70-149, 871-912`; `bot/polling.py:1115-1149`; `tests/test_interview_flow.py:107-147`; `tests/test_match_flow.py:562-600` |
| 7 | Re-running `sample_images/IMG_4646.HEIC` produces grouped reasoning plus Langfuse image-trace evidence for egg curry, chicken curry, and pita/flatbread as separate groups. | ? UNCERTAIN | Code path exists in `scripts/embed_match_smoke.py:243-408` and checklist exists in `04.1-UAT.md`, but the live run was blocked in this environment: `DATABASE_URL is empty. Set it in .env or pass --database-url for smoke modes that use Postgres.` |

**Score:** 6/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `app/services/reasoning_schema.py` | Strict grouped reasoning contract with required `food_groups[*].top_3` | ✓ VERIFIED | Schema requires grouped fields and exact 3-candidate arrays; coercion normalizes grouped payloads and rejects missing `food_groups`. |
| `app/services/reasoning_service.py` | Group-aware gate, meal aggregation, compact segment audit persistence, and grouped finalization | ✓ VERIFIED | Group-local gate logic, meal/segment persistence, trace capture, and grouped final-write handoff are implemented and exercised by tests. |
| `app/services/meal_resolution_service.py` | Grouped adapter from resolved food groups into one logical final-write item per food group | ✓ VERIFIED | `build_grouped_final_segment_resolutions()` uses `primary_segment_id` and selected candidate data to create one `FinalSegmentResolution` per group. |
| `tests/test_match_flow.py` | Regression that grouped auto-confirm writes one logical item per food group and unresolved grouped prompt reaches Telegram path | ✓ VERIFIED | Covers grouped final-write collapse and grouped unresolved prompt handoff. |
| `app/services/interview_service.py` | Grouped pending-target creation and deterministic human prompt rendering | ✓ VERIFIED | Pulls targets from grouped reasoning state and renders question copy from structured fields. |
| `bot/polling.py` | Unresolved grouped handoff from reasoning into interview-session creation and grouped prompt send | ✓ VERIFIED | Uses `prepare_interview_session()` on unresolved grouped results and sends `current_target_question()` output. |
| `scripts/embed_match_smoke.py` | Production-path grouped UAT runner reporting grouped state, prompt text, and trace metadata | ✓ VERIFIED | `grouped-uat` mode uses detect → segment → match → reason → prepare_interview_session and prints `food_groups`, `unresolved_group_ids`, `current_prompt`, and `trace_id`. |
| `.planning/phases/04.1-grouped-reasoning-human-interview-correction/04.1-UAT.md` | Repeatable operator checklist for `IMG_4646.HEIC` grouped UAT and Langfuse inspection | ✓ VERIFIED | Documents deterministic regression suite, live grouped-UAT command, expected prompt text, and trace inspection steps. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `app/services/reasoning_schema.py` | `app/services/reasoning_service.py` | strict grouped response parsing and coercion | ✓ VERIFIED | `run_reasoning_request()` and `evaluate_reasoning_gate()` both normalize through `coerce_reasoning_response()`. |
| `app/services/reasoning_service.py` | `MealLog.reasoning_state_json` and `MealSegment.ai_reasoning` | meal-owned grouped state plus compact per-segment group snapshots | ✓ VERIFIED | `persist_reasoning_results()` writes grouped meal state once and segment snapshots with `group_id`, `group_segment_ids`, `candidate_ids`, and `trace_id`. |
| `app/services/reasoning_service.py` | `app/services/meal_resolution_service.py` | resolved food groups adapted into final-write payloads | ✓ VERIFIED | `finalize_meal_from_reasoning()` calls `build_grouped_final_segment_resolutions()` then `apply_final_meal_resolution()`. |
| `MealLog.reasoning_state_json.food_groups` | `app/services/interview_service.py` | unresolved grouped targets become DB-owned `pending_targets` | ✓ VERIFIED | `_pending_targets_from_food_groups()` reads both root `food_groups` and nested `meal_reasoning.food_groups`; `prepare_interview_session()` stores them in `InterviewSession.current_prompt_payload`. |
| `app/services/interview_service.py` | `bot/handlers.py` | grouped prompt payloads and response handling | ✓ VERIFIED | `bot/handlers.py` re-exports and calls `current_target_question()`, `persist_interview_step()`, and `finalize_confirmed_interview()`. |
| `scripts/embed_match_smoke.py` | Langfuse trace surface | sample-image grouped reasoning probe output | ✓ VERIFIED | Grouped UAT path returns `trace_id` and crop-path metadata from the same production reasoning call that starts a `meal_reasoning` trace. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `app/services/reasoning_service.py` | `food_groups` | `_run_reasoning_model()` builds multimodal prompt from meal image + segment crops and calls `llm_client.chat_completion()` | Yes | ✓ FLOWING |
| `app/services/interview_service.py` | `pending_targets` | `MealLog.reasoning_state_json.food_groups` or `meal_reasoning.food_groups` written by `persist_reasoning_results()` | Yes | ✓ FLOWING |
| `app/services/meal_resolution_service.py` | `final_segments` | Group-selected candidate plus `primary_segment_id`, segment crop, and embedding from grouped reasoning/match results | Yes | ✓ FLOWING |
| `scripts/embed_match_smoke.py` | `food_groups`, `unresolved_group_ids`, `current_prompt`, `trace_id` | Production detect → segment → match → reason → finalize/interview path | Yes in code; live runtime unverified here | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Grouped reasoning/interview regression suite | `rtk .venv/bin/python -m unittest tests.test_reasoning_contract tests.test_reasoning_flow tests.test_reasoning_gate tests.test_interview_flow tests.test_match_flow tests.test_embed_match_smoke` | `Ran 66 tests in 1.556s` / `OK` | ✓ PASS |
| Live grouped sample UAT | `rtk .venv/bin/python scripts/embed_match_smoke.py --mode grouped-uat --sample sample_images/IMG_4646.HEIC` | Failed immediately: `DATABASE_URL is empty...` | ? SKIP |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| None declared for this phase | — | No `probe-*.sh` scripts were declared in plans/summaries or found under `scripts/*/tests/`. | ? SKIP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| `REASON-01` | `04.1-01`, `04.1-02` | Reasoning stage runs when unresolved identification needs meal-level reasoning | ✓ SATISFIED | `bot/polling.py:1099-1121` always routes matched meals through `_run_reasoning_pipeline()`; unresolved grouped path covered by `tests/test_match_flow.py:462-600`. |
| `REASON-02` | `04.1-01`, `04.1-02` | Reasoning emits top-3 candidates rather than a single guess | ✓ SATISFIED | `app/services/reasoning_schema.py:114-125, 285-296` enforces exactly 3 candidates per group; `tests/test_reasoning_contract.py:47-57, 91-98`. |
| `REASON-04` | `04.1-01`, `04.1-02` | Reasoning trace and decision data are written for audit | ✓ SATISFIED | `app/services/reasoning_service.py:466-510, 929-1026` writes grouped meal state, segment snapshots, and `trace_id`; `tests/test_reasoning_flow.py:314-329`. |
| `INTERVIEW-01` | `04.1-02` | Low-confidence results trigger Telegram interview session | ✓ SATISFIED | `bot/polling.py:1115-1149` prepares grouped interview session and sends grouped prompt when finalization is unresolved; `tests/test_match_flow.py:462-600`. |
| `INTERVIEW-02` | `04.1-02` | Interview follows structured roadmap/state flow | ✓ SATISFIED | `app/services/interview_service.py:70-196, 509-545, 915-930`; `tests/test_interview_flow.py:148-178, 239-279`. |
| `INTERVIEW-03` | `04.1-02` | Interview answers create/update `FoodItem`; packaged/restaurant answers hand off to re-grounding | ✓ SATISFIED | `app/services/interview_service.py:548-664` builds `FinalSegmentResolution` and `PENDING_HANDOFF` grounding state; `bot/polling.py:483-620` consumes grounding handoff. |
| `INTERVIEW-06` | `04.1-01`, `04.1-02` | `USER_CORRECTED` / correction-side invalidation behavior remains authoritative | ✓ SATISFIED | Grouped finalization still routes through `apply_final_meal_resolution()`; invalidation logic remains in `app/services/meal_resolution_service.py:503-526`. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | None in scoped files | — | No scoped `TBD`/`FIXME`/`XXX` markers, placeholders, or user-visible stub implementations found. |

## Human Verification Required

### 1. Grouped UAT Run

**Test:** Run `rtk .venv/bin/python scripts/embed_match_smoke.py --mode grouped-uat --sample sample_images/IMG_4646.HEIC` with the normal project env/secrets.
**Expected:** Output includes `meal_id`, grouped `food_groups`, `unresolved_group_ids`, `current_prompt`, and `trace_id`. The grouped result should separate egg curry, chicken curry, and pita/flatbread, and the unresolved prompt should match the egg-curry wording in `04.1-UAT.md`.
**Why human:** This path requires real database, LLM, and Telegram/OpenRouter secrets that are absent in the verification environment.

### 2. Langfuse Trace Inspection

**Test:** Open the `meal_reasoning` trace matching the `trace_id` from the grouped UAT run.
**Expected:** Trace input includes the meal image and generated crop images; trace output includes grouped `food_groups` payload data, and the unresolved group aligns with the rendered prompt.
**Why human:** Requires the live run above plus access to the external Langfuse UI.

## Residual Risks

- MVP metadata discrepancy: `gsd-sdk query user-story.validate` rejects the stored phase goal because it does not contain the strict `, so that ` user-story delimiter. Implementation evidence is still strong, but future MVP verification should normalize the roadmap goal string with `/gsd mvp-phase 4.1`.
- Compatibility root `top_3` still exists inside normalized meal reasoning as a first-group alias (`app/services/reasoning_schema.py:501-510`, `app/services/reasoning_service.py:419, 943-1025`). Current grouped consumers use `food_groups`, and segment snapshots stay compact, but future consumers should continue to ignore root `top_3`.
- The grouped correction path preserves the existing shared final-write transaction, but this phase’s new tests do not explicitly exercise a grouped `USER_CORRECTED` invalidation case; the evidence here is wiring preservation rather than a fresh correction regression.

## Gaps Summary

No code-level blocker was found in the scoped Phase 04.1 implementation. Grouped reasoning, grouped persistence, grouped final-write collapse, grouped interview targeting, and deterministic grouped prompt copy are all present and backed by passing regressions. The remaining unverified contract is the live `IMG_4646.HEIC` operator run plus Langfuse inspection, which makes the correct overall status `human_needed` rather than `passed`.

---

_Verified: 2026-05-29T20:36:31Z_
_Verifier: the agent (gsd-verifier)_
