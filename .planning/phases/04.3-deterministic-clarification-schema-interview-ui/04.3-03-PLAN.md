---
phase: 04.3-deterministic-clarification-schema-interview-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 04.3-02
files_modified:
  - scripts/assert_clarification_resolver_red.py
  - app/services/interview_schema.py
  - app/services/interview_turn_manager.py
  - app/services/interview_service.py
  - bot/handlers.py
  - tests/test_interview_schema.py
  - tests/test_match_flow.py
  - tests/test_bot_contract.py
  - .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md
  - .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md
autonomous: false
requirements:
  - REASON-04
  - INTERVIEW-03
  - INTERVIEW-04
  - MATCH-04
user_setup:
  - "During Task 4, restore or copy the `mealttracker_uat_with_meal_embeddings` checkpoint if it is not already the active UAT database, submit the repeated meal through the deployed Telegram/iOS flow, and answer the Telegram prompts as directed by the checklist."
must_haves:
  truths:
    - "After all required clarification questions are answered, only a resolver-only LLM step runs; it cannot author new user-facing prompts and emits strict `confirmation_items` per D-14 and D-15."
    - "Final confirmation still flows through `meal_resolution_service.apply_final_meal_resolution()` per D-16, preserving best-effort `is_verified=false` behavior for INTERVIEW-04 and creating FoodVisual rows when embeddings exist for MATCH-04."
    - "The phase ships with a live UAT checklist and a recorded UAT result artifact keyed to `mealttracker_uat_with_meal_embeddings`, repeated-meal vector influence, deterministic partial-reply recovery, DB row deltas, and Langfuse trace evidence per D-18, D-19, and D-20."
  artifacts:
    - path: "app/services/interview_turn_manager.py"
      provides: "Resolver-only confirmation contract that runs only after deterministic clarification is complete"
      exports: ["run_interview_turn"]
    - path: "app/services/interview_service.py"
      provides: "Final clarification-to-confirmation handoff that preserves the authoritative save path"
      contains: "finalize_confirmed_interview"
    - path: ".planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md"
      provides: "Repeatable 04.3 live UAT and redeploy checklist"
    - path: ".planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md"
      provides: "Recorded live UAT pass/fail evidence, DB row deltas, and Langfuse trace references"
  key_links:
    - from: "bot/handlers.py"
      to: "app/services/interview_turn_manager.py"
      via: "resolver runs only when `remaining_required_question_ids` is empty"
      pattern: "remaining_required_question_ids|run_interview_turn"
    - from: "app/services/interview_service.py"
      to: "app/services/meal_resolution_service.py"
      via: "structured confirmation items flow through the authoritative final write path"
      pattern: "apply_final_meal_resolution"
---

<objective>
Narrow the 04.2 interview LLM into a resolver-only finalization seam and ship the 04.3 live UAT checklist per D-14 through D-20.

Purpose: Phase 04.3 is not complete until deterministic answers hand off through the existing authoritative write path, visual learning still works, and the repeated-meal UAT path is documented against the saved checkpoint.
Output: resolver-handoff regressions, resolver-only confirmation behavior, authoritative finalization wiring, and a repeatable 04.3 UAT/redeploy checklist.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md
@.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md
@.planning/phases/04.2-llm-threaded-interview-orchestration/04.2-UAT.md
@app/services/interview_schema.py
@app/services/interview_turn_manager.py
@app/services/interview_service.py
@app/services/meal_resolution_service.py
@tests/test_interview_schema.py
@tests/test_match_flow.py
@tests/test_bot_contract.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| completed clarification state -> resolver input | Structured answers become model input and must be complete before the resolver runs. |
| resolver output -> authoritative DB write path | The resolver can influence FoodItem, DiaryEntry, and FoodVisual writes and must be tightly bounded to confirmation output only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-05 | Tampering | `app/services/interview_turn_manager.py` | mitigate | Allow resolver execution only when `remaining_required_question_ids` is empty, reject any user-facing `continue_interview` or `need_clarification` output after D-14 and D-15, and keep the contract limited to strict `confirmation_items`. |
| T-04.3-06 | Repudiation | `app/services/interview_service.py` | mitigate | Preserve `apply_final_meal_resolution()` as the sole write path per D-16, persist resolver payloads and outcome metadata for REASON-04, and keep best-effort `is_verified=false` behavior in the authoritative finalizer path for INTERVIEW-04. |
| T-04.3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages or tool-loop dependencies are allowed; preserve the existing OpenRouter resolver transport and Langfuse tracing surface only. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add failing resolver-only finalization regressions</name>
  <files>scripts/assert_clarification_resolver_red.py, tests/test_interview_schema.py, tests/test_match_flow.py, tests/test_bot_contract.py</files>
  <read_first>app/services/interview_schema.py, app/services/interview_turn_manager.py, app/services/interview_service.py, app/services/meal_resolution_service.py, tests/test_interview_schema.py, tests/test_match_flow.py, tests/test_bot_contract.py, .planning/phases/04.2-llm-threaded-interview-orchestration/04.2-UAT.md</read_first>
  <acceptance_criteria>
    - "`scripts/assert_clarification_resolver_red.py` exits `0` only when the expected pre-04.3 resolver failures are present, and exits non-zero on import failures, unexpected passes, or unrelated runtime errors."
    - "The RED tests require the post-clarification resolver to run only after `remaining_required_question_ids` is empty per D-14 and D-15, and to reject any new user-facing prompt action once deterministic questions exist."
    - "The RED tests require `finalize_confirmed_interview()` plus `apply_final_meal_resolution()` to remain the authoritative final write path per D-16, preserving FoodVisual creation when embeddings exist for MATCH-04 and best-effort unverified writes for INTERVIEW-04."
    - "The RED tests require trace metadata to include clarification answers and resolver payload fields needed for D-20."
  </acceptance_criteria>
  <action>Create `scripts/assert_clarification_resolver_red.py` and extend `tests/test_interview_schema.py`, `tests/test_match_flow.py`, and `tests/test_bot_contract.py` so they fail until the remaining 04.3 resolver seam exists: no user-facing model prompts after deterministic clarification, resolver execution only when all required answers are present per D-14 and D-15, authoritative finalization through `apply_final_meal_resolution()` per D-16, and trace-ready resolver payload persistence per D-20.</action>
  <verify>
    <automated>rtk sh -lc './.venv/bin/python scripts/assert_clarification_resolver_red.py'</automated>
  </verify>
  <done>The RED harness proves the repo still treats the interview LLM as more than a resolver and lacks the final 04.3 write-path guarantees.</done>
</task>

<task type="auto">
  <name>Task 2: Implement resolver-only confirmation handoff and preserve authoritative final writes</name>
  <files>app/services/interview_schema.py, app/services/interview_turn_manager.py, app/services/interview_service.py, bot/handlers.py, tests/test_interview_schema.py, tests/test_match_flow.py, tests/test_bot_contract.py</files>
  <read_first>app/services/interview_schema.py, app/services/interview_turn_manager.py, app/services/interview_service.py, bot/handlers.py, app/services/meal_resolution_service.py, tests/test_interview_schema.py, tests/test_match_flow.py, tests/test_bot_contract.py, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md</read_first>
  <acceptance_criteria>
    - "`app/services/interview_turn_manager.py` is reduced to a resolver-only confirmation seam per D-14 and D-15: after deterministic answers are complete it may emit strict `confirmation_items`, but it may not author new Telegram prompts."
    - "`bot/handlers.py` calls the resolver only when `remaining_required_question_ids` is empty; otherwise it deterministically re-renders the missing required questions."
    - "`app/services/interview_service.py` transforms `answers_by_question_id` and grouped reasoning into normalized confirmation items, then routes them through `finalize_confirmed_interview()` and `meal_resolution_service.apply_final_meal_resolution()` per D-16 and D-17."
    - "The finalization path preserves segment IDs, embeddings, and cropped-image references so FoodVisual rows are still written on confirmed identifications for MATCH-04, and best-effort unresolved confidence still lands as `is_verified=false` through the existing finalizer behavior for INTERVIEW-04."
    - "`rtk sh -lc './.venv/bin/python -m unittest tests.test_interview_schema tests.test_match_flow tests.test_bot_contract -q'` passes."
  </acceptance_criteria>
  <action>Implement the last 04.3 runtime seam across `app/services/interview_schema.py`, `app/services/interview_turn_manager.py`, `app/services/interview_service.py`, and `bot/handlers.py`. Keep the existing OpenRouter call path, but narrow it so the interview model is reused only as a compact final resolver per D-14; once deterministic clarification answers exist, it must emit strict confirmation data only and never ask user-facing questions per D-15. Gate resolver execution on an empty `remaining_required_question_ids` list, preserve `meal_resolution_service.apply_final_meal_resolution()` as the authoritative final write path per D-16, keep the contract small and Phase-5-replaceable per D-17, and do not add any SearXNG/Firecrawl or nutrition-resolution expansion in this phase.</action>
  <verify>
    <automated>rtk sh -lc './.venv/bin/python -m unittest tests.test_interview_schema tests.test_match_flow tests.test_bot_contract -q'</automated>
  </verify>
  <done>Structured clarification answers now hand off through a resolver-only LLM seam into the authoritative final write path without reintroducing freeform interview turns.</done>
</task>

<task type="auto">
  <name>Task 3: Add the 04.3 live UAT and redeploy checklist</name>
  <files>.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md</files>
  <read_first>.planning/phases/04.2-llm-threaded-interview-orchestration/04.2-UAT.md, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-RESEARCH.md</read_first>
  <acceptance_criteria>
    - "`04.3-UAT.md` preserves the `mealttracker_uat_with_meal_embeddings` checkpoint from D-18 and defines restore/redeploy commands plus expected row-count checks before and after the run."
    - "The checklist includes the live repeated-meal scenario from D-19: vector candidates influence reasoning, deterministic template questions are rendered, a partial reply leaves only missing required questions, and final save creates the expected DiaryEntry and FoodVisual rows."
    - "The checklist names the Langfuse evidence required by D-20: reasoning payload, `clarification_schema`, deterministic answer payload, resolver input/output, and authoritative finalization outcome."
  </acceptance_criteria>
  <action>Create `.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md` as the live operator checklist for this phase. Carry forward the safe repeated-meal checkpoint `mealttracker_uat_with_meal_embeddings` per D-18, specify the minimal redeploy commands, define the repeated-meal partial-answer scenario from D-19, and require explicit Langfuse evidence for reasoning output, clarification schema, deterministic answer state, resolver payload, and final save outcome per D-20.</action>
  <verify>
    <automated>rtk rg -n "mealttracker_uat_with_meal_embeddings|DiaryEntry|FoodVisual|clarification_schema|resolver|Langfuse" .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md</automated>
  </verify>
  <done>The phase has a concrete live-UAT checklist that validates the saved checkpoint, deterministic partial-reply recovery, and final authoritative writes before Phase 5 work begins.</done>
</task>

<task type="manual">
  <name>Task 4: Execute the saved-checkpoint live UAT and record evidence</name>
  <files>.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md</files>
  <read_first>.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-CONTEXT.md, .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-VALIDATION.md</read_first>
  <acceptance_criteria>
    - "`04.3-UAT-RESULTS.md` records the exact database state used, including whether `mealttracker_uat_with_meal_embeddings` was restored, copied, or already active, plus the pre-run and post-run `DiaryEntry` and `FoodVisual` row counts."
    - "The recorded run proves the D-19 repeated-meal scenario end to end: vector candidates influenced the reasoning/clarification schema, deterministic template questions were rendered, a deliberately partial reply re-prompted only missing required questions, and completing the flow saved the expected entries."
    - "The recorded run includes Langfuse trace links or trace IDs for D-20: grouped reasoning payload, `clarification_schema`, deterministic answer payload, resolver input/output, and authoritative finalization outcome."
    - "If the live UAT fails, `04.3-UAT-RESULTS.md` captures the failed step, observed Telegram text/callback behavior, DB row deltas, and trace IDs; the phase cannot be marked complete until the failure is fixed and the checklist is rerun."
  </acceptance_criteria>
  <action>Run the live operator checklist from `04.3-UAT.md` after implementation and redeploy. Use the saved repeated-meal checkpoint `mealttracker_uat_with_meal_embeddings`, submit the same meal, intentionally answer only part of the first clarification batch, then complete the missing answer and final confirmation. Query the database before and after for `DiaryEntry` and `FoodVisual` counts, inspect Langfuse for the required reasoning/schema/answer/resolver/finalization evidence, and write the pass/fail result to `04.3-UAT-RESULTS.md`.</action>
  <verify>
    <manual>Open `04.3-UAT-RESULTS.md` and confirm it contains checkpoint state, pre/post DB row counts, repeated-meal partial-reply transcript, final save result, and Langfuse trace IDs or links.</manual>
    <automated>rtk rg -n "PASS|FAIL|mealttracker_uat_with_meal_embeddings|DiaryEntry|FoodVisual|Langfuse|trace|partial" .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md</automated>
  </verify>
  <done>The phase has executed live evidence, not only a checklist, proving deterministic clarification and resolver-only finalization work against the saved repeated-meal checkpoint.</done>
</task>

</tasks>

<verification>
- `rtk sh -lc './.venv/bin/python scripts/assert_clarification_resolver_red.py'`
- `rtk sh -lc './.venv/bin/python -m unittest tests.test_interview_schema tests.test_match_flow tests.test_bot_contract -q'`
- `rtk sh -lc './.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_match_flow -q'`
- `rtk rg -n "apply_final_meal_resolution|remaining_required_question_ids|confirmation_items|clarification_schema|Langfuse" app/services/interview_turn_manager.py app/services/interview_service.py bot/handlers.py .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT.md`
- `rtk rg -n "PASS|FAIL|mealttracker_uat_with_meal_embeddings|DiaryEntry|FoodVisual|Langfuse|trace|partial" .planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-UAT-RESULTS.md`
</verification>

<success_criteria>
- The meal interview LLM is now only a final resolver after deterministic clarification completion.
- Final confirmation still writes through the authoritative FoodItem/DiaryEntry/FoodVisual transaction path.
- Phase 04.3 has a repeatable live-UAT checklist and recorded live evidence grounded in the saved repeated-meal checkpoint, DB row deltas, and trace expectations.
</success_criteria>

<output>
Create `.planning/phases/04.3-deterministic-clarification-schema-interview-ui/04.3-03-SUMMARY.md` when done.
</output>
