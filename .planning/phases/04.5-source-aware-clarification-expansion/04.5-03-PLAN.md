---
phase: 04.5-source-aware-clarification-expansion
plan: 03
type: tdd
wave: 3
depends_on:
  - 04.5-02
files_modified:
  - app/services/reasoning_schema.py
  - app/services/interview_service.py
  - tests/test_reasoning_contract.py
  - tests/test_interview_flow.py
  - tests/test_bot_contract.py
autonomous: true
gap_closure: true
requirements:
  - REASON-01
  - REASON-04
  - INTERVIEW-01
  - INTERVIEW-03
  - INTERVIEW-04
must_haves:
  truths:
    - "Live grouped reasoning payloads that persist `source_question_policy = DETERMINISTIC_ONLY` are repaired to a supported deterministic source policy before interview question state is built, so source capture cannot be skipped silently (per D-01, D-03)."
    - "After a bread `Other` free-text answer, the deterministic interview inserts `group-*:source_origin` before quantity/finalization whenever the repaired source policy requires source capture (per D-01, D-03)."
    - "Every rendered `SOURCE_ORIGIN` prompt comes from the canonical deterministic builder with the full option set: homemade, store bought, packaged, restaurant, and not sure (per D-03, D-06)."
    - "Legacy or unsupported deterministic-only source-policy strings are handled explicitly at the schema boundary instead of being preserved unchanged into live interview state (per D-03, D-15)."
  artifacts:
    - path: "app/services/reasoning_schema.py"
      provides: "Schema-level normalization or repair for legacy `DETERMINISTIC_ONLY` source-policy values before grouped interview state consumes them"
    - path: "app/services/interview_service.py"
      provides: "Deterministic post-detail source-question insertion using the repaired source policy plus canonical source prompt generation"
    - path: "tests/test_reasoning_contract.py"
      provides: "Regression coverage for legacy source-policy normalization and grouped contract behavior"
    - path: "tests/test_interview_flow.py"
      provides: "Bread `Other` regression coverage proving `SOURCE_ORIGIN` is inserted before quantity after free-text detail answers"
    - path: "tests/test_bot_contract.py"
      provides: "Telegram rerender coverage proving the repaired source question appears next with the full canonical option set and no `Other`"
  key_links:
    - from: "app/services/reasoning_schema.py"
      to: "app/services/interview_service.py"
      via: "normalized `source_question_policy` on grouped reasoning payloads"
      pattern: "source_question_policy|DETERMINISTIC_ONLY|ask_generic"
    - from: "app/services/interview_service.py"
      to: "tests/test_interview_flow.py"
      via: "_apply_clarification_answer / _next_dynamic_question / _build_source_origin_question"
      pattern: "DETAIL|SOURCE_ORIGIN|question_order|source_question_policy"
    - from: "app/services/interview_service.py"
      to: "tests/test_bot_contract.py"
      via: "deterministic current prompt and callback/text rerender"
      pattern: "current_question|choices|allow_other|SOURCE_ORIGIN"
---

<objective>
Close the diagnosed 04.5 clean-rerun regression where live grouped reasoning persisted `source_question_policy = DETERMINISTIC_ONLY` and the deterministic interview skipped source capture after bread `Other` free-text answers.

Purpose: Repair legacy deterministic-only source-policy values before interview state is built, then prove the repaired policy keeps source questions in the ordered deterministic flow.
Output: Focused RED/GREEN coverage for `DETERMINISTIC_ONLY` normalization, deterministic post-detail source insertion, and canonical source prompt rendering.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-CONTEXT.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-RESEARCH.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-PATTERNS.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-UAT.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-VALIDATION.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-01-SUMMARY.md
@.planning/phases/04.5-source-aware-clarification-expansion/04.5-02-SUMMARY.md
@app/services/reasoning_schema.py
@app/services/interview_service.py
@tests/test_reasoning_contract.py
@tests/test_interview_flow.py
@tests/test_bot_contract.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - reproduce the live `DETERMINISTIC_ONLY` policy regression</name>
  <read_first>
    - .planning/phases/04.5-source-aware-clarification-expansion/04.5-UAT.md
    - .planning/phases/04.5-source-aware-clarification-expansion/04.5-02-SUMMARY.md
    - app/services/reasoning_schema.py
    - app/services/interview_service.py
    - tests/test_reasoning_contract.py
    - tests/test_interview_flow.py
    - tests/test_bot_contract.py
  </read_first>
  <files>tests/test_reasoning_contract.py, tests/test_interview_flow.py, tests/test_bot_contract.py</files>
  <behavior>
    - Test 1: A grouped reasoning payload carrying `source_question_policy = DETERMINISTIC_ONLY` is normalized or repaired into the supported deterministic generic-source policy before interview state consumes it (per D-01, D-03).
    - Test 2: After a bread `DETAIL` question is answered through the `Other` free-text path, `group-*:source_origin` is inserted immediately before quantity/finalization when the repaired source policy requires source capture (per D-01, D-03).
    - Test 3: The inserted source question is built from the canonical full source option set: `HOME_COOKED`, `STORE_BOUGHT_PREPARED`, `PACKAGED_BRANDED`, `RESTAURANT`, and `UNKNOWN`, and still suppresses `Other` in Telegram rendering (per D-03, D-06).
  </behavior>
  <action>Write failing regressions that mirror the exact diagnosed live state from `04.5-UAT.md`: persisted grouped reasoning contains `source_question_policy = DETERMINISTIC_ONLY`, the bread answer goes `Other -> free text`, and current code advances directly to quantity. Add one contract test in `tests/test_reasoning_contract.py` proving the legacy policy value is no longer preserved unchanged into normalized grouped state, one `tests/test_interview_flow.py` state-machine test proving `DETAIL -> free_text -> SOURCE_ORIGIN -> QUANTITY`, and one `tests/test_bot_contract.py` rerender test proving the next prompt is the inserted canonical source question with no `Other`. Keep the RED scope limited to this regression; do not add new learned-distribution, brand, restaurant, or finalizer-path assertions here.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_reasoning_contract tests.test_interview_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>The targeted suites fail only because legacy `DETERMINISTIC_ONLY` values are still preserved or still allow the bread `Other` free-text path to skip deterministic source insertion.</acceptance_criteria>
  <done>Executable RED coverage exists for the exact clean-rerun regression and names the schema/interview behaviors that must change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - repair legacy source-policy values and keep source capture in deterministic order</name>
  <read_first>
    - .planning/phases/04.5-source-aware-clarification-expansion/04.5-CONTEXT.md
    - .planning/phases/04.5-source-aware-clarification-expansion/04.5-RESEARCH.md
    - app/services/reasoning_schema.py
    - app/services/interview_service.py
    - tests/test_reasoning_contract.py
    - tests/test_interview_flow.py
    - tests/test_bot_contract.py
  </read_first>
  <files>app/services/reasoning_schema.py, app/services/interview_service.py, tests/test_reasoning_contract.py, tests/test_interview_flow.py, tests/test_bot_contract.py</files>
  <action>Implement a focused compatibility repair for the diagnosed regression. In `app/services/reasoning_schema.py`, normalize the legacy live value `DETERMINISTIC_ONLY` into the supported deterministic generic-source policy before grouped reasoning leaves schema normalization, and make any truly unsupported deterministic-only alias handling explicit rather than silently preserving raw strings. In `app/services/interview_service.py`, consume the repaired source policy so `DETAIL`-origin `Other` free-text answers still queue `group-*:source_origin` before quantity/finalization per D-01 and D-03, while preserving current `question_order`, `pending_question_ids`, `remaining_required_question_ids`, and partial-answer recovery. Route every rendered `SOURCE_ORIGIN` question through `_build_source_origin_question()` so the canonical full choice set is authoritative and reduced live payloads cannot leak into Telegram rendering per D-03 and D-06. Keep the scope tight: do not reopen broader 04.5 heuristics, brand/restaurant follow-up design, Phase 5 grounding, or Phase 4.4 finalizer ownership.</action>
  <verify>
    <automated>rtk python -m unittest tests.test_reasoning_contract tests.test_interview_flow tests.test_bot_contract -q</automated>
  </verify>
  <acceptance_criteria>The live-regression path now repairs `DETERMINISTIC_ONLY` before interview state build, inserts `SOURCE_ORIGIN` after bread `Other` free text, and renders the canonical full-option source prompt deterministically.</acceptance_criteria>
  <done>The diagnosed 04.5 clean-rerun regression is closed at the schema/interview seam without expanding scope beyond the `DETERMINISTIC_ONLY` compatibility fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reasoning payload -> grouped interview state | Legacy source-policy strings become user-visible question ordering here |
| callback/text answer -> grouped clarification state | User free text and button answers become authoritative source fields here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-08 | Integrity | `app/services/reasoning_schema.py` | mitigate | Repair `DETERMINISTIC_ONLY` at schema normalization so unsupported source-policy strings cannot silently bypass deterministic source capture. |
| T-04.5-09 | Tampering | `app/services/interview_service.py` | mitigate | Insert source questions through the same deterministic `question_order` state after free-text detail answers so required source capture cannot be skipped by path shape. |
| T-04.5-10 | Tampering | deterministic source prompt inventory | mitigate | Canonicalize every `SOURCE_ORIGIN` prompt through `_build_source_origin_question()` so reduced or model-authored option sets cannot reach Telegram rendering. |
| T-04.5-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of this plan. |
</threat_model>

<verification>
Run `rtk python -m unittest tests.test_reasoning_contract tests.test_interview_flow tests.test_bot_contract -q`.
Then rerun the targeted 04.5 regression suite from `04.5-VALIDATION.md`.
During `$gsd-verify-work`, replay the diagnosed bread path from `04.5-UAT.md` and confirm the live prompt sequence becomes `group-1:identity -> group-1:identity (free text) -> group-1:source_origin -> group-1:quantity`.
</verification>

<success_criteria>
The clean-rerun `DETERMINISTIC_ONLY` regression is closed: legacy source-policy values no longer slip through unchanged, and bread `Other` free-text answers cannot bypass deterministic source capture.
</success_criteria>

<output>
Create `.planning/phases/04.5-source-aware-clarification-expansion/04.5-03-SUMMARY.md` when done
</output>
