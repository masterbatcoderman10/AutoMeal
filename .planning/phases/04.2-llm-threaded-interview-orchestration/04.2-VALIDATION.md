---
phase: 04.2
slug: llm-threaded-interview-orchestration
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-30
updated: 2026-05-31
---

# Phase 04.2 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Python `unittest` modules under `tests/` |
| **Config file** | none |
| **Fast smoke commands** | `./.venv/bin/python -m unittest tests.test_interview_schema tests.test_interview_flow -q`; `./.venv/bin/python -m unittest tests.test_interview_flow tests.test_bot_contract -q`; `./.venv/bin/python -m unittest tests.test_bot_contract tests.test_fix_flow -q` |
| **Targeted wave-boundary command** | `./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q` |
| **Full suite command** | `./.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` |
| **Estimated runtime** | smoke subsets ~10-25 seconds; targeted wave-boundary suite ~30-90 seconds after dependencies are installed |

---

## Sampling Rate

- **After every task commit:** Run only the smallest relevant smoke subset for the task being executed.
- **Plan 01 / schema-state smoke:** `./.venv/bin/python -m unittest tests.test_interview_schema tests.test_interview_flow -q`
- **Plan 02 / live-turn smoke:** `./.venv/bin/python -m unittest tests.test_interview_flow tests.test_bot_contract -q`
- **Plan 03 / routing-fix smoke:** `./.venv/bin/python -m unittest tests.test_bot_contract tests.test_fix_flow -q`
- **After every plan wave:** Run `./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q`
- **Before `/gsd-verify-work`:** Run `./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q` plus live UAT for the `It's bottle gourd` conversation case
- **Max feedback latency:** smoke subsets target ~10-25 seconds; targeted wave-boundary checks may take ~30-90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.2-W0-01 | 01 | 0 | all | T-04.2-01 / T-04.2-02 / T-04.2-03 | Repo-local Python 3.12 environment imports app modules and can execute the targeted suites before feature work starts | env | `python3.12 scripts/assert_phase_04_2_preflight.py` | yes | green |
| 04.2-01-01 | 01 | 0 | REASON-01, REASON-04, INTERVIEW-01 | T-04.2-02 | Grouped reasoning state seeds model-ready interview context without losing trace data, and the reasoning suite remains runnable while the 04.2 contract is introduced | unit | `./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_schema tests.test_interview_flow -q` | yes | green |
| 04.2-01-02 | 01 | 0 | INTERVIEW-02 | T-04.2-02 / T-04.2-03 | Strict structured-output validation allows one repair retry, then fails closed on invalid or contradictory payloads with no confirmation-state mutation and no finalizer call | unit | `./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_schema tests.test_interview_flow -q` | yes | green |
| 04.2-02-01 | 02 | 1 | INTERVIEW-01, INTERVIEW-02 | T-04.2-01 | Telegram free-text replies route to the intended meal/thread and stale sessions cannot steal replies | contract | `./.venv/bin/python -m unittest tests.test_bot_contract -q` | yes | green |
| 04.2-02-02 | 02 | 1 | INTERVIEW-02, INTERVIEW-03 | T-04.2-02 / T-04.2-04 | Invalid JSON is repaired once when possible, and still-invalid or contradictory output sends a neutral retry message while keeping the session open, skipping confirmation-state mutation, and skipping finalizer invocation | unit + contract | `./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract -q` | yes | green |
| 04.2-03-01 | 03 | 2 | INTERVIEW-06 | T-04.2-04 | Existing `/fix` and `USER_CORRECTED` behavior remains intact while the per-wave targeted suite still covers routing, reasoning, and finalizer guardrails | regression | `./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q` | yes | green |
| 04.2-FULL-01 | all | audit | INTERVIEW-01, INTERVIEW-02, INTERVIEW-03, INTERVIEW-06, REASON-04 | T-04.2-01 / T-04.2-04 | Full unittest discovery covers the unresolved-match worker handoff into the 04.2 meal-interview kickoff path and catches stale fixture shape regressions | integration | `./.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` | yes | green |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [x] Bootstrap a repo-local `.venv` on Python 3.12 and install `requirements.txt` so `fastapi`, `sqlalchemy`, `pydantic`, and the existing app modules import before any RED/GREEN task runs.
- [x] Add a regression for `It's bottle gourd` resolving the egg/vegetable curry target without a duplicate naming question.
- [x] Add a regression for `pita` and `chicken leg` natural answers confirming/composing target names without deterministic echo prompts.
- [x] Add a routing regression with two active interviews proving free-text replies cannot be stolen by a stale session.
- [x] Add a strict structured-output robustness pair: malformed JSON repaired successfully on the single allowed retry, and invalid/contradictory `ready_to_confirm` rejected with no confirmation-state mutation, no finalizer call, and a neutral retry prompt.
- [x] Add a regression showing clear approval groups appear in model context and final confirmation items.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live Telegram interview answers `It's bottle gourd` and reaches confirmation without asking `What should I call It's bottle gourd?` | INTERVIEW-02, INTERVIEW-03 | Confirms actual Telegram transport, model call, Langfuse trace, and DB write path together | Submit a low-confidence grouped meal, answer the curry detail with `It's bottle gourd`, confirm final prompt, then verify DiaryEntries/FoodItems/FoodVisuals are written through existing finalizer |
| Langfuse trace captures interview model, prompt context, and strict response | REASON-04, INTERVIEW-02 | External trace UI is not fully covered by unit tests | Open trace for the interview turn and verify model, response action, and structured state fields are present |

---

## Security Validation

| Threat Ref | Threat | Required Verification |
|------------|--------|-----------------------|
| T-04.2-01 | Stale active interview steals a free-text Telegram reply | Automated test with two active sessions; reply scoped to latest bot prompt resolves only the intended session or fails ambiguous |
| T-04.2-02 | Malformed LLM structured output mutates interview state | Automated test proves one repair retry is attempted, then invalid or contradictory output leaves authoritative confirmation state unchanged, skips finalization, keeps the session open, and emits a neutral retry prompt |
| T-04.2-03 | User transcript injects instructions that override authoritative state | Automated test treats transcript as data and preserves required group IDs/status coverage |
| T-04.2-04 | LLM bypasses final write transaction | Source assertion/test proves confirmed items flow through `finalize_confirmed_interview()` and `apply_final_meal_resolution()` |

---

## Validation Sign-Off

- [x] All phase behaviors have automated verification targets or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all missing references identified by research
- [x] No watch-mode flags
- [x] Feedback latency target under 90 seconds for targeted checks
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** automated validation complete

---

## Validation Audit 2026-05-31

| Metric | Count |
|--------|-------|
| Gaps found | 1 |
| Resolved | 1 |
| Escalated | 0 |

Resolved gap: full unittest discovery exposed a stale `tests/test_match_flow.py` fixture for the unresolved-match worker path. The test now verifies the 04.2 kickoff handoff through `prepare_interview_session(..., chat_id="999")` and `_start_meal_interview_turn(...)` instead of asserting the old deterministic prompt surface.

Commands rerun:

- `./.venv/bin/python scripts/assert_phase_04_2_preflight.py` - green
- `./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_schema tests.test_interview_flow -q` - green
- `./.venv/bin/python -m unittest tests.test_bot_contract tests.test_fix_flow -q` - green
- `./.venv/bin/python -m unittest tests.test_reasoning_flow tests.test_interview_flow tests.test_bot_contract tests.test_fix_flow -q` - green
- `./.venv/bin/python -m unittest tests.test_match_flow -q` - green
- `./.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` - green

Known verification noise: expected negative-path logs from bot polling tests (`telegram down`, `db down`, missing segments) and multipart boundary warnings appear during suite runs, but the commands exit successfully.

**Approval:** automated validation complete
