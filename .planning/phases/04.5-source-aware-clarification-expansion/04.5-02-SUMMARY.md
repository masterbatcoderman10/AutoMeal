---
phase: 04.5-source-aware-clarification-expansion
plan: 02
subsystem: interview
tags: [interview, telegram, source-aware, unittest]
requires:
  - phase: 04.5-01
    provides: grouped reasoning source-policy metadata and stable segment_ids for deterministic interview follow-ups
provides:
  - source-aware post-identity question insertion inside the deterministic Telegram interview state machine
  - durable packaged and restaurant follow-up answers preserved in grouped confirmation_items
  - callback-safe rerender coverage and grounding/finalizer handoff regressions for enriched source detail fields
affects: [app/services/interview_service.py, bot/handlers.py, phase-04.4-finalizer]
tech-stack:
  added: []
  patterns: [post-identity source insertion in question_order, source detail follow-ups as first-class questions, callback payload parity with parse_interview_text]
key-files:
  created:
    - .planning/phases/04.5-source-aware-clarification-expansion/04.5-02-SUMMARY.md
  modified:
    - app/services/interview_service.py
    - bot/handlers.py
    - app/services/llm_client.py
    - tests/test_interview_flow.py
    - tests/test_bot_contract.py
    - tests/test_match_flow.py
key-decisions:
  - "Source affirmation or generic source prompts are injected by interview_service after identity answers instead of being pre-expanded in bot handlers."
  - "Packaged/store-bought and restaurant detail collection stays inside questions_by_id/question_order so replay, stale callback protection, and required-first ordering remain intact."
  - "OpenRouter chat completion keeps the existing default temperature=0.3 contract because the required 04.5 bot suite depends on that transport shape."
patterns-established:
  - "Source-aware follow-up insertion: answer a deterministic question, then splice the next source question immediately after it in the same group."
  - "Source detail persistence: SOURCE_ORIGIN, BRAND_NAME, RESTAURANT_NAME, and source-affirmation corrections all normalize into the same grouped confirmation_items seam."
requirements-completed: [INTERVIEW-01, INTERVIEW-03, INTERVIEW-04, MATCH-04]
duration: 12min
completed: 2026-06-02
---

# Phase 04.5 Plan 02: Source-Aware Clarification Expansion Summary

**The deterministic Telegram interview now injects source affirmation or generic source prompts after identity selection, captures brand/restaurant follow-ups durably, and preserves enriched grouped handoff data for the existing 04.4 finalizer/grounding seam.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-06-02T15:16:00Z
- **Completed:** 2026-06-02T15:28:27Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Added RED regressions for post-identity source insertion, source-affirmation correction fallback, packaged/restaurant follow-ups, callback rerendering, and grouped handoff preservation.
- Implemented source-aware dynamic question insertion in `interview_service` without adding handler-local interview branches.
- Preserved grouped `segment_ids`, `source_type`, `source_origin_state`, `brand_name`, and `restaurant_name` through confirmation assembly and the downstream grounding/finalizer seam.

## Task Commits

1. **Task 1: RED - capture dynamic source follow-up and correction-path regressions** - `e667fab` (`test`)
2. **Task 2: GREEN - implement source-aware dynamic question insertion and durable grouped handoff** - `30018f8` (`feat`)

## Files Created/Modified

- `app/services/interview_service.py` - injects post-identity source prompts, source-specific correction prompts, and packaged/restaurant detail questions while preserving grouped confirmation output.
- `bot/handlers.py` - keeps callback answers state-driven and now forwards normalized source payload fields from deterministic callbacks.
- `app/services/llm_client.py` - restores the existing default `temperature=0.3` chat-completion contract so the required bot suite passes.
- `tests/test_interview_flow.py` - covers source affirmation insertion, generic source fallback, source correction prompts, and brand/restaurant follow-up persistence.
- `tests/test_bot_contract.py` - locks callback rerender behavior for inserted source prompts and preserves the no-`Other` source-origin rule.
- `tests/test_match_flow.py` - proves enriched source detail fields survive the packaged grounding/finalizer handoff seam.

## Decisions Made

- Source policy metadata from plan 01 is consumed at the identity-question layer, not re-derived in Telegram handlers.
- Rejected learned-source affirmations reuse the existing correction pattern, but the prompt is source-specific so the free-text answer stays semantically correct.
- Packaged/store-bought and restaurant detail capture is modeled as ordinary deterministic questions (`BRAND_NAME` / `RESTAURANT_NAME`) rather than ad hoc follow-up state.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Restored OpenRouter chat completion default temperature forwarding**
- **Found during:** Task 2 (GREEN - implement source-aware dynamic question insertion and durable grouped handoff)
- **Issue:** The required `tests.test_bot_contract` suite was already failing because `OpenRouterClient.chat_completion()` no longer forwarded the expected default `temperature=0.3` field.
- **Fix:** Added the default `temperature` argument back into the OpenRouter chat completion request path.
- **Files modified:** `app/services/llm_client.py`
- **Verification:** `python -m unittest tests.test_reasoning_contract tests.test_reasoning_gate tests.test_interview_flow tests.test_bot_contract tests.test_match_flow -q`
- **Committed in:** `30018f8`

---

**Total deviations:** 1 auto-fixed (1 Rule 3 blocking issue)
**Impact on plan:** The deviation was outside the new interview behavior itself but blocked the mandated validation suite. Fixing it kept the phase verifiable without changing the 04.5 interview design.

## Issues Encountered

- The explicit worktree did not include `./.venv`, so validation ran with the shared project interpreter at `/Users/mali/Documents/Projects/MealTracker/.venv/bin/python` plus dummy required settings env vars.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 04.4's finalizer and grounding seams now receive grouped source context with stable `segment_ids` and source detail fields.
- `$gsd-verify-work` still needs the live warm-state checks from `04.5-VALIDATION.md`: no-match generic source, few-match generic source, and ambiguous identity followed by source affirmation or fallback generic source.

## Deviations from Threat Model

None.

## Self-Check: PASSED

- Verified `.planning/phases/04.5-source-aware-clarification-expansion/04.5-02-SUMMARY.md` exists.
- Verified task commits `e667fab` and `30018f8` exist in git history.
