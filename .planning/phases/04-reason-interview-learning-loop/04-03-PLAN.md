---
phase: 04-reason-interview-learning-loop
plan: 03
type: execute
wave: 1
depends_on: [04-01, 04-02]
files_modified:
  - app/models/__init__.py
  - app/models/meal_log.py
  - app/models/meal_segment.py
  - app/models/diary_entry.py
  - app/models/food_visual.py
  - app/models/interview_session.py
  - app/models/interview_message.py
  - app/models/correction_event.py
  - migrations/versions/002_phase4_state_surfaces.py
  - scripts/check_schema_contract.py
autonomous: true
requirements:
  - REASON-04
  - PIPELINE-01
  - INTERVIEW-01
  - INTERVIEW-02
  - INTERVIEW-05
  - INTERVIEW-06
  - INFRA-04
user_setup: []
must_haves:
  truths:
    - "D-36, D-37, D-38, and D-50: reasoning, interview, correction, and recovery state live in Postgres rather than PTB memory."
    - "D-42: rich quantity JSON becomes the source of truth while terse display fields remain derivable for bot output."
    - "D-57, D-60, D-70, D-74, and D-76: invalidation metadata, correction audit state, and recovery metadata are persisted explicitly instead of inferred from ad hoc text."
  artifacts:
    - path: "migrations/versions/002_phase4_state_surfaces.py"
      provides: "Phase 4 schema additions for reasoning, interview, correction, quantity, and recovery metadata"
    - path: "app/models/interview_session.py"
      provides: "DB-owned interview session source of truth"
    - path: "app/models/correction_event.py"
      provides: "Durable correction audit surface"
    - path: "scripts/check_schema_contract.py"
      provides: "Executable schema contract verification for the new Phase 4 surfaces"
  key_links:
    - from: "app/models/meal_log.py"
      to: "migrations/versions/002_phase4_state_surfaces.py"
      via: "meal-level reasoning and recovery metadata"
      pattern: "reasoning_state_json|last_stage_started_at|recovery_attempt_count"
    - from: "app/models/meal_segment.py"
      to: "migrations/versions/002_phase4_state_surfaces.py"
      via: "candidate snapshot and trace persistence"
      pattern: "match_candidates_json|reasoning_trace_id|ai_reasoning"
    - from: "app/models/correction_event.py"
      to: "app/models/food_visual.py"
      via: "identity-correction audit and invalidation metadata"
      pattern: "before_json|after_json|invalidated_at|invalidation_reason"
---

## Phase Goal

**As a** MealTracker user, **I want to** have Phase 4 state stored durably in the database, **so that** reasoning, interview, correction, and recovery can survive process restarts instead of relying on in-memory bot state.

<objective>
Create the durable schema and ORM foundation for Phase 4: new reasoning, quantity, interview, correction, and recovery state surfaces plus an executable schema contract check.

Purpose: The old Plan 01 was too large and only proved itself with grep. This split keeps the database/state work focused and gives it a real smokeable contract before higher-level services are added.
Output: schema migration, ORM model updates, new state models, and an extended schema contract script.
Implements: D-36, D-37, D-38, D-39, D-40, D-41, D-42, D-50, D-57, D-60, D-66, D-70, D-71, D-72, D-73, D-74, D-75, D-76, D-77.
</objective>

<execution_context>
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/workflows/execute-plan.md
@/Users/mali/Documents/Projects/MealTracker/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-reason-interview-learning-loop/04-CONTEXT.md
@.planning/phases/04-reason-interview-learning-loop/04-RESEARCH.md
@.planning/phases/04-reason-interview-learning-loop/04-VALIDATION.md
@.planning/phases/04-reason-interview-learning-loop/04-PATTERNS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transient worker state -> Postgres | Phase 4 domain truth crosses from process memory into durable reasoning, interview, correction, and recovery records. |
| rich quantity state -> user-visible summaries | The new quantity truth must remain precise enough for nutrition logic without regressing to fake precision or bucket-only storage. |

## STRIDE Threat Register

High-severity gate: none in this plan. Every listed threat is medium or low severity and non-blocking once the listed mitigation is implemented.

| Threat ID | Category | Component | Severity | Blocking | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `migrations/versions/002_phase4_state_surfaces.py` | medium | no | mitigate | Add explicit JSON, timestamp, and audit columns plus new tables instead of overloading free-text blobs or PTB state. |
| T-04-02 | Repudiation | `app/models/correction_event.py` | medium | no | mitigate | Persist before/after payloads, trace IDs, and visual-learning eligibility so `/fix` changes are durable and auditable. |
| T-04-03 | Information Disclosure | `app/models/interview_message.py` | low | no | mitigate | Keep DB-owned interview state limited to prompts, answers, and routing metadata; no raw provider secrets or hidden reasoning fields. |
| T-04-04 | Denial of Service | `scripts/check_schema_contract.py` | medium | no | mitigate | Extend the contract script so the plan fails on broken schema wiring rather than passing on string presence alone. |
| T-04-SC | Tampering | recovery metadata | medium | no | mitigate | Persist exact recovery counters and notification timestamps so later janitor logic cannot infer retry state loosely. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add existing-table Phase 4 state surfaces and extend the schema contract script</name>
  <files>app/models/meal_log.py, app/models/meal_segment.py, app/models/diary_entry.py, app/models/food_visual.py, migrations/versions/002_phase4_state_surfaces.py, scripts/check_schema_contract.py</files>
  <read_first>app/models/meal_log.py, app/models/meal_segment.py, app/models/diary_entry.py, app/models/food_visual.py, migrations/versions/001_initial_schema.py, scripts/check_schema_contract.py, .planning/phases/04-reason-interview-learning-loop/04-CONTEXT.md</read_first>
  <action>Extend the existing Phase 1 tables instead of inventing parallel stores. Add concrete Phase 4 columns for `meal_logs.reasoning_state_json`, `meal_logs.last_stage_started_at`, `meal_logs.recovery_attempt_count`, `meal_logs.last_recovery_notified_at`; `meal_segments.match_candidates_json`, `meal_segments.reasoning_trace_id`, and a structured `ai_reasoning` payload; `diary_entries.quantity_json` plus a terse derived display field; and `food_visuals.invalidated_at` plus `invalidation_reason`. Update `scripts/check_schema_contract.py` so it imports these models and fails if the new fields or migration contracts are broken, giving this plan executable verification instead of grep-only checks.</action>
  <verify>
    <automated>rtk .venv/bin/python scripts/check_schema_contract.py</automated>
  </verify>
  <done>The core Phase 4 state surfaces exist on the existing tables and are covered by an executable schema contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add DB-owned interview and correction models per D-50 and D-60</name>
  <files>app/models/interview_session.py, app/models/interview_message.py, app/models/correction_event.py, app/models/__init__.py, migrations/versions/002_phase4_state_surfaces.py, scripts/check_schema_contract.py</files>
  <read_first>app/models/__init__.py, app/models/meal_log.py, app/models/meal_segment.py, migrations/versions/001_initial_schema.py, scripts/check_schema_contract.py, .planning/phases/04-reason-interview-learning-loop/04-PATTERNS.md</read_first>
  <action>Create first-class Postgres models for `InterviewSession`, `InterviewMessage`, and `CorrectionEvent`, then export them from `app/models/__init__.py` and add their table definitions to the Phase 4 migration. `InterviewSession` must own the pinned `chat_id`, the dynamic state key, the current prompt payload, the last bot message id, and reminder timestamps so PTB memory stays optional per D-50 and D-51. `CorrectionEvent` must store `before_json`, `after_json`, `visual_learning_eligible`, `trace_id`, and the target `diary_entry_id` so D-60 has a durable audit trail. Extend `scripts/check_schema_contract.py` to import and assert these new models and required columns.</action>
  <verify>
    <automated>rtk .venv/bin/python scripts/check_schema_contract.py</automated>
  </verify>
  <done>The database and ORM layer can now persist the interview, correction, and recovery state every later Phase 4 service will rely on.</done>
</task>

</tasks>

<verification>
- `rtk .venv/bin/python scripts/check_schema_contract.py`
- `rtk rg -n "reasoning_state_json|match_candidates_json|quantity_json|class InterviewSession|class CorrectionEvent|recovery_attempt_count" app/models migrations/versions/002_phase4_state_surfaces.py scripts/check_schema_contract.py`
</verification>

<success_criteria>
- The Phase 4 durable state surfaces exist in both the migration and ORM layer.
- Schema verification is executable and fails on broken wiring instead of passing on string presence alone.
- Later service plans can treat Postgres as the source of truth for reasoning, interview, correction, and recovery state.
</success_criteria>

<output>
Create `.planning/phases/04-reason-interview-learning-loop/04-03-SUMMARY.md` when done.
</output>
